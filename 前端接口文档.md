# Frontend API Contract (code synced)

Generated from `backend/routers/router.go`, `backend/controllers/*`, and `backend/models/*`. When older notes conflict, this document is authoritative for method/path/query/body JSON tags and response data shape.

Protocol baseline: `seedx_sepolia_20260821_22d71686`. Legacy contract fields and request shapes are intentionally not exposed.

## Common Rules

- Normal APIs return `APIResponse`: `{"code":0,"message":"ok","data":...}`. Error responses use the same envelope; HTTP status can be 400/401/403/404.
- APIs using `DecodeJSON` enable `DisallowUnknownFields`; unknown JSON body fields return `invalid request body`.
- Browser authentication uses the `__Host-seedx_session` HttpOnly + Secure + SameSite=Strict Cookie. JavaScript must never read or persist the raw token. Admin APIs additionally enforce RBAC permissions.
- The shared Axios instance must use a fixed `baseURL` whose origin is the configured trusted backend, set `withCredentials: true`, and reject any absolute URL outside that origin.
- Cookie-authenticated POST/PATCH/PUT/DELETE requests must send `X-SeedX-Client: web`; the backend also verifies `Origin`. Never put the Cookie or token in a URL.
- `Authorization: Bearer` remains a compatibility input for trusted non-browser tools only. Browser code must not read a token from login JSON, localStorage, sessionStorage, query strings, or hash fragments.
- List limits follow controller parsing: invalid or <=0 uses defaults; most APIs cap at 200; kline limit caps at 1000.
- Amount fields are decimal strings. Chain addresses are `0x` strings. Database and chain display timestamps use RFC3339/ISO-8601 Beijing time (`+08:00`); Unix integer fields remain Unix seconds unless an interface explicitly declares milliseconds. Kline `from/to/time/closeTime/nextBefore` are milliseconds.
- Standard list responses use `data.items / data.total / data.limit / data.offset`.
- Transaction-param responses expose `preflight.readyToSubmit / approvalsComplete / simulationStatus / estimatedGas`.
- Project creation never accepts `nodeSaleDuration`; read it from `scaleTierSnapshot.nodeSaleDuration`.
- `execute_graduation` has no graduation-price or tick parameters; Uniswap v4 position fields are read-only results after graduation.

## Routes

<!-- ROUTE: GET /api/ws -->
### GET /api/ws

- Controller: `WebSocketController.HandleWebSocket`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response: HTTP `101 Switching Protocols`; this route does not return the standard JSON envelope.

Client message JSON:
```json
{"action":"subscribe | unsubscribe | subscribe_support | unsubscribe_support | subscribe_support_string","project":"string","conversationId":"uint64"}
```

Kline server message JSON:
```json
{"type":"kline.updated","project":"0x...","data":{"interval":"1m","priceBasis":"curve_spot","bar":"PriceCandle","floorPoint":"KlineFloorPoint|null","summary":"KlineChartSummary|null"},"time":1785542400000,"sequence":1}
```

Notes: optional real-time hint channel; the test server enables it while production remains disabled by default. The backend enforces Origin, connection, frame, message-rate and subscription limits. Subscribe by project, filter `kline.updated` by `data.interval`, and refetch HTTP after reconnect or a sequence gap. Browser private-support authentication is not enabled by default.

<!-- ROUTE: GET /api/swagger.json -->
### GET /api/swagger.json

- Controller: `SwaggerController.GetSwaggerJSON`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response: raw Swagger document, not the standard API envelope.
```json
{"openapi":"string","info":{"title":"string","description":"string","version":"string"},"host":"string","basePath":"string","paths":{"<path>":{"<method>":"Operation"}}}
```

Notes: Swagger routes are disabled when runmode=prod unless explicitly enabled.

<!-- ROUTE: GET /api/docs -->
### GET /api/docs

- Controller: `SwaggerController.GetSwaggerUI`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response: HTTP `200 text/html`; this route returns the Swagger UI HTML page and has no JSON body.

Notes: Swagger routes are disabled when runmode=prod unless explicitly enabled.

<!-- ROUTE: GET /api/metrics -->
### GET /api/metrics

- Controller: `MetricsController.GetMetrics`
- Auth: admin.read Cookie session

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "uptime_seconds": "int64",
    "total_requests": "int64",
    "successful_reqs": "int64",
    "failed_reqs": "int64",
    "avg_response_ms": "float64",
    "total_projects": "int64",
    "active_projects": "int64",
    "total_users": "int64",
    "total_trades": "int64",
    "total_volume_stable": "float64",
    "cache_hits": "int64",
    "cache_misses": "int64",
    "cache_hit_rate": "float64",
    "last_request": "RFC3339 string",
    "rpc": "RPC observer snapshot"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.uptime_seconds` | `int64` | 后端代码字段 uptime_seconds；含义由所属响应模型和接口业务决定 |
| `data.total_requests` | `int64` | 后端代码字段 total_requests；含义由所属响应模型和接口业务决定 |
| `data.successful_reqs` | `int64` | 后端代码字段 successful_reqs；含义由所属响应模型和接口业务决定 |
| `data.failed_reqs` | `int64` | 后端代码字段 failed_reqs；含义由所属响应模型和接口业务决定 |
| `data.avg_response_ms` | `float64` | 后端代码字段 avg_response_ms；含义由所属响应模型和接口业务决定 |
| `data.total_projects` | `int64` | 后端代码字段 total_projects；含义由所属响应模型和接口业务决定 |
| `data.active_projects` | `int64` | 后端代码字段 active_projects；含义由所属响应模型和接口业务决定 |
| `data.total_users` | `int64` | 后端代码字段 total_users；含义由所属响应模型和接口业务决定 |
| `data.total_trades` | `int64` | 后端代码字段 total_trades；含义由所属响应模型和接口业务决定 |
| `data.total_volume_stable` | `float64` | 后端代码字段 total_volume_stable；含义由所属响应模型和接口业务决定 |
| `data.cache_hits` | `int64` | 后端代码字段 cache_hits；含义由所属响应模型和接口业务决定 |
| `data.cache_misses` | `int64` | 后端代码字段 cache_misses；含义由所属响应模型和接口业务决定 |
| `data.cache_hit_rate` | `float64` | 后端代码字段 cache_hit_rate；含义由所属响应模型和接口业务决定 |
| `data.last_request` | `RFC3339 string` | 后端代码字段 last_request；含义由所属响应模型和接口业务决定 |
| `data.rpc` | `RPC observer snapshot` | 后端代码字段 rpc；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/health/detailed -->
### GET /api/health/detailed

- Controller: `MetricsController.GetHealth`
- Auth: admin.read Cookie session

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "status": "healthy",
    "uptime": "int64 seconds",
    "cache_size": "int",
    "metrics": "runtime metrics map including redacted chain RPC statistics"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `healthy` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.uptime` | `int64 seconds` | 后端代码字段 uptime；含义由所属响应模型和接口业务决定 |
| `data.cache_size` | `int` | 后端代码字段 cache_size；含义由所属响应模型和接口业务决定 |
| `data.metrics` | `runtime metrics map including redacted chain RPC statistics` | 后端代码字段 metrics；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/health -->
### GET /api/health

- Controller: `HealthController.Get`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "service": "string",
    "status": "string",
    "version": "string",
    "storage": "string"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.service` | `string` | 后端代码字段 service；含义由所属响应模型和接口业务决定 |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.version` | `string` | 积分规则版本；当前固定为 seedx-points-20260807 |
| `data.storage` | `string` | 后端代码字段 storage；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

Notes: public liveness response only. Ethereum Sepolia uses the official Uniswap v4 dependencies recorded in the handoff. Detailed runtime checks and metrics require an admin.read session.

<!-- ROUTE: GET /api/auth/nonce -->
### GET /api/auth/nonce

- Controller: `AuthController.Nonce`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "address": "wallet address"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.address` | `wallet address` | wallet address |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "address": "string",
      "nonce": "string",
      "message": "string",
      "issuedAt": "string",
      "expiresAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.nonce` | `string` | 后端代码字段 nonce；含义由所属响应模型和接口业务决定 |
| `data.message` | `string` | 人类可读的结果或错误信息 |
| `data.issuedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.expiresAt` | `string` | 绝对过期时间，RFC3339；nonce 有效期为 10 分钟，普通钱包会话有效期为 12 小时，拥有任一后台角色的管理员会话为 30 分钟 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: POST /api/auth/login -->
### POST /api/auth/login

- Controller: `AuthController.Login`
- Auth: none

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "address": "string",
    "message": "string",
    "signature": "string",
    "referrerAddress": "string",
    "inviteCode": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `body.message` | `string` | 人类可读的结果或错误信息 |
| `body.signature` | `string` | 后端代码字段 signature；含义由所属响应模型和接口业务决定 |
| `body.referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `body.inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "address": "string",
    "expiresAt": "string"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.expiresAt` | `string` | 绝对过期时间，RFC3339；nonce 有效期为 10 分钟，普通钱包会话有效期为 12 小时，拥有任一后台角色的管理员会话为 30 分钟 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `LoginRequest`; unknown fields return 400. Successful SIWE login sets `Set-Cookie: __Host-seedx_session=...; Path=/; HttpOnly; Secure; SameSite=Strict`; the JSON response never exposes the token. Ordinary wallet sessions expire after 12 hours; a wallet with any admin role expires after 30 minutes. Granting or revoking any admin role immediately revokes all active sessions for that wallet, so it must sign in again. Administrators use the same wallet login and RBAC, and no password-login route exists. Browser access therefore requires HTTPS (loopback localhost is development-only).

<!-- ROUTE: POST /api/auth/logout -->
### POST /api/auth/logout

- Controller: `AuthController.Logout`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "loggedOut": "bool"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.loggedOut` | `bool` | 后端代码字段 loggedOut；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

Notes: revokes the current Cookie session and returns an expired `__Host-seedx_session` Cookie. The response confirms loggedOut only and never reflects the raw token.

<!-- ROUTE: GET /api/auth/session -->
### GET /api/auth/session

- Controller: `AuthController.Session`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "authenticated": "bool",
    "address": "string"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.authenticated` | `bool` | 后端代码字段 authenticated；含义由所属响应模型和接口业务决定 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

## 后台管理接口总览

后台管理接口统一以 `/api/admin/*` 为入口，除 `public` 子路由外，全部要求浏览器携带 `__Host-seedx_session` 安全 Cookie，并继续校验 RBAC。管理前端登录成功后应首先请求 `GET /api/admin/meta`，以服务端返回的 `currentAdmin.permissionKeys` 控制菜单、按钮和路由守卫，不要在前端写死角色名称。

### 权限矩阵

| 权限 | 中文能力 | 典型接口 | 无权限时前端行为 |
|---|---|---|---|
| `admin.read` | 平台管理读取 | 元数据、平台配置、诊断、多签、总览、运行状态 | 隐藏平台管理模块；403 不重试 |
| `admin.write` | 平台管理写入 | 配置、公告、灰黑名单、RBAC、链上管理交易参数 | 隐藏写按钮；二次确认后调用 |
| `project.read` | 项目管理读取 | 项目列表、全量详情、对账、K 线健康 | 隐藏项目管理模块 |
| `member.read` | 用户管理读取 | 用户列表、用户全量详情、用户分析 | 隐藏用户管理模块 |
| `support.read` | 工单读取 | 工单列表、详情、指标、快捷回复列表 | 允许只读查看 |
| `support.write` | 工单处理 | 分配、回复、流转状态、维护快捷回复 | 隐藏处理按钮 |
| `audit.read` | 审计读取 | 审计日志、操作流水、交易分析 | 隐藏审计模块 |
| `treasury.read` | 资金读取 | 手续费收入、平台收入记录 | 隐藏资金模块 |

默认角色由后端种子数据维护：`super_admin` 为全部权限；`operator` 负责平台、项目、用户、审计和资金；`support_agent` 负责用户查询与工单；`auditor` 为项目、用户、工单、审计和资金只读。一个钱包可以绑定多个角色，最终权限取并集。

### 通用字段与单位

| 字段/类型 | 规则 | 前端要求 |
|---|---|---|
| `items/total/limit/offset` | 所有管理列表使用 offset 分页；`total` 是筛选后的总数 | 页码换算为 `offset=(page-1)*limit` |
| 金额、Token、价格、大整数 | 十进制字符串，避免 JavaScript 精度丢失 | 使用大数/十进制库，不得直接 `Number(value)` |
| `*Bps` | 万分比，`10000=100%` | 展示百分比时除以 100 |
| `*Ppm` | 百万分比，`1000000=100%` | 展示百分比时除以 10000 |
| 数据库时间字符串 | RFC3339/ISO-8601 | 北京时间（+08:00）；必须按返回偏移解析，不得再次手工加 8 小时 |
| 链上时间整数 | Unix 秒 | 转毫秒时乘以 1000 |
| EVM 地址 | `0x` 开头 20 字节十六进制 | 比较时忽略大小写，展示可 checksum |
| JSON 字符串字段 | 如 `metadataJson/tagsJson/summaryJson` | 先判空再 `JSON.parse`，解析失败保留原文 |

### 核心枚举

完整且可机读的枚举由 `GET /api/admin/meta.data.enums` 返回。`strict=true` 表示写接口会拒绝表外值；`strict=false` 表示当前仅为推荐值并兼容历史自定义值。

| 枚举 | 值 | 中文 | 关键业务含义 |
|---|---|---|---|
| `projectState` | `node_sale` | 节点认购中 | 首轮且唯一一轮节点认购；尚不能曲线交易 |
| `projectState` | `launch_failed` | 发射失败 | 认购未达条件；停止认购和交易，可按合约退款 |
| `projectState` | `trading` | 曲线交易中 | 可买卖、质押、发起治理 |
| `projectState` | `graduating` | 毕业公告中 | 停止新买入，等待公告期后执行毕业 |
| `projectState` | `graduated` | 已毕业 | 曲线交易结束；治理接口仍允许 1-11，UI 推荐 10/11 |
| `supportStatus` | `open` | 待处理 | 活跃待办 |
| `supportStatus` | `pending` | 等待反馈 | 等待用户或外部信息 |
| `supportStatus` | `resolved` | 已解决 | 可重新打开或关闭 |
| `supportStatus` | `closed` | 已关闭 | 终态；继续沟通需重新打开 |
| `riskLevel` | 空/`low/medium/high/critical` | 未评级/低/中/高/严重 | 仅后台风险标签，不会限制链上行为 |
| `adminTargetStatus` | 空/`normal/watch/restricted/blocked` | 未设置/正常/观察/限制建议/阻断建议 | 仅运营处理状态；真正限制需另走灰黑名单或链上规则 |
| `graylistLevel` | `gray/black` | 灰名单/黑名单 | 公开风险记录；不会直接修改项目合约状态 |
| `simulationStatus` | `not_run` | 未模拟 | 不应发送交易 |
| `simulationStatus` | `blocked_by_approval` | 等待授权 | 先授权并确认，再重新生成参数 |
| `simulationStatus` | `insufficient_balance` | 余额不足 | 禁止发送交易 |
| `simulationStatus` | `contract_wallet_not_allowed` | 非 EOA 钱包 | 合约 onlyEOA 限制，当前钱包不可执行 |
| `simulationStatus` | `simulation_failed` | 链上模拟回滚 | 展示 `preflight.reason`，刷新状态后重试，不得发送交易 |
| `simulationStatus` | `gas_estimation_failed` | Gas 估算失败 | 展示 `preflight.reason`，刷新状态后重试，不得发送交易 |
| `simulationStatus` | `insufficient_native_balance` | ETH 不足 | 补充 ETH 后重新生成交易参数 |
| `simulationStatus` | `passed` | 模拟通过 | 可按返回参数发送交易 |

所有返回 `TxPreflight` 的接口统一提供 `reasonCode/reason/nextAction`。`readyToSubmit=false` 不是独立错误原因，前端必须优先读取 `reasonCode`；其中 `approval_required` 表示交易参数有效但 allowance 不足，应按 `requiredApprovals` 完成精确授权，等待确认后重新请求 tx-params。

工单状态允许的流转为：`open -> pending/resolved/closed`、`pending -> open/resolved/closed`、`resolved -> open/closed`、`closed -> open`；同状态重复提交允许且幂等。

分析列表排序值：项目支持 `updated_at/created_at/volume_stable/transaction_count/holder_count/open_debt_stable/fdv`；用户支持 `updated_at/created_at/point_balance/transaction_count/open_debt_stable`；交易支持 `block_number/created_at`；`sortOrder` 为 `asc/desc`。当前代码对非法排序值会回退默认值，因此前端应从元数据/本文档选择合法值，不应依赖静默回退。

### 写操作后果

| 操作 | API 成功代表什么 | 还需要什么确认 |
|---|---|---|
| `POST /api/admin/configs` | 新增一个版本化后台配置，`effectiveAt` 后的新事件使用新值 | 不上链；历史积分不重算 |
| `POST /api/admin/platform/scale-tier-tx-params` | 只生成并模拟 `setScaleTier` calldata | `platformAdmin` 钱包发送且链上确认后才生效 |
| `POST /api/admin/platform/creation-fee-tx-params` | 只生成 Multisig `submit` 包装交易 | 提交、达到阈值并执行后才更新创建费 |
| `POST /api/admin/platform-announcements/authorization-message` | 生成绑定标题、正文、deadline 和当前多签版本的待签消息 | 仍需收集当前 signer 达到 threshold 的有效签名 |
| `POST /api/admin/platform-announcements` | 多签授权通过后，公告已原子写入全平台消息中心和审计记录 | API 成功即发布；授权消息成功不代表发布 |
| `POST/DELETE /api/admin/graylist*` | 风险名单和审计记录已更新 | 不改变合约状态；前端按名单做风险提示/入口限制 |
| `POST /api/admin/targets/*/metadata` | 标签、备注、状态、风险等级已更新 | 只影响后台读模型，不改变账户、项目或链上权限 |
| `POST/DELETE /api/admin/rbac/bindings*` | 角色已授予或撤销 | 下一次管理请求立即按新权限校验，属于高风险操作 |
| 工单分配/回复/状态接口 | 工单和审计记录已更新，并推送 WebSocket 事件 | API 成功即生效 |

前端接后台管理页时，建议按下面的页面分组来理解：

- 平台与诊断：`/api/admin/dashboard`、`/api/admin/diagnostics`、`/api/admin/indexer/runs`、`/api/admin/rpc-health`、`/api/admin/smoke-runs`
- 配置与运营：`/api/admin/configs`、`/api/admin/platform-announcements/authorization-message`、`/api/admin/platform-announcements`、`/api/admin/config-audit-logs`
- 多签与公开治理成员：`/api/admin/multisig`、`/api/admin/multisig/tx-params`、`/api/admin/public/multisig-members`
- 项目管理：`/api/admin/projects`、`/api/admin/projects/market`、`/api/admin/projects/:address/full`、`/api/admin/projects/:address/reconcile`、`/api/admin/projects/:address/kline-health`
- 用户管理：`/api/admin/members`、`/api/admin/members/:address/full`
- 分析与标签运营：`/api/admin/analytics/*`、`/api/admin/targets/:targetType/:targetId/metadata`
- 工单后台：`/api/admin/support/*`
- 审计与资金流水：`/api/admin/audit-logs`、`/api/admin/operation-ledger`、`/api/admin/fee-income-ledger`、`/api/admin/treasury/income-records`
- RBAC 与灰名单：`/api/admin/rbac/*`、`/api/admin/graylist*`、`/api/admin/public/graylist-rules`

调用建议：

- 登录后先调 `meta`；权限确认后，后台首页优先调 `dashboard`，再并行补 `diagnostics`、`rpc-health`、`indexer/runs`
- 项目后台详情优先调 `projects/:address/full`；只有在单独做诊断页时才再单独调 `reconcile` 或 `kline-health`
- 工单页先调会话列表，再按需拉取单条详情，避免一次性把所有消息明细全载入
- RBAC 和灰名单接口失败时，前端应直接展示 401/403 的权限态，而不是静默吞掉

Notes: validates the Cookie session and returns authenticated/address only; the raw token is never echoed. An invalid Cookie is actively expired by the backend.

<!-- ROUTE: GET /api/admin/meta -->
### GET /api/admin/meta

- Controller: `AdminController.Metadata`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "protocolVersion": "seedx_sepolia_20260821_22d71686",
    "currentAdmin": {
      "address": "0x...",
      "roleKeys": ["operator"],
      "permissionKeys": ["admin.read", "admin.write", "project.read"]
    },
    "roles": [
      {
        "roleKey": "operator",
        "roleName": "Operator",
        "description": "string",
        "permissions": [{"permissionKey": "admin.read", "description": "string"}],
        "createdAt": "RFC3339 string"
      }
    ],
    "availablePermissions": [
      {"permissionKey": "admin.read", "description": "Read admin dashboard and diagnostics"}
    ],
    "enums": [
      {
        "key": "projectState",
        "labelZh": "项目链上状态",
        "strict": true,
        "description": "由项目合约状态机决定，后台只能读取，不能直接修改。",
        "options": [
          {
            "value": "trading",
            "labelZh": "曲线交易中",
            "description": "节点认购成功并进入联合曲线交易。",
            "terminal": false,
            "changeEffect": "允许买卖、质押及参数 1-11 的治理提案。"
          }
        ]
      }
    ],
    "conventions": {
      "responseEnvelope": "string",
      "pagination": "string",
      "timeFormat": "string",
      "decimalEncoding": "string",
      "rateUnits": "string",
      "addressFormat": "string"
    },
    "actions": [
      {
        "key": "admin_role_bind",
        "labelZh": "授予管理角色",
        "permission": "admin.write",
        "effect": "立即改变目标钱包可访问的管理接口。",
        "confirmation": "API 成功后目标管理员下一次请求立即按新权限校验。",
        "riskLevel": "critical"
      }
    ]
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.protocolVersion` | `seedx_sepolia_20260821_22d71686` | 当前后端对齐的 SeedX 协议版本；版本不一致时前端应阻止高风险写操作 |
| `data.currentAdmin` | `object` | 当前 HttpOnly Cookie 会话对应的管理员身份、角色和权限并集 |
| `data.roles` | `object` | 全量 RBAC 角色目录，包含每个角色的权限 |
| `data.availablePermissions` | `object` | 后端当前支持的全量权限目录 |
| `data.enums` | `object` | 管理端字段枚举目录；strict=true 表示写接口拒绝表外值 |
| `data.conventions` | `object` | 管理 API 响应、分页、时间、大数、比例和地址约定 |
| `data.actions` | `object` | 管理写操作目录，说明所需权限、API 成功的真实影响和确认方式 |
<!-- /RESPONSE_FIELDS -->
Notes: 管理后台初始化接口。`roles` 是全量角色目录，`currentAdmin` 是当前登录钱包的角色和权限并集；菜单按 `permissionKeys` 控制。`enums.strict=true` 才表示后端拒绝表外值，`actions` 明确写操作的真实后果与二次确认方式。

<!-- ROUTE: GET /api/admin/configs -->
### GET /api/admin/configs

- Controller: `AdminController.ListConfigs`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": [
    {
      "configKey": "string",
      "configValueJson": "string",
      "version": "int",
      "effectiveAt": "string",
      "createdBy": "string",
      "createdAt": "string"
    }
  ]
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: POST /api/admin/configs -->
### POST /api/admin/configs

- Controller: `AdminController.UpsertConfig`
- Auth: admin.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "configKey": "string",
    "configValueJson": "string",
    "effectiveAt": "string",
    "createdBy": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.configKey` | `string` | 版本化后端配置键；档位积分倍率使用 points.scale_tier_multiplier.<bytes32 scaleTierId>，项目覆盖使用 points.project_multiplier.<projectAddress> |
| `body.configValueJson` | `string` | 字符串形式的 JSON；积分倍率格式为 {"multiplier":"1.5"}，必须大于 0 且最多 18 位小数 |
| `body.effectiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `body.createdBy` | `string` | 后端代码字段 createdBy；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "configKey": "string",
      "configValueJson": "string",
      "version": "int",
      "effectiveAt": "string",
      "createdBy": "string",
      "createdAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.configKey` | `string` | 版本化后端配置键；档位积分倍率使用 points.scale_tier_multiplier.<bytes32 scaleTierId>，项目覆盖使用 points.project_multiplier.<projectAddress> |
| `data.configValueJson` | `string` | 字符串形式的 JSON；积分倍率格式为 {"multiplier":"1.5"}，必须大于 0 且最多 18 位小数 |
| `data.version` | `int` | 积分规则版本；当前固定为 seedx-points-20260807 |
| `data.effectiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.createdBy` | `string` | 后端代码字段 createdBy；含义由所属响应模型和接口业务决定 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `UpsertPlatformConfigRequest`; unknown fields return 400. Scale Tier point multiplier uses `configKey=points.scale_tier_multiplier.<bytes32 scaleTierId>`; project override uses `points.project_multiplier.<projectAddress>`. Both use `configValueJson={"multiplier":"1.5"}`. Prefer the dedicated project multiplier endpoint for project overrides. `effectiveAt` controls the first event time that uses the new multiplier.

<!-- ROUTE: GET /api/admin/platform -->
### GET /api/admin/platform

- Controller: `AdminController.ChainPlatformConfig`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "protocolVersion": "string",
    "chainId": "int64",
    "chainName": "string",
    "rpcUrl": "string",
    "explorerUrl": "string",
    "stableTokenAddress": "string",
    "stableTokenSymbol": "string",
    "stableTokenDecimals": "uint8",
    "platformAddress": "string",
    "platformMultisig": "string",
    "platformPayout": "string",
    "platformAdmin": "string",
    "creationFeeAdmin": "string",
    "curveTableRegistry": "string",
    "projectImplementation": "string",
    "projectRules": "string",
    "graduationAdapter": "string",
    "graduationEnabled": "bool",
    "graduationDisabledReason": "string",
    "bridgeEnabled": "bool",
    "bridgeDisabledReason": "string",
    "uniswapV4Hook": "string",
    "uniswapV4PositionManager": "string",
    "uniswapV4PoolManager": "string",
    "uniswapV4Permit2": "string",
    "creationFee": "string",
    "nodeRaiseTarget": "string",
    "sourceBundleHash": "string",
    "deploymentStartBlock": "uint64",
    "scaleTiers": "[]ScaleTierSnapshot",
    "dependencyIntegrity": "DependencyIntegritySnapshot",
    "walletConnect": "WalletConnectConfig"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.protocolVersion` | `string` | 当前后端对齐的 SeedX 协议版本；版本不一致时前端应阻止高风险写操作 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.chainName` | `string` | 后端代码字段 chainName；含义由所属响应模型和接口业务决定 |
| `data.rpcUrl` | `string` | 可公开给前端的当前网络 RPC；后端内部使用的付费 RPC 及密钥绝不通过此字段返回 |
| `data.explorerUrl` | `string` | 后端代码字段 explorerUrl；含义由所属响应模型和接口业务决定 |
| `data.stableTokenAddress` | `string` | 当前网络配置的稳定币合约地址 |
| `data.stableTokenSymbol` | `string` | 后端代码字段 stableTokenSymbol；含义由所属响应模型和接口业务决定 |
| `data.stableTokenDecimals` | `uint8` | Stable Token 的真实精度；Ethereum Sepolia 为 USDGTest 6 位，项目 Token 仍为 18 位 |
| `data.platformAddress` | `string` | 合约或钱包的 EVM 地址 |
| `data.platformMultisig` | `string` | 后端代码字段 platformMultisig；含义由所属响应模型和接口业务决定 |
| `data.platformPayout` | `string` | 后端代码字段 platformPayout；含义由所属响应模型和接口业务决定 |
| `data.platformAdmin` | `string` | 后端代码字段 platformAdmin；含义由所属响应模型和接口业务决定 |
| `data.creationFeeAdmin` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.curveTableRegistry` | `string` | 后端代码字段 curveTableRegistry；含义由所属响应模型和接口业务决定 |
| `data.projectImplementation` | `string` | 后端代码字段 projectImplementation；含义由所属响应模型和接口业务决定 |
| `data.projectRules` | `string` | 当前部署 SeedXProjectRules 库地址；用于核对后端预检与合约规则版本 |
| `data.graduationAdapter` | `string` | 毕业 Adapter 地址；enabled=true 时必须等于 Platform.trustedGraduationAdapter() |
| `data.graduationEnabled` | `bool` | 是否可以发起毕业及 LP 手续费收取写操作；Ethereum Sepolia 已使用官方 Uniswap v4 完成验证，因此为 true |
| `data.graduationDisabledReason` | `string` | 后端代码字段 graduationDisabledReason；含义由所属响应模型和接口业务决定 |
| `data.bridgeEnabled` | `bool` | 是否开放 Relay 跨链报价和订单；当前测试服独立开启 Sepolia 与 Base Sepolia 原生 ETH 双向测试路线 |
| `data.bridgeDisabledReason` | `string` | 跨链桥禁用原因；前端应展示或用于禁用提示 |
| `data.uniswapV4Hook` | `string` | 后端代码字段 uniswapV4Hook；含义由所属响应模型和接口业务决定 |
| `data.uniswapV4PositionManager` | `string` | 后端代码字段 uniswapV4PositionManager；含义由所属响应模型和接口业务决定 |
| `data.uniswapV4PoolManager` | `string` | 后端代码字段 uniswapV4PoolManager；含义由所属响应模型和接口业务决定 |
| `data.uniswapV4Permit2` | `string` | 后端代码字段 uniswapV4Permit2；含义由所属响应模型和接口业务决定 |
| `data.creationFee` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.nodeRaiseTarget` | `string` | 平台固定节点募集目标；当前为 30000 USDGTest，建项必须满足 nodeCount * nodePrice = nodeRaiseTarget |
| `data.sourceBundleHash` | `string` | Sepolia 20260821 交接包 source bundle SHA-256 哈希；必须为 22d7168618d2674546aeae149eaec03055cb3fdfa13830ddafacd3a03f8c2a92 |
| `data.deploymentStartBlock` | `uint64` | 当前协议部署首区块 11535261；Worker 全量重扫起点 |
| `data.scaleTiers` | `[]ScaleTierSnapshot` | 后端代码字段 scaleTiers；含义由所属响应模型和接口业务决定 |
| `data.dependencyIntegrity` | `DependencyIntegritySnapshot` | 当前 Platform、固定 Uniswap v4 Adapter 和官方外部依赖的链上 runtime 完整性快照 |
| `data.walletConnect` | `WalletConnectConfig` | 后端代码字段 walletConnect；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: POST /api/admin/platform/scale-tier-tx-params -->
### POST /api/admin/platform/scale-tier-tx-params

- Controller: `AdminController.ScaleTierTxParams`
- Auth: admin.write; authenticated wallet must equal `platformAdmin`

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "scaleTier": "string",
    "tierName": "string",
    "curveTableAddress": "string",
    "graduationAdapter": "string",
    "fOpen": "string",
    "minNodeCount": "string",
    "defaultNodeCount": "string",
    "maxNodeCount": "string",
    "minNodePrice": "string",
    "defaultNodePrice": "string",
    "maxNodePrice": "string",
    "nodeSaleDuration": "uint64",
    "nodeVestingCliffDuration": "uint64",
    "nodeVestingTotalDuration": "uint64",
    "governanceVotingPeriod": "uint64",
    "governanceTimelock": "uint64",
    "graduationMinTradingDuration": "uint64",
    "graduationNotice": "uint64",
    "graduationVolumeWindow": "uint64",
    "curveParamsHash": "string",
    "curvePointsHash": "string",
    "enabled": "bool"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `body.tierName` | `string` | 后端展示用档位名称；不编码进 setScaleTier calldata |
| `body.curveTableAddress` | `string` | 必须是固定 CurveTableRegistry 工厂创建并已注册的 CurveTable 地址 |
| `body.graduationAdapter` | `string` | 毕业 Adapter 地址；enabled=true 时必须等于 Platform.trustedGraduationAdapter() |
| `body.fOpen` | `string` | 创世地板价的可读 USDGTest 6 位小数字符串；必须等于 CurveTable.rawPriceAtSupply(0) |
| `body.minNodeCount` | `string` | 该档位最小节点数；enabled=true 时不得小于 100 |
| `body.defaultNodeCount` | `string` | 默认节点数；必须位于 minNodeCount 和 maxNodeCount 之间 |
| `body.maxNodeCount` | `string` | 该档位最大节点数 |
| `body.minNodePrice` | `string` | 单节点最低 USDGTest 价格的可读 6 位小数字符串；enabled=true 时不得低于 100 |
| `body.defaultNodePrice` | `string` | 默认单节点 USDGTest 价格；必须位于 minNodePrice 和 maxNodePrice 之间 |
| `body.maxNodePrice` | `string` | 单节点最高 USDGTest 价格的可读 6 位小数字符串 |
| `body.nodeSaleDuration` | `uint64` | 节点认购期秒数；建项时从 Scale Tier 固化，不是 CreateProjectParams 字段 |
| `body.nodeVestingCliffDuration` | `uint64` | 节点 Token 开始线性释放前的 cliff 秒数；建项时从 Scale Tier 固化 |
| `body.nodeVestingTotalDuration` | `uint64` | 节点 Token 从锁定开始到全部释放的总秒数；必须大于 nodeVestingCliffDuration |
| `body.governanceVotingPeriod` | `uint64` | 治理投票期秒数；enabled=true 时必须大于 0 |
| `body.governanceTimelock` | `uint64` | 提案通过后执行 timelock 秒数；enabled=true 时必须大于 0 |
| `body.graduationMinTradingDuration` | `uint64` | 进入毕业条件前的最小交易时长秒数 |
| `body.graduationNotice` | `uint64` | 毕业公告期秒数；triggerGraduation 后须等待该时长才可 executeGraduation |
| `body.graduationVolumeWindow` | `uint64` | 毕业成交量滚动窗口秒数；后端只展示链上窗口语义 |
| `body.curveParamsHash` | `string` | 曲线配置参数的 bytes32 哈希；用于核对 Scale Tier 与 CurveTable 参数版本 |
| `body.curvePointsHash` | `string` | 曲线分段点数组的 bytes32 哈希；必须与 CurveTable.pointsHash() 一致 |
| `body.enabled` | `bool` | 档位是否启用；true 时执行全部 Registry/codehash/Adapter/参数约束，false 时链上清除 curveCodeHashByTier |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "target": "string",
    "method": "string",
    "value": "string",
    "chainId": "int64",
    "calldata": "string",
    "args": "map[string]string",
    "expectedCaller": "string",
    "nestedTarget": "string",
    "nestedCalldata": "string",
    "snapshotBlock": "uint64",
    "preflight": "TxPreflight"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.target` | `string` | 交易目标合约地址 |
| `data.method` | `string` | 后端代码字段 method；含义由所属响应模型和接口业务决定 |
| `data.value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `data.args` | `map[string]string` | 后端代码字段 args；含义由所属响应模型和接口业务决定 |
| `data.expectedCaller` | `string` | 后端代码字段 expectedCaller；含义由所属响应模型和接口业务决定 |
| `data.nestedTarget` | `string` | 后端代码字段 nestedTarget；含义由所属响应模型和接口业务决定 |
| `data.nestedCalldata` | `string` | 后端代码字段 nestedCalldata；含义由所属响应模型和接口业务决定 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `data.preflight` | `TxPreflight` | 后端代码字段 preflight；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `ScaleTierTxParamsRequest`; integrity fields returned by `ScaleTierSnapshot` are read-only and must not be sent. For `enabled=true`, `graduationAdapter` must equal the Platform fixed `trustedGraduationAdapter`, and the Curve must be factory-created and registered by the fixed CurveTableRegistry with matching paramsHash, pointsHash, and runtime codehash. The endpoint generates and simulates calldata; the platformAdmin wallet must still submit it on chain.

<!-- ROUTE: POST /api/admin/platform/creation-fee-tx-params -->
### POST /api/admin/platform/creation-fee-tx-params

- Controller: `AdminController.CreationFeeTxParams`
- Auth: admin.write; authenticated wallet must be a platform multisig signer

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "newCreationFee": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.newCreationFee` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "target": "string",
    "method": "string",
    "value": "string",
    "chainId": "int64",
    "calldata": "string",
    "args": "map[string]string",
    "expectedCaller": "string",
    "nestedTarget": "string",
    "nestedCalldata": "string",
    "snapshotBlock": "uint64",
    "preflight": "TxPreflight"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.target` | `string` | 交易目标合约地址 |
| `data.method` | `string` | 后端代码字段 method；含义由所属响应模型和接口业务决定 |
| `data.value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `data.args` | `map[string]string` | 后端代码字段 args；含义由所属响应模型和接口业务决定 |
| `data.expectedCaller` | `string` | 后端代码字段 expectedCaller；含义由所属响应模型和接口业务决定 |
| `data.nestedTarget` | `string` | 后端代码字段 nestedTarget；含义由所属响应模型和接口业务决定 |
| `data.nestedCalldata` | `string` | 后端代码字段 nestedCalldata；含义由所属响应模型和接口业务决定 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `data.preflight` | `TxPreflight` | 后端代码字段 preflight；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `CreationFeeTxParamsRequest`; response wraps `updateCreationFee` in multisig `submit` calldata.

<!-- ROUTE: GET /api/admin/diagnostics -->
### GET /api/admin/diagnostics

- Controller: `AdminController.Diagnostics`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "storage": "string",
      "runtimeChecks": "[]RuntimeConfigCheck",
      "indexerCheckpoints": "[]IndexerCheckpointItem",
      "workerStatusRows": "[]WorkerStatusItem",
      "latestEventBlock": "uint64",
      "latestSnapshotBlock": "uint64",
      "industrialGate": "IndustrialGateSummary",
      "latestWorkerRun": "WorkerRunSummary",
      "latestSmokeRun": "SmokeRunSummary",
      "latestRpcHealth": "RPCHealthSummary"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.storage` | `string` | 后端代码字段 storage；含义由所属响应模型和接口业务决定 |
| `data.runtimeChecks` | `[]RuntimeConfigCheck` | 后端代码字段 runtimeChecks；含义由所属响应模型和接口业务决定 |
| `data.indexerCheckpoints` | `[]IndexerCheckpointItem` | 后端代码字段 indexerCheckpoints；含义由所属响应模型和接口业务决定 |
| `data.workerStatusRows` | `[]WorkerStatusItem` | 后端代码字段 workerStatusRows；含义由所属响应模型和接口业务决定 |
| `data.latestEventBlock` | `uint64` | 后端代码字段 latestEventBlock；含义由所属响应模型和接口业务决定 |
| `data.latestSnapshotBlock` | `uint64` | 后端代码字段 latestSnapshotBlock；含义由所属响应模型和接口业务决定 |
| `data.industrialGate` | `IndustrialGateSummary` | 后端代码字段 industrialGate；含义由所属响应模型和接口业务决定 |
| `data.latestWorkerRun` | `WorkerRunSummary` | 后端代码字段 latestWorkerRun；含义由所属响应模型和接口业务决定 |
| `data.latestSmokeRun` | `SmokeRunSummary` | 后端代码字段 latestSmokeRun；含义由所属响应模型和接口业务决定 |
| `data.latestRpcHealth` | `RPCHealthSummary` | 后端代码字段 latestRpcHealth；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/admin/multisig -->
### GET /api/admin/multisig

- Controller: `AdminController.Multisig`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "policy": "MultisigPolicy",
    "items": "[]MultisigTransactionItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int",
    "snapshotBlock": "uint64"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.policy` | `MultisigPolicy` | 后端代码字段 policy；含义由所属响应模型和接口业务决定 |
| `data.items` | `[]MultisigTransactionItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->
Notes: list data comes from the indexed database. `policy.configVersion` is the current on-chain signer/threshold version; each transaction stores its submit-time `configVersion`, and `stale=true` means a later signer or threshold change makes approval/execution revert with `StaleTransaction`.

<!-- ROUTE: GET /api/admin/multisig/platform-revenue -->
### GET /api/admin/multisig/platform-revenue

- Controller: `AdminController.PlatformRevenueClaimables`
- Auth: treasury.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]PlatformRevenueProjectItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]PlatformRevenueProjectItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: requires `treasury.read`. Returns standard `items/total/limit/offset`. Each item is one indexed project and includes `claimableFeeStable`, `claimableTaxStable`, and `claimableTotalStable` for the PlatformMultisig at one snapshot block. Use projects with a positive selected balance to build `batch_claim_platform_revenue`; refresh immediately before preparing the batch because balances can change on every trade.

<!-- ROUTE: POST /api/admin/multisig/tx-params -->
### POST /api/admin/multisig/tx-params

- Controller: `AdminController.MultisigTxParams`
- Auth: admin.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "action": "string",
    "target": "string",
    "value": "string",
    "calldata": "string",
    "transactionId": "uint64",
    "signer": "string",
    "oldSigner": "string",
    "newSigner": "string",
    "newThreshold": "uint64",
    "revenueClaims": "[]PlatformRevenueClaimRequest"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.action` | `string` | 后端代码字段 action；含义由所属响应模型和接口业务决定 |
| `body.target` | `string` | 交易目标合约地址 |
| `body.value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `body.calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `body.transactionId` | `uint64` | 后端代码字段 transactionId；含义由所属响应模型和接口业务决定 |
| `body.signer` | `string` | 多签 remove_signer 要移除的现有签名人地址 |
| `body.oldSigner` | `string` | 多签 replace_signer 要替换的现有签名人地址 |
| `body.newSigner` | `string` | 多签 add_signer/replace_signer 的新签名人地址 |
| `body.newThreshold` | `uint64` | 多签 update_threshold 的新批准阈值；必须满足合约 2/3 规则 |
| `body.revenueClaims` | `[]PlatformRevenueClaimRequest` | 后端代码字段 revenueClaims；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "target": "string",
      "method": "string",
      "calldata": "string",
      "value": "string",
      "chainId": "int64",
      "args": "map[string]string",
      "requiredApprovals": "[]RequiredApproval",
      "balanceChecks": "[]BalanceCheck",
      "warnings": "[]string",
      "snapshotBlock": "uint64"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.target` | `string` | 交易目标合约地址 |
| `data.method` | `string` | 后端代码字段 method；含义由所属响应模型和接口业务决定 |
| `data.calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `data.value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.args` | `map[string]string` | 后端代码字段 args；含义由所属响应模型和接口业务决定 |
| `data.requiredApprovals` | `[]RequiredApproval` | 后端代码字段 requiredApprovals；含义由所属响应模型和接口业务决定 |
| `data.balanceChecks` | `[]BalanceCheck` | 后端代码字段 balanceChecks；含义由所属响应模型和接口业务决定 |
| `data.warnings` | `[]string` | 后端代码字段 warnings；含义由所属响应模型和接口业务决定 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `MultisigTxParamsRequest`. `action` supports `submit`, `approve`, `execute`, `add_signer`, `remove_signer`, `replace_signer`, `update_threshold`, and `batch_claim_platform_revenue`. Configuration changes and platform-revenue batch claims generate a multisig self-call wrapped in `submit`. A revenue batch accepts 1-32 unique projects; each item selects claimFees, claimTaxes, or both. The backend reads current claimable amounts at snapshotBlock and rejects selected zero balances before returning calldata. Signer-set changes increment `configVersion` and invalidate all unexecuted transactions created under older versions.

<!-- ROUTE: POST /api/admin/platform-announcements/authorization-message -->
### POST /api/admin/platform-announcements/authorization-message

- Controller: `AdminController.PlatformAnnouncementAuthorization`
- Auth: admin.read

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "title": "string",
    "body": "string",
    "deadline": "int64",
    "approvals": "[]PlatformAnnouncementApproval"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `body.body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `body.deadline` | `int64` | Unix-second deadline; the validity window and enforcement rule are defined by the route notes. |
| `body.approvals` | `[]PlatformAnnouncementApproval` | 后端代码字段 approvals；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "message": "string",
    "digest": "string",
    "deadline": "int64",
    "policy": "MultisigPolicy"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.message` | `string` | 人类可读的结果或错误信息 |
| `data.digest` | `string` | 后端代码字段 digest；含义由所属响应模型和接口业务决定 |
| `data.deadline` | `int64` | Unix-second deadline; the validity window and enforcement rule are defined by the route notes. |
| `data.policy` | `MultisigPolicy` | 后端代码字段 policy；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

Notes: body strictly matches `PublishPlatformAnnouncementRequest`; use an empty approvals array at this step. The deadline must be in the next 15 minutes. Every current multisig signer signs the returned message with personal_sign; publishing requires distinct valid signatures at the live threshold.

<!-- ROUTE: POST /api/admin/platform-announcements -->
### POST /api/admin/platform-announcements

- Controller: `AdminController.PublishPlatformAnnouncement`
- Auth: admin.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "title": "string",
    "body": "string",
    "deadline": "int64",
    "approvals": "[]PlatformAnnouncementApproval"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `body.body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `body.deadline` | `int64` | Unix-second deadline; the validity window and enforcement rule are defined by the route notes. |
| `body.approvals` | `[]PlatformAnnouncementApproval` | 后端代码字段 approvals；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "published": "bool",
    "message": "MessageItem"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.published` | `bool` | 后端代码字段 published；含义由所属响应模型和接口业务决定 |
| `data.message` | `MessageItem` | 人类可读的结果或错误信息 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `PublishPlatformAnnouncementRequest`; unknown fields return 400. First request the authorization message with the same title/body/deadline, collect distinct personal_sign approvals from the current PlatformMultisig signer set, then publish. The backend re-reads signer, threshold, and configVersion state and atomically consumes the authorization digest, so stale, duplicate, replayed, or sub-threshold approvals are rejected.

<!-- ROUTE: GET /api/admin/graylist -->
### GET /api/admin/graylist

- Controller: `AdminController.Graylist`
- Auth: project.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "level": "optional",
    "limit": "default 50, max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.level` | `optional` | optional |
| `query.limit` | `default 50, max 200` | default 50, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]ProjectGraylist",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]ProjectGraylist` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: POST /api/admin/graylist -->
### POST /api/admin/graylist

- Controller: `AdminController.UpsertGraylist`
- Auth: admin.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "projectAddress": "string",
    "level": "string",
    "reason": "string",
    "judgmentRule": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.projectAddress` | `string` | 项目合约地址 |
| `body.level` | `string` | 后端代码字段 level；含义由所属响应模型和接口业务决定 |
| `body.reason` | `string` | 本次操作原因，用于审计追溯 |
| `body.judgmentRule` | `string` | 后端代码字段 judgmentRule；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"status": "ok"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `ok` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `GraylistUpsertRequest`; unknown fields return 400.

<!-- ROUTE: DELETE /api/admin/graylist/:address -->
### DELETE /api/admin/graylist/:address

- Controller: `AdminController.RemoveGraylist`
- Auth: admin.write

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web"
  },
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"status": "ok"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `ok` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/admin/config-audit-logs -->
### GET /api/admin/config-audit-logs

- Controller: `AdminController.ConfigAuditLogs`
- Auth: audit.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "paramKey": "optional",
    "limit": "default 50, max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.paramKey` | `optional` | optional |
| `query.limit` | `default 50, max 200` | default 50, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]ConfigAuditLog",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]ConfigAuditLog` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/audit-logs -->
### GET /api/admin/audit-logs

- Controller: `AdminController.AdminActionAuditLogs`
- Auth: audit.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "actionType": "optional",
    "actor": "optional",
    "limit": "default 50, max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.actionType` | `optional` | optional |
| `query.actor` | `optional` | optional |
| `query.limit` | `default 50, max 200` | default 50, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]AdminActionAuditLog",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]AdminActionAuditLog` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/rbac/roles -->
### GET /api/admin/rbac/roles

- Controller: `AdminController.AdminRoles`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "limit": "default 50, max 100",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.limit` | `default 50, max 100` | default 50, max 100 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]AdminRoleItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]AdminRoleItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/admin/rbac/bindings -->
### GET /api/admin/rbac/bindings

- Controller: `AdminController.AdminRoleBindings`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "address": "optional",
    "limit": "default 50, max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.address` | `optional` | optional |
| `query.limit` | `default 50, max 200` | default 50, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]AdminRoleBindingItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]AdminRoleBindingItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: POST /api/admin/rbac/bindings -->
### POST /api/admin/rbac/bindings

- Controller: `AdminController.UpsertAdminRoleBinding`
- Auth: admin.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "adminAddress": "string",
    "roleKey": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.adminAddress` | `string` | 合约或钱包的 EVM 地址 |
| `body.roleKey` | `string` | 后端代码字段 roleKey；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "binding": "AdminRoleBindingItem"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.binding` | `AdminRoleBindingItem` | 后端代码字段 binding；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `AdminRoleBindingRequest`; unknown fields return 400.

<!-- ROUTE: DELETE /api/admin/rbac/bindings/:address/:role -->
### DELETE /api/admin/rbac/bindings/:address/:role

- Controller: `AdminController.DeleteAdminRoleBinding`
- Auth: admin.write

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web"
  },
  "path": {
    "address": "path address",
    "role": "path role"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `path.role` | `path role` | 后端代码字段 role；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"deleted": true}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.deleted` | `object` | 后端代码字段 deleted；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/admin/bridge/orders -->
### GET /api/admin/bridge/orders

- Controller: `BridgeController.AdminOrders`
- Auth: `audit.read`

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "userAddress": "optional wallet",
    "status": "optional bridge status",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.userAddress` | `optional wallet` | optional wallet |
| `query.status` | `optional bridge status` | optional bridge status |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]BridgeOrder",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]BridgeOrder` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 仅客服/审计查询；不能在后台替用户签名、授权、桥接或垫付。

<!-- ROUTE: GET /api/admin/points/overview -->
### GET /api/admin/points/overview

- Controller: `PointController.AdminOverview`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "totalEffectivePoints": "string",
      "totalBasePoints": "string",
      "totalDirectReferralRewards": "string",
      "totalTeamOperationRewards": "string",
      "pointAccountCount": "uint64",
      "teamCount": "uint64",
      "personalLevelDistribution": "map[string]int"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.totalEffectivePoints` | `string` | 后端代码字段 totalEffectivePoints；含义由所属响应模型和接口业务决定 |
| `data.totalBasePoints` | `string` | 后端代码字段 totalBasePoints；含义由所属响应模型和接口业务决定 |
| `data.totalDirectReferralRewards` | `string` | 后端代码字段 totalDirectReferralRewards；含义由所属响应模型和接口业务决定 |
| `data.totalTeamOperationRewards` | `string` | 后端代码字段 totalTeamOperationRewards；含义由所属响应模型和接口业务决定 |
| `data.pointAccountCount` | `uint64` | 数量统计 |
| `data.teamCount` | `uint64` | 数量统计 |
| `data.personalLevelDistribution` | `map[string]int` | 按 L0-L5 分组的积分账户数量 |
<!-- /RESPONSE_FIELDS -->

Notes: 积分总览只读；基础积分、直属奖励和团队运营奖励分别统计，不提供人工改分。

<!-- ROUTE: GET /api/admin/points/users -->
### GET /api/admin/points/users

- Controller: `PointController.AdminUsers`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "keyword": "optional wallet substring",
    "personalLevel": "optional L0-L5",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.keyword` | `optional wallet substring` | optional wallet substring |
| `query.personalLevel` | `optional L0-L5` | optional L0-L5 |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]AdminPointUserItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]AdminPointUserItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 按个人有效积分降序分页；personalLevel 是严格 L0-L5 枚举。

<!-- ROUTE: GET /api/admin/points/users/:address -->
### GET /api/admin/points/users/:address

- Controller: `PointController.AdminUser`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {
    "address": "wallet address"
  },
  "query": {},
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `wallet address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "address": "string",
      "inviteCode": "string",
      "totalPoints": "string",
      "personalEffectivePoints": "string",
      "personalLevel": "string",
      "personalLevelName": "string",
      "nextPersonalLevel": "string",
      "pointsToNextPersonalLevel": "string",
      "basePointsTotal": "string",
      "directReferralRewardTotal": "string",
      "teamOperationRewardTotal": "string",
      "referrerAddress": "string",
      "referrerInviteCode": "string",
      "referralRewardTotal": "string",
      "directReferralCount": "int",
      "networkSummary": "PointNetworkSummary",
      "recentActivity": "PointActivitySummary",
      "downlinePoints": "string",
      "teamLeaderAddress": "string",
      "isTeamLeader": "bool",
      "teamDownlineBasePoints": "string",
      "teamRewardRateBps": "uint64",
      "nextTeamRewardThreshold": "string",
      "directReferrals": "[]DirectReferralItem",
      "ledger": "[]PointLedgerItem"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
| `data.totalPoints` | `string` | 个人有效积分兼容别名；数值等于 personalEffectivePoints |
| `data.personalEffectivePoints` | `string` | 个人累计有效积分；包含基础积分、10% 直属邀请奖励和团队运营奖励 |
| `data.personalLevel` | `string` | 个人积分等级严格枚举：L0/L1/L2/L3/L4/L5；达到后永久保留 |
| `data.personalLevelName` | `string` | 个人等级中文名称：普通/青铜/白银/黄金/铂金/黑金 |
| `data.nextPersonalLevel` | `string` | 下一个个人等级；L5 时为空字符串 |
| `data.pointsToNextPersonalLevel` | `string` | 距下一个人等级还需的有效积分；L5 时为 0.000000 |
| `data.basePointsTotal` | `string` | 本人 Buy 和 PledgeCreated 的真实净入曲线金额乘项目倍率后累计的基础积分 |
| `data.directReferralRewardTotal` | `string` | 作为实际直属邀请人累计获得的 10% 邀请奖励 |
| `data.teamOperationRewardTotal` | `string` | 作为推荐树根节点团队长累计获得的 0%-3% 团队运营奖励 |
| `data.referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `data.referrerInviteCode` | `string` | 后端代码字段 referrerInviteCode；含义由所属响应模型和接口业务决定 |
| `data.referralRewardTotal` | `string` | 后端代码字段 referralRewardTotal；含义由所属响应模型和接口业务决定 |
| `data.directReferralCount` | `int` | 数量统计 |
| `data.networkSummary` | `PointNetworkSummary` | 后端代码字段 networkSummary；含义由所属响应模型和接口业务决定 |
| `data.recentActivity` | `PointActivitySummary` | 后端代码字段 recentActivity；含义由所属响应模型和接口业务决定 |
| `data.downlinePoints` | `string` | 当前查询钱包自己的全部下线累计基础积分；不是所属根团队总额，不含奖励积分 |
| `data.teamLeaderAddress` | `string` | 当前推荐树没有上级的唯一根节点钱包地址 |
| `data.isTeamLeader` | `bool` | 当前查询钱包是否为所属推荐树的根节点 |
| `data.teamDownlineBasePoints` | `string` | 根团队全部下线历史累计基础积分；不包含团队长本人、邀请奖励或团队奖励 |
| `data.teamRewardRateBps` | `uint64` | 根团队当前团队运营奖励 BPS；0/50/100/150/200/300 |
| `data.nextTeamRewardThreshold` | `string` | 下一个团队奖励阶梯所需的全部下线累计基础积分；最高档为空 |
| `data.directReferrals` | `[]DirectReferralItem` | 后端代码字段 directReferrals；含义由所属响应模型和接口业务决定 |
| `data.ledger` | `[]PointLedgerItem` | 后端代码字段 ledger；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

Notes: 返回用户积分、个人等级、直属关系及所属根团队的完整摘要。

<!-- ROUTE: GET /api/admin/points/ledger -->
### GET /api/admin/points/ledger

- Controller: `PointController.AdminLedger`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "address": "optional beneficiary",
    "sourceUserAddress": "optional base-point source wallet",
    "teamLeaderAddress": "optional event-time root leader",
    "projectAddress": "optional project",
    "sourceType": "optional point entry type",
    "txHash": "optional transaction hash",
    "startTime": "optional RFC3339",
    "endTime": "optional RFC3339",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.address` | `optional beneficiary` | optional beneficiary |
| `query.sourceUserAddress` | `optional base-point source wallet` | optional base-point source wallet |
| `query.teamLeaderAddress` | `optional event-time root leader` | optional event-time root leader |
| `query.projectAddress` | `optional project` | optional project |
| `query.sourceType` | `optional point entry type` | optional point entry type |
| `query.txHash` | `optional transaction hash` | optional transaction hash |
| `query.startTime` | `optional RFC3339` | optional RFC3339 |
| `query.endTime` | `optional RFC3339` | optional RFC3339 |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]PointLedgerItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]PointLedgerItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 流水不可编辑或删除；金额和积分均为十进制字符串。

<!-- ROUTE: GET /api/admin/points/teams -->
### GET /api/admin/points/teams

- Controller: `PointController.AdminTeams`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]AdminPointTeamItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]AdminPointTeamItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 只列当前没有上级的根节点团队长；团队长没有等级。

<!-- ROUTE: GET /api/admin/points/teams/:address/members -->
### GET /api/admin/points/teams/:address/members

- Controller: `PointController.AdminTeamMembers`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {
    "address": "root team leader wallet"
  },
  "query": {
    "keyword": "optional wallet substring",
    "personalLevel": "optional L0-L5",
    "depth": "optional positive integer",
    "minBasePoints": "optional non-negative decimal with at most 6 places; inclusive minimum",
    "maxBasePoints": "optional non-negative decimal with at most 6 places; inclusive maximum",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `root team leader wallet` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.keyword` | `optional wallet substring` | optional wallet substring |
| `query.personalLevel` | `optional L0-L5` | optional L0-L5 |
| `query.depth` | `optional positive integer` | optional positive integer |
| `query.minBasePoints` | `optional non-negative decimal with at most 6 places; inclusive minimum` | optional non-negative decimal with at most 6 places; inclusive minimum |
| `query.maxBasePoints` | `optional non-negative decimal with at most 6 places; inclusive maximum` | optional non-negative decimal with at most 6 places; inclusive maximum |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]TeamMemberItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]TeamMemberItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 返回整棵推荐树全部下线，不截断为三层；address 已并入其他团队、不是当前根节点时返回参数错误。

<!-- ROUTE: GET /api/admin/points/level-history -->
### GET /api/admin/points/level-history

- Controller: `PointController.AdminLevelHistory`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "address": "optional wallet",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.address` | `optional wallet` | optional wallet |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]PointLevelHistoryItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]PointLevelHistoryItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 个人等级达到后永久保留；一次跨越多个门槛会记录每个跨越等级。

<!-- ROUTE: GET /api/admin/points/reward-rate-history -->
### GET /api/admin/points/reward-rate-history

- Controller: `PointController.AdminRewardRateHistory`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "teamLeaderAddress": "optional root wallet",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.teamLeaderAddress` | `optional root wallet` | optional root wallet |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]TeamRewardRateHistoryItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]TeamRewardRateHistoryItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 团队跨门槛事件记录新比例；新比例从下一笔下线基础积分开始使用。

<!-- ROUTE: GET /api/admin/points/reconciliation -->
### GET /api/admin/points/reconciliation

- Controller: `PointController.AdminReconciliation`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "ledgerEffectivePoints": "string",
      "accountEffectivePoints": "string",
      "ledgerBasePoints": "string",
      "accountBasePoints": "string",
      "teamLedgerBasePoints": "string",
      "teamProfileBasePoints": "string",
      "accountMismatchCount": "uint64",
      "teamMismatchCount": "uint64",
      "consistent": "bool",
      "checkedAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.ledgerEffectivePoints` | `string` | 全部积分流水 points 合计 |
| `data.accountEffectivePoints` | `string` | 全部个人积分账户 effectivePoints 合计；必须等于 ledgerEffectivePoints |
| `data.ledgerBasePoints` | `string` | 流水中 base_buy/base_pledge 基础积分合计 |
| `data.accountBasePoints` | `string` | 个人积分账户 basePoints 合计；必须等于 ledgerBasePoints |
| `data.teamLedgerBasePoints` | `string` | 按当前推荐闭包重算的全部根团队下线基础积分合计 |
| `data.teamProfileBasePoints` | `string` | 当前激活根团队 cumulativeBasePoints 合计；必须等于 teamLedgerBasePoints |
| `data.accountMismatchCount` | `uint64` | 逐钱包比较有效/基础/直属/团队四类账户投影、流水和 L0-L5 后的不一致钱包数 |
| `data.teamMismatchCount` | `uint64` | 逐根团队比较下线人数、基础积分、奖励档位和下一门槛后的不一致团队数 |
| `data.consistent` | `bool` | 后端代码字段 consistent；含义由所属响应模型和接口业务决定 |
| `data.checkedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->

Notes: 逐钱包核对有效/基础/直属/团队四类余额，并逐根团队核对下线人数、累计基础积分和奖励档位；consistent=false 时应执行全量积分重放。

<!-- ROUTE: GET /api/admin/points/project-multipliers -->
### GET /api/admin/points/project-multipliers

- Controller: `PointController.AdminProjectMultipliers`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "keyword": "optional project address/name/symbol",
    "projectAddress": "optional exact project address",
    "state": "optional exact project state",
    "scaleTier": "optional bytes32 tier id",
    "configuredOnly": "optional boolean; project overrides only",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.keyword` | `optional project address/name/symbol` | optional project address/name/symbol |
| `query.projectAddress` | `optional exact project address` | optional exact project address |
| `query.state` | `optional exact project state` | optional exact project state |
| `query.scaleTier` | `optional bytes32 tier id` | optional bytes32 tier id |
| `query.configuredOnly` | `optional boolean; project overrides only` | optional boolean; project overrides only |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "items": "[]AdminProjectPointMultiplierItem",
      "total": "uint64",
      "limit": "int",
      "offset": "int",
      "defaultMultiplier": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]AdminProjectPointMultiplierItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
| `data.defaultMultiplier` | `string` | 全局兜底积分倍率；当前固定为 1 |
<!-- /RESPONSE_FIELDS -->

Notes: 统一查询所有项目的积分倍率。effectiveSource 枚举为 project/scale_tier/default；每个项目默认 canModify=true。configuredOnly=true 表示仅查看存在项目级版本的项目，空结果仍返回 items=[]。

<!-- ROUTE: GET /api/admin/points/projects/:address/multiplier -->
### GET /api/admin/points/projects/:address/multiplier

- Controller: `PointController.AdminProjectMultiplier`
- Auth: points.read

Request:
```json
{
  "headers": {},
  "path": {
    "address": "project contract address"
  },
  "query": {},
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `project contract address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "projectAddress": "string",
      "tokenName": "string",
      "tokenSymbol": "string",
      "state": "string",
      "scaleTier": "string",
      "effectiveMultiplier": "string",
      "effectiveSource": "string",
      "projectMultiplier": "string",
      "scaleTierMultiplier": "string",
      "defaultMultiplier": "string",
      "hasProjectOverride": "bool",
      "canModify": "bool",
      "currentVersion": "int",
      "effectiveAt": "string",
      "updatedBy": "string",
      "scheduledMultiplier": "string",
      "scheduledVersion": "int",
      "scheduledEffectiveAt": "string",
      "createdAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.tokenName` | `string` | 项目 Token 名称 |
| `data.tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `data.state` | `string` | 后端代码字段 state；含义由所属响应模型和接口业务决定 |
| `data.scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `data.effectiveMultiplier` | `string` | 当前时间真正用于该项目新 Buy/PledgeCreated 积分的倍率 |
| `data.effectiveSource` | `string` | 当前倍率来源枚举：project/scale_tier/default，优先级依次降低 |
| `data.projectMultiplier` | `string` | 当前已生效的项目级覆盖倍率；未配置时为空字符串 |
| `data.scaleTierMultiplier` | `string` | 项目所属 Scale Tier 当前倍率；项目无覆盖时继承该值 |
| `data.defaultMultiplier` | `string` | 全局兜底积分倍率；当前固定为 1 |
| `data.hasProjectOverride` | `bool` | 是否存在已生效的项目级倍率覆盖 |
| `data.canModify` | `bool` | 当前项目是否允许新增倍率版本；当前所有项目和状态均为 true |
| `data.currentVersion` | `int` | 后端代码字段 currentVersion；含义由所属响应模型和接口业务决定 |
| `data.effectiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.updatedBy` | `string` | 后端代码字段 updatedBy；含义由所属响应模型和接口业务决定 |
| `data.scheduledMultiplier` | `string` | 最早一个尚未生效的项目级倍率；无计划时为空 |
| `data.scheduledVersion` | `int` | 后端代码字段 scheduledVersion；含义由所属响应模型和接口业务决定 |
| `data.scheduledEffectiveAt` | `string` | 计划项目倍率的 RFC3339 生效时间 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->

Notes: 查询单个项目当前有效倍率、继承的档位倍率、项目覆盖版本和最早待生效版本。项目级倍率优先于 Scale Tier，未配置时最终默认 1。

<!-- ROUTE: POST /api/admin/points/projects/:address/multiplier -->
### POST /api/admin/points/projects/:address/multiplier

- Controller: `PointController.UpsertProjectMultiplier`
- Auth: points.write

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web"
  },
  "path": {
    "address": "project contract address"
  },
  "query": {},
  "body": {
    "multiplier": "string",
    "effectiveAt": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.address` | `project contract address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `body.multiplier` | `string` | 后端代码字段 multiplier；含义由所属响应模型和接口业务决定 |
| `body.effectiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "executed": "bool",
      "config": "*PlatformConfigItem",
      "project": "AdminProjectPointMultiplierItem"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.executed` | `bool` | 后端代码字段 executed；含义由所属响应模型和接口业务决定 |
| `data.config` | `*PlatformConfigItem` | 后端代码字段 config；含义由所属响应模型和接口业务决定 |
| `data.project` | `AdminProjectPointMultiplierItem` | 后端代码字段 project；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

Notes: body strictly matches `UpsertProjectPointMultiplierRequest`; unknown fields return 400. 为单个项目新增不可变版本，不修改合约。只影响 effectiveAt 之后新发生的 Buy/PledgeCreated；历史流水、等级和团队奖励不自动重算。所有项目状态均允许配置，但已毕业或失败项目通常不会再产生基础积分。

<!-- ROUTE: GET /api/admin/operation-ledger -->
### GET /api/admin/operation-ledger

- Controller: `MarketController.AdminOperationLedger`
- Auth: audit.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "actorAddress": "optional",
    "projectAddress": "optional",
    "source": "optional",
    "action": "optional",
    "limit": "default 50 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.actorAddress` | `optional` | optional |
| `query.projectAddress` | `optional` | optional |
| `query.source` | `optional` | optional |
| `query.action` | `optional` | optional |
| `query.limit` | `default 50 max 200` | default 50 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]OperationLedgerItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]OperationLedgerItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/fee-income-ledger -->
### GET /api/admin/fee-income-ledger

- Controller: `MarketController.AdminFeeIncomeLedger`
- Auth: treasury.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "projectAddress": "optional",
    "recipientAddress": "optional",
    "incomeType": "optional",
    "limit": "default 50 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.projectAddress` | `optional` | optional |
| `query.recipientAddress` | `optional` | optional |
| `query.incomeType` | `optional` | optional |
| `query.limit` | `default 50 max 200` | default 50 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]FeeIncomeLedgerItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]FeeIncomeLedgerItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/dashboard -->
### GET /api/admin/dashboard

- Controller: `AdminOpsController.Dashboard`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"overview": "TestnetOverviewResponse", "supportMetrics": "SupportMetrics"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.overview` | `TestnetOverviewResponse` | 后端代码字段 overview；含义由所属响应模型和接口业务决定 |
| `data.supportMetrics` | `SupportMetrics` | 后端代码字段 supportMetrics；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/admin/analytics/overview -->
### GET /api/admin/analytics/overview

- Controller: `AnalyticsController.AdminPlatform`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "window": "optional analytics time window"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.window` | `optional analytics time window` | optional analytics time window |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "window": "string",
      "dataSource": "string",
      "generatedAt": "string",
      "overview": "PlatformAnalyticsOverview",
      "capital": "PlatformCapitalStats",
      "operations": "PlatformOperationStats",
      "indexer": "PlatformIndexerStats",
      "support": "PlatformSupportStats"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.window` | `string` | 后端代码字段 window；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `data.generatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.overview` | `PlatformAnalyticsOverview` | 后端代码字段 overview；含义由所属响应模型和接口业务决定 |
| `data.capital` | `PlatformCapitalStats` | 后端代码字段 capital；含义由所属响应模型和接口业务决定 |
| `data.operations` | `PlatformOperationStats` | 后端代码字段 operations；含义由所属响应模型和接口业务决定 |
| `data.indexer` | `PlatformIndexerStats` | 后端代码字段 indexer；含义由所属响应模型和接口业务决定 |
| `data.support` | `PlatformSupportStats` | 后端代码字段 support；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/admin/analytics/projects -->
### GET /api/admin/analytics/projects

- Controller: `AnalyticsController.AdminProjects`
- Auth: project.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "keyword": "optional",
    "status": "optional",
    "projectAddress": "optional",
    "userAddress": "optional",
    "actionType": "optional",
    "source": "optional",
    "dateFrom": "optional",
    "dateTo": "optional",
    "sortBy": "optional; preferred",
    "sort": "optional alias",
    "sortOrder": "optional; preferred",
    "direction": "optional alias",
    "limit": "default 30 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.keyword` | `optional` | optional |
| `query.status` | `optional` | optional |
| `query.projectAddress` | `optional` | optional |
| `query.userAddress` | `optional` | optional |
| `query.actionType` | `optional` | optional |
| `query.source` | `optional` | optional |
| `query.dateFrom` | `optional` | optional |
| `query.dateTo` | `optional` | optional |
| `query.sortBy` | `optional; preferred` | optional; preferred |
| `query.sort` | `optional alias` | optional alias |
| `query.sortOrder` | `optional; preferred` | optional; preferred |
| `query.direction` | `optional alias` | optional alias |
| `query.limit` | `default 30 max 200` | default 30 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"items":"[]AdminProjectQueryItem","total":"int64","limit":"int","offset":"int","sort":"AnalyticsSort"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]AdminProjectQueryItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `int64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
| `data.sort` | `AnalyticsSort` | 后端代码字段 sort；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/analytics/users -->
### GET /api/admin/analytics/users

- Controller: `AnalyticsController.AdminUsers`
- Auth: member.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "keyword": "optional",
    "status": "optional",
    "projectAddress": "optional",
    "userAddress": "optional",
    "actionType": "optional",
    "source": "optional",
    "dateFrom": "optional",
    "dateTo": "optional",
    "sortBy": "optional; preferred",
    "sort": "optional alias",
    "sortOrder": "optional; preferred",
    "direction": "optional alias",
    "limit": "default 30 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.keyword` | `optional` | optional |
| `query.status` | `optional` | optional |
| `query.projectAddress` | `optional` | optional |
| `query.userAddress` | `optional` | optional |
| `query.actionType` | `optional` | optional |
| `query.source` | `optional` | optional |
| `query.dateFrom` | `optional` | optional |
| `query.dateTo` | `optional` | optional |
| `query.sortBy` | `optional; preferred` | optional; preferred |
| `query.sort` | `optional alias` | optional alias |
| `query.sortOrder` | `optional; preferred` | optional; preferred |
| `query.direction` | `optional alias` | optional alias |
| `query.limit` | `default 30 max 200` | default 30 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"items":"[]AdminUserQueryItem","total":"int64","limit":"int","offset":"int","sort":"AnalyticsSort"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]AdminUserQueryItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `int64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
| `data.sort` | `AnalyticsSort` | 后端代码字段 sort；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/analytics/transactions -->
### GET /api/admin/analytics/transactions

- Controller: `AnalyticsController.AdminTransactions`
- Auth: audit.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "keyword": "optional",
    "status": "optional",
    "projectAddress": "optional",
    "userAddress": "optional",
    "actionType": "optional",
    "source": "optional",
    "dateFrom": "optional",
    "dateTo": "optional",
    "sortBy": "optional; preferred",
    "sort": "optional alias",
    "sortOrder": "optional; preferred",
    "direction": "optional alias",
    "limit": "default 30 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.keyword` | `optional` | optional |
| `query.status` | `optional` | optional |
| `query.projectAddress` | `optional` | optional |
| `query.userAddress` | `optional` | optional |
| `query.actionType` | `optional` | optional |
| `query.source` | `optional` | optional |
| `query.dateFrom` | `optional` | optional |
| `query.dateTo` | `optional` | optional |
| `query.sortBy` | `optional; preferred` | optional; preferred |
| `query.sort` | `optional alias` | optional alias |
| `query.sortOrder` | `optional; preferred` | optional; preferred |
| `query.direction` | `optional alias` | optional alias |
| `query.limit` | `default 30 max 200` | default 30 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"items":"[]AdminTransactionQueryItem","total":"int64","limit":"int","offset":"int","sort":"AnalyticsSort"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]AdminTransactionQueryItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `int64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
| `data.sort` | `AnalyticsSort` | 后端代码字段 sort；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: POST /api/admin/targets/:targetType/:targetId/metadata -->
### POST /api/admin/targets/:targetType/:targetId/metadata

- Controller: `AnalyticsController.UpsertTargetMetadata`
- Auth: admin.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {
    "targetType": "path targetType",
    "targetId": "path targetId"
  },
  "query": {},
  "body": {
    "tags": "[]string",
    "note": "string",
    "status": "string",
    "riskLevel": "string",
    "reason": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.targetType` | `path targetType` | 运营标签目标类型；user/member 都规范化为 user，project 表示项目 |
| `path.targetId` | `path targetId` | 目标用户或项目的 EVM 地址 |
| `body.tags` | `[]string` | 运营自定义标签列表，只影响后台读模型 |
| `body.note` | `string` | 运营备注，最长 2000 字符，不改变链上状态 |
| `body.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `body.riskLevel` | `string` | 风险等级；运营标签允许空、low、medium、high、critical，不会自动改变链上权限 |
| `body.reason` | `string` | 本次操作原因，用于审计追溯 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "targetType": "string",
      "targetId": "string",
      "tags": "[]string",
      "tagsJson": "string",
      "note": "string",
      "status": "string",
      "riskLevel": "string",
      "updatedBy": "string",
      "createdAt": "string",
      "updatedAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.targetType` | `string` | 运营标签目标类型；user/member 都规范化为 user，project 表示项目 |
| `data.targetId` | `string` | 目标用户或项目的 EVM 地址 |
| `data.tags` | `[]string` | 运营自定义标签列表，只影响后台读模型 |
| `data.tagsJson` | `string` | 后端代码字段 tagsJson；含义由所属响应模型和接口业务决定 |
| `data.note` | `string` | 运营备注，最长 2000 字符，不改变链上状态 |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.riskLevel` | `string` | 风险等级；运营标签允许空、low、medium、high、critical，不会自动改变链上权限 |
| `data.updatedBy` | `string` | 后端代码字段 updatedBy；含义由所属响应模型和接口业务决定 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `AdminTargetMetadataRequest`; unknown fields return 400. `targetType` 接受 `user/member/project`，其中 `member` 会规范化为 `user`；`targetId` 必须是有效 EVM 地址。`status` 仅接受空字符串、`normal/watch/restricted/blocked`，`riskLevel` 仅接受空字符串、`low/medium/high/critical`。这些字段只改变后台运营读模型，不会限制钱包、项目或链上交易；实际限制需使用灰黑名单或合约规则。`note` 最长 2000 字符，`reason` 最长 512 字符并进入审计上下文。

<!-- ROUTE: GET /api/admin/projects -->
### GET /api/admin/projects

- Controller: `AdminOpsController.Projects`
- Auth: project.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "keyword": "optional",
    "status": "optional",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.keyword` | `optional` | optional |
| `query.status` | `optional` | optional |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]ProjectListItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]ProjectListItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/projects/market -->
### GET /api/admin/projects/market

- Controller: `MarketController.AdminProjectMarketCatalog`
- Auth: project.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "keyword": "optional",
    "tab": "optional",
    "sort": "optional",
    "direction": "optional",
    "state": "optional",
    "scaleTier": "optional",
    "minMarketCap": "optional",
    "maxMarketCap": "optional",
    "minVolume24h": "optional",
    "verified": "optional",
    "limit": "default 30 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.keyword` | `optional` | optional |
| `query.tab` | `optional` | optional |
| `query.sort` | `optional` | optional |
| `query.direction` | `optional` | optional |
| `query.state` | `optional` | optional |
| `query.scaleTier` | `optional` | optional |
| `query.minMarketCap` | `optional` | optional |
| `query.maxMarketCap` | `optional` | optional |
| `query.minVolume24h` | `optional` | optional |
| `query.verified` | `optional` | optional |
| `query.limit` | `default 30 max 200` | default 30 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "query": "ProjectMarketQuery",
      "summary": "ProjectMarketSummary",
      "facets": "ProjectMarketFacets",
      "items": "[]ProjectMarketItem",
      "total": "uint64",
      "limit": "int",
      "offset": "int",
      "generatedAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.query` | `ProjectMarketQuery` | 后端代码字段 query；含义由所属响应模型和接口业务决定 |
| `data.summary` | `ProjectMarketSummary` | 后端代码字段 summary；含义由所属响应模型和接口业务决定 |
| `data.facets` | `ProjectMarketFacets` | 后端代码字段 facets；含义由所属响应模型和接口业务决定 |
| `data.items` | `[]ProjectMarketItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
| `data.generatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/projects/:address/full -->
### GET /api/admin/projects/:address/full

- Controller: `AdminOpsController.ProjectFull`
- Auth: project.read

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "runtime": "ProjectRuntimeResponse",
      "klineHealth": "KlineHealthResponse",
      "reconcile": "ProjectReconcileResponse",
      "fees": "ProjectFeesResponse",
      "feeIncomeLedger": "[]FeeIncomeLedgerItem",
      "operationLedger": "[]OperationLedgerItem",
      "incomeRecords": "[]TreasuryIncomeRecord",
      "auditLogs": "[]AdminActionAuditLog",
      "operationalFlags": "[]string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.runtime` | `ProjectRuntimeResponse` | 后端代码字段 runtime；含义由所属响应模型和接口业务决定 |
| `data.klineHealth` | `KlineHealthResponse` | 后端代码字段 klineHealth；含义由所属响应模型和接口业务决定 |
| `data.reconcile` | `ProjectReconcileResponse` | 后端代码字段 reconcile；含义由所属响应模型和接口业务决定 |
| `data.fees` | `ProjectFeesResponse` | 后端代码字段 fees；含义由所属响应模型和接口业务决定 |
| `data.feeIncomeLedger` | `[]FeeIncomeLedgerItem` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.operationLedger` | `[]OperationLedgerItem` | 后端代码字段 operationLedger；含义由所属响应模型和接口业务决定 |
| `data.incomeRecords` | `[]TreasuryIncomeRecord` | 后端代码字段 incomeRecords；含义由所属响应模型和接口业务决定 |
| `data.auditLogs` | `[]AdminActionAuditLog` | 后端代码字段 auditLogs；含义由所属响应模型和接口业务决定 |
| `data.operationalFlags` | `[]string` | 后端代码字段 operationalFlags；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/admin/projects/:address/reconcile -->
### GET /api/admin/projects/:address/reconcile

- Controller: `AdminOpsController.ProjectReconcile`
- Auth: project.read

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "projectAddress": "string",
      "status": "string",
      "dataSource": "string",
      "differences": "[]ReconcileDiff",
      "chainState": "map[string]string",
      "indexedState": "map[string]string",
      "recommendedAction": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `data.differences` | `[]ReconcileDiff` | 后端代码字段 differences；含义由所属响应模型和接口业务决定 |
| `data.chainState` | `map[string]string` | 后端代码字段 chainState；含义由所属响应模型和接口业务决定 |
| `data.indexedState` | `map[string]string` | 后端代码字段 indexedState；含义由所属响应模型和接口业务决定 |
| `data.recommendedAction` | `string` | 后端代码字段 recommendedAction；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/admin/projects/:address/kline-health -->
### GET /api/admin/projects/:address/kline-health

- Controller: `AdminOpsController.ProjectKlineHealth`
- Auth: project.read

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "projectAddress": "string",
      "status": "string",
      "dataSource": "string",
      "tradeEvents": "int64",
      "candleCount": "int64",
      "duplicateEvents": "int64",
      "lastTradeAt": "string",
      "lastCandleAt": "string",
      "lastTradeBlock": "int64",
      "workerBlockLag": "uint64",
      "needsRebuild": "bool",
      "recommendedAction": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `data.tradeEvents` | `int64` | 后端代码字段 tradeEvents；含义由所属响应模型和接口业务决定 |
| `data.candleCount` | `int64` | 数量统计 |
| `data.duplicateEvents` | `int64` | 后端代码字段 duplicateEvents；含义由所属响应模型和接口业务决定 |
| `data.lastTradeAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.lastCandleAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.lastTradeBlock` | `int64` | 后端代码字段 lastTradeBlock；含义由所属响应模型和接口业务决定 |
| `data.workerBlockLag` | `uint64` | 后端代码字段 workerBlockLag；含义由所属响应模型和接口业务决定 |
| `data.needsRebuild` | `bool` | 后端代码字段 needsRebuild；含义由所属响应模型和接口业务决定 |
| `data.recommendedAction` | `string` | 后端代码字段 recommendedAction；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/admin/members -->
### GET /api/admin/members

- Controller: `AdminOpsController.Members`
- Auth: member.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "keyword": "optional",
    "status": "optional",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.keyword` | `optional` | optional |
| `query.status` | `optional` | optional |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]AdminMemberListItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]AdminMemberListItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/admin/members/:address/full -->
### GET /api/admin/members/:address/full

- Controller: `AdminOpsController.MemberFull`
- Auth: member.read

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "summary": "MemberSummary",
      "supportConversations": "[]SupportConversation",
      "activity": "[]MemberActivityLog",
      "transactions": "[]UserTransactionItem"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.summary` | `MemberSummary` | 后端代码字段 summary；含义由所属响应模型和接口业务决定 |
| `data.supportConversations` | `[]SupportConversation` | 后端代码字段 supportConversations；含义由所属响应模型和接口业务决定 |
| `data.activity` | `[]MemberActivityLog` | 后端代码字段 activity；含义由所属响应模型和接口业务决定 |
| `data.transactions` | `[]UserTransactionItem` | 后端代码字段 transactions；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/admin/support/conversations -->
### GET /api/admin/support/conversations

- Controller: `AdminOpsController.SupportConversations`
- Auth: support.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "status": "optional",
    "category": "optional",
    "priority": "optional",
    "assignedAdmin": "optional",
    "userAddress": "optional",
    "keyword": "optional",
    "limit": "default 30 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.status` | `optional` | optional |
| `query.category` | `optional` | optional |
| `query.priority` | `optional` | optional |
| `query.assignedAdmin` | `optional` | optional |
| `query.userAddress` | `optional` | optional |
| `query.keyword` | `optional` | optional |
| `query.limit` | `default 30 max 200` | default 30 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]SupportConversation",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]SupportConversation` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/support/conversations/:id -->
### GET /api/admin/support/conversations/:id

- Controller: `AdminOpsController.SupportConversationDetail`
- Auth: support.read

Request:
```json
{
  "headers": {},
  "path": {
    "id": "path id"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.id` | `path id` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "conversation": "SupportConversation",
      "messages": "[]SupportMessage"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.conversation` | `SupportConversation` | 后端代码字段 conversation；含义由所属响应模型和接口业务决定 |
| `data.messages` | `[]SupportMessage` | 后端代码字段 messages；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: POST /api/admin/support/conversations/:id/assign -->
### POST /api/admin/support/conversations/:id/assign

- Controller: `AdminOpsController.SupportAssign`
- Auth: support.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {
    "id": "path id"
  },
  "query": {},
  "body": {
    "assignedAdmin": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.id` | `path id` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `body.assignedAdmin` | `string` | 后端代码字段 assignedAdmin；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"status": "assigned"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `assigned` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `AssignSupportConversationRequest`; unknown fields return 400. Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: POST /api/admin/support/conversations/:id/messages -->
### POST /api/admin/support/conversations/:id/messages

- Controller: `AdminOpsController.SupportReply`
- Auth: support.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {
    "id": "path id"
  },
  "query": {},
  "body": {
    "body": "string",
    "attachmentsJson": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.id` | `path id` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `body.body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `body.attachmentsJson` | `string` | 后端代码字段 attachmentsJson；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "id": "uint64",
      "conversationId": "uint64",
      "senderType": "string",
      "senderAddress": "string",
      "body": "string",
      "attachmentsJson": "string",
      "createdAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `data.conversationId` | `uint64` | 后端代码字段 conversationId；含义由所属响应模型和接口业务决定 |
| `data.senderType` | `string` | 后端代码字段 senderType；含义由所属响应模型和接口业务决定 |
| `data.senderAddress` | `string` | 合约或钱包的 EVM 地址 |
| `data.body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `data.attachmentsJson` | `string` | 后端代码字段 attachmentsJson；含义由所属响应模型和接口业务决定 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `SendSupportMessageRequest`; unknown fields return 400. Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: POST /api/admin/support/conversations/:id/status -->
### POST /api/admin/support/conversations/:id/status

- Controller: `AdminOpsController.SupportStatus`
- Auth: support.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {
    "id": "path id"
  },
  "query": {},
  "body": {
    "status": "string",
    "reason": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.id` | `path id` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `body.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `body.reason` | `string` | 本次操作原因，用于审计追溯 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"status": "<body.status>"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `<body.status>` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `UpdateSupportStatusRequest`; unknown fields return 400. Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/admin/support/metrics -->
### GET /api/admin/support/metrics

- Controller: `AdminOpsController.SupportMetrics`
- Auth: support.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "openCount": "int",
      "pendingCount": "int",
      "resolvedCount": "int",
      "closedCount": "int",
      "unassignedCount": "int",
      "avgFirstReplyMinutes": "float64"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.openCount` | `int` | 数量统计 |
| `data.pendingCount` | `int` | 数量统计 |
| `data.resolvedCount` | `int` | 数量统计 |
| `data.closedCount` | `int` | 数量统计 |
| `data.unassignedCount` | `int` | 数量统计 |
| `data.avgFirstReplyMinutes` | `float64` | 后端代码字段 avgFirstReplyMinutes；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/admin/support/canned-replies -->
### GET /api/admin/support/canned-replies

- Controller: `AdminOpsController.CannedReplies`
- Auth: support.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "category": "optional",
    "includeInactive": "optional bool string",
    "limit": "default 30, max 100",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.category` | `optional` | optional |
| `query.includeInactive` | `optional bool string` | optional bool string |
| `query.limit` | `default 30, max 100` | default 30, max 100 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]SupportCannedReply",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]SupportCannedReply` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: POST /api/admin/support/canned-replies -->
### POST /api/admin/support/canned-replies

- Controller: `AdminOpsController.UpsertCannedReply`
- Auth: support.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "id": "uint64",
    "title": "string",
    "body": "string",
    "category": "string",
    "isActive": "*bool"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `body.title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `body.body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `body.category` | `string` | 后端代码字段 category；含义由所属响应模型和接口业务决定 |
| `body.isActive` | `*bool` | 布尔状态标记 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "id": "uint64",
      "title": "string",
      "body": "string",
      "category": "string",
      "isActive": "bool",
      "createdBy": "string",
      "updatedBy": "string",
      "createdAt": "string",
      "updatedAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `data.title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `data.body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `data.category` | `string` | 后端代码字段 category；含义由所属响应模型和接口业务决定 |
| `data.isActive` | `bool` | 布尔状态标记 |
| `data.createdBy` | `string` | 后端代码字段 createdBy；含义由所属响应模型和接口业务决定 |
| `data.updatedBy` | `string` | 后端代码字段 updatedBy；含义由所属响应模型和接口业务决定 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `UpsertSupportCannedReplyRequest`; unknown fields return 400.

<!-- ROUTE: DELETE /api/admin/support/canned-replies/:id -->
### DELETE /api/admin/support/canned-replies/:id

- Controller: `AdminOpsController.DeleteCannedReply`
- Auth: support.write

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web"
  },
  "path": {
    "id": "path id"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.id` | `path id` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"status": "disabled"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `disabled` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/admin/treasury/income-records -->
### GET /api/admin/treasury/income-records

- Controller: `AdminOpsController.TreasuryIncomeRecords`
- Auth: treasury.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "projectAddress": "optional",
    "source": "optional",
    "limit": "default 50, max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.projectAddress` | `optional` | optional |
| `query.source` | `optional` | optional |
| `query.limit` | `default 50, max 200` | default 50, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]TreasuryIncomeRecord",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]TreasuryIncomeRecord` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/smoke-runs -->
### GET /api/admin/smoke-runs

- Controller: `TestnetController.SmokeRuns`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "limit": "default 20, max 100",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.limit` | `default 20, max 100` | default 20, max 100 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]SmokeRunSummary",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]SmokeRunSummary` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: POST /api/admin/smoke-runs -->
### POST /api/admin/smoke-runs

- Controller: `TestnetController.RegisterSmokeRun`
- Auth: admin.write

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "runId": "string",
    "network": "string",
    "chainId": "int64",
    "deploymentProfile": "string",
    "platformAddress": "string",
    "projectAddress": "string",
    "projectSymbol": "string",
    "status": "string",
    "currentStage": "string",
    "startedAt": "string",
    "finishedAt": "string",
    "summaryJson": "string",
    "reportPath": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.runId` | `string` | 后端代码字段 runId；含义由所属响应模型和接口业务决定 |
| `body.network` | `string` | 后端代码字段 network；含义由所属响应模型和接口业务决定 |
| `body.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `body.deploymentProfile` | `string` | 后端代码字段 deploymentProfile；含义由所属响应模型和接口业务决定 |
| `body.platformAddress` | `string` | 合约或钱包的 EVM 地址 |
| `body.projectAddress` | `string` | 项目合约地址 |
| `body.projectSymbol` | `string` | 后端代码字段 projectSymbol；含义由所属响应模型和接口业务决定 |
| `body.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `body.currentStage` | `string` | 后端代码字段 currentStage；含义由所属响应模型和接口业务决定 |
| `body.startedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `body.finishedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `body.summaryJson` | `string` | 后端代码字段 summaryJson；含义由所属响应模型和接口业务决定 |
| `body.reportPath` | `string` | 后端代码字段 reportPath；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"status": "registered"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `registered` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `SmokeRunRegisterRequest`; unknown fields return 400.

<!-- ROUTE: GET /api/admin/indexer/runs -->
### GET /api/admin/indexer/runs

- Controller: `TestnetController.IndexerRuns`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "limit": "default 30, max 100",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.limit` | `default 30, max 100` | default 30, max 100 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]WorkerRunSummary",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]WorkerRunSummary` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/admin/rpc-health -->
### GET /api/admin/rpc-health

- Controller: `TestnetController.RPCHealth`
- Auth: admin.read

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "status": "string",
      "rpcUrl": "string",
      "latestBlock": "uint64",
      "latencyMs": "int",
      "sampledAt": "string",
      "errorMessage": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.rpcUrl` | `string` | 可公开给前端的当前网络 RPC；后端内部使用的付费 RPC 及密钥绝不通过此字段返回 |
| `data.latestBlock` | `uint64` | 后端代码字段 latestBlock；含义由所属响应模型和接口业务决定 |
| `data.latencyMs` | `int` | 后端代码字段 latencyMs；含义由所属响应模型和接口业务决定 |
| `data.sampledAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.errorMessage` | `string` | 后端代码字段 errorMessage；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/admin/public/graylist-rules -->
### GET /api/admin/public/graylist-rules

- Controller: `AdminController.PublicGraylistRules`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "rule": "string",
    "items": "[]ProjectGraylist"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.rule` | `string` | 后端代码字段 rule；含义由所属响应模型和接口业务决定 |
| `data.items` | `[]ProjectGraylist` | 当前页数据列表；空结果返回 [] |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/admin/public/multisig-members -->
### GET /api/admin/public/multisig-members

- Controller: `AdminController.PublicMultisigMembers`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "contractAddress": "string",
    "signers": "[]string",
    "threshold": "uint64",
    "snapshotBlock": "uint64"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.contractAddress` | `string` | 合约或钱包的 EVM 地址 |
| `data.signers` | `[]string` | 后端代码字段 signers；含义由所属响应模型和接口业务决定 |
| `data.threshold` | `uint64` | 后端代码字段 threshold；含义由所属响应模型和接口业务决定 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/config/public -->
### GET /api/config/public

- Controller: `ConfigController.Public`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "protocolVersion": "string",
    "chainId": "int64",
    "chainName": "string",
    "rpcUrl": "string",
    "explorerUrl": "string",
    "stableTokenAddress": "string",
    "stableTokenSymbol": "string",
    "stableTokenDecimals": "uint8",
    "platformAddress": "string",
    "platformMultisig": "string",
    "platformPayout": "string",
    "platformAdmin": "string",
    "creationFeeAdmin": "string",
    "curveTableRegistry": "string",
    "projectImplementation": "string",
    "projectRules": "string",
    "graduationAdapter": "string",
    "graduationEnabled": "bool",
    "graduationDisabledReason": "string",
    "bridgeEnabled": "bool",
    "bridgeDisabledReason": "string",
    "uniswapV4Hook": "string",
    "uniswapV4PositionManager": "string",
    "uniswapV4PoolManager": "string",
    "uniswapV4Permit2": "string",
    "creationFee": "string",
    "nodeRaiseTarget": "string",
    "sourceBundleHash": "string",
    "deploymentStartBlock": "uint64",
    "scaleTiers": "[]ScaleTierSnapshot",
    "dependencyIntegrity": "DependencyIntegritySnapshot",
    "walletConnect": "WalletConnectConfig"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.protocolVersion` | `string` | 当前后端对齐的 SeedX 协议版本；版本不一致时前端应阻止高风险写操作 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.chainName` | `string` | 后端代码字段 chainName；含义由所属响应模型和接口业务决定 |
| `data.rpcUrl` | `string` | 可公开给前端的当前网络 RPC；后端内部使用的付费 RPC 及密钥绝不通过此字段返回 |
| `data.explorerUrl` | `string` | 后端代码字段 explorerUrl；含义由所属响应模型和接口业务决定 |
| `data.stableTokenAddress` | `string` | 当前网络配置的稳定币合约地址 |
| `data.stableTokenSymbol` | `string` | 后端代码字段 stableTokenSymbol；含义由所属响应模型和接口业务决定 |
| `data.stableTokenDecimals` | `uint8` | Stable Token 的真实精度；Ethereum Sepolia 为 USDGTest 6 位，项目 Token 仍为 18 位 |
| `data.platformAddress` | `string` | 合约或钱包的 EVM 地址 |
| `data.platformMultisig` | `string` | 后端代码字段 platformMultisig；含义由所属响应模型和接口业务决定 |
| `data.platformPayout` | `string` | 后端代码字段 platformPayout；含义由所属响应模型和接口业务决定 |
| `data.platformAdmin` | `string` | 后端代码字段 platformAdmin；含义由所属响应模型和接口业务决定 |
| `data.creationFeeAdmin` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.curveTableRegistry` | `string` | 后端代码字段 curveTableRegistry；含义由所属响应模型和接口业务决定 |
| `data.projectImplementation` | `string` | 后端代码字段 projectImplementation；含义由所属响应模型和接口业务决定 |
| `data.projectRules` | `string` | 当前部署 SeedXProjectRules 库地址；用于核对后端预检与合约规则版本 |
| `data.graduationAdapter` | `string` | 毕业 Adapter 地址；enabled=true 时必须等于 Platform.trustedGraduationAdapter() |
| `data.graduationEnabled` | `bool` | 是否可以发起毕业及 LP 手续费收取写操作；Ethereum Sepolia 已使用官方 Uniswap v4 完成验证，因此为 true |
| `data.graduationDisabledReason` | `string` | 后端代码字段 graduationDisabledReason；含义由所属响应模型和接口业务决定 |
| `data.bridgeEnabled` | `bool` | 是否开放 Relay 跨链报价和订单；当前测试服独立开启 Sepolia 与 Base Sepolia 原生 ETH 双向测试路线 |
| `data.bridgeDisabledReason` | `string` | 跨链桥禁用原因；前端应展示或用于禁用提示 |
| `data.uniswapV4Hook` | `string` | 后端代码字段 uniswapV4Hook；含义由所属响应模型和接口业务决定 |
| `data.uniswapV4PositionManager` | `string` | 后端代码字段 uniswapV4PositionManager；含义由所属响应模型和接口业务决定 |
| `data.uniswapV4PoolManager` | `string` | 后端代码字段 uniswapV4PoolManager；含义由所属响应模型和接口业务决定 |
| `data.uniswapV4Permit2` | `string` | 后端代码字段 uniswapV4Permit2；含义由所属响应模型和接口业务决定 |
| `data.creationFee` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.nodeRaiseTarget` | `string` | 平台固定节点募集目标；当前为 30000 USDGTest，建项必须满足 nodeCount * nodePrice = nodeRaiseTarget |
| `data.sourceBundleHash` | `string` | Sepolia 20260821 交接包 source bundle SHA-256 哈希；必须为 22d7168618d2674546aeae149eaec03055cb3fdfa13830ddafacd3a03f8c2a92 |
| `data.deploymentStartBlock` | `uint64` | 当前协议部署首区块 11535261；Worker 全量重扫起点 |
| `data.scaleTiers` | `[]ScaleTierSnapshot` | 后端代码字段 scaleTiers；含义由所属响应模型和接口业务决定 |
| `data.dependencyIntegrity` | `DependencyIntegritySnapshot` | 当前 Platform、固定 Uniswap v4 Adapter 和官方外部依赖的链上 runtime 完整性快照 |
| `data.walletConnect` | `WalletConnectConfig` | 后端代码字段 walletConnect；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: `protocolVersion=seedx_sepolia_20260821_22d71686`; `sourceBundleHash` identifies the Sepolia 20260821 handoff. `stableTokenSymbol=USDGTest` and `stableTokenDecimals=6`; project Token uses 18 decimals. `nodeRaiseTarget=30000` requires `nodeCount * nodePrice = 30000 USDGTest`. The enabled Tier is `sepolia-uniswap-v4-production-v2`; `scaleTiers[].version` is 2, with `curveFinalized=true`, `curvePointCount=513`, and verified `dependencyIntegrity`. Its project economic rates are read-only: `buyFeeRatePpm=30000` (3%), `sellFeeRatePpm=50000` (5%), `profitTaxRatePpm=200000` (20%). Initial fee and tax splits are floor/node/issuer/platform = 6000/2000/1000/1000 BPS. Floor and platform shares are immutable; governance may only rebalance the 3000 BPS node+issuer portion through paramId 5/8. `graduationEnabled=true`; `bridgeEnabled=true` is an independent Relay testnet capability. Frontend must use GET /api/bridge/config for the active route whitelist and require matching curve/dependency integrity before high-risk writes.

<!-- ROUTE: GET /api/bridge/config -->
### GET /api/bridge/config

- Controller: `BridgeController.Config`
- Auth: 无

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "enabled": "bool",
    "disabledReason": "string",
    "mode": "string",
    "slippageBps": "int",
    "routes": "[]BridgeRouteConfig"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.enabled` | `bool` | 档位是否启用；true 时执行全部 Registry/codehash/Adapter/参数约束，false 时链上清除 curveCodeHashByTier |
| `data.disabledReason` | `string` | 后端代码字段 disabledReason；含义由所属响应模型和接口业务决定 |
| `data.mode` | `string` | 后端代码字段 mode；含义由所属响应模型和接口业务决定 |
| `data.slippageBps` | `int` | 万分比整数，10000 表示 100% |
| `data.routes` | `[]BridgeRouteConfig` | 后端代码字段 routes；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

Notes: 前端必须先读取 `enabled` 和 `routes`。当前测试服返回 `enabled=true`、`mode=testnet`，仅开放 Sepolia `11155111` 与 Base Sepolia `84532` 的原生 ETH 双向路线；测试币没有价值，禁止混用主网资产或自行扩展路线。

<!-- ROUTE: POST /api/bridge/quote -->
### POST /api/bridge/quote

- Controller: `BridgeController.Quote`
- Auth: 登录钱包 Cookie 会话；浏览器必须携带 `X-SeedX-Client: web`

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web",
    "Content-Type": "application/json"
  },
  "path": {},
  "query": {},
  "body": {
    "originChainId": "int64",
    "originCurrency": "string",
    "destinationChainId": "int64",
    "destinationCurrency": "string",
    "amount": "string",
    "recipient": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `body.originChainId` | `int64` | 后端代码字段 originChainId；含义由所属响应模型和接口业务决定 |
| `body.originCurrency` | `string` | 后端代码字段 originCurrency；含义由所属响应模型和接口业务决定 |
| `body.destinationChainId` | `int64` | 后端代码字段 destinationChainId；含义由所属响应模型和接口业务决定 |
| `body.destinationCurrency` | `string` | 后端代码字段 destinationCurrency；含义由所属响应模型和接口业务决定 |
| `body.amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `body.recipient` | `string` | 后端代码字段 recipient；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "orderId": "string",
    "relayRequestId": "string",
    "originChainId": "int64",
    "originCurrency": "string",
    "amount": "string",
    "destinationChainId": "int64",
    "destinationCurrency": "string",
    "recipient": "string",
    "statusEndpointPresent": "bool",
    "quote": "json.RawMessage",
    "createdAt": "string"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.orderId` | `string` | 后端代码字段 orderId；含义由所属响应模型和接口业务决定 |
| `data.relayRequestId` | `string` | 后端代码字段 relayRequestId；含义由所属响应模型和接口业务决定 |
| `data.originChainId` | `int64` | 后端代码字段 originChainId；含义由所属响应模型和接口业务决定 |
| `data.originCurrency` | `string` | 后端代码字段 originCurrency；含义由所属响应模型和接口业务决定 |
| `data.amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `data.destinationChainId` | `int64` | 后端代码字段 destinationChainId；含义由所属响应模型和接口业务决定 |
| `data.destinationCurrency` | `string` | 后端代码字段 destinationCurrency；含义由所属响应模型和接口业务决定 |
| `data.recipient` | `string` | 后端代码字段 recipient；含义由所属响应模型和接口业务决定 |
| `data.statusEndpointPresent` | `bool` | 后端代码字段 statusEndpointPresent；含义由所属响应模型和接口业务决定 |
| `data.quote` | `json.RawMessage` | 后端代码字段 quote；含义由所属响应模型和接口业务决定 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->

Notes: body strictly matches `BridgeQuoteRequest`. The current test server enables only native ETH between Sepolia `11155111` and Base Sepolia `84532` in both directions. `amount` is an integer in wei, `originCurrency` and `destinationCurrency` are the zero address, and `recipient` must equal the authenticated wallet. The backend calls Relay testnet, enforces the route whitelist, validates Relay currency/chain/wallet fields and every dynamic transaction item, and stores the resulting audit order. The frontend must execute only the returned steps in order and must not call Relay directly.

<!-- ROUTE: GET /api/bridge/status/:requestId -->
### GET /api/bridge/status/:requestId

- Controller: `BridgeController.Status`
- Auth: 登录钱包 Cookie 会话；只能读取本人订单

Request:
```json
{
  "headers": {},
  "path": {
    "requestId": "BridgeQuoteResponse.orderId 或 Relay requestId"
  },
  "query": {},
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.requestId` | `BridgeQuoteResponse.orderId 或 Relay requestId` | 后端代码字段 requestId；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "order": "BridgeOrder",
    "relay": "json.RawMessage"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.order` | `BridgeOrder` | 后端代码字段 order；含义由所属响应模型和接口业务决定 |
| `data.relay` | `json.RawMessage` | 后端代码字段 relay；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

Notes: 后端仅调用报价中由 Relay 返回、且已校验为 `https://api.relay.link/...` 的状态 endpoint，不接受客户端传入 URL，因此不会成为 SSRF 代理。状态枚举为 `quoted`（仅已拿到报价）、`waiting_for_approval`、`waiting_for_deposit`、`source_confirmed`、`processing`、`success`、`refunded`、`failed`。`success` 后仍须在 Robinhood 主网重新读取官方 USDG 余额，不能直接自动发起 SeedX 买入。

<!-- ROUTE: GET /api/bridge/orders -->
### GET /api/bridge/orders

- Controller: `BridgeController.Orders`
- Auth: 登录钱包 Cookie 会话；仅返回当前钱包订单

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "status": "optional bridge status",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.status` | `optional bridge status` | optional bridge status |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]BridgeOrder",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]BridgeOrder` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 列表属于桥接审计记录，不是 SeedX 买卖、积分或项目流水。

<!-- ROUTE: POST /api/bridge/orders/:orderId/transactions -->
### POST /api/bridge/orders/:orderId/transactions

- Controller: `BridgeController.RecordTransactions`
- Auth: 登录钱包 Cookie 会话；只能更新本人订单

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web",
    "Content-Type": "application/json"
  },
  "path": {
    "orderId": "BridgeQuoteResponse.orderId"
  },
  "query": {},
  "body": {
    "sourceTxHash": "string",
    "destinationTxHash": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `path.orderId` | `BridgeQuoteResponse.orderId` | 后端代码字段 orderId；含义由所属响应模型和接口业务决定 |
| `body.sourceTxHash` | `string` | 后端代码字段 sourceTxHash；含义由所属响应模型和接口业务决定 |
| `body.destinationTxHash` | `string` | 后端代码字段 destinationTxHash；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "orderId": "string",
    "relayRequestId": "string",
    "userAddress": "string",
    "originChainId": "int64",
    "originCurrency": "string",
    "amount": "string",
    "destinationChainId": "int64",
    "destinationCurrency": "string",
    "recipient": "string",
    "status": "string",
    "sourceTxHash": "string",
    "destinationTxHash": "string",
    "quote": "string",
    "lastStatus": "string",
    "createdAt": "string",
    "updatedAt": "string",
    "lastStatusAt": "string"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.orderId` | `string` | 后端代码字段 orderId；含义由所属响应模型和接口业务决定 |
| `data.relayRequestId` | `string` | 后端代码字段 relayRequestId；含义由所属响应模型和接口业务决定 |
| `data.userAddress` | `string` | 用户钱包地址 |
| `data.originChainId` | `int64` | 后端代码字段 originChainId；含义由所属响应模型和接口业务决定 |
| `data.originCurrency` | `string` | 后端代码字段 originCurrency；含义由所属响应模型和接口业务决定 |
| `data.amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `data.destinationChainId` | `int64` | 后端代码字段 destinationChainId；含义由所属响应模型和接口业务决定 |
| `data.destinationCurrency` | `string` | 后端代码字段 destinationCurrency；含义由所属响应模型和接口业务决定 |
| `data.recipient` | `string` | 后端代码字段 recipient；含义由所属响应模型和接口业务决定 |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.sourceTxHash` | `string` | 后端代码字段 sourceTxHash；含义由所属响应模型和接口业务决定 |
| `data.destinationTxHash` | `string` | 后端代码字段 destinationTxHash；含义由所属响应模型和接口业务决定 |
| `data.quote` | `string` | 后端代码字段 quote；含义由所属响应模型和接口业务决定 |
| `data.lastStatus` | `string` | 后端代码字段 lastStatus；含义由所属响应模型和接口业务决定 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.lastStatusAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->

Notes: body strictly matches `BridgeTransactionReportRequest`; provide at least one valid EVM transaction hash. This records audit evidence only: it does not broadcast, replace, or confirm a wallet transaction.

<!-- ROUTE: GET /api/analytics/platform -->
### GET /api/analytics/platform

- Controller: `AnalyticsController.Platform`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "window": "optional"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.window` | `optional` | optional |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "window": "string",
      "dataSource": "string",
      "generatedAt": "string",
      "overview": "PlatformAnalyticsOverview",
      "capital": "PlatformCapitalStats",
      "operations": "PlatformOperationStats",
      "indexer": "PlatformIndexerStats",
      "support": "PlatformSupportStats"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.window` | `string` | 后端代码字段 window；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `data.generatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.overview` | `PlatformAnalyticsOverview` | 后端代码字段 overview；含义由所属响应模型和接口业务决定 |
| `data.capital` | `PlatformCapitalStats` | 后端代码字段 capital；含义由所属响应模型和接口业务决定 |
| `data.operations` | `PlatformOperationStats` | 后端代码字段 operations；含义由所属响应模型和接口业务决定 |
| `data.indexer` | `PlatformIndexerStats` | 后端代码字段 indexer；含义由所属响应模型和接口业务决定 |
| `data.support` | `PlatformSupportStats` | 后端代码字段 support；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/testnet/overview -->
### GET /api/testnet/overview

- Controller: `TestnetController.Overview`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "network": "string",
      "chainId": "int64",
      "deployment": "DeploymentSummary",
      "stats": "TestnetStats",
      "indexing": "ProjectIndexingStatus",
      "worker": "WorkerRunSummary",
      "smoke": "SmokeRunSummary",
      "rpc": "RPCHealthSummary",
      "latestProject": "*ProjectListItem",
      "recentProjects": "[]ProjectListItem",
      "recentEvents": "[]ProjectHistoryItem",
      "operationalNotes": "[]string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.network` | `string` | 后端代码字段 network；含义由所属响应模型和接口业务决定 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.deployment` | `DeploymentSummary` | 后端代码字段 deployment；含义由所属响应模型和接口业务决定 |
| `data.stats` | `TestnetStats` | 后端代码字段 stats；含义由所属响应模型和接口业务决定 |
| `data.indexing` | `ProjectIndexingStatus` | 后端代码字段 indexing；含义由所属响应模型和接口业务决定 |
| `data.worker` | `WorkerRunSummary` | 后端代码字段 worker；含义由所属响应模型和接口业务决定 |
| `data.smoke` | `SmokeRunSummary` | 后端代码字段 smoke；含义由所属响应模型和接口业务决定 |
| `data.rpc` | `RPCHealthSummary` | 后端代码字段 rpc；含义由所属响应模型和接口业务决定 |
| `data.latestProject` | `*ProjectListItem` | 后端代码字段 latestProject；含义由所属响应模型和接口业务决定 |
| `data.recentProjects` | `[]ProjectListItem` | 后端代码字段 recentProjects；含义由所属响应模型和接口业务决定 |
| `data.recentEvents` | `[]ProjectHistoryItem` | 后端代码字段 recentEvents；含义由所属响应模型和接口业务决定 |
| `data.operationalNotes` | `[]string` | 后端代码字段 operationalNotes；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

Notes: `rpc.status=not_sampled` means this passive overview has no persisted RPC sample yet; it is neutral and does not mean RPC is unhealthy. Use the admin diagnostics or RPC-health endpoint to request an active sample. Only `error` and `unconfigured` create an RPC operational warning. Decimal values are normalized for display, so MySQL DECIMAL padding must not be treated as token precision.

<!-- ROUTE: GET /api/messages -->
### GET /api/messages

- Controller: `MessageController.List`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "projectAddress": "optional",
    "messageType": "optional",
    "limit": "optional; service default applies",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.projectAddress` | `optional` | optional |
| `query.messageType` | `optional` | optional |
| `query.limit` | `optional; service default applies` | optional; service default applies |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]MessageItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]MessageItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/members/me -->
### GET /api/members/me

- Controller: `MemberController.Me`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "address": "string",
      "inviteCode": "string",
      "displayName": "string",
      "avatarUrl": "string",
      "memberLevel": "string",
      "status": "string",
      "riskFlagsJson": "string",
      "lastActiveAt": "string",
      "createdAt": "string",
      "updatedAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
| `data.displayName` | `string` | 后端代码字段 displayName；含义由所属响应模型和接口业务决定 |
| `data.avatarUrl` | `string` | 后端代码字段 avatarUrl；含义由所属响应模型和接口业务决定 |
| `data.memberLevel` | `string` | 后端代码字段 memberLevel；含义由所属响应模型和接口业务决定 |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.riskFlagsJson` | `string` | 后端代码字段 riskFlagsJson；含义由所属响应模型和接口业务决定 |
| `data.lastActiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: PATCH /api/members/me -->
### PATCH /api/members/me

- Controller: `MemberController.UpdateMe`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "displayName": "string",
    "avatarUrl": "string",
    "language": "string",
    "notificationChannelsJson": "string",
    "displaySettingsJson": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.displayName` | `string` | 后端代码字段 displayName；含义由所属响应模型和接口业务决定 |
| `body.avatarUrl` | `string` | 后端代码字段 avatarUrl；含义由所属响应模型和接口业务决定 |
| `body.language` | `string` | 后端代码字段 language；含义由所属响应模型和接口业务决定 |
| `body.notificationChannelsJson` | `string` | 后端代码字段 notificationChannelsJson；含义由所属响应模型和接口业务决定 |
| `body.displaySettingsJson` | `string` | 后端代码字段 displaySettingsJson；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "profile": "MemberProfile",
    "preferences": "MemberPreferences",
    "pointBalance": "string",
    "badgeCount": "int",
    "projectCount": "int",
    "transactionCount": "int",
    "unreadSupport": "int",
    "recentActivity": "[]MemberActivityLog"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.profile` | `MemberProfile` | 后端代码字段 profile；含义由所属响应模型和接口业务决定 |
| `data.preferences` | `MemberPreferences` | 后端代码字段 preferences；含义由所属响应模型和接口业务决定 |
| `data.pointBalance` | `string` | 后端代码字段 pointBalance；含义由所属响应模型和接口业务决定 |
| `data.badgeCount` | `int` | 数量统计 |
| `data.projectCount` | `int` | 数量统计 |
| `data.transactionCount` | `int` | 数量统计 |
| `data.unreadSupport` | `int` | 后端代码字段 unreadSupport；含义由所属响应模型和接口业务决定 |
| `data.recentActivity` | `[]MemberActivityLog` | 后端代码字段 recentActivity；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `UpdateMemberProfileRequest`; unknown fields return 400.

<!-- ROUTE: GET /api/members/me/dashboard -->
### GET /api/members/me/dashboard

- Controller: `MemberController.DashboardMember`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "address": "string",
      "member": "*MemberSummary",
      "portfolio": "*UserPortfolioSummary",
      "transactions": "[]UserTransactionItem",
      "notifications": "[]NotificationItem",
      "dataSource": "string",
      "indexing": "ProjectIndexingStatus"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.member` | `*MemberSummary` | 后端代码字段 member；含义由所属响应模型和接口业务决定 |
| `data.portfolio` | `*UserPortfolioSummary` | 后端代码字段 portfolio；含义由所属响应模型和接口业务决定 |
| `data.transactions` | `[]UserTransactionItem` | 后端代码字段 transactions；含义由所属响应模型和接口业务决定 |
| `data.notifications` | `[]NotificationItem` | 后端代码字段 notifications；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `data.indexing` | `ProjectIndexingStatus` | 后端代码字段 indexing；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

<!-- ROUTE: GET /api/members/me/activity -->
### GET /api/members/me/activity

- Controller: `MemberController.Activity`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "limit": "default 30, max 100",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.limit` | `default 30, max 100` | default 30, max 100 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]MemberActivityLog",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]MemberActivityLog` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/members/:address/summary -->
### GET /api/members/:address/summary

- Controller: `MemberController.Summary`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "profile": "MemberProfile",
      "preferences": "MemberPreferences",
      "pointBalance": "string",
      "badgeCount": "int",
      "projectCount": "int",
      "transactionCount": "int",
      "unreadSupport": "int",
      "recentActivity": "[]MemberActivityLog"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.profile` | `MemberProfile` | 后端代码字段 profile；含义由所属响应模型和接口业务决定 |
| `data.preferences` | `MemberPreferences` | 后端代码字段 preferences；含义由所属响应模型和接口业务决定 |
| `data.pointBalance` | `string` | 后端代码字段 pointBalance；含义由所属响应模型和接口业务决定 |
| `data.badgeCount` | `int` | 数量统计 |
| `data.projectCount` | `int` | 数量统计 |
| `data.transactionCount` | `int` | 数量统计 |
| `data.unreadSupport` | `int` | 后端代码字段 unreadSupport；含义由所属响应模型和接口业务决定 |
| `data.recentActivity` | `[]MemberActivityLog` | 后端代码字段 recentActivity；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/support/conversations -->
### GET /api/support/conversations

- Controller: `SupportController.ListConversations`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "limit": "default 30 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.limit` | `default 30 max 200` | default 30 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]SupportConversation",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]SupportConversation` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: POST /api/support/conversations -->
### POST /api/support/conversations

- Controller: `SupportController.CreateConversation`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "category": "string",
    "priority": "string",
    "subject": "string",
    "body": "string",
    "attachmentsJson": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.category` | `string` | 后端代码字段 category；含义由所属响应模型和接口业务决定 |
| `body.priority` | `string` | 后端代码字段 priority；含义由所属响应模型和接口业务决定 |
| `body.subject` | `string` | 后端代码字段 subject；含义由所属响应模型和接口业务决定 |
| `body.body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `body.attachmentsJson` | `string` | 后端代码字段 attachmentsJson；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "id": "uint64",
      "conversationNo": "string",
      "userAddress": "string",
      "category": "string",
      "priority": "string",
      "status": "string",
      "subject": "string",
      "assignedAdmin": "string",
      "lastMessageAt": "string",
      "closedAt": "string",
      "createdAt": "string",
      "updatedAt": "string",
      "unreadCount": "int"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `data.conversationNo` | `string` | 后端代码字段 conversationNo；含义由所属响应模型和接口业务决定 |
| `data.userAddress` | `string` | 用户钱包地址 |
| `data.category` | `string` | 后端代码字段 category；含义由所属响应模型和接口业务决定 |
| `data.priority` | `string` | 后端代码字段 priority；含义由所属响应模型和接口业务决定 |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.subject` | `string` | 后端代码字段 subject；含义由所属响应模型和接口业务决定 |
| `data.assignedAdmin` | `string` | 后端代码字段 assignedAdmin；含义由所属响应模型和接口业务决定 |
| `data.lastMessageAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.closedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.unreadCount` | `int` | 数量统计 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `CreateSupportConversationRequest`; unknown fields return 400.

<!-- ROUTE: GET /api/support/conversations/:id -->
### GET /api/support/conversations/:id

- Controller: `SupportController.ConversationDetail`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {
    "id": "path id"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.id` | `path id` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "conversation": "SupportConversation",
      "messages": "[]SupportMessage"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.conversation` | `SupportConversation` | 后端代码字段 conversation；含义由所属响应模型和接口业务决定 |
| `data.messages` | `[]SupportMessage` | 后端代码字段 messages；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: POST /api/support/conversations/:id/messages -->
### POST /api/support/conversations/:id/messages

- Controller: `SupportController.SendMessage`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {
    "id": "path id"
  },
  "query": {},
  "body": {
    "body": "string",
    "attachmentsJson": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.id` | `path id` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `body.body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `body.attachmentsJson` | `string` | 后端代码字段 attachmentsJson；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "id": "uint64",
      "conversationId": "uint64",
      "senderType": "string",
      "senderAddress": "string",
      "body": "string",
      "attachmentsJson": "string",
      "createdAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `data.conversationId` | `uint64` | 后端代码字段 conversationId；含义由所属响应模型和接口业务决定 |
| `data.senderType` | `string` | 后端代码字段 senderType；含义由所属响应模型和接口业务决定 |
| `data.senderAddress` | `string` | 合约或钱包的 EVM 地址 |
| `data.body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `data.attachmentsJson` | `string` | 后端代码字段 attachmentsJson；含义由所属响应模型和接口业务决定 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `SendSupportMessageRequest`; unknown fields return 400. Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: POST /api/support/conversations/:id/read -->
### POST /api/support/conversations/:id/read

- Controller: `SupportController.MarkRead`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web"
  },
  "path": {
    "id": "path id"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.id` | `path id` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"status": "read"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `read` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: POST /api/support/conversations/:id/close -->
### POST /api/support/conversations/:id/close

- Controller: `SupportController.Close`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web"
  },
  "path": {
    "id": "path id"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.id` | `path id` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"status": "closed"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `closed` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: POST /api/support/conversations/:id/reopen -->
### POST /api/support/conversations/:id/reopen

- Controller: `SupportController.Reopen`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web"
  },
  "path": {
    "id": "path id"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.id` | `path id` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"status": "open"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.status` | `open` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: POST /api/projects/:address/announcements -->
### POST /api/projects/:address/announcements

- Controller: `MessageController.PublishProjectAnnouncement`
- Auth: none

Request:
```json
{
  "headers": {
    "Content-Type": "application/json"
  },
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": {
    "title": "string",
    "body": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `body.title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `body.body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "published": "bool",
      "message": "MessageItem"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.published` | `bool` | 后端代码字段 published；含义由所属响应模型和接口业务决定 |
| `data.message` | `MessageItem` | 人类可读的结果或错误信息 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `PublishProjectAnnouncementRequest`; unknown fields return 400. Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/projects -->
### GET /api/projects

- Controller: `ProjectController.ProjectList`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "keyword": "optional",
    "status": "optional",
    "limit": "default 100, valid 1-200 else 100",
    "offset": "default 0, negative becomes 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.keyword` | `optional` | optional |
| `query.status` | `optional` | optional |
| `query.limit` | `default 100, valid 1-200 else 100` | default 100, valid 1-200 else 100 |
| `query.offset` | `default 0, negative becomes 0` | default 0, negative becomes 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "items": "[]ProjectListItem",
      "total": "uint64",
      "limit": "int",
      "offset": "int"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]ProjectListItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/projects/market -->
### GET /api/projects/market

- Controller: `MarketController.ProjectMarketCatalog`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "keyword": "optional",
    "tab": "optional",
    "sort": "optional",
    "direction": "optional",
    "state": "optional",
    "scaleTier": "optional",
    "minMarketCap": "optional",
    "maxMarketCap": "optional",
    "minVolume24h": "optional",
    "verified": "optional",
    "limit": "default 30 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.keyword` | `optional` | optional |
| `query.tab` | `optional` | optional |
| `query.sort` | `optional` | optional |
| `query.direction` | `optional` | optional |
| `query.state` | `optional` | optional |
| `query.scaleTier` | `optional` | optional |
| `query.minMarketCap` | `optional` | optional |
| `query.maxMarketCap` | `optional` | optional |
| `query.minVolume24h` | `optional` | optional |
| `query.verified` | `optional` | optional |
| `query.limit` | `default 30 max 200` | default 30 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "query": "ProjectMarketQuery",
      "summary": "ProjectMarketSummary",
      "facets": "ProjectMarketFacets",
      "items": "[]ProjectMarketItem",
      "total": "uint64",
      "limit": "int",
      "offset": "int",
      "generatedAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.query` | `ProjectMarketQuery` | 后端代码字段 query；含义由所属响应模型和接口业务决定 |
| `data.summary` | `ProjectMarketSummary` | 后端代码字段 summary；含义由所属响应模型和接口业务决定 |
| `data.facets` | `ProjectMarketFacets` | 后端代码字段 facets；含义由所属响应模型和接口业务决定 |
| `data.items` | `[]ProjectMarketItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
| `data.generatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: POST /api/projects/preview -->
### POST /api/projects/preview

- Controller: `ProjectController.Preview`
- Auth: none

Request:
```json
{
  "headers": {
    "Content-Type": "application/json"
  },
  "path": {},
  "query": {},
  "body": {
    "tokenName": "string",
    "tokenSymbol": "string",
    "logoUrl": "string",
    "website": "string",
    "twitter": "string",
    "telegram": "string",
    "description": "string",
    "scaleTier": "string",
    "nodeCount": "int64",
    "nodePrice": "string",
    "maxNodesPerAddress": "int64",
    "minSingleBuy": "string",
    "altTokenAddress": "string",
    "altTokenPerNode": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `body.tokenName` | `string` | 项目 Token 名称 |
| `body.tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `body.logoUrl` | `string` | 项目 Logo 资源 URL |
| `body.website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `body.twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `body.telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `body.description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `body.scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `body.nodeCount` | `int64` | 数量统计 |
| `body.nodePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `body.maxNodesPerAddress` | `int64` | 建项时固化的单地址节点认购数量上限；必须在 1..nodeCount 之间，后端会结合 nodeSoldBy(wallet) 计算剩余可认购数 |
| `body.minSingleBuy` | `string` | 后端代码字段 minSingleBuy；含义由所属响应模型和接口业务决定 |
| `body.altTokenAddress` | `string` | 可选 Alt Token 合约地址；未启用时传空字符串或零地址，后端统一编码为零地址 |
| `body.altTokenPerNode` | `string` | 每节点托管的 Alt Token 数量；未启用时可传空字符串、0 或 0.0，启用时必须是按该 Token 实际 decimals 解析后的正数 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "valid": "bool",
    "symbolAvailable": "bool",
    "warnings": "[]string",
    "normalizedConfig": "NormalizedProjectConfig",
    "scaleTierSnapshot": "ScaleTierSnapshot",
    "projection": "ProjectionSummary"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.valid` | `bool` | 后端代码字段 valid；含义由所属响应模型和接口业务决定 |
| `data.symbolAvailable` | `bool` | 布尔状态标记 |
| `data.warnings` | `[]string` | 后端代码字段 warnings；含义由所属响应模型和接口业务决定 |
| `data.normalizedConfig` | `NormalizedProjectConfig` | 后端完成默认值、大小写、decimals 和零地址归一化后的最终建项配置；前端确认页应以此为准 |
| `data.scaleTierSnapshot` | `ScaleTierSnapshot` | 后端代码字段 scaleTierSnapshot；含义由所属响应模型和接口业务决定 |
| `data.projection` | `ProjectionSummary` | 后端代码字段 projection；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `ProjectPreviewRequest`; unknown fields return 400. The request must not contain fee-rate or fee/tax split fields. Backend reads the enabled Tier policy at one chain snapshot and returns immutable buy/sell/profit-tax rates 30000/50000/200000 PPM and initial floor/node/issuer/platform splits 6000/2000/1000/1000 BPS. Contract-aligned constraints include tokenName UTF-8 byte length 1-32, `maxNodesPerAddress` in 1..nodeCount, exact `nodeCount * nodePrice = nodeRaiseTarget` (30000 USDGTest), and minSingleBuy >= 1 stable token. `twitter` and `telegram` are optional MySQL metadata and never enter calldata. Alt Token must be readable and non-stable; `normalizedConfig.altToken.decimals` and `normalizedConfig.altToken.amountPerNodeWei` are the exact encoding basis. Backend requires finalized=true, pointCount=513, productionTable=true and canonical hashes. Projection labels the actual shoulder x2 and S_ref/P_ref anchors; it does not fabricate 2x/10x semantics.

<!-- ROUTE: POST /api/projects/create-tx-params -->
### POST /api/projects/create-tx-params

- Controller: `ProjectController.CreateTxParams`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {},
  "query": {},
  "body": {
    "tokenName": "string",
    "tokenSymbol": "string",
    "logoUrl": "string",
    "website": "string",
    "twitter": "string",
    "telegram": "string",
    "description": "string",
    "scaleTier": "string",
    "nodeCount": "int64",
    "nodePrice": "string",
    "maxNodesPerAddress": "int64",
    "minSingleBuy": "string",
    "altTokenAddress": "string",
    "altTokenPerNode": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `body.tokenName` | `string` | 项目 Token 名称 |
| `body.tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `body.logoUrl` | `string` | 项目 Logo 资源 URL |
| `body.website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `body.twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `body.telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `body.description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `body.scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `body.nodeCount` | `int64` | 数量统计 |
| `body.nodePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `body.maxNodesPerAddress` | `int64` | 建项时固化的单地址节点认购数量上限；必须在 1..nodeCount 之间，后端会结合 nodeSoldBy(wallet) 计算剩余可认购数 |
| `body.minSingleBuy` | `string` | 后端代码字段 minSingleBuy；含义由所属响应模型和接口业务决定 |
| `body.altTokenAddress` | `string` | 可选 Alt Token 合约地址；未启用时传空字符串或零地址，后端统一编码为零地址 |
| `body.altTokenPerNode` | `string` | 每节点托管的 Alt Token 数量；未启用时可传空字符串、0 或 0.0，启用时必须是按该 Token 实际 decimals 解析后的正数 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "target": "string",
    "method": "string",
    "value": "string",
    "chainId": "int64",
    "calldata": "string",
    "args": "map[string]string",
    "maxCreationFee": "string",
    "maxCreationFeeWei": "string",
    "expectedTierVersion": "uint64",
    "deadline": "uint64",
    "normalizedConfig": "NormalizedProjectConfig",
    "scaleTierSnapshot": "ScaleTierSnapshot",
    "requiredApprovals": "[]RequiredApproval",
    "balanceChecks": "[]BalanceCheck",
    "warnings": "[]string",
    "snapshotBlock": "uint64",
    "preflight": "TxPreflight"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.target` | `string` | 交易目标合约地址 |
| `data.method` | `string` | 后端代码字段 method；含义由所属响应模型和接口业务决定 |
| `data.value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `data.args` | `map[string]string` | 后端代码字段 args；含义由所属响应模型和接口业务决定 |
| `data.maxCreationFee` | `string` | 用户本次签名接受的最高建项费，由后端从快照区块的 creationFee() 生成，单位为可读 Stable |
| `data.maxCreationFeeWei` | `string` | 编码进 createProject.maxCreationFee 的原始 uint256 整数字符串 |
| `data.expectedTierVersion` | `uint64` | 编码进 createProject.expectedTierVersion 的链上 Scale Tier uint64 版本；发送前版本变化会以 ScaleTierChanged 回滚 |
| `data.deadline` | `uint64` | Unix-second deadline; the validity window and enforcement rule are defined by the route notes. |
| `data.normalizedConfig` | `NormalizedProjectConfig` | 后端完成默认值、大小写、decimals 和零地址归一化后的最终建项配置；前端确认页应以此为准 |
| `data.scaleTierSnapshot` | `ScaleTierSnapshot` | 后端代码字段 scaleTierSnapshot；含义由所属响应模型和接口业务决定 |
| `data.requiredApprovals` | `[]RequiredApproval` | 后端代码字段 requiredApprovals；含义由所属响应模型和接口业务决定 |
| `data.balanceChecks` | `[]BalanceCheck` | 后端代码字段 balanceChecks；含义由所属响应模型和接口业务决定 |
| `data.warnings` | `[]string` | 后端代码字段 warnings；含义由所属响应模型和接口业务决定 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `data.preflight` | `TxPreflight` | 后端代码字段 preflight；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `CreateTxParamsRequest`; unknown fee-rate or split fields return 400. It enforces `maxNodesPerAddress` in 1..nodeCount and `nodeCount * nodePrice = nodeRaiseTarget`, then encodes the 20260821 16-field CreateProjectParams tuple. The four encoded node/issuer split values are 0/0/0/0 so the contract deterministically selects Tier defaults 2000/1000 for both fee and tax; floor/platform remain fixed at 6000/1000. `twitter` and `telegram` are pending MySQL metadata and never enter calldata. `normalizedConfig.altToken.decimals` and `normalizedConfig.altToken.amountPerNodeWei` define the exact Alt Token integer encoded. Backend reads `creationFee()`, `scaleTierVersion(scaleTier)`, current Tier economic policy and block timestamp at one snapshot, then encodes `maxCreationFee`, `expectedTierVersion` and a 30-minute `deadline`. A Tier version or policy mismatch requires fresh parameters and user confirmation.

<!-- ROUTE: GET /api/projects/symbol-available -->
### GET /api/projects/symbol-available

- Controller: `ProjectController.SymbolAvailable`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {
    "symbol": "required"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `query.symbol` | `required` | required |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"symbol": "<query.symbol>", "available": true}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.symbol` | `<query.symbol>` | 后端代码字段 symbol；含义由所属响应模型和接口业务决定 |
| `data.available` | `object` | 后端代码字段 available；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: POST /api/projects/:address/action-tx-params -->
### POST /api/projects/:address/action-tx-params

- Controller: `ProjectController.ActionTxParams`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": {
    "action": "string",
    "nodeAmount": "int64",
    "cashIn": "string",
    "tokenIn": "string",
    "pledgeToken": "string",
    "buyToken": "string",
    "maxUserPay": "string",
    "tokenAmount": "string",
    "minTokenOut": "string",
    "minCashOut": "string",
    "proposalId": "FlexibleUint64",
    "support": "bool",
    "paramId": "uint8",
    "newValue": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `body.action` | `string` | 后端代码字段 action；含义由所属响应模型和接口业务决定 |
| `body.nodeAmount` | `int64` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `body.cashIn` | `string` | 买入时输入的稳定币金额 |
| `body.tokenIn` | `string` | 卖出时输入的项目 Token 数量 |
| `body.pledgeToken` | `string` | 用于质押的项目 Token 数量；字段名必须是 pledgeToken |
| `body.buyToken` | `string` | 质押买时目标买入的项目 Token 数量 |
| `body.maxUserPay` | `string` | 质押买入可接受的最大用户自付稳定币金额；必须大于 0 且不小于当前报价 userPayStable，建议加上滑点容差 |
| `body.tokenAmount` | `string` | 项目 Token 数量 |
| `body.minTokenOut` | `string` | 买入时可接受的最小 Token 输出，必填且必须大于 0；建议使用报价 tokenOut 扣除滑点后的值 |
| `body.minCashOut` | `string` | 卖出时可接受的最小稳定币到账；后端按稳定币 6 位精度向下取整，保证滑点下限不被抬高 |
| `body.proposalId` | `FlexibleUint64` | 后端代码字段 proposalId；含义由所属响应模型和接口业务决定 |
| `body.support` | `bool` | 后端代码字段 support；含义由所属响应模型和接口业务决定 |
| `body.paramId` | `uint8` | 后端代码字段 paramId；含义由所属响应模型和接口业务决定 |
| `body.newValue` | `string` | 治理提案新值的非负整数字符串；20260821 仅允许 paramId 5/8/10/11/12，后端同步校验 hard cap 和单次 20% 调整幅度 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "target": "string",
    "method": "string",
    "value": "string",
    "chainId": "int64",
    "calldata": "string",
    "args": "map[string]string",
    "requiredApprovals": "[]RequiredApproval",
    "balanceChecks": "[]BalanceCheck",
    "warnings": "[]string",
    "snapshotBlock": "uint64",
    "preflight": "TxPreflight",
    "collectPreview": {
      "canCollect": "bool",
      "stableFees": "string",
      "stableFeesWei": "string",
      "tokenFees": "string",
      "tokenFeesWei": "string",
      "stableTokenAddress": "string",
      "stableTokenSymbol": "string",
      "stableTokenDecimals": "uint8",
      "projectTokenAddress": "string",
      "projectTokenSymbol": "string",
      "projectTokenDecimals": "uint8",
      "snapshotBlock": "uint64",
      "dataSource": "chain_state | chain_eth_call | unavailable",
      "unavailableReason": "string; omitted when canCollect=true"
    }
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.target` | `string` | 交易目标合约地址 |
| `data.method` | `string` | 后端代码字段 method；含义由所属响应模型和接口业务决定 |
| `data.value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `data.args` | `map[string]string` | 后端代码字段 args；含义由所属响应模型和接口业务决定 |
| `data.requiredApprovals` | `[]RequiredApproval` | 后端代码字段 requiredApprovals；含义由所属响应模型和接口业务决定 |
| `data.balanceChecks` | `[]BalanceCheck` | 后端代码字段 balanceChecks；含义由所属响应模型和接口业务决定 |
| `data.warnings` | `[]string` | 后端代码字段 warnings；含义由所属响应模型和接口业务决定 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `data.preflight` | `TxPreflight` | 后端代码字段 preflight；含义由所属响应模型和接口业务决定 |
| `data.collectPreview` | `object` | 外部流动性池手续费的链上收取模拟快照；用于决定是否允许发起 collect_and_distribute |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `ProjectActionTxParamsRequest`; unknown fields return 400. The backend requires the path address to exist in the active deployment indexed project registry and rejects arbitrary targets. `buy` encodes selector `0xd6febde8`; `sell` minCashOut is rounded down to Stable precision; `pledge_buy` encodes selector `0x45bd87e8` and `maxUserPay` is rounded up; `claim_alt_token` encodes selector `0x1282cfb6`; `execute_graduation` encodes selector `0x30e033fe`. On Ethereum Sepolia, `trigger_graduation`, `execute_graduation`, and `collect_and_distribute` require public config `graduationEnabled=true`; trigger additionally requires the authenticated wallet to equal live `issuer()`, while execute is permissionless after notice expiry. Governance only accepts paramId 5/8/10/11/12 and enforces the contract 20% adjustment limit. ParamId 5/8 rebalance node and issuer inside the fixed 3000 BPS flexible share; buy 3%, sell 5%, profit tax 20%, floor 6000 BPS and platform 1000 BPS cannot be proposed. Every approval spender must equal response target; frontend must compare locally, whitelist target/token/spender and decode selector/arguments before approve or send.

<!-- ROUTE: GET /api/projects/:address/runtime -->
### GET /api/projects/:address/runtime

- Controller: `TestnetController.ProjectRuntime`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "userAddress": "optional; preferred",
    "address": "optional alias"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.userAddress` | `optional; preferred` | optional; preferred |
| `query.address` | `optional alias` | optional alias |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "project": "ProjectDetail",
      "indexing": "ProjectIndexingStatus",
      "pool": "ProjectPoolResponse",
      "governance": "ProjectGovernanceSummary",
      "fees": "ProjectFeesResponse",
      "incomeSummary": "FeeSummary",
      "operationStats": "OperationStats",
      "userPosition": "*UserPosition",
      "recentEvents": "[]ProjectHistoryItem"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.project` | `ProjectDetail` | 后端代码字段 project；含义由所属响应模型和接口业务决定 |
| `data.indexing` | `ProjectIndexingStatus` | 后端代码字段 indexing；含义由所属响应模型和接口业务决定 |
| `data.pool` | `ProjectPoolResponse` | 后端代码字段 pool；含义由所属响应模型和接口业务决定 |
| `data.governance` | `ProjectGovernanceSummary` | 后端代码字段 governance；含义由所属响应模型和接口业务决定 |
| `data.fees` | `ProjectFeesResponse` | 后端代码字段 fees；含义由所属响应模型和接口业务决定 |
| `data.incomeSummary` | `FeeSummary` | 后端代码字段 incomeSummary；含义由所属响应模型和接口业务决定 |
| `data.operationStats` | `OperationStats` | 后端代码字段 operationStats；含义由所属响应模型和接口业务决定 |
| `data.userPosition` | `*UserPosition` | 后端代码字段 userPosition；含义由所属响应模型和接口业务决定 |
| `data.recentEvents` | `[]ProjectHistoryItem` | 后端代码字段 recentEvents；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/projects/:address/fees -->
### GET /api/projects/:address/fees

- Controller: `ProjectController.Fees`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "projectAddress": "string",
      "platformPayout": "string",
      "buyFeeRatePpm": "uint64",
      "sellFeeRatePpm": "uint64",
      "profitTaxRatePpm": "uint64",
      "feeFloorBps": "uint16",
      "feeNodeBps": "uint16",
      "feeIssuerBps": "uint16",
      "feePlatformBps": "uint16",
      "lpFeeNodeBps": "uint16",
      "lpFeeCreatorBps": "uint16",
      "lpFeePlatformBps": "uint16",
      "snapshotBlock": "uint64"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.platformPayout` | `string` | 后端代码字段 platformPayout；含义由所属响应模型和接口业务决定 |
| `data.buyFeeRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `data.sellFeeRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `data.profitTaxRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `data.feeFloorBps` | `uint16` | 手续费地板分账 BPS；固定 6000（60%），不可修改 |
| `data.feeNodeBps` | `uint16` | 手续费节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `data.feeIssuerBps` | `uint16` | 手续费发射方分账 BPS；初始 1000（10%），可通过 paramId=5 在 3000 BPS 灵活份额内调整 |
| `data.feePlatformBps` | `uint16` | 手续费平台分账 BPS；固定 1000（10%），不可修改 |
| `data.lpFeeNodeBps` | `uint16` | 万分比整数，10000 表示 100% |
| `data.lpFeeCreatorBps` | `uint16` | 万分比整数，10000 表示 100% |
| `data.lpFeePlatformBps` | `uint16` | 万分比整数，10000 表示 100% |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/projects/:address/curve -->
### GET /api/projects/:address/curve

- Controller: `ProjectController.Curve`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "projectAddress": "string",
      "curveTableAddress": "string",
      "curveParamsHash": "string",
      "currentSupply": "string",
      "floorPrice": "string",
      "currentPrice": "string",
      "points": "[]CurvePoint",
      "snapshotBlock": "uint64"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.curveTableAddress` | `string` | 必须是固定 CurveTableRegistry 工厂创建并已注册的 CurveTable 地址 |
| `data.curveParamsHash` | `string` | 曲线配置参数的 bytes32 哈希；用于核对 Scale Tier 与 CurveTable 参数版本 |
| `data.currentSupply` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `data.points` | `[]CurvePoint` | 后端代码字段 points；含义由所属响应模型和接口业务决定 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->
Notes: `curveParamsHash` identifies the numeric curve configuration and `curvePointsHash` identifies the actual piecewise points. Both hashes are read from chain and should match the project scale-tier snapshot before the frontend enables trading.

<!-- ROUTE: GET /api/projects/:address/fee-ledger -->
### GET /api/projects/:address/fee-ledger

- Controller: `MarketController.ProjectFeeLedger`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "recipientAddress": "optional",
    "incomeType": "optional",
    "limit": "default 50 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.recipientAddress` | `optional` | optional |
| `query.incomeType` | `optional` | optional |
| `query.limit` | `default 50 max 200` | default 50 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]FeeIncomeLedgerItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]FeeIncomeLedgerItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/projects/:address/pool -->
### GET /api/projects/:address/pool

- Controller: `TestnetController.ProjectPool`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "projectAddress": "string",
    "graduatedPoolId": "string",
    "graduationAdapter": "string",
    "graduationHookOrLock": "string",
    "graduationAnnounceTime": "uint64",
    "graduationExecuteAt": "uint64",
    "graduationNotice": "uint64",
    "lpNodeBps": "string",
    "lpCreatorBps": "string",
    "lpPlatformBps": "string",
    "graduatedAt": "uint64",
    "blockNumber": "uint64",
    "dataSource": "string",
    "recentAnnouncements": "[]GraduationAnnouncement",
    "recentPoolEvents": "[]ProjectHistoryItem",
    "uniswapV4Position": "*ExternalPoolPosition",
    "collectPreview": {
      "canCollect": "bool",
      "stableFees": "string",
      "stableFeesWei": "string",
      "tokenFees": "string",
      "tokenFeesWei": "string",
      "stableTokenAddress": "string",
      "stableTokenSymbol": "string",
      "stableTokenDecimals": "uint8",
      "projectTokenAddress": "string",
      "projectTokenSymbol": "string",
      "projectTokenDecimals": "uint8",
      "snapshotBlock": "uint64",
      "dataSource": "chain_state | chain_eth_call | unavailable",
      "unavailableReason": "string; omitted when canCollect=true"
    }
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.graduatedPoolId` | `string` | 后端代码字段 graduatedPoolId；含义由所属响应模型和接口业务决定 |
| `data.graduationAdapter` | `string` | 毕业 Adapter 地址；enabled=true 时必须等于 Platform.trustedGraduationAdapter() |
| `data.graduationHookOrLock` | `string` | 后端代码字段 graduationHookOrLock；含义由所属响应模型和接口业务决定 |
| `data.graduationAnnounceTime` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `data.graduationExecuteAt` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `data.graduationNotice` | `uint64` | 毕业公告期秒数；triggerGraduation 后须等待该时长才可 executeGraduation |
| `data.lpNodeBps` | `string` | 万分比整数，10000 表示 100% |
| `data.lpCreatorBps` | `string` | 万分比整数，10000 表示 100% |
| `data.lpPlatformBps` | `string` | 万分比整数，10000 表示 100% |
| `data.graduatedAt` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `data.blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `data.recentAnnouncements` | `[]GraduationAnnouncement` | 后端代码字段 recentAnnouncements；含义由所属响应模型和接口业务决定 |
| `data.recentPoolEvents` | `[]ProjectHistoryItem` | 后端代码字段 recentPoolEvents；含义由所属响应模型和接口业务决定 |
| `data.uniswapV4Position` | `*ExternalPoolPosition` | 后端代码字段 uniswapV4Position；含义由所属响应模型和接口业务决定 |
| `data.collectPreview` | `object` | 外部流动性池手续费的链上收取模拟快照；用于决定是否允许发起 collect_and_distribute |
<!-- /RESPONSE_FIELDS -->
Notes: `collectPreview` is produced by simulating the adapter `collectFees(poolId)` without persisting state. `canCollect` is true only when `stableFeesWei` or `tokenFeesWei` is greater than zero. The display endpoint may reuse a result for up to 2 seconds; always request fresh action-tx-params immediately before wallet submission. If dataSource is unavailable or canCollect is false, disable the collect button and show unavailableReason.

<!-- ROUTE: GET /api/projects/:address/governance/summary -->
### GET /api/projects/:address/governance/summary

- Controller: `TestnetController.ProjectGovernanceSummary`
- Auth: none

治理状态说明：

- `active`：提案仍在投票期内。
- `pending_tally`：投票期已结束，但链上还没有人调用 `tallyVotes`，前端应显示为“待结算”或“待计票”。
- `passed`：已完成 `tallyVotes`，提案通过，等待 timelock 到期后执行。
- `rejected`：已完成 `tallyVotes`，提案未通过。
- `executed`：已执行完成。

治理时间字段说明：

- Sepolia 20260812 基线下，治理提案统一使用 `voteStartTime`、`voteEndTime`、`queuedTime`，并携带节点投票 epoch 快照字段。
- 三个字段都是 Unix 秒级时间戳，不是区块号。
- 倒计时、是否进入 `pending_tally`、是否进入 timelock，都应基于这些时间字段判断。

汇总字段说明：

- `activeCount` 当前仅统计 `status = active` 的提案数量，不包含 `pending_tally`。
- 如果前端要展示“待用户处理”的治理提案数，建议自行统计 `active + pending_tally + passed`。

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "userAddress": "optional; preferred",
    "address": "optional alias"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.userAddress` | `optional; preferred` | optional; preferred |
| `query.address` | `optional alias` | optional alias |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "projectAddress": "string",
    "proposalCount": "int",
    "activeCount": "int",
    "executedCount": "int",
    "currentUser": "string",
    "currentUserCanPropose": "bool",
    "currentUserVotingPower": "string",
    "currentUserVotingPowerDisplay": "string",
    "totalVotingPower": "string",
    "totalVotingPowerDisplay": "string",
    "nextProposalId": "uint64",
    "canProposeReason": "string",
    "allowedParamIds": "[]uint8",
    "recommendedParamIds": "[]uint8",
    "unavailableParamIds": "[]uint8",
    "proposals": "[]GovernanceProposalItem"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.proposalCount` | `int` | 数量统计 |
| `data.activeCount` | `int` | 数量统计 |
| `data.executedCount` | `int` | 数量统计 |
| `data.currentUser` | `string` | 后端代码字段 currentUser；含义由所属响应模型和接口业务决定 |
| `data.currentUserCanPropose` | `bool` | 后端代码字段 currentUserCanPropose；含义由所属响应模型和接口业务决定 |
| `data.currentUserVotingPower` | `string` | 后端代码字段 currentUserVotingPower；含义由所属响应模型和接口业务决定 |
| `data.currentUserVotingPowerDisplay` | `string` | 后端代码字段 currentUserVotingPowerDisplay；含义由所属响应模型和接口业务决定 |
| `data.totalVotingPower` | `string` | 后端代码字段 totalVotingPower；含义由所属响应模型和接口业务决定 |
| `data.totalVotingPowerDisplay` | `string` | 后端代码字段 totalVotingPowerDisplay；含义由所属响应模型和接口业务决定 |
| `data.nextProposalId` | `uint64` | 后端代码字段 nextProposalId；含义由所属响应模型和接口业务决定 |
| `data.canProposeReason` | `string` | 后端代码字段 canProposeReason；含义由所属响应模型和接口业务决定 |
| `data.allowedParamIds` | `[]uint8` | 后端代码字段 allowedParamIds；含义由所属响应模型和接口业务决定 |
| `data.recommendedParamIds` | `[]uint8` | 后端代码字段 recommendedParamIds；含义由所属响应模型和接口业务决定 |
| `data.unavailableParamIds` | `[]uint8` | 后端代码字段 unavailableParamIds；含义由所属响应模型和接口业务决定 |
| `data.proposals` | `[]GovernanceProposalItem` | 后端代码字段 proposals；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/projects/:address -->
### GET /api/projects/:address

- Controller: `ProjectController.Detail`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "projectAddress": "string",
    "tokenName": "string",
    "tokenSymbol": "string",
    "issuerAddress": "string",
    "scaleTier": "string",
    "state": "string",
    "description": "string",
    "platformPayoutAddr": "string",
    "altTokenAddress": "string",
    "altTokenPerNode": "string",
    "altTokenDecimals": "uint8",
    "nodeCount": "int64",
    "nodeSold": "int64",
    "nodePriceStable": "string",
    "maxNodesPerAddress": "int64",
    "buyFeeRate": "string",
    "sellFeeRate": "string",
    "profitTaxRate": "string",
    "minSingleBuyStable": "string",
    "createdAt": "string",
    "logoUrl": "string",
    "website": "string",
    "twitter": "string",
    "telegram": "string",
    "metrics": "ProjectMetrics",
    "onChainState": "OnChainProjectState",
    "userPosition": "*UserPosition",
    "scaleTierSnapshot": "ScaleTierSnapshot"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.tokenName` | `string` | 项目 Token 名称 |
| `data.tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `data.issuerAddress` | `string` | 项目创建者钱包地址 |
| `data.scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `data.state` | `string` | 后端代码字段 state；含义由所属响应模型和接口业务决定 |
| `data.description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `data.platformPayoutAddr` | `string` | 后端代码字段 platformPayoutAddr；含义由所属响应模型和接口业务决定 |
| `data.altTokenAddress` | `string` | 可选 Alt Token 合约地址；未启用时传空字符串或零地址，后端统一编码为零地址 |
| `data.altTokenPerNode` | `string` | 每节点托管的 Alt Token 数量；未启用时可传空字符串、0 或 0.0，启用时必须是按该 Token 实际 decimals 解析后的正数 |
| `data.altTokenDecimals` | `uint8` | 后端代码字段 altTokenDecimals；含义由所属响应模型和接口业务决定 |
| `data.nodeCount` | `int64` | 数量统计 |
| `data.nodeSold` | `int64` | 后端代码字段 nodeSold；含义由所属响应模型和接口业务决定 |
| `data.nodePriceStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.maxNodesPerAddress` | `int64` | 建项时固化的单地址节点认购数量上限；必须在 1..nodeCount 之间，后端会结合 nodeSoldBy(wallet) 计算剩余可认购数 |
| `data.buyFeeRate` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.sellFeeRate` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.profitTaxRate` | `string` | 后端代码字段 profitTaxRate；含义由所属响应模型和接口业务决定 |
| `data.minSingleBuyStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.logoUrl` | `string` | 项目 Logo 资源 URL |
| `data.website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `data.twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `data.telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `data.metrics` | `ProjectMetrics` | 后端代码字段 metrics；含义由所属响应模型和接口业务决定 |
| `data.onChainState` | `OnChainProjectState` | 后端代码字段 onChainState；含义由所属响应模型和接口业务决定 |
| `data.userPosition` | `*UserPosition` | 后端代码字段 userPosition；含义由所属响应模型和接口业务决定 |
| `data.scaleTierSnapshot` | `ScaleTierSnapshot` | 后端代码字段 scaleTierSnapshot；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/projects/:address/metrics -->
### GET /api/projects/:address/metrics

- Controller: `ProjectController.Metrics`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "projectAddress": "string",
      "currentPrice": "string",
      "floorPrice": "string",
      "supplyTotal": "string",
      "supplyUserTotal": "string",
      "supplyNode": "string",
      "supplyPledged": "string",
      "floorPool": "string",
      "marketPool": "string",
      "reserveCash": "string",
      "circulatingMarketCap": "string",
      "fdv": "string",
      "nodeSold": "int64",
      "nodeCount": "int64",
      "nodeProgressPct": "string",
      "availableReserve": "string",
      "redeemableValue": "string",
      "floorCoverageRatio": "string",
      "maxSafeFloor": "string",
      "isFloorSolvent": "bool",
      "lastUpdatedAt": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `data.floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.supplyTotal` | `string` | 后端代码字段 supplyTotal；含义由所属响应模型和接口业务决定 |
| `data.supplyUserTotal` | `string` | 后端代码字段 supplyUserTotal；含义由所属响应模型和接口业务决定 |
| `data.supplyNode` | `string` | 后端代码字段 supplyNode；含义由所属响应模型和接口业务决定 |
| `data.supplyPledged` | `string` | 后端代码字段 supplyPledged；含义由所属响应模型和接口业务决定 |
| `data.floorPool` | `string` | 后端代码字段 floorPool；含义由所属响应模型和接口业务决定 |
| `data.marketPool` | `string` | 后端代码字段 marketPool；含义由所属响应模型和接口业务决定 |
| `data.reserveCash` | `string` | 后端代码字段 reserveCash；含义由所属响应模型和接口业务决定 |
| `data.circulatingMarketCap` | `string` | 流通市值 = marketPrice * supplyUserTotal |
| `data.fdv` | `string` | 已废弃语义的兼容字段；当前数值等于 totalMarketCap，合约无固定 maxSupply，不应解释为传统 FDV |
| `data.nodeSold` | `int64` | 后端代码字段 nodeSold；含义由所属响应模型和接口业务决定 |
| `data.nodeCount` | `int64` | 数量统计 |
| `data.nodeProgressPct` | `string` | 百分比数值 |
| `data.availableReserve` | `string` | 后端代码字段 availableReserve；含义由所属响应模型和接口业务决定 |
| `data.redeemableValue` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.floorCoverageRatio` | `string` | 后端代码字段 floorCoverageRatio；含义由所属响应模型和接口业务决定 |
| `data.maxSafeFloor` | `string` | 后端代码字段 maxSafeFloor；含义由所属响应模型和接口业务决定 |
| `data.isFloorSolvent` | `bool` | 后端代码字段 isFloorSolvent；含义由所属响应模型和接口业务决定 |
| `data.lastUpdatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/projects/:address/user-position -->
### GET /api/projects/:address/user-position

- Controller: `ProjectController.UserPosition`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "address": "required user wallet"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.address` | `required user wallet` | required user wallet |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "address": "string",
      "tokenBalance": "string",
      "costBasisStable": "string",
      "nodeTokenAmount": "string",
      "nodeLockedTotal": "string",
      "unreleasedNodeToken": "string",
      "nodeReleaseProgressPct": "string",
      "nodeStartedAt": "string",
      "votingPower": "string",
      "isFullNode": "bool",
      "nodeCount": "int64",
      "pledgedAmount": "string",
      "pledgeDebtStable": "string",
      "claimableFeeStable": "string",
      "claimableTaxStable": "string",
      "claimableUnlockedToken": "string",
      "claimableAltToken": "string",
      "altTokenLocked": "string",
      "altTokenClaimed": "string",
      "points": "string",
      "referrerAddress": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.tokenBalance` | `string` | 后端代码字段 tokenBalance；含义由所属响应模型和接口业务决定 |
| `data.costBasisStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.nodeTokenAmount` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.nodeLockedTotal` | `string` | 后端代码字段 nodeLockedTotal；含义由所属响应模型和接口业务决定 |
| `data.unreleasedNodeToken` | `string` | 后端代码字段 unreleasedNodeToken；含义由所属响应模型和接口业务决定 |
| `data.nodeReleaseProgressPct` | `string` | 百分比数值 |
| `data.nodeStartedAt` | `string` | 节点锁仓开始时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `data.votingPower` | `string` | 后端代码字段 votingPower；含义由所属响应模型和接口业务决定 |
| `data.isFullNode` | `bool` | 后端代码字段 isFullNode；含义由所属响应模型和接口业务决定 |
| `data.nodeCount` | `int64` | 数量统计 |
| `data.pledgedAmount` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.pledgeDebtStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.claimableFeeStable` | `string` | 链上 claimableFees(user) 返回的完整实时可领手续费；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `data.claimableTaxStable` | `string` | 链上 claimableTaxes(user) 返回的完整实时可领利润税；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `data.claimableUnlockedToken` | `string` | 通过合约 claimableUnlocked(user) 实时计算的已解锁未领项目 Token |
| `data.claimableAltToken` | `string` | 链上 claimableAltToken(user) 返回的已归属未领 Alt Token；通过 claim_alt_token 单独领取 |
| `data.altTokenLocked` | `string` | 后端代码字段 altTokenLocked；含义由所属响应模型和接口业务决定 |
| `data.altTokenClaimed` | `string` | 后端代码字段 altTokenClaimed；含义由所属响应模型和接口业务决定 |
| `data.points` | `string` | 后端代码字段 points；含义由所属响应模型和接口业务决定 |
| `data.referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/users/:address/portfolio -->
### GET /api/users/:address/portfolio

- Controller: `ProjectController.Portfolio`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "address": "string",
      "projectCount": "int",
      "totalBalance": "string",
      "totalCostBasis": "string",
      "totalNodeCount": "int64",
      "totalVotingPower": "string",
      "totalPledged": "string",
      "totalDebtStable": "string",
      "totalClaimableStable": "string",
      "totalClaimableFeesStable": "string",
      "totalClaimableTaxesStable": "string",
      "totalClaimableUnlockedToken": "string",
      "totalAltClaimable": "string",
      "positions": "[]PortfolioItem"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.projectCount` | `int` | 数量统计 |
| `data.totalBalance` | `string` | 后端代码字段 totalBalance；含义由所属响应模型和接口业务决定 |
| `data.totalCostBasis` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalNodeCount` | `int64` | 数量统计 |
| `data.totalVotingPower` | `string` | 后端代码字段 totalVotingPower；含义由所属响应模型和接口业务决定 |
| `data.totalPledged` | `string` | 后端代码字段 totalPledged；含义由所属响应模型和接口业务决定 |
| `data.totalDebtStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalClaimableStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalClaimableFeesStable` | `string` | 所有仓位链上实时可领手续费合计 |
| `data.totalClaimableTaxesStable` | `string` | 所有仓位链上实时可领利润税合计 |
| `data.totalClaimableUnlockedToken` | `string` | 所有仓位链上实时已解锁未领 Token 合计 |
| `data.totalAltClaimable` | `string` | 后端代码字段 totalAltClaimable；含义由所属响应模型和接口业务决定 |
| `data.positions` | `[]PortfolioItem` | 后端代码字段 positions；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Claimable fields are refreshed from chain. `claimableUnlockedToken` uses `claimableUnlocked(user)`; `claimableAltToken` is separate and is claimed through `claimAltToken()`. `claimableFeeStable` and `claimableTaxStable` come directly from the final `claimableFees(user)` / `claimableTaxes(user)` getters, which already include lazy node rewards. Pledge amount, debt, and cost basis are read atomically from `pledgePosition(user)`. An RPC refresh failure returns an error instead of replacing values with zero.

<!-- ROUTE: GET /api/users/:address/analytics -->
### GET /api/users/:address/analytics

- Controller: `AnalyticsController.UserAnalytics`
- Auth: HttpOnly Cookie session and address must match path

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "keyword": "optional",
    "status": "optional",
    "projectAddress": "optional",
    "userAddress": "read then overwritten by authenticated path address",
    "actionType": "optional",
    "source": "optional",
    "dateFrom": "optional",
    "dateTo": "optional",
    "sortBy": "optional; preferred",
    "sort": "optional alias",
    "sortOrder": "optional; preferred",
    "direction": "optional alias",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.keyword` | `optional` | optional |
| `query.status` | `optional` | optional |
| `query.projectAddress` | `optional` | optional |
| `query.userAddress` | `read then overwritten by authenticated path address` | read then overwritten by authenticated path address |
| `query.actionType` | `optional` | optional |
| `query.source` | `optional` | optional |
| `query.dateFrom` | `optional` | optional |
| `query.dateTo` | `optional` | optional |
| `query.sortBy` | `optional; preferred` | optional; preferred |
| `query.sort` | `optional alias` | optional alias |
| `query.sortOrder` | `optional; preferred` | optional; preferred |
| `query.direction` | `optional alias` | optional alias |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"address": "string", "business": "UserBusinessSummary", "transactions": {"items": "[]AdminTransactionQueryItem", "total": "int64", "limit": "int", "offset": "int", "sort": "AnalyticsSort"}}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.business` | `UserBusinessSummary` | 后端代码字段 business；含义由所属响应模型和接口业务决定 |
| `data.transactions` | `object` | 后端代码字段 transactions；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/users/:address/business-summary -->
### GET /api/users/:address/business-summary

- Controller: `MemberController.BusinessSummary`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "address": "string",
      "member": "*MemberSummary",
      "portfolio": "*UserPortfolioSummary",
      "transactions": "[]UserTransactionItem",
      "notifications": "[]NotificationItem",
      "dataSource": "string",
      "indexing": "ProjectIndexingStatus"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.member` | `*MemberSummary` | 后端代码字段 member；含义由所属响应模型和接口业务决定 |
| `data.portfolio` | `*UserPortfolioSummary` | 后端代码字段 portfolio；含义由所属响应模型和接口业务决定 |
| `data.transactions` | `[]UserTransactionItem` | 后端代码字段 transactions；含义由所属响应模型和接口业务决定 |
| `data.notifications` | `[]NotificationItem` | 后端代码字段 notifications；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `data.indexing` | `ProjectIndexingStatus` | 后端代码字段 indexing；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/users/:address/created-projects -->
### GET /api/users/:address/created-projects

- Controller: `ProjectController.CreatedProjects`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "address": "string",
    "projectCount": "int",
    "totalClaimableFeeStable": "string",
    "totalClaimableTaxStable": "string",
    "totalClaimableStable": "string",
    "totalClaimedFeeStable": "string",
    "totalClaimedTaxStable": "string",
    "totalClaimedStable": "string",
    "totalEarnedFeeStable": "string",
    "totalEarnedTaxStable": "string",
    "totalEarnedStable": "string",
    "totalCreatorLpFeeDistributedStable": "string",
    "items": [
      {
        "projectAddress": "string",
        "tokenName": "string",
        "tokenSymbol": "string",
        "state": "string",
        "scaleTier": "string",
        "logoUrl": "string",
        "createdAt": "string",
        "updatedAt": "string",
        "currentPrice": "string",
        "floorPrice": "string",
        "currentSupply": "string",
        "nodeSold": "int64",
        "nodeCount": "int64",
        "nodeProgressPct": "string",
        "claimableFeeStable": "string",
        "claimableTaxStable": "string",
        "claimableTotalStable": "string",
        "claimedFeeStable": "string",
        "claimedTaxStable": "string",
        "claimedTotalStable": "string",
        "totalEarnedFeeStable": "string",
        "totalEarnedTaxStable": "string",
        "totalEarnedStable": "string",
        "totalCreatorLpFeeDistributedStable": "string"
      }
    ]
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.projectCount` | `int` | 数量统计 |
| `data.totalClaimableFeeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalClaimableTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalClaimableStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalClaimedFeeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalClaimedTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalClaimedStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalEarnedFeeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalEarnedTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalEarnedStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalCreatorLpFeeDistributedStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.items` | `object` | 当前页数据列表；空结果返回 [] |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/projects/:address/history -->
### GET /api/projects/:address/history

- Controller: `ProjectController.History`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "limit": "default 50, max 100",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.limit` | `default 50, max 100` | default 50, max 100 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]ProjectHistoryItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]ProjectHistoryItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/projects/:address/pledge-history -->
### GET /api/projects/:address/pledge-history

- Controller: `ProjectController.PledgeHistory`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "address": "required user address query",
    "eventType": "default all",
    "startBlock": "optional uint64",
    "endBlock": "optional uint64",
    "limit": "optional integer",
    "offset": "optional integer"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.address` | `required user address query` | required user address query |
| `query.eventType` | `default all` | default all |
| `query.startBlock` | `optional uint64` | optional uint64 |
| `query.endBlock` | `optional uint64` | optional uint64 |
| `query.limit` | `optional integer` | optional integer |
| `query.offset` | `optional integer` | optional integer |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]PledgeHistoryItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]PledgeHistoryItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: eventType is case-sensitive. For PledgeCreated, tokenAmount is collateral locked, boughtToken is newly purchased Token, borrowedStable is floor-backed debt, and userPayStable is wallet payment. For Unpledged, tokenAmount is unlocked Token and unlockCostStable is Stable debt repaid. Display these dimensions separately; never show boughtToken as the pledged amount.

<!-- ROUTE: GET /api/projects/:address/governance -->
### GET /api/projects/:address/governance

- Controller: `ProjectController.Governance`
- Auth: none

治理状态说明：

- `active`：提案仍在投票期内。
- `pending_tally`：投票期已结束，等待链上调用 `tallyVotes`。
- `passed`：已 tally 且通过，等待 timelock 到期后执行。
- `rejected`：已 tally 且未通过。
- `executed`：已执行完成。

治理时间字段说明：

- 提案列表直接返回 `voteStartTime`、`voteEndTime`、`queuedTime`。
- 前端显示倒计时时，应以 `voteEndTime` 作为投票结束时间，以 `queuedTime + timelock` 作为通过后可执行时间参考。

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "limit": "default 50, max 100",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.limit` | `default 50, max 100` | default 50, max 100 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "projectAddress": "string",
    "proposalCount": "int",
    "items": "[]GovernanceProposalItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int",
    "nodeTokenUnit": "string",
    "allowedParamIds": "[]uint8",
    "recommendedParamIds": "[]uint8",
    "unavailableParamIds": "[]uint8"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.proposalCount` | `int` | 数量统计 |
| `data.items` | `[]GovernanceProposalItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
| `data.nodeTokenUnit` | `string` | 后端代码字段 nodeTokenUnit；含义由所属响应模型和接口业务决定 |
| `data.allowedParamIds` | `[]uint8` | 后端代码字段 allowedParamIds；含义由所属响应模型和接口业务决定 |
| `data.recommendedParamIds` | `[]uint8` | 后端代码字段 recommendedParamIds；含义由所属响应模型和接口业务决定 |
| `data.unavailableParamIds` | `[]uint8` | 后端代码字段 unavailableParamIds；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: `snapshotNodeEpoch` freezes node eligibility at proposal creation and `snapshotTotalVotingPower` freezes total voting power for tallying. A wallet whose node eligibility epoch is newer than the proposal snapshot cannot vote. A voter may change support during the active window; the latest VoteCast replaces that voter previous support/weight in the indexed read model.

<!-- ROUTE: GET /api/projects/:address/onchain-state -->
### GET /api/projects/:address/onchain-state

- Controller: `ProjectController.OnChainState`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "projectAddress": "string",
    "chainId": "int64",
    "blockNumber": "uint64",
    "projectState": "string",
    "issuer": "string",
    "platformPayout": "string",
    "stableToken": "string",
    "stableTokenDecimals": "uint8",
    "curveTable": "string",
    "curveParamsHash": "string",
    "altToken": "string",
    "altTokenDecimals": "uint8",
    "altTokenPerNode": "string",
    "altTokenPerNodeWei": "string",
    "currentPrice": "string",
    "marketPrice": "string",
    "marketPriceSource": "string",
    "marketPriceLive": "bool",
    "marketPriceSnapshotBlock": "uint64",
    "floorPrice": "string",
    "supplyTotal": "string",
    "supplyUserTotal": "string",
    "supplyNode": "string",
    "supplyPledged": "string",
    "totalPledgeDebtStable": "string",
    "curveSupply": "string",
    "floorPool": "string",
    "marketPool": "string",
    "reserveCash": "string",
    "floorAllocationBps": "string",
    "ratchetThresholdBps": "string",
    "ratchetThreshold": "string",
    "buyFeeRate": "string",
    "sellFeeRate": "string",
    "profitTaxRate": "string",
    "minSingleBuyStable": "string",
    "nodeSold": "uint64",
    "nodeCount": "uint64",
    "nodePriceStable": "string",
    "nodeTargetStable": "string",
    "nodeTokenUnit": "string",
    "nodeSaleDeadline": "uint64",
    "presaleLockStart": "uint64",
    "nodeProgressPct": "string",
    "tradingStartedAt": "uint64",
    "graduationWindowVolume": "string",
    "maxNodesPerAddress": "uint64",
    "nodeVestingCliffDuration": "uint64",
    "nodeVestingTotalDuration": "uint64",
    "graduationAnnounceTime": "uint64",
    "graduatedPoolId": "string",
    "graduationAdapter": "string",
    "graduationHookOrLock": "string",
    "graduatedAt": "uint64",
    "graduationReserveThreshold": "string",
    "graduationTradingDuration": "uint64",
    "graduationAnnounceDelay": "uint64",
    "graduationVolumeWindow": "uint64",
    "votingPeriodDuration": "uint64",
    "timelockDelayDuration": "uint64",
    "feeFloorBps": "string",
    "feeNodeBps": "string",
    "feeIssuerBps": "string",
    "feePlatformBps": "string",
    "taxFloorBps": "string",
    "taxNodeBps": "string",
    "taxIssuerBps": "string",
    "taxPlatformBps": "string",
    "lpFeeNodeBps": "string",
    "lpFeeCreatorBps": "string",
    "lpFeePlatformBps": "string",
    "nextProposalId": "uint64",
    "nodeVotingEpoch": "uint64",
    "totalVotingPower": "string",
    "lastSyncedAt": "string",
    "dataSource": "string"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `data.projectState` | `string` | 后端代码字段 projectState；含义由所属响应模型和接口业务决定 |
| `data.issuer` | `string` | 后端代码字段 issuer；含义由所属响应模型和接口业务决定 |
| `data.platformPayout` | `string` | 后端代码字段 platformPayout；含义由所属响应模型和接口业务决定 |
| `data.stableToken` | `string` | 后端代码字段 stableToken；含义由所属响应模型和接口业务决定 |
| `data.stableTokenDecimals` | `uint8` | Stable Token 的真实精度；Ethereum Sepolia 为 USDGTest 6 位，项目 Token 仍为 18 位 |
| `data.curveTable` | `string` | 后端代码字段 curveTable；含义由所属响应模型和接口业务决定 |
| `data.curveParamsHash` | `string` | 曲线配置参数的 bytes32 哈希；用于核对 Scale Tier 与 CurveTable 参数版本 |
| `data.altToken` | `string` | 后端代码字段 altToken；含义由所属响应模型和接口业务决定 |
| `data.altTokenDecimals` | `uint8` | 后端代码字段 altTokenDecimals；含义由所属响应模型和接口业务决定 |
| `data.altTokenPerNode` | `string` | 每节点托管的 Alt Token 数量；未启用时可传空字符串、0 或 0.0，启用时必须是按该 Token 实际 decimals 解析后的正数 |
| `data.altTokenPerNodeWei` | `string` | 后端代码字段 altTokenPerNodeWei；含义由所属响应模型和接口业务决定 |
| `data.currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `data.marketPrice` | `string` | 用于市值计算的统一市场价；Trading/Graduating 取曲线价，Graduated 优先取官方 Uniswap v4 StateView slot0 现价 |
| `data.marketPriceSource` | `string` | 市场价来源枚举：bonding_curve/uniswap_v4_slot0/graduation_price_fallback/unavailable |
| `data.marketPriceLive` | `bool` | 市场价是否为当前快照的可用实时价；fallback 或 unavailable 时为 false |
| `data.marketPriceSnapshotBlock` | `uint64` | 读取价格和供应量的同一链上区块高度 |
| `data.floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.supplyTotal` | `string` | 后端代码字段 supplyTotal；含义由所属响应模型和接口业务决定 |
| `data.supplyUserTotal` | `string` | 后端代码字段 supplyUserTotal；含义由所属响应模型和接口业务决定 |
| `data.supplyNode` | `string` | 后端代码字段 supplyNode；含义由所属响应模型和接口业务决定 |
| `data.supplyPledged` | `string` | 后端代码字段 supplyPledged；含义由所属响应模型和接口业务决定 |
| `data.totalPledgeDebtStable` | `string` | 项目当前链上质押债务总额，USDGTest 可读数量字符串；毕业执行后归零 |
| `data.curveSupply` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.floorPool` | `string` | 后端代码字段 floorPool；含义由所属响应模型和接口业务决定 |
| `data.marketPool` | `string` | 后端代码字段 marketPool；含义由所属响应模型和接口业务决定 |
| `data.reserveCash` | `string` | 后端代码字段 reserveCash；含义由所属响应模型和接口业务决定 |
| `data.floorAllocationBps` | `string` | 万分比整数，10000 表示 100% |
| `data.ratchetThresholdBps` | `string` | 万分比整数，10000 表示 100% |
| `data.ratchetThreshold` | `string` | 后端代码字段 ratchetThreshold；含义由所属响应模型和接口业务决定 |
| `data.buyFeeRate` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.sellFeeRate` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.profitTaxRate` | `string` | 后端代码字段 profitTaxRate；含义由所属响应模型和接口业务决定 |
| `data.minSingleBuyStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.nodeSold` | `uint64` | 后端代码字段 nodeSold；含义由所属响应模型和接口业务决定 |
| `data.nodeCount` | `uint64` | 数量统计 |
| `data.nodePriceStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.nodeTargetStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.nodeTokenUnit` | `string` | 后端代码字段 nodeTokenUnit；含义由所属响应模型和接口业务决定 |
| `data.nodeSaleDeadline` | `uint64` | 后端代码字段 nodeSaleDeadline；含义由所属响应模型和接口业务决定 |
| `data.presaleLockStart` | `uint64` | 预售节点统一锁仓起点，Unix 秒；只有节点售罄并进入 Trading 后才大于 0 |
| `data.nodeProgressPct` | `string` | 百分比数值 |
| `data.tradingStartedAt` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `data.graduationWindowVolume` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.maxNodesPerAddress` | `uint64` | 建项时固化的单地址节点认购数量上限；必须在 1..nodeCount 之间，后端会结合 nodeSoldBy(wallet) 计算剩余可认购数 |
| `data.nodeVestingCliffDuration` | `uint64` | 节点 Token 开始线性释放前的 cliff 秒数；建项时从 Scale Tier 固化 |
| `data.nodeVestingTotalDuration` | `uint64` | 节点 Token 从锁定开始到全部释放的总秒数；必须大于 nodeVestingCliffDuration |
| `data.graduationAnnounceTime` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `data.graduatedPoolId` | `string` | 后端代码字段 graduatedPoolId；含义由所属响应模型和接口业务决定 |
| `data.graduationAdapter` | `string` | 毕业 Adapter 地址；enabled=true 时必须等于 Platform.trustedGraduationAdapter() |
| `data.graduationHookOrLock` | `string` | 后端代码字段 graduationHookOrLock；含义由所属响应模型和接口业务决定 |
| `data.graduatedAt` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `data.graduationReserveThreshold` | `string` | 后端代码字段 graduationReserveThreshold；含义由所属响应模型和接口业务决定 |
| `data.graduationTradingDuration` | `uint64` | 后端代码字段 graduationTradingDuration；含义由所属响应模型和接口业务决定 |
| `data.graduationAnnounceDelay` | `uint64` | 后端代码字段 graduationAnnounceDelay；含义由所属响应模型和接口业务决定 |
| `data.graduationVolumeWindow` | `uint64` | 毕业成交量滚动窗口秒数；后端只展示链上窗口语义 |
| `data.votingPeriodDuration` | `uint64` | 后端代码字段 votingPeriodDuration；含义由所属响应模型和接口业务决定 |
| `data.timelockDelayDuration` | `uint64` | 后端代码字段 timelockDelayDuration；含义由所属响应模型和接口业务决定 |
| `data.feeFloorBps` | `string` | 手续费地板分账 BPS；固定 6000（60%），不可修改 |
| `data.feeNodeBps` | `string` | 手续费节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `data.feeIssuerBps` | `string` | 手续费发射方分账 BPS；初始 1000（10%），可通过 paramId=5 在 3000 BPS 灵活份额内调整 |
| `data.feePlatformBps` | `string` | 手续费平台分账 BPS；固定 1000（10%），不可修改 |
| `data.taxFloorBps` | `string` | 盈利税地板分账 BPS；固定 6000（60%），不可修改 |
| `data.taxNodeBps` | `string` | 盈利税节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `data.taxIssuerBps` | `string` | 盈利税发射方分账 BPS；初始 1000（10%），可通过 paramId=8 在 3000 BPS 灵活份额内调整 |
| `data.taxPlatformBps` | `string` | 盈利税平台分账 BPS；固定 1000（10%），不可修改 |
| `data.lpFeeNodeBps` | `string` | 万分比整数，10000 表示 100% |
| `data.lpFeeCreatorBps` | `string` | 万分比整数，10000 表示 100% |
| `data.lpFeePlatformBps` | `string` | 万分比整数，10000 表示 100% |
| `data.nextProposalId` | `uint64` | 后端代码字段 nextProposalId；含义由所属响应模型和接口业务决定 |
| `data.nodeVotingEpoch` | `uint64` | 当前节点投票资格代际；新获得节点资格时递增 |
| `data.totalVotingPower` | `string` | 后端代码字段 totalVotingPower；含义由所属响应模型和接口业务决定 |
| `data.lastSyncedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/projects/:address/indexing-status -->
### GET /api/projects/:address/indexing-status

- Controller: `ProjectController.IndexingStatus`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "projectAddress": "string",
      "chainId": "int64",
      "status": "string",
      "lastEventBlock": "uint64",
      "lastEventAt": "string",
      "lastSnapshotBlock": "uint64",
      "lastSnapshotAt": "string",
      "metricsBlock": "uint64",
      "metricsUpdatedAt": "string",
      "projectCheckpoint": "uint64",
      "platformCheckpoint": "uint64",
      "eventsIndexed": "uint64",
      "unappliedEventLag": "uint64",
      "snapshotLag": "uint64",
      "checkpointLag": "uint64",
      "needsReconcile": "bool",
      "recommendedAction": "string",
      "dataSource": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.lastEventBlock` | `uint64` | 后端代码字段 lastEventBlock；含义由所属响应模型和接口业务决定 |
| `data.lastEventAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.lastSnapshotBlock` | `uint64` | 后端代码字段 lastSnapshotBlock；含义由所属响应模型和接口业务决定 |
| `data.lastSnapshotAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.metricsBlock` | `uint64` | 后端代码字段 metricsBlock；含义由所属响应模型和接口业务决定 |
| `data.metricsUpdatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.projectCheckpoint` | `uint64` | 后端代码字段 projectCheckpoint；含义由所属响应模型和接口业务决定 |
| `data.platformCheckpoint` | `uint64` | 后端代码字段 platformCheckpoint；含义由所属响应模型和接口业务决定 |
| `data.eventsIndexed` | `uint64` | 布尔状态标记 |
| `data.unappliedEventLag` | `uint64` | 后端代码字段 unappliedEventLag；含义由所属响应模型和接口业务决定 |
| `data.snapshotLag` | `uint64` | 后端代码字段 snapshotLag；含义由所属响应模型和接口业务决定 |
| `data.checkpointLag` | `uint64` | 后端代码字段 checkpointLag；含义由所属响应模型和接口业务决定 |
| `data.needsReconcile` | `bool` | 后端代码字段 needsReconcile；含义由所属响应模型和接口业务决定 |
| `data.recommendedAction` | `string` | 后端代码字段 recommendedAction；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/transactions/:hash/status -->
### GET /api/transactions/:hash/status

- Controller: `ProjectController.TransactionStatus`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "hash": "path hash"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.hash` | `path hash` | 后端代码字段 hash；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "txHash": "string",
      "chainId": "int64",
      "status": "string",
      "receiptFound": "bool",
      "receiptStatus": "string",
      "failureReason": "string",
      "indexed": "bool",
      "blockNumber": "uint64",
      "latestBlock": "uint64",
      "confirmations": "uint64",
      "indexedBlockNumber": "uint64",
      "indexedAt": "string",
      "checkpointBlock": "uint64",
      "checkpointLag": "uint64",
      "events": "[]TransactionIndexedEvent",
      "dataSource": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.txHash` | `string` | 链上交易哈希 |
| `data.chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.receiptFound` | `bool` | 后端代码字段 receiptFound；含义由所属响应模型和接口业务决定 |
| `data.receiptStatus` | `string` | 后端代码字段 receiptStatus；含义由所属响应模型和接口业务决定 |
| `data.failureReason` | `string` | 后端代码字段 failureReason；含义由所属响应模型和接口业务决定 |
| `data.indexed` | `bool` | 后端代码字段 indexed；含义由所属响应模型和接口业务决定 |
| `data.blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `data.latestBlock` | `uint64` | 后端代码字段 latestBlock；含义由所属响应模型和接口业务决定 |
| `data.confirmations` | `uint64` | 后端代码字段 confirmations；含义由所属响应模型和接口业务决定 |
| `data.indexedBlockNumber` | `uint64` | 后端代码字段 indexedBlockNumber；含义由所属响应模型和接口业务决定 |
| `data.indexedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.checkpointBlock` | `uint64` | 后端代码字段 checkpointBlock；含义由所属响应模型和接口业务决定 |
| `data.checkpointLag` | `uint64` | 后端代码字段 checkpointLag；含义由所属响应模型和接口业务决定 |
| `data.events` | `[]TransactionIndexedEvent` | 后端代码字段 events；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/points/rules -->
### GET /api/points/rules

- Controller: `PointController.Rules`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {},
  "query": {},
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求无 Header、Path、Query 或 Body 业务字段。
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "version": "string",
      "precision": "int",
      "rounding": "string",
      "directReferralRateBps": "uint64",
      "basePointSources": "[]string",
      "effectivePointComponents": "[]string",
      "personalLevels": "[]PointLevelRule",
      "teamRewardTiers": "[]TeamRewardTier",
      "teamRewardTiming": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.version` | `string` | 积分规则版本；当前固定为 seedx-points-20260807 |
| `data.precision` | `int` | 积分存储与结算小数位数；当前为 6 |
| `data.rounding` | `string` | 舍入规则严格枚举；floor=向下截断 |
| `data.directReferralRateBps` | `uint64` | 直属邀请奖励 BPS；当前为 1000，即基础积分的 10% |
| `data.basePointSources` | `[]string` | 会产生基础积分的流水类型；仅 base_buy/base_pledge |
| `data.effectivePointComponents` | `[]string` | 个人有效积分组成；基础积分+直属邀请奖励+团队运营奖励 |
| `data.personalLevels` | `[]PointLevelRule` | 个人 L0-L5 门槛列表；前端必须使用此列表而不是硬编码 |
| `data.teamRewardTiers` | `[]TeamRewardTier` | 全部下线累计基础积分对应的 0%-3% 团队奖励阶梯 |
| `data.teamRewardTiming` | `string` | 团队比例生效时点；当前笔用交易前比例，跨门槛后下一笔生效 |
<!-- /RESPONSE_FIELDS -->

Notes: 公开、无登录依赖；前端必须以此返回的 L0-L5、团队阶梯、精度和结算时点展示规则。

<!-- ROUTE: GET /api/users/:address/points -->
### GET /api/users/:address/points

- Controller: `PointController.UserPoints`
- Auth: address-matched HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {
    "address": "logged-in wallet address"
  },
  "query": {},
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `logged-in wallet address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "address": "string",
    "inviteCode": "string",
    "totalPoints": "string",
    "personalEffectivePoints": "string",
    "personalLevel": "string",
    "personalLevelName": "string",
    "nextPersonalLevel": "string",
    "pointsToNextPersonalLevel": "string",
    "basePointsTotal": "string",
    "directReferralRewardTotal": "string",
    "teamOperationRewardTotal": "string",
    "referrerAddress": "string",
    "referrerInviteCode": "string",
    "referralRewardTotal": "string",
    "directReferralCount": "int",
    "networkSummary": "PointNetworkSummary",
    "recentActivity": "PointActivitySummary",
    "downlinePoints": "string",
    "teamLeaderAddress": "string",
    "isTeamLeader": "bool",
    "teamDownlineBasePoints": "string",
    "teamRewardRateBps": "uint64",
    "nextTeamRewardThreshold": "string",
    "directReferrals": "[]DirectReferralItem",
    "ledger": "[]PointLedgerItem"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
| `data.totalPoints` | `string` | 个人有效积分兼容别名；数值等于 personalEffectivePoints |
| `data.personalEffectivePoints` | `string` | 个人累计有效积分；包含基础积分、10% 直属邀请奖励和团队运营奖励 |
| `data.personalLevel` | `string` | 个人积分等级严格枚举：L0/L1/L2/L3/L4/L5；达到后永久保留 |
| `data.personalLevelName` | `string` | 个人等级中文名称：普通/青铜/白银/黄金/铂金/黑金 |
| `data.nextPersonalLevel` | `string` | 下一个个人等级；L5 时为空字符串 |
| `data.pointsToNextPersonalLevel` | `string` | 距下一个人等级还需的有效积分；L5 时为 0.000000 |
| `data.basePointsTotal` | `string` | 本人 Buy 和 PledgeCreated 的真实净入曲线金额乘项目倍率后累计的基础积分 |
| `data.directReferralRewardTotal` | `string` | 作为实际直属邀请人累计获得的 10% 邀请奖励 |
| `data.teamOperationRewardTotal` | `string` | 作为推荐树根节点团队长累计获得的 0%-3% 团队运营奖励 |
| `data.referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `data.referrerInviteCode` | `string` | 后端代码字段 referrerInviteCode；含义由所属响应模型和接口业务决定 |
| `data.referralRewardTotal` | `string` | 后端代码字段 referralRewardTotal；含义由所属响应模型和接口业务决定 |
| `data.directReferralCount` | `int` | 数量统计 |
| `data.networkSummary` | `PointNetworkSummary` | 后端代码字段 networkSummary；含义由所属响应模型和接口业务决定 |
| `data.recentActivity` | `PointActivitySummary` | 后端代码字段 recentActivity；含义由所属响应模型和接口业务决定 |
| `data.downlinePoints` | `string` | 当前查询钱包自己的全部下线累计基础积分；不是所属根团队总额，不含奖励积分 |
| `data.teamLeaderAddress` | `string` | 当前推荐树没有上级的唯一根节点钱包地址 |
| `data.isTeamLeader` | `bool` | 当前查询钱包是否为所属推荐树的根节点 |
| `data.teamDownlineBasePoints` | `string` | 根团队全部下线历史累计基础积分；不包含团队长本人、邀请奖励或团队奖励 |
| `data.teamRewardRateBps` | `uint64` | 根团队当前团队运营奖励 BPS；0/50/100/150/200/300 |
| `data.nextTeamRewardThreshold` | `string` | 下一个团队奖励阶梯所需的全部下线累计基础积分；最高档为空 |
| `data.directReferrals` | `[]DirectReferralItem` | 后端代码字段 directReferrals；含义由所属响应模型和接口业务决定 |
| `data.ledger` | `[]PointLedgerItem` | 后端代码字段 ledger；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

Notes: 返回新版个人有效积分、L0-L5、直属关系和唯一根团队摘要；directReferrals/ledger 是兼容预览，完整列表必须调用分页子接口。

<!-- ROUTE: GET /api/users/:address/points/ledger -->
### GET /api/users/:address/points/ledger

- Controller: `PointController.UserLedger`
- Auth: address-matched HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {
    "address": "logged-in wallet address"
  },
  "query": {
    "projectAddress": "optional project",
    "sourceType": "optional point entry type",
    "txHash": "optional transaction hash",
    "startTime": "optional RFC3339",
    "endTime": "optional RFC3339",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `logged-in wallet address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.projectAddress` | `optional project` | optional project |
| `query.sourceType` | `optional point entry type` | optional point entry type |
| `query.txHash` | `optional transaction hash` | optional transaction hash |
| `query.startTime` | `optional RFC3339` | optional RFC3339 |
| `query.endTime` | `optional RFC3339` | optional RFC3339 |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]PointLedgerItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]PointLedgerItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 只允许钱包本人查询；sourceType 使用 pointEntryType 枚举。

<!-- ROUTE: GET /api/users/:address/points/level-history -->
### GET /api/users/:address/points/level-history

- Controller: `PointController.LevelHistory`
- Auth: address-matched HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {
    "address": "logged-in wallet address"
  },
  "query": {
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `logged-in wallet address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]PointLevelHistoryItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]PointLevelHistoryItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 返回个人等级永久升级历史。

<!-- ROUTE: GET /api/users/:address/referrals/direct -->
### GET /api/users/:address/referrals/direct

- Controller: `PointController.DirectReferrals`
- Auth: address-matched HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {
    "address": "logged-in wallet address"
  },
  "query": {
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `logged-in wallet address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]DirectReferralItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]DirectReferralItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 仅返回第一层实际邀请关系；referralReward 是当前钱包从该下线累计获得的 10% 奖励，joinedAt 是推荐绑定生效时间。

<!-- ROUTE: GET /api/users/:address/team/summary -->
### GET /api/users/:address/team/summary

- Controller: `PointController.TeamSummary`
- Auth: address-matched HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {
    "address": "logged-in wallet address"
  },
  "query": {},
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `logged-in wallet address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "address": "string",
      "teamLeaderAddress": "string",
      "isTeamLeader": "bool",
      "downlineCount": "uint64",
      "cumulativeBasePoints": "string",
      "currentRewardRateBps": "uint64",
      "nextRewardThreshold": "string",
      "pointsToNextRewardTier": "string",
      "teamOperationRewardTotal": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.teamLeaderAddress` | `string` | 当前推荐树没有上级的唯一根节点钱包地址 |
| `data.isTeamLeader` | `bool` | 当前查询钱包是否为所属推荐树的根节点 |
| `data.downlineCount` | `uint64` | 数量统计 |
| `data.cumulativeBasePoints` | `string` | 当前根团队全部下线累计基础积分；不含根节点本人 |
| `data.currentRewardRateBps` | `uint64` | 当前根团队后续交易使用的奖励 BPS；0/50/100/150/200/300 |
| `data.nextRewardThreshold` | `string` | 后端代码字段 nextRewardThreshold；含义由所属响应模型和接口业务决定 |
| `data.pointsToNextRewardTier` | `string` | 距下一团队奖励阶梯还需的下线基础积分 |
| `data.teamOperationRewardTotal` | `string` | 作为推荐树根节点团队长累计获得的 0%-3% 团队运营奖励 |
<!-- /RESPONSE_FIELDS -->

Notes: 普通成员返回其所属根团队摘要；isTeamLeader 仅在当前钱包是根节点时为 true。

<!-- ROUTE: GET /api/users/:address/team/members -->
### GET /api/users/:address/team/members

- Controller: `PointController.TeamMembers`
- Auth: address-matched HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {
    "address": "logged-in wallet address"
  },
  "query": {
    "keyword": "optional wallet substring",
    "personalLevel": "optional L0-L5",
    "depth": "optional positive integer",
    "minBasePoints": "optional non-negative decimal with at most 6 places; inclusive minimum",
    "maxBasePoints": "optional non-negative decimal with at most 6 places; inclusive maximum",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `logged-in wallet address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.keyword` | `optional wallet substring` | optional wallet substring |
| `query.personalLevel` | `optional L0-L5` | optional L0-L5 |
| `query.depth` | `optional positive integer` | optional positive integer |
| `query.minBasePoints` | `optional non-negative decimal with at most 6 places; inclusive minimum` | optional non-negative decimal with at most 6 places; inclusive minimum |
| `query.maxBasePoints` | `optional non-negative decimal with at most 6 places; inclusive maximum` | optional non-negative decimal with at most 6 places; inclusive maximum |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]TeamMemberItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]TeamMemberItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 返回所属根团队全部下线；depth=1 表示根团队长的直属下线，贡献区间按成员累计 basePoints 闭区间筛选。

<!-- ROUTE: GET /api/users/:address/team/contributions -->
### GET /api/users/:address/team/contributions

- Controller: `PointController.TeamContributions`
- Auth: address-matched HttpOnly Cookie session

Request:
```json
{
  "headers": {},
  "path": {
    "address": "logged-in wallet address"
  },
  "query": {
    "sourceUserAddress": "optional contributing wallet",
    "projectAddress": "optional project",
    "startTime": "optional RFC3339",
    "endTime": "optional RFC3339",
    "limit": "default 30, max 200",
    "offset": "default 0"
  },
  "body": {}
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `logged-in wallet address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.sourceUserAddress` | `optional contributing wallet` | optional contributing wallet |
| `query.projectAddress` | `optional project` | optional project |
| `query.startTime` | `optional RFC3339` | optional RFC3339 |
| `query.endTime` | `optional RFC3339` | optional RFC3339 |
| `query.limit` | `default 30, max 200` | default 30, max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]PointLedgerItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]PointLedgerItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->

Notes: 只返回当前团队全部下线的 base_buy/base_pledge，不包含邀请和团队奖励。

<!-- ROUTE: POST /api/users/:address/referrer -->
### POST /api/users/:address/referrer

- Controller: `PointController.BindReferrer`
- Auth: HttpOnly Cookie session and address must match path

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": {
    "referrerAddress": "string",
    "inviteCode": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `body.referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `body.inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "address": "string",
    "inviteCode": "string",
    "referrerAddress": "string",
    "referrerInviteCode": "string",
    "teamLeaderAddress": "string",
    "bound": "bool",
    "status": "string"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
| `data.referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `data.referrerInviteCode` | `string` | 后端代码字段 referrerInviteCode；含义由所属响应模型和接口业务决定 |
| `data.teamLeaderAddress` | `string` | 当前推荐树没有上级的唯一根节点钱包地址 |
| `data.bound` | `bool` | 后端代码字段 bound；含义由所属响应模型和接口业务决定 |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `BindReferrerRequest`; unknown fields return 400. Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: POST /api/quotes/preview-buy -->
### POST /api/quotes/preview-buy

- Controller: `QuoteController.PreviewBuy`
- Auth: none

Request:
```json
{
  "headers": {
    "Content-Type": "application/json"
  },
  "path": {},
  "query": {},
  "body": {
    "projectAddress": "string",
    "cashIn": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `body.projectAddress` | `string` | 项目合约地址 |
| `body.cashIn` | `string` | 买入时输入的稳定币金额 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "tokenOut": "string",
      "feeStable": "string",
      "netCash": "string",
      "currentPrice": "string",
      "newPrice": "string",
      "averagePrice": "string",
      "priceImpactPct": "string",
      "snapshotBlock": "uint64"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.tokenOut` | `string` | 后端代码字段 tokenOut；含义由所属响应模型和接口业务决定 |
| `data.feeStable` | `string` | 本次操作的稳定币手续费 |
| `data.netCash` | `string` | 后端代码字段 netCash；含义由所属响应模型和接口业务决定 |
| `data.currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `data.newPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.averagePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.priceImpactPct` | `string` | 百分比数值 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `BuyQuoteRequest`; unknown fields return 400.

<!-- ROUTE: POST /api/quotes/preview-buy-by-token -->
### POST /api/quotes/preview-buy-by-token

- Controller: `QuoteController.PreviewBuyByToken`
- Auth: none

Request:
```json
{
  "headers": {
    "Content-Type": "application/json"
  },
  "path": {},
  "query": {},
  "body": {
    "projectAddress": "string",
    "tokenOut": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `body.projectAddress` | `string` | 项目合约地址 |
| `body.tokenOut` | `string` | 后端代码字段 tokenOut；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "requestedTokenOut": "string",
    "cashIn": "string",
    "tokenOut": "string",
    "feeStable": "string",
    "netCash": "string",
    "currentPrice": "string",
    "newPrice": "string",
    "averagePrice": "string",
    "priceImpactPct": "string",
    "snapshotBlock": "uint64"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.requestedTokenOut` | `string` | 后端代码字段 requestedTokenOut；含义由所属响应模型和接口业务决定 |
| `data.cashIn` | `string` | 买入时输入的稳定币金额 |
| `data.tokenOut` | `string` | 后端代码字段 tokenOut；含义由所属响应模型和接口业务决定 |
| `data.feeStable` | `string` | 本次操作的稳定币手续费 |
| `data.netCash` | `string` | 后端代码字段 netCash；含义由所属响应模型和接口业务决定 |
| `data.currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `data.newPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.averagePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.priceImpactPct` | `string` | 百分比数值 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->

Notes: body strictly matches `BuyByTokenQuoteRequest`; unknown fields return 400. Exact-output buy quote: `tokenOut` is the minimum desired project Token. The backend solves the smallest cashIn and verifies it with the canonical quote path. The target must satisfy project state and minSingleBuy; the 0821 contract has no maximum single-buy or supply-percentage cap. Use response cashIn for action-tx-params and apply slippage to tokenOut.

<!-- ROUTE: POST /api/quotes/preview-sell -->
### POST /api/quotes/preview-sell

- Controller: `QuoteController.PreviewSell`
- Auth: none

Request:
```json
{
  "headers": {
    "Content-Type": "application/json"
  },
  "path": {},
  "query": {},
  "body": {
    "projectAddress": "string",
    "userAddress": "string",
    "tokenIn": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `body.projectAddress` | `string` | 项目合约地址 |
| `body.userAddress` | `string` | 用户钱包地址 |
| `body.tokenIn` | `string` | 卖出时输入的项目 Token 数量 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "grossStable": "string",
      "feeStable": "string",
      "profitStable": "string",
      "profitTaxStable": "string",
      "cashOut": "string",
      "currentPrice": "string",
      "newPrice": "string",
      "averagePrice": "string",
      "priceImpactPct": "string",
      "snapshotBlock": "uint64"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.grossStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.feeStable` | `string` | 本次操作的稳定币手续费 |
| `data.profitStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.profitTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.cashOut` | `string` | 后端代码字段 cashOut；含义由所属响应模型和接口业务决定 |
| `data.currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `data.newPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.averagePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.priceImpactPct` | `string` | 百分比数值 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `SellQuoteRequest`; unknown fields return 400.

<!-- ROUTE: POST /api/quotes/preview-sell-by-cash -->
### POST /api/quotes/preview-sell-by-cash

- Controller: `QuoteController.PreviewSellByCash`
- Auth: none

Request:
```json
{
  "headers": {
    "Content-Type": "application/json"
  },
  "path": {},
  "query": {},
  "body": {
    "projectAddress": "string",
    "userAddress": "string",
    "cashOut": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `body.projectAddress` | `string` | 项目合约地址 |
| `body.userAddress` | `string` | 用户钱包地址 |
| `body.cashOut` | `string` | 后端代码字段 cashOut；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "requestedCashOut": "string",
    "tokenIn": "string",
    "grossStable": "string",
    "feeStable": "string",
    "profitStable": "string",
    "profitTaxStable": "string",
    "cashOut": "string",
    "currentPrice": "string",
    "newPrice": "string",
    "averagePrice": "string",
    "priceImpactPct": "string",
    "snapshotBlock": "uint64"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.requestedCashOut` | `string` | 后端代码字段 requestedCashOut；含义由所属响应模型和接口业务决定 |
| `data.tokenIn` | `string` | 卖出时输入的项目 Token 数量 |
| `data.grossStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.feeStable` | `string` | 本次操作的稳定币手续费 |
| `data.profitStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.profitTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.cashOut` | `string` | 后端代码字段 cashOut；含义由所属响应模型和接口业务决定 |
| `data.currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `data.newPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.averagePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.priceImpactPct` | `string` | 百分比数值 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->

Notes: body strictly matches `SellByCashQuoteRequest`; unknown fields return 400. Exact-output sell quote: `cashOut` is the minimum desired net USDGTest receipt and is rounded down to Stable precision. `userAddress` is mandatory because profit tax depends on wallet balance and costBasis. The backend finds minimum tokenIn within wallet balance and verifies it against on-chain sellReturn. The 0821 contract has no single-sell percentage cap.

<!-- ROUTE: POST /api/quotes/pledge-buy-range -->
### POST /api/quotes/pledge-buy-range

- Controller: `QuoteController.PledgeBuyRange`
- Auth: none

Request:
```json
{
  "headers": {
    "Content-Type": "application/json"
  },
  "path": {},
  "query": {},
  "body": {
    "projectAddress": "string",
    "userAddress": "string",
    "pledgeToken": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `body.projectAddress` | `string` | 项目合约地址 |
| `body.userAddress` | `string` | 用户钱包地址 |
| `body.pledgeToken` | `string` | 用于质押的项目 Token 数量；字段名必须是 pledgeToken |
<!-- /REQUEST_FIELDS -->

Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "projectAddress": "string",
    "userAddress": "string",
    "pledgeToken": "string",
    "rangeAvailable": "bool",
    "protocolRangeAvailable": "bool",
    "collateralSufficient": "bool",
    "minBuyToken": "string",
    "maxBuyToken": "string",
    "maxAffordableBuyToken": "string",
    "effectiveMaxBuyToken": "string",
    "recommendedBuyToken": "string",
    "projectTokenBalance": "string",
    "stableBalance": "string",
    "stableTokenAddress": "string",
    "stableTokenSymbol": "string",
    "stableTokenDecimals": "uint8",
    "snapshotBlock": "uint64",
    "constraints": "[]PledgeBuyConstraint"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.userAddress` | `string` | 用户钱包地址 |
| `data.pledgeToken` | `string` | 用于质押的项目 Token 数量；字段名必须是 pledgeToken |
| `data.rangeAvailable` | `bool` | 项目状态、质押 Token 余额、协议区间与 Stable 可支付能力全部通过时为 true |
| `data.protocolRangeAvailable` | `bool` | 不考虑钱包余额时，合约数学约束是否仍有非空 buyToken 区间 |
| `data.collateralSufficient` | `bool` | 钱包项目 Token 余额是否覆盖 pledgeToken |
| `data.minBuyToken` | `string` | 同一 snapshotBlock 下同时满足所有链上约束的最小 buyToken |
| `data.maxBuyToken` | `string` | 仅考虑协议规则的最大 buyToken；未扣除当前钱包 Stable 余额约束 |
| `data.maxAffordableBuyToken` | `string` | 按当前钱包 Stable 余额可支付的最大 buyToken |
| `data.effectiveMaxBuyToken` | `string` | 协议上限与钱包可支付上限的较小值 |
| `data.recommendedBuyToken` | `string` | 区间可用时等于 minBuyToken，可用作前端“推荐”按钮填充值；不可用时为 0 |
| `data.projectTokenBalance` | `string` | 后端代码字段 projectTokenBalance；含义由所属响应模型和接口业务决定 |
| `data.stableBalance` | `string` | 后端代码字段 stableBalance；含义由所属响应模型和接口业务决定 |
| `data.stableTokenAddress` | `string` | 当前网络配置的稳定币合约地址 |
| `data.stableTokenSymbol` | `string` | 后端代码字段 stableTokenSymbol；含义由所属响应模型和接口业务决定 |
| `data.stableTokenDecimals` | `uint8` | Stable Token 的真实精度；Ethereum Sepolia 为 USDGTest 6 位，项目 Token 仍为 18 位 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `data.constraints` | `[]PledgeBuyConstraint` | 后端代码字段 constraints；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->

Notes: body strictly matches `PledgeBuyRangeRequest`; unknown fields return code 30011. At one `snapshotBlock`, the backend intersects `pledgeToken <= buyToken`, project minSingleBuy, minimum 20% self-pay, maximum 70% post-pledge ratio, collateral balance and Stable affordability. The 0821 contract has no maxSingleBuy or maxSingleTradeBps. `minBuyToken..effectiveMaxBuyToken` is executable only while `rangeAvailable=true`; `recommendedBuyToken=minBuyToken`. `constraints[].reasonCode` enums: PROJECT_STATE_TRADING, PROJECT_TOKEN_BALANCE, PLEDGE_NOT_GREATER_THAN_BUY, MIN_SINGLE_BUY, SELF_PAY_MIN_20_PCT, POST_PLEDGE_RATIO_LIMIT, STABLE_BALANCE.

<!-- ROUTE: POST /api/quotes/preview-pledge -->
### POST /api/quotes/preview-pledge

- Controller: `QuoteController.PreviewPledge`
- Auth: none

Request:
```json
{
  "headers": {
    "Content-Type": "application/json"
  },
  "path": {},
  "query": {},
  "body": {
    "projectAddress": "string",
    "pledgeToken": "string",
    "buyToken": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `body.projectAddress` | `string` | 项目合约地址 |
| `body.pledgeToken` | `string` | 用于质押的项目 Token 数量；字段名必须是 pledgeToken |
| `body.buyToken` | `string` | 质押买时目标买入的项目 Token 数量 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "curveCostStable": "string",
      "borrowedStable": "string",
      "feeStable": "string",
      "userPayStable": "string",
      "selfPayPct": "string",
      "postPledgeRatioPct": "string",
      "snapshotBlock": "uint64"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.curveCostStable` | `string` | 按 CurveTable 曲线计算的购买成本 |
| `data.borrowedStable` | `string` | 质押价值可覆盖的稳定币金额 |
| `data.feeStable` | `string` | 本次操作的稳定币手续费 |
| `data.userPayStable` | `string` | 用户本次需自付的稳定币金额 |
| `data.selfPayPct` | `string` | 百分比数值 |
| `data.postPledgeRatioPct` | `string` | 百分比数值 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `PledgeQuoteRequest`; unknown fields return code 30007. On code 30008 caused by a known pledge constraint, `data` is `PledgeQuoteErrorData` instead of null. `reasonCode` enums: INVALID_PLEDGE_TOKEN, INVALID_BUY_TOKEN, PROJECT_NOT_TRADING, PLEDGE_EXCEEDS_BUY, SELF_PAY_MIN_20_PCT, MIN_SINGLE_BUY, POST_PLEDGE_RATIO_LIMIT. `direction` enums: input/eligibility/min/max; `suggestedAction` enums: CORRECT_INPUT, WAIT_FOR_TRADING, INCREASE_BUY_TOKEN, DECREASE_BUY_TOKEN. Do not parse `message`; call `/api/quotes/pledge-buy-range` for the exact valid interval.

<!-- ROUTE: POST /api/quotes/preview-unpledge -->
### POST /api/quotes/preview-unpledge

- Controller: `QuoteController.PreviewUnpledge`
- Auth: none

Request:
```json
{
  "headers": {
    "Content-Type": "application/json"
  },
  "path": {},
  "query": {},
  "body": {
    "projectAddress": "string",
    "userAddress": "string",
    "tokenAmount": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `body.projectAddress` | `string` | 项目合约地址 |
| `body.userAddress` | `string` | 用户钱包地址 |
| `body.tokenAmount` | `string` | 项目 Token 数量 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "pledgedAmount": "string",
      "pledgeDebtStable": "string",
      "unlockCostStable": "string",
      "snapshotBlock": "uint64"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.pledgedAmount` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.pledgeDebtStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.unlockCostStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `UnpledgeQuoteRequest`; unknown fields return 400.

<!-- ROUTE: GET /api/projects/:address/klines -->
### GET /api/projects/:address/klines

- Controller: `KlineController.QueryKlines`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "interval": "enum 1m, 5m, 15m, 1h, 4h, 1d, 1w, 1mo; default 1h",
    "limit": "default 500, valid 1-1000",
    "from": "optional unix milliseconds; preferred history start",
    "to": "optional unix milliseconds; preferred history end",
    "startTime": "optional legacy alias of from, unix milliseconds",
    "endTime": "optional legacy alias of to, unix milliseconds",
    "fill": "enum carry, none; default carry",
    "priceBasis": "curve_spot only"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.interval` | `enum 1m, 5m, 15m, 1h, 4h, 1d, 1w, 1mo; default 1h` | enum 1m, 5m, 15m, 1h, 4h, 1d, 1w, 1mo; default 1h |
| `query.limit` | `default 500, valid 1-1000` | default 500, valid 1-1000 |
| `query.from` | `optional unix milliseconds; preferred history start` | optional unix milliseconds; preferred history start |
| `query.to` | `optional unix milliseconds; preferred history end` | optional unix milliseconds; preferred history end |
| `query.startTime` | `optional legacy alias of from, unix milliseconds` | optional legacy alias of from, unix milliseconds |
| `query.endTime` | `optional legacy alias of to, unix milliseconds` | optional legacy alias of to, unix milliseconds |
| `query.fill` | `enum carry, none; default carry` | enum carry, none; default carry |
| `query.priceBasis` | `curve_spot only` | curve_spot only |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "projectAddress": "string",
    "interval": "string",
    "candles": "[]PriceCandle compatibility alias",
    "bars": "[]PriceCandle canonical bars",
    "floorSeries": [
      {
        "time": "int64 unix milliseconds",
        "at": "RFC3339 +08:00",
        "floorPrice": "decimal string",
        "blockNumber": "uint64",
        "synthetic": "bool",
        "source": "enum floor_ratchet_event, floor_ratchet_carry, floor_ratchet_initial_backfill, project_onchain_snapshot, snapshot_carry, earliest_snapshot_backfill, current_snapshot_fallback"
      }
    ],
    "summary": {
      "tokenName": "string",
      "tokenSymbol": "string",
      "logoUrl": "string",
      "currentPrice": "decimal string",
      "floorPrice": "decimal string",
      "open24h": "decimal string",
      "high24h": "decimal string",
      "low24h": "decimal string",
      "close24h": "decimal string",
      "change24h": "decimal string",
      "change24hPct": "decimal string percentage",
      "volume24hStable": "decimal string",
      "volume24hToken": "decimal string",
      "trades24h": "int64",
      "lastTradeAt": "RFC3339 +08:00 or empty",
      "snapshotBlock": "uint64"
    },
    "intervalMs": "int64; 1mo is nominal 30d, use bar closeTime for the exact calendar boundary",
    "calendarAligned": "bool",
    "timeUnit": "milliseconds",
    "timezone": "Asia/Shanghai",
    "priceBasis": "curve_spot",
    "priceScale": "int64",
    "fillPolicy": "enum carry, none",
    "hasMore": "bool",
    "nextBefore": "int64|null",
    "quality": {
      "realBars": "int",
      "syntheticBars": "int",
      "missingBuckets": "int",
      "lastRealBarTime": "int64|null"
    },
    "lastUpdate": "RFC3339 string",
    "status": "enum healthy, rebuilt, no_trades, unavailable",
    "dataSource": "string",
    "recommendedAction": "string"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.interval` | `string` | 后端代码字段 interval；含义由所属响应模型和接口业务决定 |
| `data.candles` | `[]PriceCandle compatibility alias` | 后端代码字段 candles；含义由所属响应模型和接口业务决定 |
| `data.bars` | `[]PriceCandle canonical bars` | 后端代码字段 bars；含义由所属响应模型和接口业务决定 |
| `data.floorSeries` | `object` | 后端代码字段 floorSeries；含义由所属响应模型和接口业务决定 |
| `data.summary` | `object` | 后端代码字段 summary；含义由所属响应模型和接口业务决定 |
| `data.intervalMs` | `int64; 1mo is nominal 30d, use bar closeTime for the exact calendar boundary` | 后端代码字段 intervalMs；含义由所属响应模型和接口业务决定 |
| `data.calendarAligned` | `bool` | 后端代码字段 calendarAligned；含义由所属响应模型和接口业务决定 |
| `data.timeUnit` | `milliseconds` | 后端代码字段 timeUnit；含义由所属响应模型和接口业务决定 |
| `data.timezone` | `Asia/Shanghai` | 后端代码字段 timezone；含义由所属响应模型和接口业务决定 |
| `data.priceBasis` | `curve_spot` | 后端代码字段 priceBasis；含义由所属响应模型和接口业务决定 |
| `data.priceScale` | `int64` | 后端代码字段 priceScale；含义由所属响应模型和接口业务决定 |
| `data.fillPolicy` | `enum carry, none` | 后端代码字段 fillPolicy；含义由所属响应模型和接口业务决定 |
| `data.hasMore` | `bool` | 后端代码字段 hasMore；含义由所属响应模型和接口业务决定 |
| `data.nextBefore` | `int64\|null` | 后端代码字段 nextBefore；含义由所属响应模型和接口业务决定 |
| `data.quality` | `object` | 后端代码字段 quality；含义由所属响应模型和接口业务决定 |
| `data.lastUpdate` | `RFC3339 string` | 后端代码字段 lastUpdate；含义由所属响应模型和接口业务决定 |
| `data.status` | `enum healthy, rebuilt, no_trades, unavailable` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `data.recommendedAction` | `string` | 后端代码字段 recommendedAction；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: `bars` is the canonical ascending series and `candles` is an identical compatibility alias. Every bar includes `time/closeTime` as Unix milliseconds, decimal-string OHLC and volumes, per-bar `floorPrice`, `synthetic`, and `confirmed`. Buckets use Beijing civil time: `1d` starts at 00:00, `1w` starts Monday 00:00 and `1mo` starts on day 1; never model `1mo` as a fixed 30-day boundary. `floorSeries` is an ascending step-line series reconstructed from exact `FloorRatchet` chain events with snapshot fallback; render it with step-after interpolation and optional area fill, never smooth interpolation. `summary` provides the chart header and 24-hour OHLC/change/volume/trade values. `curve_spot` means the post-trade bonding-curve spot price from Buy/Sell events, not average execution price. `fill=carry` creates display-only zero-volume bars from the previous close; synthetic bars are never persisted and never count as trades. Use `nextBefore` as the next request `to` only when `hasMore=true`. HTTP is authoritative; WebSocket `kline.updated` carries `bar`, optional `floorPoint`, and optional `summary` for latest-bar replacement or append.

<!-- ROUTE: GET /api/projects/:address/kline-stats -->
### GET /api/projects/:address/kline-stats

- Controller: `KlineController.GetKlineStats`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "volume24h": "string",
    "trades24h": "int",
    "openPrice24h": "string",
    "closePrice24h": "string",
    "status": "string",
    "dataSource": "string",
    "recommendedAction": "string",
    "tradeEvents": "int64",
    "candleCount": "int64",
    "lastTradeAt": "string",
    "lastCandleAt": "string",
    "needsRebuild": "bool"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.volume24h` | `string` | 后端代码字段 volume24h；含义由所属响应模型和接口业务决定 |
| `data.trades24h` | `int` | 后端代码字段 trades24h；含义由所属响应模型和接口业务决定 |
| `data.openPrice24h` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.closePrice24h` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `data.recommendedAction` | `string` | 后端代码字段 recommendedAction；含义由所属响应模型和接口业务决定 |
| `data.tradeEvents` | `int64` | 后端代码字段 tradeEvents；含义由所属响应模型和接口业务决定 |
| `data.candleCount` | `int64` | 数量统计 |
| `data.lastTradeAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.lastCandleAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `data.needsRebuild` | `bool` | 后端代码字段 needsRebuild；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: POST /api/upload/logo -->
### POST /api/upload/logo

- Controller: `UploadController.UploadLogo`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "X-SeedX-Client": "web",
    "Content-Type": "multipart/form-data"
  },
  "path": {},
  "query": {},
  "body": {
    "file": "binary; required; jpg/jpeg/png/gif/webp only; SVG is rejected; max 2 MiB"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `headers.Content-Type` | `multipart/form-data` | 请求体类型：multipart/form-data |
| `body.file` | `binary; required; jpg/jpeg/png/gif/webp only; SVG is rejected; max 2 MiB` | 后端代码字段 file；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {"url": "/uploads/logos/<file>", "filename": "<file>"}
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.url` | `/uploads/logos/<file>` | 后端代码字段 url；含义由所属响应模型和接口业务决定 |
| `data.filename` | `<file>` | 后端代码字段 filename；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Authenticated upload only. The backend validates extension, detected MIME, and a 2 MiB limit. SVG is permanently rejected to prevent stored XSS.

<!-- ROUTE: POST /api/projects/:address/metadata -->
### POST /api/projects/:address/metadata

- Controller: `ProjectController.UpdateMetadata`
- Auth: HttpOnly Cookie session

Request:
```json
{
  "headers": {
    "Content-Type": "application/json",
    "X-SeedX-Client": "web"
  },
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": {
    "logoUrl": "string",
    "website": "string",
    "twitter": "string",
    "telegram": "string",
    "description": "string"
  }
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `headers.Content-Type` | `application/json` | 请求体类型：application/json |
| `headers.X-SeedX-Client` | `web` | 浏览器可信客户端标识；固定为 web，对 Cookie 认证的 POST/PATCH/PUT/DELETE 必填，后端还会校验 Origin |
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `body.logoUrl` | `string` | 项目 Logo 资源 URL |
| `body.website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `body.twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `body.telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `body.description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "projectAddress": "string",
      "tokenName": "string",
      "tokenSymbol": "string",
      "logoUrl": "string",
      "website": "string",
      "twitter": "string",
      "telegram": "string",
      "description": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.tokenName` | `string` | 项目 Token 名称 |
| `data.tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `data.logoUrl` | `string` | 项目 Logo 资源 URL |
| `data.website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `data.twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `data.telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `data.description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: body strictly matches `UpdateProjectMetadataRequest`; unknown fields return 400. Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/users/:address/transactions -->
### GET /api/users/:address/transactions

- Controller: `UserTransactionController.List`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "projectAddress": "optional",
    "txType": "default all",
    "startBlock": "optional uint64",
    "endBlock": "optional uint64",
    "limit": "optional integer",
    "offset": "optional integer"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.projectAddress` | `optional` | optional |
| `query.txType` | `default all` | default all |
| `query.startBlock` | `optional uint64` | optional uint64 |
| `query.endBlock` | `optional uint64` | optional uint64 |
| `query.limit` | `optional integer` | optional integer |
| `query.offset` | `optional integer` | optional integer |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "items": "[]UserTransactionItem",
      "total": "uint64",
      "limit": "int",
      "offset": "int"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]UserTransactionItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: `amountStable` and `amountToken` are non-negative decimal strings and never encode direction. Frontend signs: buy = +Token/-Stable; sell = -Token/+Stable; pledge_buy = +newly bought Token/-user-paid Stable; unpledge = +unlocked Token/-repaid Stable; subscribe_node = -Stable. For pledge_buy, amountToken is boughtToken only; read pledged collateral tokenAmount from `/api/projects/:address/pledge-history`. Use amountStable as the settled total and do not recompute it as amountToken*price because fees and taxes may differ.

<!-- ROUTE: GET /api/users/:address/operation-ledger -->
### GET /api/users/:address/operation-ledger

- Controller: `MarketController.UserOperationLedger`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {
    "projectAddress": "optional",
    "source": "optional",
    "action": "optional",
    "limit": "default 50 max 200",
    "offset": "default 0"
  },
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `query.projectAddress` | `optional` | optional |
| `query.source` | `optional` | optional |
| `query.action` | `optional` | optional |
| `query.limit` | `default 50 max 200` | default 50 max 200 |
| `query.offset` | `default 0` | default 0 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": "[]OperationLedgerItem",
    "total": "uint64",
    "limit": "int",
    "offset": "int"
  }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.items` | `[]OperationLedgerItem` | 当前页数据列表；空结果返回 [] |
| `data.total` | `uint64` | 符合条件的总记录数 |
| `data.limit` | `int` | 本次最多返回的记录数 |
| `data.offset` | `int` | 分页起始偏移量 |
<!-- /RESPONSE_FIELDS -->
Notes: Only listed query fields are read by code; unlisted query fields are ignored.

<!-- ROUTE: GET /api/users/:address/fee-summary -->
### GET /api/users/:address/fee-summary

- Controller: `MarketController.UserFeeSummary`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "actorAddress": "string",
      "projectAddress": "string",
      "totalFeePaid": "string",
      "totalTaxPaid": "string",
      "totalClaimedFees": "string",
      "totalClaimedTaxes": "string",
      "totalPlatformIncome": "string",
      "dataSource": "string"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.actorAddress` | `string` | 合约或钱包的 EVM 地址 |
| `data.projectAddress` | `string` | 项目合约地址 |
| `data.totalFeePaid` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalTaxPaid` | `string` | 后端代码字段 totalTaxPaid；含义由所属响应模型和接口业务决定 |
| `data.totalClaimedFees` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.totalClaimedTaxes` | `string` | 后端代码字段 totalClaimedTaxes；含义由所属响应模型和接口业务决定 |
| `data.totalPlatformIncome` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `data.dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ROUTE: GET /api/users/:address/badges -->
### GET /api/users/:address/badges

- Controller: `BadgeController.GetUserBadges`
- Auth: none

Request:
```json
{
  "headers": {},
  "path": {
    "address": "path address"
  },
  "query": {},
  "body": null
}
```
<!-- REQUEST_FIELDS -->
请求字段：

| 位置.字段 | 类型/取值 | 含义、逻辑与限制 |
|---|---|---|
| `path.address` | `path address` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
<!-- /REQUEST_FIELDS -->
Response:
```json
{
  "code": 0,
  "message": "ok",
  "data": {
      "address": "string",
      "badges": "[]BadgeProgress",
      "earned": "[]UserBadge",
      "totalEarned": "int",
      "totalBadges": "int"
    }
}
```
<!-- RESPONSE_FIELDS -->
返回字段：

| 字段 | 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功，非 0 表示业务失败 |
| `message` | `string` | 成功或错误信息 |
| `data` | `object/array/null` | 业务数据；错误通常为 null，支持结构化业务错误的接口按 Notes 返回错误对象 |
| `data.address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `data.badges` | `[]BadgeProgress` | 后端代码字段 badges；含义由所属响应模型和接口业务决定 |
| `data.earned` | `[]UserBadge` | 后端代码字段 earned；含义由所属响应模型和接口业务决定 |
| `data.totalEarned` | `int` | 后端代码字段 totalEarned；含义由所属响应模型和接口业务决定 |
| `data.totalBadges` | `int` | 后端代码字段 totalBadges；含义由所属响应模型和接口业务决定 |
<!-- /RESPONSE_FIELDS -->
Notes: Path params come from Beego `Ctx.Input.Param`.

<!-- ADMIN_TYPE_DICTIONARIES -->
## 管理后台类型字典

> 所有 `type/eventType/action/incomeType/source/status` 字段均返回稳定英文机器值，便于筛选、审计和兼容。管理端展示中文时必须使用本节或 `GET /api/admin/meta` 返回的字典；面向用户的 `summary` 已由后台生成中文。

### 用户交易类型

- 对应字段：`UserTransactionItem.txType`、`AdminTransactionQueryItem.txType`
- 规则：`summary` 始终由后台生成中文；金额正负方向仍按 `txType` 判断。

| 英文机器值 | 中文含义 |
|---|---|
| `create_project` | 创建项目 |
| `subscribe_node` | 认购节点 |
| `buy` | 买入 |
| `sell` | 卖出 |
| `pledge_buy` | 质押买入 |
| `unpledge` | 解除质押 |
| `lock_node` | 锁定节点 |
| `claim_unlocked` | 领取解锁代币 |
| `claim_alt_token` | 领取附属代币 |
| `claim_fees` | 领取手续费 |
| `claim_taxes` | 领取盈利税 |
| `claim_refund` | 领取退款 |
| `transfer_in` | 转入 |
| `transfer_out` | 转出 |

### 链上事件类型

- 对应字段：`ProjectHistoryItem.eventType`、`PledgeHistoryItem.eventType`、`FeeIncomeLedgerItem.sourceEvent`
- 规则：`eventType` 保留合约事件原始大小写；`summary` 返回中文，不允许直接回显英文事件名。

| 英文机器值 | 中文含义 |
|---|---|
| `AltTokenClaimed` | 领取附属代币 |
| `Approval` | 代币授权 |
| `Buy` | 买入 |
| `CostBasisUpdated` | 成本基准更新 |
| `CreationFeeUpdated` | 项目创建费更新 |
| `CurveTableCommitmentRegistered` | 曲线表承诺登记 |
| `CurveTableRegistered` | 曲线表登记 |
| `ExternalPoolMigrationCreated` | 外部池迁移创建 |
| `FeePolicyUpdated` | 费率策略更新 |
| `FeesClaimed` | 领取手续费 |
| `FloorRatchet` | 地板价上调 |
| `GraduationAnnounced` | 毕业公告 |
| `GraduationExecuted` | 毕业执行 |
| `LaunchFailed` | 发射失败 |
| `LPFeeDistributed` | LP 手续费分配 |
| `NativeReceived` | 收到原生币 |
| `NodeLocked` | 节点锁定 |
| `NodeSubscribed` | 节点认购 |
| `PlatformIncomeRecorded` | 平台收入登记 |
| `PledgeCreated` | 质押买入 |
| `ProductionCurveCreated` | 生产曲线创建 |
| `ProfitTaxCharged` | 盈利税计提 |
| `ProjectCreated` | 项目创建 |
| `ProposalCreated` | 提案创建 |
| `ProposalExecuted` | 提案执行 |
| `ProposalTallied` | 提案计票 |
| `RefundClaimed` | 领取退款 |
| `ScaleTierUpdated` | 规模档位更新 |
| `ScaleTierVersionUpdated` | 规模档位版本更新 |
| `Sell` | 卖出 |
| `SignerAdded` | 新增多签人 |
| `SignerRemoved` | 移除多签人 |
| `SignerReplaced` | 替换多签人 |
| `TaxesClaimed` | 领取盈利税 |
| `ThresholdUpdated` | 多签阈值更新 |
| `TradingStarted` | 曲线交易开启 |
| `TransactionApproved` | 多签交易批准 |
| `TransactionExecuted` | 多签交易执行 |
| `TransactionSubmitted` | 多签交易提交 |
| `Transfer` | 代币转账 |
| `PlatformRevenueClaimProcessed` | 平台收入领取结果 |
| `PlatformRevenueBatchCompleted` | 平台收入批量领取完成 |
| `UniswapV4Graduated` | Uniswap v4 毕业回调 |
| `UnlockedClaimed` | 领取解锁代币 |
| `Unpledged` | 解除质押 |
| `VoteCast` | 治理投票 |

### 操作账本动作

- 对应字段：`OperationLedgerItem.action`、`OperationStats.byAction` 的键
- 规则：稳定筛选机器码，不改成中文；显示时使用本表或 `/api/admin/metadata` 的 `operationLedgerAction`。

| 英文机器值 | 中文含义 |
|---|---|
| `create_project` | 创建项目 |
| `subscribe_node` | 认购节点 |
| `tradingstarted` | 开启交易 |
| `buy` | 买入 |
| `sell` | 卖出 |
| `profit_tax` | 利润税入账 |
| `pledge_buy` | 质押买入 |
| `unpledge` | 解除质押 |
| `nodelocked` | 节点锁定 |
| `unlockedclaimed` | 领取解锁代币 |
| `alttokenclaimed` | 领取 Alt Token |
| `approval` | ERC20 授权 |
| `costbasisupdated` | 成本基准更新 |
| `claim_fees` | 领取手续费 |
| `claim_taxes` | 领取利润税 |
| `transfer` | ERC20 Transfer 转账 |
| `floorratchet` | 地板价上调 |
| `launchfailed` | 发射失败 |
| `refundclaimed` | 领取退款 |
| `proposalcreated` | 提案创建 |
| `votecast` | 治理投票 |
| `proposalexecuted` | 提案执行 |
| `proposaltallied` | 提案计票 |
| `fee_policy_updated` | 费率策略更新 |
| `graduationannounced` | 毕业公告 |
| `externalpoolmigrationcreated` | 外部流动性池迁移创建 |
| `graduationexecuted` | 毕业执行 |
| `lp_fee_distributed` | LP 分账 |
| `treasury_income` | 平台收入 |
| `scaletierupdated` | 平台 Scale Tier 更新 |
| `scaletierversionupdated` | 平台 Scale Tier 链上版本更新 |
| `creationfeeupdated` | 建项费更新 |
| `transactionsubmitted` | 多签交易提交 |
| `transactionapproved` | 多签交易批准 |
| `transactionexecuted` | 多签交易执行 |
| `signeradded` | 多签签名人新增 |
| `signerremoved` | 多签签名人移除 |
| `signerreplaced` | 多签签名人替换 |
| `thresholdupdated` | 多签阈值更新 |
| `nativereceived` | 多签收到原生币 |
| `platformrevenueclaimprocessed` | 平台收入领取结果 |
| `platformrevenuebatchcompleted` | 平台收入批量领取完成 |
| `uniswapv4graduated` | Uniswap v4 Adapter 毕业回调 |
| `productioncurvecreated` | 生产曲线创建 |
| `curvetableregistered` | 曲线表登记 |
| `curvetablecommitmentregistered` | 曲线表承诺登记 |
| `upsert_config` | 新增平台后端配置版本 |
| `project_point_multiplier_upsert` | 修改项目积分倍率 |
| `publish_announcement` | 发布平台公告 |
| `graylist_upsert` | 新增或更新灰/黑名单 |
| `graylist_remove` | 移除灰/黑名单 |
| `admin_target_metadata_upsert` | 更新运营标签/风险备注 |
| `support_reply` | 客服回复 |
| `support_assign` | 客服分配 |
| `support_update_status` | 客服状态更新 |
| `support_upsert_canned_reply` | 新增或更新快捷回复 |
| `support_delete_canned_reply` | 删除快捷回复 |
| `admin_role_bind` | 授予管理员角色 |
| `admin_role_revoke` | 撤销管理员角色 |
| `admin_role_unbind` | 撤销管理员角色（管理元数据历史别名） |
| `treasury_income_indexed` | 平台收入索引入库 |

### 费用收入类型

- 对应字段：`FeeIncomeLedgerItem.incomeType`
- 规则：前缀表示费用来源，后缀表示最终受益方。

| 英文机器值 | 中文含义 |
|---|---|
| `buy_fee_floor` | 买入手续费（地板池） |
| `buy_fee_node` | 买入手续费（节点） |
| `buy_fee_issuer` | 买入手续费（项目方） |
| `buy_fee_platform` | 买入手续费（平台） |
| `sell_fee_floor` | 卖出手续费（地板池） |
| `sell_fee_node` | 卖出手续费（节点） |
| `sell_fee_issuer` | 卖出手续费（项目方） |
| `sell_fee_platform` | 卖出手续费（平台） |
| `profit_tax_floor` | 盈利税（地板池） |
| `profit_tax_node` | 盈利税（节点） |
| `profit_tax_issuer` | 盈利税（项目方） |
| `profit_tax_platform` | 盈利税（平台） |
| `project_creation_fee` | 项目创建费 |
| `lp_fee_node` | LP 手续费（节点） |
| `lp_fee_creator` | LP 手续费（项目方） |
| `lp_fee_platform` | LP 手续费（平台） |

### 操作人角色

- 对应字段：`OperationLedgerItem.actorRole`
- 规则：用于区分链上钱包动作与后台管理员动作。

| 英文机器值 | 中文含义 |
|---|---|
| `chain_user` | 链上用户 |
| `admin` | 后台管理员 |

### 记录来源

- 对应字段：`OperationLedgerItem.source`、`FeeIncomeLedgerItem.source`
- 规则：表示记录由链上事件还是后台管理动作产生。

| 英文机器值 | 中文含义 |
|---|---|
| `chain` | 链上事件 |
| `admin` | 后台管理操作 |

### 操作状态

- 对应字段：`OperationLedgerItem.status`
- 规则：当前链上已确认投影通常为 `success`。

| 英文机器值 | 中文含义 |
|---|---|
| `success` | 成功 |
| `failed` | 失败 |
| `pending` | 处理中 |

### 历史价格来源

- 对应字段：`ProjectHistoryItem.priceSource`、`UserTransactionItem.priceSource`
- 规则：空字符串表示该事件不适用价格；禁止把机器值直接展示给用户。

| 英文机器值 | 中文含义 |
|---|---|
| `event.currentPrice` | 链上事件快照价格 |
| `derived.amounts` | 按事件金额推导价格 |

### 平台收入来源

- 对应字段：`TreasuryIncomeRecord.source`、`TreasuryIncomeRecord.sourceCode`
- 规则：当前合约只定义项目创建费来源；`incomeId` 是链上收入唯一引用，不是中文枚举。

| 英文机器值 | 中文含义 |
|---|---|
| `project_creation_fee` | 项目创建费 |
| `0` | 项目创建费（链上 sourceCode） |
<!-- /ADMIN_TYPE_DICTIONARIES -->

## JSON Schema

> This catalog is generated from `backend/models/*.go` JSON tags. Request DTOs are strictly decoded: unknown fields are rejected.

### PlatformConfigItem

```json
{
  "configKey": "string",
  "configValueJson": "string",
  "version": "int",
  "effectiveAt": "string",
  "createdBy": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `configKey` | `string` | 版本化后端配置键；档位积分倍率使用 points.scale_tier_multiplier.<bytes32 scaleTierId>，项目覆盖使用 points.project_multiplier.<projectAddress> |
| `configValueJson` | `string` | 字符串形式的 JSON；积分倍率格式为 {"multiplier":"1.5"}，必须大于 0 且最多 18 位小数 |
| `version` | `int` | 积分规则版本；当前固定为 seedx-points-20260807 |
| `effectiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `createdBy` | `string` | 后端代码字段 createdBy；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### UpsertPlatformConfigRequest

```json
{
  "configKey": "string",
  "configValueJson": "string",
  "effectiveAt": "string",
  "createdBy": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `configKey` | `string` | 版本化后端配置键；档位积分倍率使用 points.scale_tier_multiplier.<bytes32 scaleTierId>，项目覆盖使用 points.project_multiplier.<projectAddress> |
| `configValueJson` | `string` | 字符串形式的 JSON；积分倍率格式为 {"multiplier":"1.5"}，必须大于 0 且最多 18 位小数 |
| `effectiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `createdBy` | `string` | 后端代码字段 createdBy；含义由所属响应模型和接口业务决定 |

### PublishPlatformAnnouncementRequest

```json
{
  "title": "string",
  "body": "string",
  "deadline": "int64",
  "approvals": "[]PlatformAnnouncementApproval"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `deadline` | `int64` | Platform announcement authorization expiry as Unix seconds; it must be in the next 15 minutes and the publish request must reuse the same value. |
| `approvals` | `[]PlatformAnnouncementApproval` | Distinct current PlatformMultisig signer approvals; final publish requires at least the live threshold. |

### PlatformAnnouncementApproval

```json
{
  "signer": "string",
  "signature": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `signer` | `string` | Current PlatformMultisig signer EVM address; duplicate signers are rejected. |
| `signature` | `string` | personal_sign signature over the exact authorization message returned by the backend. |

### PlatformAnnouncementAuthorizationResponse

```json
{
  "message": "string",
  "digest": "string",
  "deadline": "int64",
  "policy": "MultisigPolicy"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `message` | `string` | 人类可读的结果或错误信息 |
| `digest` | `string` | Single-use SHA-256 authorization digest recorded atomically when the announcement is published. |
| `deadline` | `int64` | Platform announcement authorization expiry as Unix seconds; it must be in the next 15 minutes and the publish request must reuse the same value. |
| `policy` | `MultisigPolicy` | Live PlatformMultisig contract, signer, threshold, and configVersion snapshot used for signing. |

### PublishPlatformAnnouncementResponse

```json
{
  "published": "bool",
  "message": "MessageItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `published` | `bool` | 后端代码字段 published；含义由所属响应模型和接口业务决定 |
| `message` | `MessageItem` | 人类可读的结果或错误信息 |

### AdminDiagnosticsResponse

```json
{
  "storage": "string",
  "runtimeChecks": "[]RuntimeConfigCheck",
  "indexerCheckpoints": "[]IndexerCheckpointItem",
  "workerStatusRows": "[]WorkerStatusItem",
  "latestEventBlock": "uint64",
  "latestSnapshotBlock": "uint64",
  "industrialGate": "IndustrialGateSummary",
  "latestWorkerRun": "WorkerRunSummary",
  "latestSmokeRun": "SmokeRunSummary",
  "latestRpcHealth": "RPCHealthSummary"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `storage` | `string` | 后端代码字段 storage；含义由所属响应模型和接口业务决定 |
| `runtimeChecks` | `[]RuntimeConfigCheck` | 后端代码字段 runtimeChecks；含义由所属响应模型和接口业务决定 |
| `indexerCheckpoints` | `[]IndexerCheckpointItem` | 后端代码字段 indexerCheckpoints；含义由所属响应模型和接口业务决定 |
| `workerStatusRows` | `[]WorkerStatusItem` | 后端代码字段 workerStatusRows；含义由所属响应模型和接口业务决定 |
| `latestEventBlock` | `uint64` | 后端代码字段 latestEventBlock；含义由所属响应模型和接口业务决定 |
| `latestSnapshotBlock` | `uint64` | 后端代码字段 latestSnapshotBlock；含义由所属响应模型和接口业务决定 |
| `industrialGate` | `IndustrialGateSummary` | 后端代码字段 industrialGate；含义由所属响应模型和接口业务决定 |
| `latestWorkerRun` | `WorkerRunSummary` | 后端代码字段 latestWorkerRun；含义由所属响应模型和接口业务决定 |
| `latestSmokeRun` | `SmokeRunSummary` | 后端代码字段 latestSmokeRun；含义由所属响应模型和接口业务决定 |
| `latestRpcHealth` | `RPCHealthSummary` | 后端代码字段 latestRpcHealth；含义由所属响应模型和接口业务决定 |

### IndustrialGateSummary

```json
{
  "status": "string",
  "localModeAvailable": "bool",
  "testnetEvidenceReady": "bool",
  "verifyAdminRequired": "bool",
  "lastIndexedBlock": "uint64",
  "latestWorkerStatus": "string",
  "latestSmokeStatus": "string",
  "latestRpcStatus": "string",
  "recommendedAction": "string",
  "requiredAuditActions": "[]string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `localModeAvailable` | `bool` | 布尔状态标记 |
| `testnetEvidenceReady` | `bool` | 后端代码字段 testnetEvidenceReady；含义由所属响应模型和接口业务决定 |
| `verifyAdminRequired` | `bool` | 布尔状态标记 |
| `lastIndexedBlock` | `uint64` | 布尔状态标记 |
| `latestWorkerStatus` | `string` | 后端代码字段 latestWorkerStatus；含义由所属响应模型和接口业务决定 |
| `latestSmokeStatus` | `string` | 后端代码字段 latestSmokeStatus；含义由所属响应模型和接口业务决定 |
| `latestRpcStatus` | `string` | 后端代码字段 latestRpcStatus；含义由所属响应模型和接口业务决定 |
| `recommendedAction` | `string` | 后端代码字段 recommendedAction；含义由所属响应模型和接口业务决定 |
| `requiredAuditActions` | `[]string` | 后端代码字段 requiredAuditActions；含义由所属响应模型和接口业务决定 |

### IndexerCheckpointItem

```json
{
  "indexerName": "string",
  "chainId": "int64",
  "contractAddress": "string",
  "lastScannedBlock": "uint64",
  "lastFinalizedBlock": "uint64",
  "updatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `indexerName` | `string` | 后端代码字段 indexerName；含义由所属响应模型和接口业务决定 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `contractAddress` | `string` | 合约或钱包的 EVM 地址 |
| `lastScannedBlock` | `uint64` | 后端代码字段 lastScannedBlock；含义由所属响应模型和接口业务决定 |
| `lastFinalizedBlock` | `uint64` | 后端代码字段 lastFinalizedBlock；含义由所属响应模型和接口业务决定 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### WorkerStatusItem

```json
{
  "workerName": "string",
  "chainId": "int64",
  "contractAddress": "string",
  "latestSeenBlock": "uint64",
  "lastScannedBlock": "uint64",
  "blockLag": "uint64",
  "lastRunAt": "string",
  "updatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `workerName` | `string` | 后端代码字段 workerName；含义由所属响应模型和接口业务决定 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `contractAddress` | `string` | 合约或钱包的 EVM 地址 |
| `latestSeenBlock` | `uint64` | 后端代码字段 latestSeenBlock；含义由所属响应模型和接口业务决定 |
| `lastScannedBlock` | `uint64` | 后端代码字段 lastScannedBlock；含义由所属响应模型和接口业务决定 |
| `blockLag` | `uint64` | 后端代码字段 blockLag；含义由所属响应模型和接口业务决定 |
| `lastRunAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminActionAuditLog

```json
{
  "id": "uint64",
  "actorAddress": "string",
  "actionType": "string",
  "targetType": "string",
  "targetId": "string",
  "txHash": "string",
  "beforeJson": "string",
  "afterJson": "string",
  "reason": "string",
  "requestId": "string",
  "metadataJson": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `actorAddress` | `string` | 合约或钱包的 EVM 地址 |
| `actionType` | `string` | 管理员审计动作机器码；枚举：`upsert_config`=新增平台后端配置版本；`project_point_multiplier_upsert`=修改项目积分倍率；`publish_announcement`=发布平台公告；`graylist_upsert`=新增或更新灰/黑名单；`graylist_remove`=移除灰/黑名单；`admin_target_metadata_upsert`=更新运营标签/风险备注；`support_reply`=客服回复；`support_assign`=客服分配；`support_update_status`=客服状态更新；`support_upsert_canned_reply`=新增或更新快捷回复；`support_delete_canned_reply`=删除快捷回复；`admin_role_bind`=授予管理员角色；`admin_role_revoke`=撤销管理员角色；`admin_role_unbind`=撤销管理员角色（管理元数据历史别名）；`treasury_income_indexed`=平台收入索引入库 |
| `targetType` | `string` | 运营标签目标类型；user/member 都规范化为 user，project 表示项目 |
| `targetId` | `string` | 目标用户或项目的 EVM 地址 |
| `txHash` | `string` | 链上交易哈希 |
| `beforeJson` | `string` | 后端代码字段 beforeJson；含义由所属响应模型和接口业务决定 |
| `afterJson` | `string` | 后端代码字段 afterJson；含义由所属响应模型和接口业务决定 |
| `reason` | `string` | 本次操作原因，用于审计追溯 |
| `requestId` | `string` | 后端代码字段 requestId；含义由所属响应模型和接口业务决定 |
| `metadataJson` | `string` | 后端代码字段 metadataJson；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminPermissionItem

```json
{
  "permissionKey": "string",
  "description": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `permissionKey` | `string` | 后端代码字段 permissionKey；含义由所属响应模型和接口业务决定 |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |

### AdminRoleItem

```json
{
  "roleKey": "string",
  "roleName": "string",
  "description": "string",
  "permissions": "[]AdminPermissionItem",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `roleKey` | `string` | 后端代码字段 roleKey；含义由所属响应模型和接口业务决定 |
| `roleName` | `string` | 后端代码字段 roleName；含义由所属响应模型和接口业务决定 |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `permissions` | `[]AdminPermissionItem` | 后端代码字段 permissions；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminRoleBindingItem

```json
{
  "adminAddress": "string",
  "roleKey": "string",
  "roleName": "string",
  "grantedBy": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `adminAddress` | `string` | 合约或钱包的 EVM 地址 |
| `roleKey` | `string` | 后端代码字段 roleKey；含义由所属响应模型和接口业务决定 |
| `roleName` | `string` | 后端代码字段 roleName；含义由所属响应模型和接口业务决定 |
| `grantedBy` | `string` | 后端代码字段 grantedBy；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminRoleBindingRequest

```json
{
  "adminAddress": "string",
  "roleKey": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `adminAddress` | `string` | 合约或钱包的 EVM 地址 |
| `roleKey` | `string` | 后端代码字段 roleKey；含义由所属响应模型和接口业务决定 |

### AdminRoleBindingResponse

```json
{
  "binding": "AdminRoleBindingItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `binding` | `AdminRoleBindingItem` | 后端代码字段 binding；含义由所属响应模型和接口业务决定 |

### AdminCurrentIdentity

```json
{
  "address": "string",
  "roleKeys": "[]string",
  "permissionKeys": "[]string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `roleKeys` | `[]string` | 当前管理员绑定的角色 key 列表 |
| `permissionKeys` | `[]string` | 当前管理员全部角色合并后的权限 key；前端菜单和按钮以此为准 |

### AdminEnumOption

```json
{
  "value": "string",
  "labelZh": "string",
  "description": "string",
  "terminal": "bool",
  "changeEffect": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `labelZh` | `string` | 给中文管理界面展示的名称 |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `terminal` | `bool` | 是否为业务终态；终态不等于永远不可恢复，以 changeEffect 为准 |
| `changeEffect` | `string` | 该值对后续操作、统计或界面能力的实际影响 |

### AdminEnumDefinition

```json
{
  "key": "string",
  "labelZh": "string",
  "strict": "bool",
  "description": "string",
  "options": "[]AdminEnumOption"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `key` | `string` | 后端代码字段 key；含义由所属响应模型和接口业务决定 |
| `labelZh` | `string` | 给中文管理界面展示的名称 |
| `strict` | `bool` | 是否为后端强校验枚举；false 时只是推荐值 |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `options` | `[]AdminEnumOption` | 枚举可用值、中文、含义、终态和变更影响 |

### AdminAPIConventions

```json
{
  "responseEnvelope": "string",
  "pagination": "string",
  "timeFormat": "string",
  "decimalEncoding": "string",
  "rateUnits": "string",
  "addressFormat": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `responseEnvelope` | `string` | 后端代码字段 responseEnvelope；含义由所属响应模型和接口业务决定 |
| `pagination` | `string` | 后端代码字段 pagination；含义由所属响应模型和接口业务决定 |
| `timeFormat` | `string` | 后端代码字段 timeFormat；含义由所属响应模型和接口业务决定 |
| `decimalEncoding` | `string` | 后端代码字段 decimalEncoding；含义由所属响应模型和接口业务决定 |
| `rateUnits` | `string` | 后端代码字段 rateUnits；含义由所属响应模型和接口业务决定 |
| `addressFormat` | `string` | 后端代码字段 addressFormat；含义由所属响应模型和接口业务决定 |

### AdminActionDefinition

```json
{
  "key": "string",
  "labelZh": "string",
  "permission": "string",
  "effect": "string",
  "confirmation": "string",
  "riskLevel": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `key` | `string` | 后端代码字段 key；含义由所属响应模型和接口业务决定 |
| `labelZh` | `string` | 给中文管理界面展示的名称 |
| `permission` | `string` | 执行该管理操作必须具备的 RBAC 权限 key |
| `effect` | `string` | 后端 API 成功后已经发生的业务变化 |
| `confirmation` | `string` | 确认操作最终生效所需的后续步骤或核验条件 |
| `riskLevel` | `string` | 风险等级；运营标签允许空、low、medium、high、critical，不会自动改变链上权限 |

### AdminMetadataResponse

```json
{
  "protocolVersion": "string",
  "currentAdmin": "AdminCurrentIdentity",
  "roles": "[]AdminRoleItem",
  "availablePermissions": "[]AdminPermissionItem",
  "enums": "[]AdminEnumDefinition",
  "conventions": "AdminAPIConventions",
  "actions": "[]AdminActionDefinition"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `protocolVersion` | `string` | 当前后端对齐的 SeedX 协议版本；版本不一致时前端应阻止高风险写操作 |
| `currentAdmin` | `AdminCurrentIdentity` | 当前 HttpOnly Cookie 会话对应的管理员身份、角色和权限并集 |
| `roles` | `[]AdminRoleItem` | 全量 RBAC 角色目录，包含每个角色的权限 |
| `availablePermissions` | `[]AdminPermissionItem` | 后端当前支持的全量权限目录 |
| `enums` | `[]AdminEnumDefinition` | 管理端字段枚举目录；strict=true 表示写接口拒绝表外值 |
| `conventions` | `AdminAPIConventions` | 管理 API 响应、分页、时间、大数、比例和地址约定 |
| `actions` | `[]AdminActionDefinition` | 管理写操作目录，说明所需权限、API 成功的真实影响和确认方式 |

### TreasuryIncomeRecord

```json
{
  "id": "uint64",
  "incomeId": "string",
  "projectAddress": "string",
  "tokenAddress": "string",
  "amount": "string",
  "source": "string",
  "sourceCode": "uint8",
  "payerAddress": "string",
  "payoutAddress": "string",
  "incomeReference": "string",
  "txHash": "string",
  "blockNumber": "uint64",
  "blockTime": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `incomeId` | `string` | 收入事件唯一引用；PlatformIncomeRecorded 不携带独立 ID，后台按 txHash + logIndex 确定性生成 bytes32，保证同一交易多条收入事件不冲突；不是交易哈希、自增 ID 或类型枚举 |
| `projectAddress` | `string` | 项目合约地址 |
| `tokenAddress` | `string` | 合约或钱包的 EVM 地址 |
| `amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `source` | `string` | 平台收入来源机器码；当前为 project_creation_fee=项目创建费 |
| `sourceCode` | `uint8` | 链上 PlatformIncomeRecorded.source 原始代码；当前 0=项目创建费 |
| `payerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `payoutAddress` | `string` | 合约或钱包的 EVM 地址 |
| `incomeReference` | `string` | 后端代码字段 incomeReference；含义由所属响应模型和接口业务决定 |
| `txHash` | `string` | 链上交易哈希 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `string` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### ProjectReconcileResponse

```json
{
  "projectAddress": "string",
  "status": "string",
  "dataSource": "string",
  "differences": "[]ReconcileDiff",
  "chainState": "map[string]string",
  "indexedState": "map[string]string",
  "recommendedAction": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `differences` | `[]ReconcileDiff` | 后端代码字段 differences；含义由所属响应模型和接口业务决定 |
| `chainState` | `map[string]string` | 后端代码字段 chainState；含义由所属响应模型和接口业务决定 |
| `indexedState` | `map[string]string` | 后端代码字段 indexedState；含义由所属响应模型和接口业务决定 |
| `recommendedAction` | `string` | 后端代码字段 recommendedAction；含义由所属响应模型和接口业务决定 |

### ReconcileDiff

```json
{
  "field": "string",
  "chainValue": "string",
  "indexedValue": "string",
  "severity": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `field` | `string` | 后端代码字段 field；含义由所属响应模型和接口业务决定 |
| `chainValue` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `indexedValue` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `severity` | `string` | 后端代码字段 severity；含义由所属响应模型和接口业务决定 |

### ScaleTierTxParamsRequest

```json
{
  "scaleTier": "string",
  "tierName": "string",
  "curveTableAddress": "string",
  "graduationAdapter": "string",
  "fOpen": "string",
  "minNodeCount": "string",
  "defaultNodeCount": "string",
  "maxNodeCount": "string",
  "minNodePrice": "string",
  "defaultNodePrice": "string",
  "maxNodePrice": "string",
  "nodeSaleDuration": "uint64",
  "nodeVestingCliffDuration": "uint64",
  "nodeVestingTotalDuration": "uint64",
  "governanceVotingPeriod": "uint64",
  "governanceTimelock": "uint64",
  "graduationMinTradingDuration": "uint64",
  "graduationNotice": "uint64",
  "graduationVolumeWindow": "uint64",
  "curveParamsHash": "string",
  "curvePointsHash": "string",
  "enabled": "bool"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `tierName` | `string` | 后端展示用档位名称；不编码进 setScaleTier calldata |
| `curveTableAddress` | `string` | 必须是固定 CurveTableRegistry 工厂创建并已注册的 CurveTable 地址 |
| `graduationAdapter` | `string` | 毕业 Adapter 地址；enabled=true 时必须等于 Platform.trustedGraduationAdapter() |
| `fOpen` | `string` | 创世地板价的可读 USDGTest 6 位小数字符串；必须等于 CurveTable.rawPriceAtSupply(0) |
| `minNodeCount` | `string` | 该档位最小节点数；enabled=true 时不得小于 100 |
| `defaultNodeCount` | `string` | 默认节点数；必须位于 minNodeCount 和 maxNodeCount 之间 |
| `maxNodeCount` | `string` | 该档位最大节点数 |
| `minNodePrice` | `string` | 单节点最低 USDGTest 价格的可读 6 位小数字符串；enabled=true 时不得低于 100 |
| `defaultNodePrice` | `string` | 默认单节点 USDGTest 价格；必须位于 minNodePrice 和 maxNodePrice 之间 |
| `maxNodePrice` | `string` | 单节点最高 USDGTest 价格的可读 6 位小数字符串 |
| `nodeSaleDuration` | `uint64` | 节点认购期秒数；建项时从 Scale Tier 固化，不是 CreateProjectParams 字段 |
| `nodeVestingCliffDuration` | `uint64` | 节点 Token 开始线性释放前的 cliff 秒数；建项时从 Scale Tier 固化 |
| `nodeVestingTotalDuration` | `uint64` | 节点 Token 从锁定开始到全部释放的总秒数；必须大于 nodeVestingCliffDuration |
| `governanceVotingPeriod` | `uint64` | 治理投票期秒数；enabled=true 时必须大于 0 |
| `governanceTimelock` | `uint64` | 提案通过后执行 timelock 秒数；enabled=true 时必须大于 0 |
| `graduationMinTradingDuration` | `uint64` | 进入毕业条件前的最小交易时长秒数 |
| `graduationNotice` | `uint64` | 毕业公告期秒数；triggerGraduation 后须等待该时长才可 executeGraduation |
| `graduationVolumeWindow` | `uint64` | 毕业成交量滚动窗口秒数；后端只展示链上窗口语义 |
| `curveParamsHash` | `string` | 曲线配置参数的 bytes32 哈希；用于核对 Scale Tier 与 CurveTable 参数版本 |
| `curvePointsHash` | `string` | 曲线分段点数组的 bytes32 哈希；必须与 CurveTable.pointsHash() 一致 |
| `enabled` | `bool` | 档位是否启用；true 时执行全部 Registry/codehash/Adapter/参数约束，false 时链上清除 curveCodeHashByTier |

### CreationFeeTxParamsRequest

```json
{
  "newCreationFee": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `newCreationFee` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |

### PlatformTxParamsResponse

```json
{
  "target": "string",
  "method": "string",
  "value": "string",
  "chainId": "int64",
  "calldata": "string",
  "args": "map[string]string",
  "expectedCaller": "string",
  "nestedTarget": "string",
  "nestedCalldata": "string",
  "snapshotBlock": "uint64",
  "preflight": "TxPreflight"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `target` | `string` | 交易目标合约地址 |
| `method` | `string` | 后端代码字段 method；含义由所属响应模型和接口业务决定 |
| `value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `args` | `map[string]string` | 后端代码字段 args；含义由所属响应模型和接口业务决定 |
| `expectedCaller` | `string` | 后端代码字段 expectedCaller；含义由所属响应模型和接口业务决定 |
| `nestedTarget` | `string` | 后端代码字段 nestedTarget；含义由所属响应模型和接口业务决定 |
| `nestedCalldata` | `string` | 后端代码字段 nestedCalldata；含义由所属响应模型和接口业务决定 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `preflight` | `TxPreflight` | 后端代码字段 preflight；含义由所属响应模型和接口业务决定 |

### AdminProjectFullView

```json
{
  "runtime": "ProjectRuntimeResponse",
  "klineHealth": "KlineHealthResponse",
  "reconcile": "ProjectReconcileResponse",
  "fees": "ProjectFeesResponse",
  "feeIncomeLedger": "[]FeeIncomeLedgerItem",
  "operationLedger": "[]OperationLedgerItem",
  "incomeRecords": "[]TreasuryIncomeRecord",
  "auditLogs": "[]AdminActionAuditLog",
  "operationalFlags": "[]string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `runtime` | `ProjectRuntimeResponse` | 后端代码字段 runtime；含义由所属响应模型和接口业务决定 |
| `klineHealth` | `KlineHealthResponse` | 后端代码字段 klineHealth；含义由所属响应模型和接口业务决定 |
| `reconcile` | `ProjectReconcileResponse` | 后端代码字段 reconcile；含义由所属响应模型和接口业务决定 |
| `fees` | `ProjectFeesResponse` | 后端代码字段 fees；含义由所属响应模型和接口业务决定 |
| `feeIncomeLedger` | `[]FeeIncomeLedgerItem` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `operationLedger` | `[]OperationLedgerItem` | 后端代码字段 operationLedger；含义由所属响应模型和接口业务决定 |
| `incomeRecords` | `[]TreasuryIncomeRecord` | 后端代码字段 incomeRecords；含义由所属响应模型和接口业务决定 |
| `auditLogs` | `[]AdminActionAuditLog` | 后端代码字段 auditLogs；含义由所属响应模型和接口业务决定 |
| `operationalFlags` | `[]string` | 后端代码字段 operationalFlags；含义由所属响应模型和接口业务决定 |

### AnalyticsListQuery

```json
{
  "keyword": "string",
  "status": "string",
  "projectAddress": "string",
  "userAddress": "string",
  "actionType": "string",
  "source": "string",
  "dateFrom": "string",
  "dateTo": "string",
  "sortBy": "string",
  "sortOrder": "string",
  "limit": "int",
  "offset": "int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `keyword` | `string` | 后端代码字段 keyword；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `userAddress` | `string` | 用户钱包地址 |
| `actionType` | `string` | 后端代码字段 actionType；含义由所属响应模型和接口业务决定 |
| `source` | `string` | 后端代码字段 source；含义由所属响应模型和接口业务决定 |
| `dateFrom` | `string` | 后端代码字段 dateFrom；含义由所属响应模型和接口业务决定 |
| `dateTo` | `string` | 后端代码字段 dateTo；含义由所属响应模型和接口业务决定 |
| `sortBy` | `string` | 排序字段；不同列表支持值不同，见管理端总览 |
| `sortOrder` | `string` | 排序方向 asc/desc；默认 desc |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |

### AnalyticsListResponse

```json
{
  "items": "[]T",
  "total": "uint64",
  "limit": "int",
  "offset": "int",
  "filters": "AnalyticsListQuery",
  "sort": "AnalyticsSort",
  "dataSource": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `items` | `[]T` | 当前页数据列表；空结果返回 [] |
| `total` | `uint64` | 符合条件的总记录数 |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |
| `filters` | `AnalyticsListQuery` | 后端代码字段 filters；含义由所属响应模型和接口业务决定 |
| `sort` | `AnalyticsSort` | 后端代码字段 sort；含义由所属响应模型和接口业务决定 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |

### AnalyticsSort

```json
{
  "sortBy": "string",
  "sortOrder": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `sortBy` | `string` | 排序字段；不同列表支持值不同，见管理端总览 |
| `sortOrder` | `string` | 排序方向 asc/desc；默认 desc |

### PlatformAnalyticsResponse

```json
{
  "window": "string",
  "dataSource": "string",
  "generatedAt": "string",
  "overview": "PlatformAnalyticsOverview",
  "capital": "PlatformCapitalStats",
  "operations": "PlatformOperationStats",
  "indexer": "PlatformIndexerStats",
  "support": "PlatformSupportStats"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `window` | `string` | 后端代码字段 window；含义由所属响应模型和接口业务决定 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `generatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `overview` | `PlatformAnalyticsOverview` | 后端代码字段 overview；含义由所属响应模型和接口业务决定 |
| `capital` | `PlatformCapitalStats` | 后端代码字段 capital；含义由所属响应模型和接口业务决定 |
| `operations` | `PlatformOperationStats` | 后端代码字段 operations；含义由所属响应模型和接口业务决定 |
| `indexer` | `PlatformIndexerStats` | 后端代码字段 indexer；含义由所属响应模型和接口业务决定 |
| `support` | `PlatformSupportStats` | 后端代码字段 support；含义由所属响应模型和接口业务决定 |

### PlatformAnalyticsOverview

```json
{
  "userCount": "uint64",
  "projectCount": "uint64",
  "tradingCount": "uint64",
  "graduatedCount": "uint64",
  "transactionCount": "uint64",
  "activeUsers": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `userCount` | `uint64` | 数量统计 |
| `projectCount` | `uint64` | 数量统计 |
| `tradingCount` | `uint64` | 数量统计 |
| `graduatedCount` | `uint64` | 数量统计 |
| `transactionCount` | `uint64` | 数量统计 |
| `activeUsers` | `uint64` | 后端代码字段 activeUsers；含义由所属响应模型和接口业务决定 |

### PlatformCapitalStats

```json
{
  "volumeStable": "string",
  "feeIncomeStable": "string",
  "treasuryIncomeStable": "string",
  "totalFdv": "string",
  "totalMarketCap": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `volumeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `feeIncomeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `treasuryIncomeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalFdv` | `string` | 后端代码字段 totalFdv；含义由所属响应模型和接口业务决定 |
| `totalMarketCap` | `string` | 总市值 = 流通市值 + 锁定市值 = marketPrice * supplyTotal |

### PlatformOperationStats

```json
{
  "operationCount": "uint64",
  "auditLogCount": "uint64",
  "byAction": "map[string]uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `operationCount` | `uint64` | 数量统计 |
| `auditLogCount` | `uint64` | 数量统计 |
| `byAction` | `map[string]uint64` | 后端代码字段 byAction；含义由所属响应模型和接口业务决定 |

### PlatformIndexerStats

```json
{
  "eventsIndexed": "uint64",
  "latestEventBlock": "uint64",
  "latestFinalizedBlock": "uint64",
  "failureCount": "uint64",
  "replayRunCount": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `eventsIndexed` | `uint64` | 布尔状态标记 |
| `latestEventBlock` | `uint64` | 后端代码字段 latestEventBlock；含义由所属响应模型和接口业务决定 |
| `latestFinalizedBlock` | `uint64` | 后端代码字段 latestFinalizedBlock；含义由所属响应模型和接口业务决定 |
| `failureCount` | `uint64` | 数量统计 |
| `replayRunCount` | `uint64` | 数量统计 |

### PlatformSupportStats

```json
{
  "openCount": "uint64",
  "pendingCount": "uint64",
  "resolvedCount": "uint64",
  "unassignedCount": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `openCount` | `uint64` | 数量统计 |
| `pendingCount` | `uint64` | 数量统计 |
| `resolvedCount` | `uint64` | 数量统计 |
| `unassignedCount` | `uint64` | 数量统计 |

### AdminProjectQueryItem

```json
{
  "projectAddress": "string",
  "tokenName": "string",
  "tokenSymbol": "string",
  "issuerAddress": "string",
  "state": "string",
  "scaleTier": "string",
  "currentPrice": "string",
  "fdv": "string",
  "volumeStable": "string",
  "transactionCount": "uint64",
  "holderCount": "uint64",
  "riskLevel": "string",
  "tagsJson": "string",
  "adminStatus": "string",
  "adminNote": "string",
  "createdAt": "string",
  "updatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `issuerAddress` | `string` | 项目创建者钱包地址 |
| `state` | `string` | 后端代码字段 state；含义由所属响应模型和接口业务决定 |
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `fdv` | `string` | 已废弃语义的兼容字段；当前数值等于 totalMarketCap，合约无固定 maxSupply，不应解释为传统 FDV |
| `volumeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `transactionCount` | `uint64` | 数量统计 |
| `holderCount` | `uint64` | 数量统计 |
| `riskLevel` | `string` | 风险等级；运营标签允许空、low、medium、high、critical，不会自动改变链上权限 |
| `tagsJson` | `string` | 后端代码字段 tagsJson；含义由所属响应模型和接口业务决定 |
| `adminStatus` | `string` | 后端代码字段 adminStatus；含义由所属响应模型和接口业务决定 |
| `adminNote` | `string` | 后端代码字段 adminNote；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminUserQueryItem

```json
{
  "address": "string",
  "displayName": "string",
  "memberLevel": "string",
  "status": "string",
  "pointBalance": "string",
  "projectCount": "uint64",
  "transactionCount": "uint64",
  "supportOpenCount": "uint64",
  "riskLevel": "string",
  "tagsJson": "string",
  "adminStatus": "string",
  "adminNote": "string",
  "lastActiveAt": "string",
  "createdAt": "string",
  "updatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `displayName` | `string` | 后端代码字段 displayName；含义由所属响应模型和接口业务决定 |
| `memberLevel` | `string` | 后端代码字段 memberLevel；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `pointBalance` | `string` | 后端代码字段 pointBalance；含义由所属响应模型和接口业务决定 |
| `projectCount` | `uint64` | 数量统计 |
| `transactionCount` | `uint64` | 数量统计 |
| `supportOpenCount` | `uint64` | 数量统计 |
| `riskLevel` | `string` | 风险等级；运营标签允许空、low、medium、high、critical，不会自动改变链上权限 |
| `tagsJson` | `string` | 后端代码字段 tagsJson；含义由所属响应模型和接口业务决定 |
| `adminStatus` | `string` | 后端代码字段 adminStatus；含义由所属响应模型和接口业务决定 |
| `adminNote` | `string` | 后端代码字段 adminNote；含义由所属响应模型和接口业务决定 |
| `lastActiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminTransactionQueryItem

```json
{
  "id": "uint64",
  "address": "string",
  "projectAddress": "string",
  "txHash": "string",
  "txType": "string",
  "amountStable": "string",
  "amountToken": "string",
  "counterparty": "string",
  "blockNumber": "uint64",
  "blockTime": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `txHash` | `string` | 链上交易哈希 |
| `txType` | `string` | 用户交易机器码；完整枚举：`create_project`=创建项目；`subscribe_node`=认购节点；`buy`=买入；`sell`=卖出；`pledge_buy`=质押买入；`unpledge`=解除质押；`lock_node`=锁定节点；`claim_unlocked`=领取解锁代币；`claim_alt_token`=领取附属代币；`claim_fees`=领取手续费；`claim_taxes`=领取盈利税；`claim_refund`=领取退款；`transfer_in`=转入；`transfer_out`=转出 |
| `amountStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `amountToken` | `string` | 后端代码字段 amountToken；含义由所属响应模型和接口业务决定 |
| `counterparty` | `string` | 后端代码字段 counterparty；含义由所属响应模型和接口业务决定 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `string` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminTargetMetadata

```json
{
  "targetType": "string",
  "targetId": "string",
  "tags": "[]string",
  "tagsJson": "string",
  "note": "string",
  "status": "string",
  "riskLevel": "string",
  "updatedBy": "string",
  "createdAt": "string",
  "updatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `targetType` | `string` | 运营标签目标类型；user/member 都规范化为 user，project 表示项目 |
| `targetId` | `string` | 目标用户或项目的 EVM 地址 |
| `tags` | `[]string` | 运营自定义标签列表，只影响后台读模型 |
| `tagsJson` | `string` | 后端代码字段 tagsJson；含义由所属响应模型和接口业务决定 |
| `note` | `string` | 运营备注，最长 2000 字符，不改变链上状态 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `riskLevel` | `string` | 风险等级；运营标签允许空、low、medium、high、critical，不会自动改变链上权限 |
| `updatedBy` | `string` | 后端代码字段 updatedBy；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminTargetMetadataRequest

```json
{
  "tags": "[]string",
  "note": "string",
  "status": "string",
  "riskLevel": "string",
  "reason": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `tags` | `[]string` | 运营自定义标签列表，只影响后台读模型 |
| `note` | `string` | 运营备注，最长 2000 字符，不改变链上状态 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `riskLevel` | `string` | 风险等级；运营标签允许空、low、medium、high、critical，不会自动改变链上权限 |
| `reason` | `string` | 本次操作原因，用于审计追溯 |

### APIResponse

```json
{
  "code": "int",
  "message": "string",
  "data": "interface{}"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `code` | `int` | 业务码；0 表示成功 |
| `message` | `string` | 人类可读的结果或错误信息 |
| `data` | `interface{}` | 业务响应数据；失败时为 null |

### HealthResponse

```json
{
  "service": "string",
  "status": "string",
  "version": "string",
  "storage": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `service` | `string` | 后端代码字段 service；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `version` | `string` | 积分规则版本；当前固定为 seedx-points-20260807 |
| `storage` | `string` | 后端代码字段 storage；含义由所属响应模型和接口业务决定 |

### RuntimeConfigCheck

```json
{
  "name": "string",
  "status": "string",
  "severity": "string",
  "message": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `name` | `string` | 后端代码字段 name；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `severity` | `string` | 后端代码字段 severity；含义由所属响应模型和接口业务决定 |
| `message` | `string` | 人类可读的结果或错误信息 |

### NonceResponse

```json
{
  "address": "string",
  "nonce": "string",
  "message": "string",
  "issuedAt": "string",
  "expiresAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `nonce` | `string` | 后端代码字段 nonce；含义由所属响应模型和接口业务决定 |
| `message` | `string` | 人类可读的结果或错误信息 |
| `issuedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `expiresAt` | `string` | 绝对过期时间，RFC3339；nonce 有效期为 10 分钟，普通钱包会话有效期为 12 小时，拥有任一后台角色的管理员会话为 30 分钟 |

### LoginRequest

```json
{
  "address": "string",
  "message": "string",
  "signature": "string",
  "referrerAddress": "string",
  "inviteCode": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `message` | `string` | 人类可读的结果或错误信息 |
| `signature` | `string` | 后端代码字段 signature；含义由所属响应模型和接口业务决定 |
| `referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |

### LoginResponse

```json
{
  "address": "string",
  "expiresAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `expiresAt` | `string` | 绝对过期时间，RFC3339；nonce 有效期为 10 分钟，普通钱包会话有效期为 12 小时，拥有任一后台角色的管理员会话为 30 分钟 |

### LogoutResponse

```json
{
  "loggedOut": "bool"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `loggedOut` | `bool` | 后端代码字段 loggedOut；含义由所属响应模型和接口业务决定 |

### SessionResponse

```json
{
  "authenticated": "bool",
  "address": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `authenticated` | `bool` | 后端代码字段 authenticated；含义由所属响应模型和接口业务决定 |
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |

### Badge

```json
{
  "id": "string",
  "name": "string",
  "description": "string",
  "icon": "string",
  "category": "string",
  "threshold": "int64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `string` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `name` | `string` | 后端代码字段 name；含义由所属响应模型和接口业务决定 |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `icon` | `string` | 后端代码字段 icon；含义由所属响应模型和接口业务决定 |
| `category` | `string` | 后端代码字段 category；含义由所属响应模型和接口业务决定 |
| `threshold` | `int64` | 后端代码字段 threshold；含义由所属响应模型和接口业务决定 |

### UserBadge

```json
{
  "badgeId": "string",
  "name": "string",
  "description": "string",
  "icon": "string",
  "earnedAt": "time.Time",
  "progress": "int64",
  "threshold": "int64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `badgeId` | `string` | 后端代码字段 badgeId；含义由所属响应模型和接口业务决定 |
| `name` | `string` | 后端代码字段 name；含义由所属响应模型和接口业务决定 |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `icon` | `string` | 后端代码字段 icon；含义由所属响应模型和接口业务决定 |
| `earnedAt` | `time.Time` | 时间值；具体格式以该接口返回类型为准 |
| `progress` | `int64` | 后端代码字段 progress；含义由所属响应模型和接口业务决定 |
| `threshold` | `int64` | 后端代码字段 threshold；含义由所属响应模型和接口业务决定 |

### BadgeProgress

```json
{
  "badgeId": "string",
  "name": "string",
  "icon": "string",
  "progress": "int64",
  "threshold": "int64",
  "earned": "bool"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `badgeId` | `string` | 后端代码字段 badgeId；含义由所属响应模型和接口业务决定 |
| `name` | `string` | 后端代码字段 name；含义由所属响应模型和接口业务决定 |
| `icon` | `string` | 后端代码字段 icon；含义由所属响应模型和接口业务决定 |
| `progress` | `int64` | 后端代码字段 progress；含义由所属响应模型和接口业务决定 |
| `threshold` | `int64` | 后端代码字段 threshold；含义由所属响应模型和接口业务决定 |
| `earned` | `bool` | 后端代码字段 earned；含义由所属响应模型和接口业务决定 |

### UserBadgeResponse

```json
{
  "address": "string",
  "badges": "[]BadgeProgress",
  "earned": "[]UserBadge",
  "totalEarned": "int",
  "totalBadges": "int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `badges` | `[]BadgeProgress` | 后端代码字段 badges；含义由所属响应模型和接口业务决定 |
| `earned` | `[]UserBadge` | 后端代码字段 earned；含义由所属响应模型和接口业务决定 |
| `totalEarned` | `int` | 后端代码字段 totalEarned；含义由所属响应模型和接口业务决定 |
| `totalBadges` | `int` | 后端代码字段 totalBadges；含义由所属响应模型和接口业务决定 |

### BridgeQuoteRequest

```json
{
  "originChainId": "int64",
  "originCurrency": "string",
  "destinationChainId": "int64",
  "destinationCurrency": "string",
  "amount": "string",
  "recipient": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `originChainId` | `int64` | 后端代码字段 originChainId；含义由所属响应模型和接口业务决定 |
| `originCurrency` | `string` | 后端代码字段 originCurrency；含义由所属响应模型和接口业务决定 |
| `destinationChainId` | `int64` | 后端代码字段 destinationChainId；含义由所属响应模型和接口业务决定 |
| `destinationCurrency` | `string` | 后端代码字段 destinationCurrency；含义由所属响应模型和接口业务决定 |
| `amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `recipient` | `string` | 后端代码字段 recipient；含义由所属响应模型和接口业务决定 |

### BridgeRouteConfig

```json
{
  "originChainId": "int64",
  "originChainName": "string",
  "originCurrency": "string",
  "originSymbol": "string",
  "originDecimals": "int",
  "destinationChainId": "int64",
  "destinationChainName": "string",
  "destinationCurrency": "string",
  "destinationSymbol": "string",
  "destinationDecimals": "int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `originChainId` | `int64` | 后端代码字段 originChainId；含义由所属响应模型和接口业务决定 |
| `originChainName` | `string` | 后端代码字段 originChainName；含义由所属响应模型和接口业务决定 |
| `originCurrency` | `string` | 后端代码字段 originCurrency；含义由所属响应模型和接口业务决定 |
| `originSymbol` | `string` | 后端代码字段 originSymbol；含义由所属响应模型和接口业务决定 |
| `originDecimals` | `int` | 后端代码字段 originDecimals；含义由所属响应模型和接口业务决定 |
| `destinationChainId` | `int64` | 后端代码字段 destinationChainId；含义由所属响应模型和接口业务决定 |
| `destinationChainName` | `string` | 后端代码字段 destinationChainName；含义由所属响应模型和接口业务决定 |
| `destinationCurrency` | `string` | 后端代码字段 destinationCurrency；含义由所属响应模型和接口业务决定 |
| `destinationSymbol` | `string` | 后端代码字段 destinationSymbol；含义由所属响应模型和接口业务决定 |
| `destinationDecimals` | `int` | 后端代码字段 destinationDecimals；含义由所属响应模型和接口业务决定 |

### BridgeConfigResponse

```json
{
  "enabled": "bool",
  "disabledReason": "string",
  "mode": "string",
  "slippageBps": "int",
  "routes": "[]BridgeRouteConfig"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `enabled` | `bool` | 档位是否启用；true 时执行全部 Registry/codehash/Adapter/参数约束，false 时链上清除 curveCodeHashByTier |
| `disabledReason` | `string` | 后端代码字段 disabledReason；含义由所属响应模型和接口业务决定 |
| `mode` | `string` | 后端代码字段 mode；含义由所属响应模型和接口业务决定 |
| `slippageBps` | `int` | 万分比整数，10000 表示 100% |
| `routes` | `[]BridgeRouteConfig` | 后端代码字段 routes；含义由所属响应模型和接口业务决定 |

### BridgeQuoteResponse

```json
{
  "orderId": "string",
  "relayRequestId": "string",
  "originChainId": "int64",
  "originCurrency": "string",
  "amount": "string",
  "destinationChainId": "int64",
  "destinationCurrency": "string",
  "recipient": "string",
  "statusEndpointPresent": "bool",
  "quote": "json.RawMessage",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `orderId` | `string` | 后端代码字段 orderId；含义由所属响应模型和接口业务决定 |
| `relayRequestId` | `string` | 后端代码字段 relayRequestId；含义由所属响应模型和接口业务决定 |
| `originChainId` | `int64` | 后端代码字段 originChainId；含义由所属响应模型和接口业务决定 |
| `originCurrency` | `string` | 后端代码字段 originCurrency；含义由所属响应模型和接口业务决定 |
| `amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `destinationChainId` | `int64` | 后端代码字段 destinationChainId；含义由所属响应模型和接口业务决定 |
| `destinationCurrency` | `string` | 后端代码字段 destinationCurrency；含义由所属响应模型和接口业务决定 |
| `recipient` | `string` | 后端代码字段 recipient；含义由所属响应模型和接口业务决定 |
| `statusEndpointPresent` | `bool` | 后端代码字段 statusEndpointPresent；含义由所属响应模型和接口业务决定 |
| `quote` | `json.RawMessage` | 后端代码字段 quote；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### BridgeTransactionReportRequest

```json
{
  "sourceTxHash": "string",
  "destinationTxHash": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `sourceTxHash` | `string` | 后端代码字段 sourceTxHash；含义由所属响应模型和接口业务决定 |
| `destinationTxHash` | `string` | 后端代码字段 destinationTxHash；含义由所属响应模型和接口业务决定 |

### BridgeOrder

```json
{
  "orderId": "string",
  "relayRequestId": "string",
  "userAddress": "string",
  "originChainId": "int64",
  "originCurrency": "string",
  "amount": "string",
  "destinationChainId": "int64",
  "destinationCurrency": "string",
  "recipient": "string",
  "status": "string",
  "sourceTxHash": "string",
  "destinationTxHash": "string",
  "quote": "string",
  "lastStatus": "string",
  "createdAt": "string",
  "updatedAt": "string",
  "lastStatusAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `orderId` | `string` | 后端代码字段 orderId；含义由所属响应模型和接口业务决定 |
| `relayRequestId` | `string` | 后端代码字段 relayRequestId；含义由所属响应模型和接口业务决定 |
| `userAddress` | `string` | 用户钱包地址 |
| `originChainId` | `int64` | 后端代码字段 originChainId；含义由所属响应模型和接口业务决定 |
| `originCurrency` | `string` | 后端代码字段 originCurrency；含义由所属响应模型和接口业务决定 |
| `amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `destinationChainId` | `int64` | 后端代码字段 destinationChainId；含义由所属响应模型和接口业务决定 |
| `destinationCurrency` | `string` | 后端代码字段 destinationCurrency；含义由所属响应模型和接口业务决定 |
| `recipient` | `string` | 后端代码字段 recipient；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `sourceTxHash` | `string` | 后端代码字段 sourceTxHash；含义由所属响应模型和接口业务决定 |
| `destinationTxHash` | `string` | 后端代码字段 destinationTxHash；含义由所属响应模型和接口业务决定 |
| `quote` | `string` | 后端代码字段 quote；含义由所属响应模型和接口业务决定 |
| `lastStatus` | `string` | 后端代码字段 lastStatus；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `lastStatusAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### BridgeOrderQuery

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### BridgeStatusResponse

```json
{
  "order": "BridgeOrder",
  "relay": "json.RawMessage"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `order` | `BridgeOrder` | 后端代码字段 order；含义由所属响应模型和接口业务决定 |
| `relay` | `json.RawMessage` | 后端代码字段 relay；含义由所属响应模型和接口业务决定 |

### IndexedChainEvent

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### IndexedReplayReport

```json
{
  "chainId": "int64",
  "projectAddress": "string",
  "totalEvents": "int",
  "appliedEvents": "int",
  "skippedEvents": "int",
  "resetReadModels": "bool",
  "eventCounts": "map[string]int",
  "lastBlockNumber": "uint64",
  "lastTxHash": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `projectAddress` | `string` | 项目合约地址 |
| `totalEvents` | `int` | 后端代码字段 totalEvents；含义由所属响应模型和接口业务决定 |
| `appliedEvents` | `int` | 后端代码字段 appliedEvents；含义由所属响应模型和接口业务决定 |
| `skippedEvents` | `int` | 后端代码字段 skippedEvents；含义由所属响应模型和接口业务决定 |
| `resetReadModels` | `bool` | 后端代码字段 resetReadModels；含义由所属响应模型和接口业务决定 |
| `eventCounts` | `map[string]int` | 后端代码字段 eventCounts；含义由所属响应模型和接口业务决定 |
| `lastBlockNumber` | `uint64` | 后端代码字段 lastBlockNumber；含义由所属响应模型和接口业务决定 |
| `lastTxHash` | `string` | 后端代码字段 lastTxHash；含义由所属响应模型和接口业务决定 |

### TransactionIndexedEvent

```json
{
  "eventName": "string",
  "projectAddress": "string",
  "userAddress": "string",
  "logIndex": "uint64",
  "blockNumber": "uint64",
  "blockTime": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `eventName` | `string` | 后端代码字段 eventName；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `userAddress` | `string` | 用户钱包地址 |
| `logIndex` | `uint64` | 后端代码字段 logIndex；含义由所属响应模型和接口业务决定 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `string` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |

### TransactionStatusResponse

```json
{
  "txHash": "string",
  "chainId": "int64",
  "status": "string",
  "receiptFound": "bool",
  "receiptStatus": "string",
  "failureReason": "string",
  "indexed": "bool",
  "blockNumber": "uint64",
  "latestBlock": "uint64",
  "confirmations": "uint64",
  "indexedBlockNumber": "uint64",
  "indexedAt": "string",
  "checkpointBlock": "uint64",
  "checkpointLag": "uint64",
  "events": "[]TransactionIndexedEvent",
  "dataSource": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `txHash` | `string` | 链上交易哈希 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `receiptFound` | `bool` | 后端代码字段 receiptFound；含义由所属响应模型和接口业务决定 |
| `receiptStatus` | `string` | 后端代码字段 receiptStatus；含义由所属响应模型和接口业务决定 |
| `failureReason` | `string` | 后端代码字段 failureReason；含义由所属响应模型和接口业务决定 |
| `indexed` | `bool` | 后端代码字段 indexed；含义由所属响应模型和接口业务决定 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `latestBlock` | `uint64` | 后端代码字段 latestBlock；含义由所属响应模型和接口业务决定 |
| `confirmations` | `uint64` | 后端代码字段 confirmations；含义由所属响应模型和接口业务决定 |
| `indexedBlockNumber` | `uint64` | 后端代码字段 indexedBlockNumber；含义由所属响应模型和接口业务决定 |
| `indexedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `checkpointBlock` | `uint64` | 后端代码字段 checkpointBlock；含义由所属响应模型和接口业务决定 |
| `checkpointLag` | `uint64` | 后端代码字段 checkpointLag；含义由所属响应模型和接口业务决定 |
| `events` | `[]TransactionIndexedEvent` | 后端代码字段 events；含义由所属响应模型和接口业务决定 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |

### PublicConfigResponse

```json
{
  "protocolVersion": "string",
  "chainId": "int64",
  "chainName": "string",
  "rpcUrl": "string",
  "explorerUrl": "string",
  "stableTokenAddress": "string",
  "stableTokenSymbol": "string",
  "stableTokenDecimals": "uint8",
  "platformAddress": "string",
  "platformMultisig": "string",
  "platformPayout": "string",
  "platformAdmin": "string",
  "creationFeeAdmin": "string",
  "curveTableRegistry": "string",
  "projectImplementation": "string",
  "projectRules": "string",
  "graduationAdapter": "string",
  "graduationEnabled": "bool",
  "graduationDisabledReason": "string",
  "bridgeEnabled": "bool",
  "bridgeDisabledReason": "string",
  "uniswapV4Hook": "string",
  "uniswapV4PositionManager": "string",
  "uniswapV4PoolManager": "string",
  "uniswapV4Permit2": "string",
  "creationFee": "string",
  "nodeRaiseTarget": "string",
  "sourceBundleHash": "string",
  "deploymentStartBlock": "uint64",
  "scaleTiers": "[]ScaleTierSnapshot",
  "dependencyIntegrity": "DependencyIntegritySnapshot",
  "walletConnect": "WalletConnectConfig"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `protocolVersion` | `string` | 当前后端对齐的 SeedX 协议版本；版本不一致时前端应阻止高风险写操作 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `chainName` | `string` | 后端代码字段 chainName；含义由所属响应模型和接口业务决定 |
| `rpcUrl` | `string` | 可公开给前端的当前网络 RPC；后端内部使用的付费 RPC 及密钥绝不通过此字段返回 |
| `explorerUrl` | `string` | 后端代码字段 explorerUrl；含义由所属响应模型和接口业务决定 |
| `stableTokenAddress` | `string` | 当前网络配置的稳定币合约地址 |
| `stableTokenSymbol` | `string` | 后端代码字段 stableTokenSymbol；含义由所属响应模型和接口业务决定 |
| `stableTokenDecimals` | `uint8` | Stable Token 的真实精度；Ethereum Sepolia 为 USDGTest 6 位，项目 Token 仍为 18 位 |
| `platformAddress` | `string` | 合约或钱包的 EVM 地址 |
| `platformMultisig` | `string` | 后端代码字段 platformMultisig；含义由所属响应模型和接口业务决定 |
| `platformPayout` | `string` | 后端代码字段 platformPayout；含义由所属响应模型和接口业务决定 |
| `platformAdmin` | `string` | 后端代码字段 platformAdmin；含义由所属响应模型和接口业务决定 |
| `creationFeeAdmin` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `curveTableRegistry` | `string` | 后端代码字段 curveTableRegistry；含义由所属响应模型和接口业务决定 |
| `projectImplementation` | `string` | 后端代码字段 projectImplementation；含义由所属响应模型和接口业务决定 |
| `projectRules` | `string` | 当前部署 SeedXProjectRules 库地址；用于核对后端预检与合约规则版本 |
| `graduationAdapter` | `string` | 毕业 Adapter 地址；enabled=true 时必须等于 Platform.trustedGraduationAdapter() |
| `graduationEnabled` | `bool` | 是否可以发起毕业及 LP 手续费收取写操作；Ethereum Sepolia 已使用官方 Uniswap v4 完成验证，因此为 true |
| `graduationDisabledReason` | `string` | 后端代码字段 graduationDisabledReason；含义由所属响应模型和接口业务决定 |
| `bridgeEnabled` | `bool` | 是否开放 Relay 跨链报价和订单；当前测试服独立开启 Sepolia 与 Base Sepolia 原生 ETH 双向测试路线 |
| `bridgeDisabledReason` | `string` | 跨链桥禁用原因；前端应展示或用于禁用提示 |
| `uniswapV4Hook` | `string` | 后端代码字段 uniswapV4Hook；含义由所属响应模型和接口业务决定 |
| `uniswapV4PositionManager` | `string` | 后端代码字段 uniswapV4PositionManager；含义由所属响应模型和接口业务决定 |
| `uniswapV4PoolManager` | `string` | 后端代码字段 uniswapV4PoolManager；含义由所属响应模型和接口业务决定 |
| `uniswapV4Permit2` | `string` | 后端代码字段 uniswapV4Permit2；含义由所属响应模型和接口业务决定 |
| `creationFee` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeRaiseTarget` | `string` | 平台固定节点募集目标；当前为 30000 USDGTest，建项必须满足 nodeCount * nodePrice = nodeRaiseTarget |
| `sourceBundleHash` | `string` | Sepolia 20260821 交接包 source bundle SHA-256 哈希；必须为 22d7168618d2674546aeae149eaec03055cb3fdfa13830ddafacd3a03f8c2a92 |
| `deploymentStartBlock` | `uint64` | 当前协议部署首区块 11535261；Worker 全量重扫起点 |
| `scaleTiers` | `[]ScaleTierSnapshot` | 后端代码字段 scaleTiers；含义由所属响应模型和接口业务决定 |
| `dependencyIntegrity` | `DependencyIntegritySnapshot` | 当前 Platform、固定 Uniswap v4 Adapter 和官方外部依赖的链上 runtime 完整性快照 |
| `walletConnect` | `WalletConnectConfig` | 后端代码字段 walletConnect；含义由所属响应模型和接口业务决定 |

### DependencyIntegritySnapshot

```json
{
  "verified": "bool",
  "snapshotBlock": "uint64",
  "curveRegistry": "string",
  "trustedGraduationAdapter": "string",
  "trustedGraduationAdapterCodeHash": "string",
  "trustedGraduationAdapterRuntimeCodeHash": "string",
  "adapterDependencies": "AdapterDependencyIntegritySnapshot"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `verified` | `bool` | 全局依赖完整性总结果；Platform Registry/Adapter、Adapter runtime 和三个官方 Uniswap v4 依赖全部匹配 Sepolia 20260821 交接基线时为 true |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `curveRegistry` | `string` | 平台构造时固定的 CurveTableRegistry 地址 |
| `trustedGraduationAdapter` | `string` | 平台构造时创建并固定的唯一毕业 Adapter 地址 |
| `trustedGraduationAdapterCodeHash` | `string` | 平台构造时固定的 Adapter runtime codehash |
| `trustedGraduationAdapterRuntimeCodeHash` | `string` | 后端在 snapshotBlock 计算的 Adapter 实际 runtime codehash；必须与固定值一致 |
| `adapterDependencies` | `AdapterDependencyIntegritySnapshot` | 固定 Adapter 内部 PositionManager、PoolManager 和 Permit2 的 codehash 与实际 runtime 校验结果 |

### AdapterDependencyIntegritySnapshot

```json
{
  "verified": "bool",
  "positionManagerCodeHash": "string",
  "positionManagerRuntimeCodeHash": "string",
  "poolManagerCodeHash": "string",
  "poolManagerRuntimeCodeHash": "string",
  "permit2CodeHash": "string",
  "permit2RuntimeCodeHash": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `verified` | `bool` | 固定 Adapter 的 PositionManager、PoolManager、Permit2 地址和 runtime codehash 是否全部与链上固化值一致 |
| `positionManagerCodeHash` | `string` | 毕业 Adapter 构造时固定的官方 Uniswap v4 PositionManager runtime codehash |
| `positionManagerRuntimeCodeHash` | `string` | 后端在 snapshotBlock 对 PositionManager 实际 runtime bytecode计算的 keccak256 |
| `poolManagerCodeHash` | `string` | 毕业 Adapter 构造时固定的官方 Uniswap v4 PoolManager runtime codehash |
| `poolManagerRuntimeCodeHash` | `string` | 后端在 snapshotBlock 对 PoolManager 实际 runtime bytecode计算的 keccak256 |
| `permit2CodeHash` | `string` | 毕业 Adapter 构造时固定的官方 Uniswap v4 Permit2 runtime codehash |
| `permit2RuntimeCodeHash` | `string` | 后端在 snapshotBlock 对 Permit2 实际 runtime bytecode计算的 keccak256 |

### WalletConnectConfig

```json
{
  "projectId": "string",
  "relayUrl": "string",
  "enabled": "bool",
  "appUniversalLink": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectId` | `string` | 后端代码字段 projectId；含义由所属响应模型和接口业务决定 |
| `relayUrl` | `string` | 后端代码字段 relayUrl；含义由所属响应模型和接口业务决定 |
| `enabled` | `bool` | 档位是否启用；true 时执行全部 Registry/codehash/Adapter/参数约束，false 时链上清除 curveCodeHashByTier |
| `appUniversalLink` | `string` | 后端代码字段 appUniversalLink；含义由所属响应模型和接口业务决定 |

### ProjectGraylist

```json
{
  "id": "int64",
  "projectAddress": "string",
  "level": "string",
  "reason": "string",
  "judgmentRule": "string",
  "addedBy": "string",
  "createdAt": "time.Time",
  "updatedAt": "time.Time"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `int64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `level` | `string` | 后端代码字段 level；含义由所属响应模型和接口业务决定 |
| `reason` | `string` | 本次操作原因，用于审计追溯 |
| `judgmentRule` | `string` | 后端代码字段 judgmentRule；含义由所属响应模型和接口业务决定 |
| `addedBy` | `string` | 后端代码字段 addedBy；含义由所属响应模型和接口业务决定 |
| `createdAt` | `time.Time` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `time.Time` | 时间值；具体格式以该接口返回类型为准 |

### GraylistUpsertRequest

```json
{
  "projectAddress": "string",
  "level": "string",
  "reason": "string",
  "judgmentRule": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `level` | `string` | 后端代码字段 level；含义由所属响应模型和接口业务决定 |
| `reason` | `string` | 本次操作原因，用于审计追溯 |
| `judgmentRule` | `string` | 后端代码字段 judgmentRule；含义由所属响应模型和接口业务决定 |

### ConfigAuditLog

```json
{
  "id": "int64",
  "paramKey": "string",
  "oldValue": "string",
  "newValue": "string",
  "operatorAddress": "string",
  "createdAt": "time.Time"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `int64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `paramKey` | `string` | 后端代码字段 paramKey；含义由所属响应模型和接口业务决定 |
| `oldValue` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `newValue` | `string` | 治理提案新值的非负整数字符串；20260821 仅允许 paramId 5/8/10/11/12，后端同步校验 hard cap 和单次 20% 调整幅度 |
| `operatorAddress` | `string` | 合约或钱包的 EVM 地址 |
| `createdAt` | `time.Time` | 时间值；具体格式以该接口返回类型为准 |

### GraduationAnnouncement

```json
{
  "id": "int64",
  "projectAddress": "string",
  "announceTxHash": "string",
  "announceTime": "time.Time",
  "executableAt": "*time.Time",
  "rollingVolume": "string",
  "turnoverRateBps": "uint64",
  "executeTxHash": "*string",
  "executeTime": "*time.Time",
  "graduationPrice": "*string",
  "rCashMigrated": "*string",
  "graduatedPoolId": "*string",
  "pledgedBurned": "string",
  "status": "string",
  "createdAt": "time.Time"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `int64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `announceTxHash` | `string` | 后端代码字段 announceTxHash；含义由所属响应模型和接口业务决定 |
| `announceTime` | `time.Time` | 毕业公告链上时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `executableAt` | `*time.Time` | 毕业公告期结束后可执行时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `rollingVolume` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `turnoverRateBps` | `uint64` | 万分比整数，10000 表示 100% |
| `executeTxHash` | `*string` | 后端代码字段 executeTxHash；含义由所属响应模型和接口业务决定 |
| `executeTime` | `*time.Time` | 毕业实际执行时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `graduationPrice` | `*string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `rCashMigrated` | `*string` | 后端代码字段 rCashMigrated；含义由所属响应模型和接口业务决定 |
| `graduatedPoolId` | `*string` | 后端代码字段 graduatedPoolId；含义由所属响应模型和接口业务决定 |
| `pledgedBurned` | `string` | 后端代码字段 pledgedBurned；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `createdAt` | `time.Time` | 时间值；具体格式以该接口返回类型为准 |

### ExternalPoolPosition

```json
{
  "projectAddress": "string",
  "adapterAddress": "string",
  "poolId": "string",
  "tokenId": "string",
  "stableAmount": "string",
  "stableUsed": "string",
  "stableDust": "string",
  "sqrtPriceX96": "string",
  "tickLower": "int32",
  "tickUpper": "int32",
  "txHash": "string",
  "blockNumber": "uint64",
  "blockTime": "time.Time"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `adapterAddress` | `string` | 合约或钱包的 EVM 地址 |
| `poolId` | `string` | 后端代码字段 poolId；含义由所属响应模型和接口业务决定 |
| `tokenId` | `string` | 后端代码字段 tokenId；含义由所属响应模型和接口业务决定 |
| `stableAmount` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `stableUsed` | `string` | 后端代码字段 stableUsed；含义由所属响应模型和接口业务决定 |
| `stableDust` | `string` | 毕业建池后未被 Uniswap v4 仓位消耗的稳定币尾差；首次 collectAndDistribute 时与 LP 手续费一起分账 |
| `sqrtPriceX96` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `tickLower` | `int32` | 后端代码字段 tickLower；含义由所属响应模型和接口业务决定 |
| `tickUpper` | `int32` | 后端代码字段 tickUpper；含义由所属响应模型和接口业务决定 |
| `txHash` | `string` | 链上交易哈希 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `time.Time` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |

### LPFeeDistribution

```json
{
  "id": "int64",
  "projectAddress": "string",
  "txHash": "string",
  "stableAmount": "string",
  "tokenAmount": "string",
  "nodeShare": "string",
  "creatorShare": "string",
  "platformShare": "string",
  "blockNumber": "uint64",
  "blockTime": "time.Time"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `int64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `txHash` | `string` | 链上交易哈希 |
| `stableAmount` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `tokenAmount` | `string` | 项目 Token 数量 |
| `nodeShare` | `string` | 后端代码字段 nodeShare；含义由所属响应模型和接口业务决定 |
| `creatorShare` | `string` | 后端代码字段 creatorShare；含义由所属响应模型和接口业务决定 |
| `platformShare` | `string` | 后端代码字段 platformShare；含义由所属响应模型和接口业务决定 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `time.Time` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |

### PriceCandle

```json
{
  "id": "int64",
  "projectAddress": "string",
  "intervalType": "string",
  "openTime": "time.Time",
  "time": "int64",
  "closeTime": "int64",
  "open": "string",
  "high": "string",
  "low": "string",
  "close": "string",
  "floorPrice": "string",
  "volumeStable": "string",
  "volumeToken": "string",
  "tradeCount": "int",
  "synthetic": "bool",
  "confirmed": "bool"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `int64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `intervalType` | `string` | 后端代码字段 intervalType；含义由所属响应模型和接口业务决定 |
| `openTime` | `time.Time` | 时间值；具体格式以该接口返回类型为准 |
| `time` | `int64` | 后端代码字段 time；含义由所属响应模型和接口业务决定 |
| `closeTime` | `int64` | 时间值；具体格式以该接口返回类型为准 |
| `open` | `string` | 后端代码字段 open；含义由所属响应模型和接口业务决定 |
| `high` | `string` | 后端代码字段 high；含义由所属响应模型和接口业务决定 |
| `low` | `string` | 后端代码字段 low；含义由所属响应模型和接口业务决定 |
| `close` | `string` | 后端代码字段 close；含义由所属响应模型和接口业务决定 |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `volumeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `volumeToken` | `string` | 后端代码字段 volumeToken；含义由所属响应模型和接口业务决定 |
| `tradeCount` | `int` | 数量统计 |
| `synthetic` | `bool` | 后端代码字段 synthetic；含义由所属响应模型和接口业务决定 |
| `confirmed` | `bool` | 后端代码字段 confirmed；含义由所属响应模型和接口业务决定 |

### KlineFloorPoint

```json
{
  "time": "int64",
  "at": "time.Time",
  "floorPrice": "string",
  "blockNumber": "uint64",
  "synthetic": "bool",
  "source": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `time` | `int64` | 后端代码字段 time；含义由所属响应模型和接口业务决定 |
| `at` | `time.Time` | 后端代码字段 at；含义由所属响应模型和接口业务决定 |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `synthetic` | `bool` | 后端代码字段 synthetic；含义由所属响应模型和接口业务决定 |
| `source` | `string` | 后端代码字段 source；含义由所属响应模型和接口业务决定 |

### KlineChartSummary

```json
{
  "tokenName": "string",
  "tokenSymbol": "string",
  "logoUrl": "string",
  "currentPrice": "string",
  "floorPrice": "string",
  "open24h": "string",
  "high24h": "string",
  "low24h": "string",
  "close24h": "string",
  "change24h": "string",
  "change24hPct": "string",
  "volume24hStable": "string",
  "volume24hToken": "string",
  "trades24h": "int64",
  "lastTradeAt": "string",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `logoUrl` | `string` | 项目 Logo 资源 URL |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `open24h` | `string` | 后端代码字段 open24h；含义由所属响应模型和接口业务决定 |
| `high24h` | `string` | 后端代码字段 high24h；含义由所属响应模型和接口业务决定 |
| `low24h` | `string` | 后端代码字段 low24h；含义由所属响应模型和接口业务决定 |
| `close24h` | `string` | 后端代码字段 close24h；含义由所属响应模型和接口业务决定 |
| `change24h` | `string` | 后端代码字段 change24h；含义由所属响应模型和接口业务决定 |
| `change24hPct` | `string` | 百分比数值 |
| `volume24hStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `volume24hToken` | `string` | 后端代码字段 volume24hToken；含义由所属响应模型和接口业务决定 |
| `trades24h` | `int64` | 后端代码字段 trades24h；含义由所属响应模型和接口业务决定 |
| `lastTradeAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### PriceSnapshot

```json
{
  "id": "int64",
  "projectAddress": "string",
  "price": "string",
  "floorPrice": "string",
  "supplyTotal": "string",
  "reserveCash": "string",
  "blockNumber": "int64",
  "txHash": "string",
  "createdAt": "time.Time"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `int64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `price` | `string` | 后端代码字段 price；含义由所属响应模型和接口业务决定 |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `supplyTotal` | `string` | 后端代码字段 supplyTotal；含义由所属响应模型和接口业务决定 |
| `reserveCash` | `string` | 后端代码字段 reserveCash；含义由所属响应模型和接口业务决定 |
| `blockNumber` | `int64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `txHash` | `string` | 链上交易哈希 |
| `createdAt` | `time.Time` | 时间值；具体格式以该接口返回类型为准 |

### KlineQueryRequest

```json
{
  "projectAddress": "string",
  "interval": "string",
  "limit": "int",
  "startTime": "*int64",
  "endTime": "*int64",
  "fillPolicy": "string",
  "priceBasis": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `interval` | `string` | 后端代码字段 interval；含义由所属响应模型和接口业务决定 |
| `limit` | `int` | 本次最多返回的记录数 |
| `startTime` | `*int64` | 时间值；具体格式以该接口返回类型为准 |
| `endTime` | `*int64` | 时间值；具体格式以该接口返回类型为准 |
| `fillPolicy` | `string` | 后端代码字段 fillPolicy；含义由所属响应模型和接口业务决定 |
| `priceBasis` | `string` | 后端代码字段 priceBasis；含义由所属响应模型和接口业务决定 |

### KlineQuality

```json
{
  "realBars": "int",
  "syntheticBars": "int",
  "missingBuckets": "int",
  "lastRealBarTime": "*int64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `realBars` | `int` | 后端代码字段 realBars；含义由所属响应模型和接口业务决定 |
| `syntheticBars` | `int` | 后端代码字段 syntheticBars；含义由所属响应模型和接口业务决定 |
| `missingBuckets` | `int` | 后端代码字段 missingBuckets；含义由所属响应模型和接口业务决定 |
| `lastRealBarTime` | `*int64` | 时间值；具体格式以该接口返回类型为准 |

### KlineQueryResponse

```json
{
  "projectAddress": "string",
  "interval": "string",
  "candles": "[]PriceCandle",
  "bars": "[]PriceCandle",
  "floorSeries": "[]KlineFloorPoint",
  "summary": "KlineChartSummary",
  "intervalMs": "int64",
  "calendarAligned": "bool",
  "timeUnit": "string",
  "timezone": "string",
  "priceBasis": "string",
  "priceScale": "int64",
  "fillPolicy": "string",
  "hasMore": "bool",
  "nextBefore": "*int64",
  "quality": "KlineQuality",
  "lastUpdate": "time.Time",
  "status": "string",
  "dataSource": "string",
  "recommendedAction": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `interval` | `string` | 后端代码字段 interval；含义由所属响应模型和接口业务决定 |
| `candles` | `[]PriceCandle` | 后端代码字段 candles；含义由所属响应模型和接口业务决定 |
| `bars` | `[]PriceCandle` | 后端代码字段 bars；含义由所属响应模型和接口业务决定 |
| `floorSeries` | `[]KlineFloorPoint` | 后端代码字段 floorSeries；含义由所属响应模型和接口业务决定 |
| `summary` | `KlineChartSummary` | 后端代码字段 summary；含义由所属响应模型和接口业务决定 |
| `intervalMs` | `int64` | 后端代码字段 intervalMs；含义由所属响应模型和接口业务决定 |
| `calendarAligned` | `bool` | 后端代码字段 calendarAligned；含义由所属响应模型和接口业务决定 |
| `timeUnit` | `string` | 后端代码字段 timeUnit；含义由所属响应模型和接口业务决定 |
| `timezone` | `string` | 后端代码字段 timezone；含义由所属响应模型和接口业务决定 |
| `priceBasis` | `string` | 后端代码字段 priceBasis；含义由所属响应模型和接口业务决定 |
| `priceScale` | `int64` | 后端代码字段 priceScale；含义由所属响应模型和接口业务决定 |
| `fillPolicy` | `string` | 后端代码字段 fillPolicy；含义由所属响应模型和接口业务决定 |
| `hasMore` | `bool` | 后端代码字段 hasMore；含义由所属响应模型和接口业务决定 |
| `nextBefore` | `*int64` | 后端代码字段 nextBefore；含义由所属响应模型和接口业务决定 |
| `quality` | `KlineQuality` | 后端代码字段 quality；含义由所属响应模型和接口业务决定 |
| `lastUpdate` | `time.Time` | 后端代码字段 lastUpdate；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `recommendedAction` | `string` | 后端代码字段 recommendedAction；含义由所属响应模型和接口业务决定 |

### TradeEvent

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### KlineHealthResponse

```json
{
  "projectAddress": "string",
  "status": "string",
  "dataSource": "string",
  "tradeEvents": "int64",
  "candleCount": "int64",
  "duplicateEvents": "int64",
  "lastTradeAt": "string",
  "lastCandleAt": "string",
  "lastTradeBlock": "int64",
  "workerBlockLag": "uint64",
  "needsRebuild": "bool",
  "recommendedAction": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `tradeEvents` | `int64` | 后端代码字段 tradeEvents；含义由所属响应模型和接口业务决定 |
| `candleCount` | `int64` | 数量统计 |
| `duplicateEvents` | `int64` | 后端代码字段 duplicateEvents；含义由所属响应模型和接口业务决定 |
| `lastTradeAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `lastCandleAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `lastTradeBlock` | `int64` | 后端代码字段 lastTradeBlock；含义由所属响应模型和接口业务决定 |
| `workerBlockLag` | `uint64` | 后端代码字段 workerBlockLag；含义由所属响应模型和接口业务决定 |
| `needsRebuild` | `bool` | 后端代码字段 needsRebuild；含义由所属响应模型和接口业务决定 |
| `recommendedAction` | `string` | 后端代码字段 recommendedAction；含义由所属响应模型和接口业务决定 |

### OperationLedgerItem

```json
{
  "id": "uint64",
  "actorAddress": "string",
  "actorRole": "string",
  "source": "string",
  "action": "string",
  "targetType": "string",
  "targetId": "string",
  "projectAddress": "string",
  "txHash": "string",
  "requestId": "string",
  "amount": "string",
  "tokenAddress": "string",
  "tokenSymbol": "string",
  "status": "string",
  "chainId": "int64",
  "blockNumber": "uint64",
  "blockTime": "string",
  "metadataJson": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `actorAddress` | `string` | 合约或钱包的 EVM 地址 |
| `actorRole` | `string` | 操作人角色；枚举：`chain_user`=链上用户；`admin`=后台管理员 |
| `source` | `string` | 记录来源；枚举：`chain`=链上事件；`admin`=后台管理操作 |
| `action` | `string` | 操作账本机器码；链上事件与后台审计动作的并集；完整枚举：`create_project`=创建项目；`subscribe_node`=认购节点；`tradingstarted`=开启交易；`buy`=买入；`sell`=卖出；`profit_tax`=利润税入账；`pledge_buy`=质押买入；`unpledge`=解除质押；`nodelocked`=节点锁定；`unlockedclaimed`=领取解锁代币；`alttokenclaimed`=领取 Alt Token；`approval`=ERC20 授权；`costbasisupdated`=成本基准更新；`claim_fees`=领取手续费；`claim_taxes`=领取利润税；`transfer`=ERC20 Transfer 转账；`floorratchet`=地板价上调；`launchfailed`=发射失败；`refundclaimed`=领取退款；`proposalcreated`=提案创建；`votecast`=治理投票；`proposalexecuted`=提案执行；`proposaltallied`=提案计票；`fee_policy_updated`=费率策略更新；`graduationannounced`=毕业公告；`externalpoolmigrationcreated`=外部流动性池迁移创建；`graduationexecuted`=毕业执行；`lp_fee_distributed`=LP 分账；`treasury_income`=平台收入；`scaletierupdated`=平台 Scale Tier 更新；`scaletierversionupdated`=平台 Scale Tier 链上版本更新；`creationfeeupdated`=建项费更新；`transactionsubmitted`=多签交易提交；`transactionapproved`=多签交易批准；`transactionexecuted`=多签交易执行；`signeradded`=多签签名人新增；`signerremoved`=多签签名人移除；`signerreplaced`=多签签名人替换；`thresholdupdated`=多签阈值更新；`nativereceived`=多签收到原生币；`platformrevenueclaimprocessed`=平台收入领取结果；`platformrevenuebatchcompleted`=平台收入批量领取完成；`uniswapv4graduated`=Uniswap v4 Adapter 毕业回调；`productioncurvecreated`=生产曲线创建；`curvetableregistered`=曲线表登记；`curvetablecommitmentregistered`=曲线表承诺登记；`upsert_config`=新增平台后端配置版本；`project_point_multiplier_upsert`=修改项目积分倍率；`publish_announcement`=发布平台公告；`graylist_upsert`=新增或更新灰/黑名单；`graylist_remove`=移除灰/黑名单；`admin_target_metadata_upsert`=更新运营标签/风险备注；`support_reply`=客服回复；`support_assign`=客服分配；`support_update_status`=客服状态更新；`support_upsert_canned_reply`=新增或更新快捷回复；`support_delete_canned_reply`=删除快捷回复；`admin_role_bind`=授予管理员角色；`admin_role_revoke`=撤销管理员角色；`admin_role_unbind`=撤销管理员角色（管理元数据历史别名）；`treasury_income_indexed`=平台收入索引入库 |
| `targetType` | `string` | 操作目标类型；常见值为 project、user、platform、support_conversation、admin_role_binding |
| `targetId` | `string` | 目标用户或项目的 EVM 地址 |
| `projectAddress` | `string` | 项目合约地址 |
| `txHash` | `string` | 链上交易哈希 |
| `requestId` | `string` | 后端代码字段 requestId；含义由所属响应模型和接口业务决定 |
| `amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `tokenAddress` | `string` | 合约或钱包的 EVM 地址 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `status` | `string` | 处理状态；枚举：`success`=成功；`failed`=失败；`pending`=处理中 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `string` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `metadataJson` | `string` | 后端代码字段 metadataJson；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### OperationLedgerQuery

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### FeeIncomeLedgerItem

```json
{
  "id": "uint64",
  "projectAddress": "string",
  "recipientAddress": "string",
  "incomeType": "string",
  "tokenAddress": "string",
  "tokenSymbol": "string",
  "amount": "string",
  "source": "string",
  "sourceTxHash": "string",
  "sourceEvent": "string",
  "chainId": "int64",
  "blockNumber": "uint64",
  "blockTime": "string",
  "metadataJson": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `recipientAddress` | `string` | 合约或钱包的 EVM 地址 |
| `incomeType` | `string` | 费用收入机器码；完整枚举：`buy_fee_floor`=买入手续费（地板池）；`buy_fee_node`=买入手续费（节点）；`buy_fee_issuer`=买入手续费（项目方）；`buy_fee_platform`=买入手续费（平台）；`sell_fee_floor`=卖出手续费（地板池）；`sell_fee_node`=卖出手续费（节点）；`sell_fee_issuer`=卖出手续费（项目方）；`sell_fee_platform`=卖出手续费（平台）；`profit_tax_floor`=盈利税（地板池）；`profit_tax_node`=盈利税（节点）；`profit_tax_issuer`=盈利税（项目方）；`profit_tax_platform`=盈利税（平台）；`project_creation_fee`=项目创建费；`lp_fee_node`=LP 手续费（节点）；`lp_fee_creator`=LP 手续费（项目方）；`lp_fee_platform`=LP 手续费（平台） |
| `tokenAddress` | `string` | 合约或钱包的 EVM 地址 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `source` | `string` | 记录来源机器码；枚举：`chain`=链上事件；`admin`=后台管理操作 |
| `sourceTxHash` | `string` | 后端代码字段 sourceTxHash；含义由所属响应模型和接口业务决定 |
| `sourceEvent` | `string` | 产生该费用流水的合约事件；使用 projectHistoryEventType 字典，常见值为 Buy、Sell、ProfitTaxCharged、LPFeeDistributed、ProjectCreated |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `string` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `metadataJson` | `string` | 后端代码字段 metadataJson；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### FeeIncomeLedgerQuery

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### FeeSummary

```json
{
  "actorAddress": "string",
  "projectAddress": "string",
  "totalFeePaid": "string",
  "totalTaxPaid": "string",
  "totalClaimedFees": "string",
  "totalClaimedTaxes": "string",
  "totalPlatformIncome": "string",
  "dataSource": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `actorAddress` | `string` | 合约或钱包的 EVM 地址 |
| `projectAddress` | `string` | 项目合约地址 |
| `totalFeePaid` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalTaxPaid` | `string` | 后端代码字段 totalTaxPaid；含义由所属响应模型和接口业务决定 |
| `totalClaimedFees` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalClaimedTaxes` | `string` | 后端代码字段 totalClaimedTaxes；含义由所属响应模型和接口业务决定 |
| `totalPlatformIncome` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |

### OperationStats

```json
{
  "totalOperations": "uint64",
  "byAction": "map[string]uint64",
  "latestAt": "string",
  "dataSource": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `totalOperations` | `uint64` | 后端代码字段 totalOperations；含义由所属响应模型和接口业务决定 |
| `byAction` | `map[string]uint64` | 按操作账本 action 机器码聚合的次数映射；键使用 operationLedgerAction 完整字典。 |
| `latestAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |

### ProjectMarketQuery

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### ProjectMarketItem

```json
{
  "projectAddress": "string",
  "tokenName": "string",
  "tokenSymbol": "string",
  "issuerAddress": "string",
  "scaleTier": "string",
  "state": "string",
  "logoUrl": "string",
  "website": "string",
  "twitter": "string",
  "telegram": "string",
  "currentPrice": "string",
  "marketPrice": "string",
  "marketPriceSource": "string",
  "floorPrice": "string",
  "currentSupply": "string",
  "lockedSupply": "string",
  "circulatingMarketCap": "string",
  "lockedMarketCap": "string",
  "totalMarketCap": "string",
  "marketCap": "string",
  "circulatingMarketCapPct": "string",
  "lockedMarketCapPct": "string",
  "fdv": "string",
  "floorMarketCap": "string",
  "nodeProgressPct": "string",
  "volume24hStable": "string",
  "volume7dStable": "string",
  "tradeCount24h": "uint64",
  "buyCount24h": "uint64",
  "sellCount24h": "uint64",
  "holderCount": "uint64",
  "nodeHolderCount": "uint64",
  "activeUsers24h": "uint64",
  "feeIncomeStable": "string",
  "operationCount": "uint64",
  "lastActivityAt": "string",
  "createdAt": "string",
  "ageSeconds": "int64",
  "hotScore": "float64",
  "trendScore": "float64",
  "nearFloorPct": "string",
  "richDevScore": "float64",
  "verified": "bool",
  "sortReason": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `issuerAddress` | `string` | 项目创建者钱包地址 |
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `state` | `string` | 后端代码字段 state；含义由所属响应模型和接口业务决定 |
| `logoUrl` | `string` | 项目 Logo 资源 URL |
| `website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `marketPrice` | `string` | 用于市值计算的统一市场价；Trading/Graduating 取曲线价，Graduated 优先取官方 Uniswap v4 StateView slot0 现价 |
| `marketPriceSource` | `string` | 市场价来源枚举：bonding_curve/uniswap_v4_slot0/graduation_price_fallback/unavailable |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `currentSupply` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `lockedSupply` | `string` | 锁定供应量，固定等于 supplyNode + supplyPledged |
| `circulatingMarketCap` | `string` | 流通市值 = marketPrice * supplyUserTotal |
| `lockedMarketCap` | `string` | 锁定市值 = marketPrice * (supplyNode + supplyPledged) |
| `totalMarketCap` | `string` | 总市值 = 流通市值 + 锁定市值 = marketPrice * supplyTotal |
| `marketCap` | `string` | 总市值兼容别名，数值等于 totalMarketCap |
| `circulatingMarketCapPct` | `string` | 流通市值占比，等于 supplyUserTotal / supplyTotal * 100；单位 % |
| `lockedMarketCapPct` | `string` | 锁定市值占比，等于 (supplyNode + supplyPledged) / supplyTotal * 100；单位 % |
| `fdv` | `string` | 已废弃语义的兼容字段；当前数值等于 totalMarketCap，合约无固定 maxSupply，不应解释为传统 FDV |
| `floorMarketCap` | `string` | 后端代码字段 floorMarketCap；含义由所属响应模型和接口业务决定 |
| `nodeProgressPct` | `string` | 百分比数值 |
| `volume24hStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `volume7dStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `tradeCount24h` | `uint64` | 后端代码字段 tradeCount24h；含义由所属响应模型和接口业务决定 |
| `buyCount24h` | `uint64` | 后端代码字段 buyCount24h；含义由所属响应模型和接口业务决定 |
| `sellCount24h` | `uint64` | 后端代码字段 sellCount24h；含义由所属响应模型和接口业务决定 |
| `holderCount` | `uint64` | 数量统计 |
| `nodeHolderCount` | `uint64` | 数量统计 |
| `activeUsers24h` | `uint64` | 后端代码字段 activeUsers24h；含义由所属响应模型和接口业务决定 |
| `feeIncomeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `operationCount` | `uint64` | 数量统计 |
| `lastActivityAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `ageSeconds` | `int64` | 后端代码字段 ageSeconds；含义由所属响应模型和接口业务决定 |
| `hotScore` | `float64` | 后端代码字段 hotScore；含义由所属响应模型和接口业务决定 |
| `trendScore` | `float64` | 后端代码字段 trendScore；含义由所属响应模型和接口业务决定 |
| `nearFloorPct` | `string` | 百分比数值 |
| `richDevScore` | `float64` | 后端代码字段 richDevScore；含义由所属响应模型和接口业务决定 |
| `verified` | `bool` | 后端代码字段 verified；含义由所属响应模型和接口业务决定 |
| `sortReason` | `string` | 后端代码字段 sortReason；含义由所属响应模型和接口业务决定 |

### ProjectMarketFacets

```json
{
  "states": "map[string]uint64",
  "scaleTiers": "map[string]uint64",
  "verified": "map[string]uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `states` | `map[string]uint64` | 后端代码字段 states；含义由所属响应模型和接口业务决定 |
| `scaleTiers` | `map[string]uint64` | 后端代码字段 scaleTiers；含义由所属响应模型和接口业务决定 |
| `verified` | `map[string]uint64` | 后端代码字段 verified；含义由所属响应模型和接口业务决定 |

### ProjectMarketSummary

```json
{
  "totalProjects": "uint64",
  "tradingProjects": "uint64",
  "graduatedProjects": "uint64",
  "totalMarketCap": "string",
  "totalFdv": "string",
  "totalVolume24hStable": "string",
  "totalFeeIncomeStable": "string",
  "totalHolders": "uint64",
  "totalOperations24h": "uint64",
  "dataSource": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `totalProjects` | `uint64` | 后端代码字段 totalProjects；含义由所属响应模型和接口业务决定 |
| `tradingProjects` | `uint64` | 后端代码字段 tradingProjects；含义由所属响应模型和接口业务决定 |
| `graduatedProjects` | `uint64` | 后端代码字段 graduatedProjects；含义由所属响应模型和接口业务决定 |
| `totalMarketCap` | `string` | 总市值 = 流通市值 + 锁定市值 = marketPrice * supplyTotal |
| `totalFdv` | `string` | 后端代码字段 totalFdv；含义由所属响应模型和接口业务决定 |
| `totalVolume24hStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalFeeIncomeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalHolders` | `uint64` | 后端代码字段 totalHolders；含义由所属响应模型和接口业务决定 |
| `totalOperations24h` | `uint64` | 后端代码字段 totalOperations24h；含义由所属响应模型和接口业务决定 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |

### ProjectMarketResponse

```json
{
  "query": "ProjectMarketQuery",
  "summary": "ProjectMarketSummary",
  "facets": "ProjectMarketFacets",
  "items": "[]ProjectMarketItem",
  "total": "uint64",
  "limit": "int",
  "offset": "int",
  "generatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `query` | `ProjectMarketQuery` | 后端代码字段 query；含义由所属响应模型和接口业务决定 |
| `summary` | `ProjectMarketSummary` | 后端代码字段 summary；含义由所属响应模型和接口业务决定 |
| `facets` | `ProjectMarketFacets` | 后端代码字段 facets；含义由所属响应模型和接口业务决定 |
| `items` | `[]ProjectMarketItem` | 当前页数据列表；空结果返回 [] |
| `total` | `uint64` | 符合条件的总记录数 |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |
| `generatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### MemberProfile

```json
{
  "address": "string",
  "inviteCode": "string",
  "displayName": "string",
  "avatarUrl": "string",
  "memberLevel": "string",
  "status": "string",
  "riskFlagsJson": "string",
  "lastActiveAt": "string",
  "createdAt": "string",
  "updatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
| `displayName` | `string` | 后端代码字段 displayName；含义由所属响应模型和接口业务决定 |
| `avatarUrl` | `string` | 后端代码字段 avatarUrl；含义由所属响应模型和接口业务决定 |
| `memberLevel` | `string` | 后端代码字段 memberLevel；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `riskFlagsJson` | `string` | 后端代码字段 riskFlagsJson；含义由所属响应模型和接口业务决定 |
| `lastActiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### MemberPreferences

```json
{
  "address": "string",
  "language": "string",
  "notificationChannelsJson": "string",
  "displaySettingsJson": "string",
  "updatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `language` | `string` | 后端代码字段 language；含义由所属响应模型和接口业务决定 |
| `notificationChannelsJson` | `string` | 后端代码字段 notificationChannelsJson；含义由所属响应模型和接口业务决定 |
| `displaySettingsJson` | `string` | 后端代码字段 displaySettingsJson；含义由所属响应模型和接口业务决定 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### UpdateMemberProfileRequest

```json
{
  "displayName": "string",
  "avatarUrl": "string",
  "language": "string",
  "notificationChannelsJson": "string",
  "displaySettingsJson": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `displayName` | `string` | 后端代码字段 displayName；含义由所属响应模型和接口业务决定 |
| `avatarUrl` | `string` | 后端代码字段 avatarUrl；含义由所属响应模型和接口业务决定 |
| `language` | `string` | 后端代码字段 language；含义由所属响应模型和接口业务决定 |
| `notificationChannelsJson` | `string` | 后端代码字段 notificationChannelsJson；含义由所属响应模型和接口业务决定 |
| `displaySettingsJson` | `string` | 后端代码字段 displaySettingsJson；含义由所属响应模型和接口业务决定 |

### MemberActivityLog

```json
{
  "id": "uint64",
  "address": "string",
  "activityType": "string",
  "targetType": "string",
  "targetId": "string",
  "metadataJson": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `activityType` | `string` | 后端代码字段 activityType；含义由所属响应模型和接口业务决定 |
| `targetType` | `string` | 运营标签目标类型；user/member 都规范化为 user，project 表示项目 |
| `targetId` | `string` | 目标用户或项目的 EVM 地址 |
| `metadataJson` | `string` | 后端代码字段 metadataJson；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### MemberSummary

```json
{
  "profile": "MemberProfile",
  "preferences": "MemberPreferences",
  "pointBalance": "string",
  "badgeCount": "int",
  "projectCount": "int",
  "transactionCount": "int",
  "unreadSupport": "int",
  "recentActivity": "[]MemberActivityLog"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `profile` | `MemberProfile` | 后端代码字段 profile；含义由所属响应模型和接口业务决定 |
| `preferences` | `MemberPreferences` | 后端代码字段 preferences；含义由所属响应模型和接口业务决定 |
| `pointBalance` | `string` | 后端代码字段 pointBalance；含义由所属响应模型和接口业务决定 |
| `badgeCount` | `int` | 数量统计 |
| `projectCount` | `int` | 数量统计 |
| `transactionCount` | `int` | 数量统计 |
| `unreadSupport` | `int` | 后端代码字段 unreadSupport；含义由所属响应模型和接口业务决定 |
| `recentActivity` | `[]MemberActivityLog` | 后端代码字段 recentActivity；含义由所属响应模型和接口业务决定 |

### AdminMemberListItem

```json
{
  "profile": "MemberProfile",
  "pointBalance": "string",
  "supportOpenCount": "int",
  "lastSupportAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `profile` | `MemberProfile` | 后端代码字段 profile；含义由所属响应模型和接口业务决定 |
| `pointBalance` | `string` | 后端代码字段 pointBalance；含义由所属响应模型和接口业务决定 |
| `supportOpenCount` | `int` | 数量统计 |
| `lastSupportAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminMemberFullView

```json
{
  "summary": "MemberSummary",
  "supportConversations": "[]SupportConversation",
  "activity": "[]MemberActivityLog",
  "transactions": "[]UserTransactionItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `summary` | `MemberSummary` | 后端代码字段 summary；含义由所属响应模型和接口业务决定 |
| `supportConversations` | `[]SupportConversation` | 后端代码字段 supportConversations；含义由所属响应模型和接口业务决定 |
| `activity` | `[]MemberActivityLog` | 后端代码字段 activity；含义由所属响应模型和接口业务决定 |
| `transactions` | `[]UserTransactionItem` | 后端代码字段 transactions；含义由所属响应模型和接口业务决定 |

### NotificationItem

```json
{
  "id": "uint64",
  "recipientAddress": "string",
  "notificationType": "string",
  "title": "string",
  "body": "string",
  "payloadJson": "string",
  "status": "string",
  "deliveredAt": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `recipientAddress` | `string` | 合约或钱包的 EVM 地址 |
| `notificationType` | `string` | 后端代码字段 notificationType；含义由所属响应模型和接口业务决定 |
| `title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `payloadJson` | `string` | 后端代码字段 payloadJson；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `deliveredAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### MarkNotificationsReadRequest

```json
{
  "ids": "[]uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `ids` | `[]uint64` | 后端代码字段 ids；含义由所属响应模型和接口业务决定 |

### MessageItem

```json
{
  "id": "uint64",
  "messageType": "string",
  "projectAddress": "string",
  "tokenSymbol": "string",
  "title": "string",
  "body": "string",
  "txHash": "string",
  "publishedBy": "string",
  "publishedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `messageType` | `string` | 后端代码字段 messageType；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `txHash` | `string` | 链上交易哈希 |
| `publishedBy` | `string` | 后端代码字段 publishedBy；含义由所属响应模型和接口业务决定 |
| `publishedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### PublishProjectAnnouncementRequest

```json
{
  "title": "string",
  "body": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |

### PublishProjectAnnouncementResponse

```json
{
  "published": "bool",
  "message": "MessageItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `published` | `bool` | 后端代码字段 published；含义由所属响应模型和接口业务决定 |
| `message` | `MessageItem` | 人类可读的结果或错误信息 |

### MultisigPolicy

```json
{
  "contractAddress": "string",
  "signers": "[]string",
  "threshold": "uint64",
  "configVersion": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `contractAddress` | `string` | 合约或钱包的 EVM 地址 |
| `signers` | `[]string` | 后端代码字段 signers；含义由所属响应模型和接口业务决定 |
| `threshold` | `uint64` | 后端代码字段 threshold；含义由所属响应模型和接口业务决定 |
| `configVersion` | `uint64` | 多签签名人/阈值配置版本；交易保存提交时版本，与当前版本不同时为过期交易 |

### MultisigTransactionItem

```json
{
  "id": "uint64",
  "target": "string",
  "value": "string",
  "calldata": "string",
  "approvalCount": "uint64",
  "configVersion": "uint64",
  "createdAt": "uint64",
  "executed": "bool",
  "stale": "bool",
  "approvedBy": "[]string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `target` | `string` | 交易目标合约地址 |
| `value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `approvalCount` | `uint64` | 数量统计 |
| `configVersion` | `uint64` | 多签签名人/阈值配置版本；交易保存提交时版本，与当前版本不同时为过期交易 |
| `createdAt` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `executed` | `bool` | 后端代码字段 executed；含义由所属响应模型和接口业务决定 |
| `stale` | `bool` | 多签交易是否因签名人或阈值变更而过期；true 时合约会以 StaleTransaction 拒绝批准/执行 |
| `approvedBy` | `[]string` | 后端代码字段 approvedBy；含义由所属响应模型和接口业务决定 |

### AdminMultisigResponse

```json
{
  "policy": "MultisigPolicy",
  "items": "[]MultisigTransactionItem",
  "total": "uint64",
  "limit": "int",
  "offset": "int",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `policy` | `MultisigPolicy` | 后端代码字段 policy；含义由所属响应模型和接口业务决定 |
| `items` | `[]MultisigTransactionItem` | 当前页数据列表；空结果返回 [] |
| `total` | `uint64` | 符合条件的总记录数 |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### MultisigTxParamsRequest

```json
{
  "action": "string",
  "target": "string",
  "value": "string",
  "calldata": "string",
  "transactionId": "uint64",
  "signer": "string",
  "oldSigner": "string",
  "newSigner": "string",
  "newThreshold": "uint64",
  "revenueClaims": "[]PlatformRevenueClaimRequest"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `action` | `string` | 后端代码字段 action；含义由所属响应模型和接口业务决定 |
| `target` | `string` | 交易目标合约地址 |
| `value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `transactionId` | `uint64` | 后端代码字段 transactionId；含义由所属响应模型和接口业务决定 |
| `signer` | `string` | 多签 remove_signer 要移除的现有签名人地址 |
| `oldSigner` | `string` | 多签 replace_signer 要替换的现有签名人地址 |
| `newSigner` | `string` | 多签 add_signer/replace_signer 的新签名人地址 |
| `newThreshold` | `uint64` | 多签 update_threshold 的新批准阈值；必须满足合约 2/3 规则 |
| `revenueClaims` | `[]PlatformRevenueClaimRequest` | 后端代码字段 revenueClaims；含义由所属响应模型和接口业务决定 |

### PlatformRevenueClaimRequest

```json
{
  "project": "string",
  "claimFees": "bool",
  "claimTaxes": "bool"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `project` | `string` | 后端代码字段 project；含义由所属响应模型和接口业务决定 |
| `claimFees` | `bool` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `claimTaxes` | `bool` | 后端代码字段 claimTaxes；含义由所属响应模型和接口业务决定 |

### PlatformRevenueClaimPreview

```json
{
  "project": "string",
  "claimFees": "bool",
  "claimTaxes": "bool",
  "claimableFeeStable": "string",
  "claimableTaxStable": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `project` | `string` | 后端代码字段 project；含义由所属响应模型和接口业务决定 |
| `claimFees` | `bool` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `claimTaxes` | `bool` | 后端代码字段 claimTaxes；含义由所属响应模型和接口业务决定 |
| `claimableFeeStable` | `string` | 链上 claimableFees(user) 返回的完整实时可领手续费；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `claimableTaxStable` | `string` | 链上 claimableTaxes(user) 返回的完整实时可领利润税；新合约已包含节点懒结算奖励，前后端不得再二次累加 |

### PlatformRevenueProjectItem

```json
{
  "projectAddress": "string",
  "tokenName": "string",
  "tokenSymbol": "string",
  "projectState": "string",
  "claimableFeeStable": "string",
  "claimableTaxStable": "string",
  "claimableTotalStable": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `projectState` | `string` | 后端代码字段 projectState；含义由所属响应模型和接口业务决定 |
| `claimableFeeStable` | `string` | 链上 claimableFees(user) 返回的完整实时可领手续费；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `claimableTaxStable` | `string` | 链上 claimableTaxes(user) 返回的完整实时可领利润税；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `claimableTotalStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |

### MultisigTxParamsResponse

```json
{
  "target": "string",
  "method": "string",
  "calldata": "string",
  "value": "string",
  "chainId": "int64",
  "args": "map[string]string",
  "requiredApprovals": "[]RequiredApproval",
  "balanceChecks": "[]BalanceCheck",
  "warnings": "[]string",
  "snapshotBlock": "uint64",
  "preflight": "TxPreflight",
  "revenueClaims": "[]PlatformRevenueClaimPreview"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `target` | `string` | 交易目标合约地址 |
| `method` | `string` | 后端代码字段 method；含义由所属响应模型和接口业务决定 |
| `calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `args` | `map[string]string` | 后端代码字段 args；含义由所属响应模型和接口业务决定 |
| `requiredApprovals` | `[]RequiredApproval` | 后端代码字段 requiredApprovals；含义由所属响应模型和接口业务决定 |
| `balanceChecks` | `[]BalanceCheck` | 后端代码字段 balanceChecks；含义由所属响应模型和接口业务决定 |
| `warnings` | `[]string` | 后端代码字段 warnings；含义由所属响应模型和接口业务决定 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `preflight` | `TxPreflight` | 后端代码字段 preflight；含义由所属响应模型和接口业务决定 |
| `revenueClaims` | `[]PlatformRevenueClaimPreview` | 后端代码字段 revenueClaims；含义由所属响应模型和接口业务决定 |

### PlatformConfigActionResponse

```json
{
  "executed": "bool",
  "config": "*PlatformConfigItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `executed` | `bool` | 后端代码字段 executed；含义由所属响应模型和接口业务决定 |
| `config` | `*PlatformConfigItem` | 后端代码字段 config；含义由所属响应模型和接口业务决定 |

### PagedResponse

```json
{
  "items": "[]T",
  "total": "uint64",
  "limit": "int",
  "offset": "int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `items` | `[]T` | 当前页数据列表；空结果返回 [] |
| `total` | `uint64` | 符合条件的总记录数 |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |

### PointLevelRule

```json
{
  "level": "string",
  "nameZh": "string",
  "minPoints": "string",
  "nextLevel": "string",
  "nextMinimum": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `level` | `string` | 后端代码字段 level；含义由所属响应模型和接口业务决定 |
| `nameZh` | `string` | 后端代码字段 nameZh；含义由所属响应模型和接口业务决定 |
| `minPoints` | `string` | 后端代码字段 minPoints；含义由所属响应模型和接口业务决定 |
| `nextLevel` | `string` | 后端代码字段 nextLevel；含义由所属响应模型和接口业务决定 |
| `nextMinimum` | `string` | 后端代码字段 nextMinimum；含义由所属响应模型和接口业务决定 |

### TeamRewardTier

```json
{
  "minDownlineBasePoints": "string",
  "rewardRateBps": "uint64",
  "rewardRate": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `minDownlineBasePoints` | `string` | 后端代码字段 minDownlineBasePoints；含义由所属响应模型和接口业务决定 |
| `rewardRateBps` | `uint64` | 该奖励流水使用的 BPS 快照；直属邀请固定 1000，团队奖励为交易前有效阶梯 |
| `rewardRate` | `string` | 后端代码字段 rewardRate；含义由所属响应模型和接口业务决定 |

### PointRulesResponse

```json
{
  "version": "string",
  "precision": "int",
  "rounding": "string",
  "directReferralRateBps": "uint64",
  "basePointSources": "[]string",
  "effectivePointComponents": "[]string",
  "personalLevels": "[]PointLevelRule",
  "teamRewardTiers": "[]TeamRewardTier",
  "teamRewardTiming": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `version` | `string` | 积分规则版本；当前固定为 seedx-points-20260807 |
| `precision` | `int` | 积分存储与结算小数位数；当前为 6 |
| `rounding` | `string` | 舍入规则严格枚举；floor=向下截断 |
| `directReferralRateBps` | `uint64` | 直属邀请奖励 BPS；当前为 1000，即基础积分的 10% |
| `basePointSources` | `[]string` | 会产生基础积分的流水类型；仅 base_buy/base_pledge |
| `effectivePointComponents` | `[]string` | 个人有效积分组成；基础积分+直属邀请奖励+团队运营奖励 |
| `personalLevels` | `[]PointLevelRule` | 个人 L0-L5 门槛列表；前端必须使用此列表而不是硬编码 |
| `teamRewardTiers` | `[]TeamRewardTier` | 全部下线累计基础积分对应的 0%-3% 团队奖励阶梯 |
| `teamRewardTiming` | `string` | 团队比例生效时点；当前笔用交易前比例，跨门槛后下一笔生效 |

### UserPointsSummary

```json
{
  "address": "string",
  "inviteCode": "string",
  "totalPoints": "string",
  "personalEffectivePoints": "string",
  "personalLevel": "string",
  "personalLevelName": "string",
  "nextPersonalLevel": "string",
  "pointsToNextPersonalLevel": "string",
  "basePointsTotal": "string",
  "directReferralRewardTotal": "string",
  "teamOperationRewardTotal": "string",
  "referrerAddress": "string",
  "referrerInviteCode": "string",
  "referralRewardTotal": "string",
  "directReferralCount": "int",
  "networkSummary": "PointNetworkSummary",
  "recentActivity": "PointActivitySummary",
  "downlinePoints": "string",
  "teamLeaderAddress": "string",
  "isTeamLeader": "bool",
  "teamDownlineBasePoints": "string",
  "teamRewardRateBps": "uint64",
  "nextTeamRewardThreshold": "string",
  "directReferrals": "[]DirectReferralItem",
  "ledger": "[]PointLedgerItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
| `totalPoints` | `string` | 个人有效积分兼容别名；数值等于 personalEffectivePoints |
| `personalEffectivePoints` | `string` | 个人累计有效积分；包含基础积分、10% 直属邀请奖励和团队运营奖励 |
| `personalLevel` | `string` | 个人积分等级严格枚举：L0/L1/L2/L3/L4/L5；达到后永久保留 |
| `personalLevelName` | `string` | 个人等级中文名称：普通/青铜/白银/黄金/铂金/黑金 |
| `nextPersonalLevel` | `string` | 下一个个人等级；L5 时为空字符串 |
| `pointsToNextPersonalLevel` | `string` | 距下一个人等级还需的有效积分；L5 时为 0.000000 |
| `basePointsTotal` | `string` | 本人 Buy 和 PledgeCreated 的真实净入曲线金额乘项目倍率后累计的基础积分 |
| `directReferralRewardTotal` | `string` | 作为实际直属邀请人累计获得的 10% 邀请奖励 |
| `teamOperationRewardTotal` | `string` | 作为推荐树根节点团队长累计获得的 0%-3% 团队运营奖励 |
| `referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `referrerInviteCode` | `string` | 后端代码字段 referrerInviteCode；含义由所属响应模型和接口业务决定 |
| `referralRewardTotal` | `string` | 后端代码字段 referralRewardTotal；含义由所属响应模型和接口业务决定 |
| `directReferralCount` | `int` | 数量统计 |
| `networkSummary` | `PointNetworkSummary` | 后端代码字段 networkSummary；含义由所属响应模型和接口业务决定 |
| `recentActivity` | `PointActivitySummary` | 后端代码字段 recentActivity；含义由所属响应模型和接口业务决定 |
| `downlinePoints` | `string` | 当前查询钱包自己的全部下线累计基础积分；不是所属根团队总额，不含奖励积分 |
| `teamLeaderAddress` | `string` | 当前推荐树没有上级的唯一根节点钱包地址 |
| `isTeamLeader` | `bool` | 当前查询钱包是否为所属推荐树的根节点 |
| `teamDownlineBasePoints` | `string` | 根团队全部下线历史累计基础积分；不包含团队长本人、邀请奖励或团队奖励 |
| `teamRewardRateBps` | `uint64` | 根团队当前团队运营奖励 BPS；0/50/100/150/200/300 |
| `nextTeamRewardThreshold` | `string` | 下一个团队奖励阶梯所需的全部下线累计基础积分；最高档为空 |
| `directReferrals` | `[]DirectReferralItem` | 后端代码字段 directReferrals；含义由所属响应模型和接口业务决定 |
| `ledger` | `[]PointLedgerItem` | 后端代码字段 ledger；含义由所属响应模型和接口业务决定 |

### DirectReferralItem

```json
{
  "address": "string",
  "inviteCode": "string",
  "personalEffectivePoints": "string",
  "personalLevel": "string",
  "basePoints": "string",
  "referralReward": "string",
  "joinedAt": "string",
  "points": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
| `personalEffectivePoints` | `string` | 个人累计有效积分；包含基础积分、10% 直属邀请奖励和团队运营奖励 |
| `personalLevel` | `string` | 个人积分等级严格枚举：L0/L1/L2/L3/L4/L5；达到后永久保留 |
| `basePoints` | `string` | 后端代码字段 basePoints；含义由所属响应模型和接口业务决定 |
| `referralReward` | `string` | 当前查询钱包因该直属下线产生基础积分而已获得的 10% 奖励累计；不是该下线自己获得的邀请奖励 |
| `joinedAt` | `string` | 该推荐关系对当前查询人/根团队开始生效的时间；RFC3339，补绑时取真实绑定时间，不是账户注册时间 |
| `points` | `string` | 后端代码字段 points；含义由所属响应模型和接口业务决定 |

### BindReferrerRequest

```json
{
  "referrerAddress": "string",
  "inviteCode": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |

### BindReferrerResponse

```json
{
  "address": "string",
  "inviteCode": "string",
  "referrerAddress": "string",
  "referrerInviteCode": "string",
  "teamLeaderAddress": "string",
  "bound": "bool",
  "status": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
| `referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `referrerInviteCode` | `string` | 后端代码字段 referrerInviteCode；含义由所属响应模型和接口业务决定 |
| `teamLeaderAddress` | `string` | 当前推荐树没有上级的唯一根节点钱包地址 |
| `bound` | `bool` | 后端代码字段 bound；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |

### PointNetworkSummary

```json
{
  "depth1Count": "int",
  "depth2Count": "int",
  "depth3Count": "int",
  "totalCount": "int",
  "depth1Points": "string",
  "depth2Points": "string",
  "depth3Points": "string",
  "totalPoints": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `depth1Count` | `int` | 数量统计 |
| `depth2Count` | `int` | 数量统计 |
| `depth3Count` | `int` | 数量统计 |
| `totalCount` | `int` | 数量统计 |
| `depth1Points` | `string` | 后端代码字段 depth1Points；含义由所属响应模型和接口业务决定 |
| `depth2Points` | `string` | 后端代码字段 depth2Points；含义由所属响应模型和接口业务决定 |
| `depth3Points` | `string` | 后端代码字段 depth3Points；含义由所属响应模型和接口业务决定 |
| `totalPoints` | `string` | 个人有效积分兼容别名；数值等于 personalEffectivePoints |

### PointActivitySummary

```json
{
  "windowStart": "string",
  "windowEnd": "string",
  "tradePoints": "string",
  "referralPoints": "string",
  "teamRewardPoints": "string",
  "tradeRewardPoints": "string",
  "referralRewardPoints": "string",
  "tradeEvents": "int",
  "referralEvents": "int",
  "teamRewardEvents": "int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `windowStart` | `string` | 后端代码字段 windowStart；含义由所属响应模型和接口业务决定 |
| `windowEnd` | `string` | 后端代码字段 windowEnd；含义由所属响应模型和接口业务决定 |
| `tradePoints` | `string` | 后端代码字段 tradePoints；含义由所属响应模型和接口业务决定 |
| `referralPoints` | `string` | 后端代码字段 referralPoints；含义由所属响应模型和接口业务决定 |
| `teamRewardPoints` | `string` | 后端代码字段 teamRewardPoints；含义由所属响应模型和接口业务决定 |
| `tradeRewardPoints` | `string` | 后端代码字段 tradeRewardPoints；含义由所属响应模型和接口业务决定 |
| `referralRewardPoints` | `string` | 后端代码字段 referralRewardPoints；含义由所属响应模型和接口业务决定 |
| `tradeEvents` | `int` | 后端代码字段 tradeEvents；含义由所属响应模型和接口业务决定 |
| `referralEvents` | `int` | 后端代码字段 referralEvents；含义由所属响应模型和接口业务决定 |
| `teamRewardEvents` | `int` | 后端代码字段 teamRewardEvents；含义由所属响应模型和接口业务决定 |

### PointLedgerItem

```json
{
  "id": "uint64",
  "address": "string",
  "projectAddress": "string",
  "sourceType": "string",
  "sourceTxHash": "string",
  "sourceLogIndex": "uint64",
  "sourceBlockNumber": "uint64",
  "sourceUserAddress": "string",
  "teamLeaderAddress": "string",
  "netCurveAmount": "string",
  "pointMultiplier": "string",
  "basePoints": "string",
  "rewardRateBps": "uint64",
  "points": "string",
  "referrerPoints": "string",
  "blockTime": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `sourceType` | `string` | 积分流水类型：base_buy/base_pledge/direct_referral_reward/team_operation_reward |
| `sourceTxHash` | `string` | 后端代码字段 sourceTxHash；含义由所属响应模型和接口业务决定 |
| `sourceLogIndex` | `uint64` | 链上日志序号；与交易哈希、受益人和流水类型共同组成幂等键 |
| `sourceBlockNumber` | `uint64` | 产生该积分的链上区块高度 |
| `sourceUserAddress` | `string` | 触发本笔基础积分及派生奖励的原始交易钱包 |
| `teamLeaderAddress` | `string` | 当前推荐树没有上级的唯一根节点钱包地址 |
| `netCurveAmount` | `string` | 本次真实进入联合曲线的稳定币净额；不包含手续费和质押借入部分 |
| `pointMultiplier` | `string` | 事件发生时有效的积分倍率；优先使用项目级配置，其次 Scale Tier 配置，都未配置时为 1 |
| `basePoints` | `string` | 后端代码字段 basePoints；含义由所属响应模型和接口业务决定 |
| `rewardRateBps` | `uint64` | 该奖励流水使用的 BPS 快照；直属邀请固定 1000，团队奖励为交易前有效阶梯 |
| `points` | `string` | 后端代码字段 points；含义由所属响应模型和接口业务决定 |
| `referrerPoints` | `string` | 后端代码字段 referrerPoints；含义由所属响应模型和接口业务决定 |
| `blockTime` | `string` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### PointLedgerQuery

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### TeamPointsSummary

```json
{
  "address": "string",
  "teamLeaderAddress": "string",
  "isTeamLeader": "bool",
  "downlineCount": "uint64",
  "cumulativeBasePoints": "string",
  "currentRewardRateBps": "uint64",
  "nextRewardThreshold": "string",
  "pointsToNextRewardTier": "string",
  "teamOperationRewardTotal": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `teamLeaderAddress` | `string` | 当前推荐树没有上级的唯一根节点钱包地址 |
| `isTeamLeader` | `bool` | 当前查询钱包是否为所属推荐树的根节点 |
| `downlineCount` | `uint64` | 数量统计 |
| `cumulativeBasePoints` | `string` | 当前根团队全部下线累计基础积分；不含根节点本人 |
| `currentRewardRateBps` | `uint64` | 当前根团队后续交易使用的奖励 BPS；0/50/100/150/200/300 |
| `nextRewardThreshold` | `string` | 后端代码字段 nextRewardThreshold；含义由所属响应模型和接口业务决定 |
| `pointsToNextRewardTier` | `string` | 距下一团队奖励阶梯还需的下线基础积分 |
| `teamOperationRewardTotal` | `string` | 作为推荐树根节点团队长累计获得的 0%-3% 团队运营奖励 |

### TeamMemberItem

```json
{
  "address": "string",
  "directReferrerAddress": "string",
  "depth": "uint64",
  "joinedAt": "string",
  "personalEffectivePoints": "string",
  "personalLevel": "string",
  "basePoints": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `directReferrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `depth` | `uint64` | 后端代码字段 depth；含义由所属响应模型和接口业务决定 |
| `joinedAt` | `string` | 该推荐关系对当前查询人/根团队开始生效的时间；RFC3339，补绑时取真实绑定时间，不是账户注册时间 |
| `personalEffectivePoints` | `string` | 个人累计有效积分；包含基础积分、10% 直属邀请奖励和团队运营奖励 |
| `personalLevel` | `string` | 个人积分等级严格枚举：L0/L1/L2/L3/L4/L5；达到后永久保留 |
| `basePoints` | `string` | 后端代码字段 basePoints；含义由所属响应模型和接口业务决定 |

### TeamMemberQuery

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### PointLevelHistoryItem

```json
{
  "id": "uint64",
  "address": "string",
  "oldLevel": "string",
  "newLevel": "string",
  "effectivePoints": "string",
  "sourceTxHash": "string",
  "sourceLogIndex": "uint64",
  "achievedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `oldLevel` | `string` | 后端代码字段 oldLevel；含义由所属响应模型和接口业务决定 |
| `newLevel` | `string` | 后端代码字段 newLevel；含义由所属响应模型和接口业务决定 |
| `effectivePoints` | `string` | 后端代码字段 effectivePoints；含义由所属响应模型和接口业务决定 |
| `sourceTxHash` | `string` | 后端代码字段 sourceTxHash；含义由所属响应模型和接口业务决定 |
| `sourceLogIndex` | `uint64` | 链上日志序号；与交易哈希、受益人和流水类型共同组成幂等键 |
| `achievedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### TeamRewardRateHistoryItem

```json
{
  "id": "uint64",
  "rootAddress": "string",
  "oldRateBps": "uint64",
  "newRateBps": "uint64",
  "cumulativeBasePoints": "string",
  "sourceTxHash": "string",
  "sourceLogIndex": "uint64",
  "effectiveAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `rootAddress` | `string` | 合约或钱包的 EVM 地址 |
| `oldRateBps` | `uint64` | 万分比整数，10000 表示 100% |
| `newRateBps` | `uint64` | 万分比整数，10000 表示 100% |
| `cumulativeBasePoints` | `string` | 当前根团队全部下线累计基础积分；不含根节点本人 |
| `sourceTxHash` | `string` | 后端代码字段 sourceTxHash；含义由所属响应模型和接口业务决定 |
| `sourceLogIndex` | `uint64` | 链上日志序号；与交易哈希、受益人和流水类型共同组成幂等键 |
| `effectiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminPointsOverview

```json
{
  "totalEffectivePoints": "string",
  "totalBasePoints": "string",
  "totalDirectReferralRewards": "string",
  "totalTeamOperationRewards": "string",
  "pointAccountCount": "uint64",
  "teamCount": "uint64",
  "personalLevelDistribution": "map[string]int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `totalEffectivePoints` | `string` | 后端代码字段 totalEffectivePoints；含义由所属响应模型和接口业务决定 |
| `totalBasePoints` | `string` | 后端代码字段 totalBasePoints；含义由所属响应模型和接口业务决定 |
| `totalDirectReferralRewards` | `string` | 后端代码字段 totalDirectReferralRewards；含义由所属响应模型和接口业务决定 |
| `totalTeamOperationRewards` | `string` | 后端代码字段 totalTeamOperationRewards；含义由所属响应模型和接口业务决定 |
| `pointAccountCount` | `uint64` | 数量统计 |
| `teamCount` | `uint64` | 数量统计 |
| `personalLevelDistribution` | `map[string]int` | 按 L0-L5 分组的积分账户数量 |

### AdminPointUserItem

```json
{
  "address": "string",
  "inviteCode": "string",
  "referrerAddress": "string",
  "teamLeaderAddress": "string",
  "personalEffectivePoints": "string",
  "personalLevel": "string",
  "basePointsTotal": "string",
  "directReferralRewardTotal": "string",
  "teamOperationRewardTotal": "string",
  "updatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `inviteCode` | `string` | 后端代码字段 inviteCode；含义由所属响应模型和接口业务决定 |
| `referrerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `teamLeaderAddress` | `string` | 当前推荐树没有上级的唯一根节点钱包地址 |
| `personalEffectivePoints` | `string` | 个人累计有效积分；包含基础积分、10% 直属邀请奖励和团队运营奖励 |
| `personalLevel` | `string` | 个人积分等级严格枚举：L0/L1/L2/L3/L4/L5；达到后永久保留 |
| `basePointsTotal` | `string` | 本人 Buy 和 PledgeCreated 的真实净入曲线金额乘项目倍率后累计的基础积分 |
| `directReferralRewardTotal` | `string` | 作为实际直属邀请人累计获得的 10% 邀请奖励 |
| `teamOperationRewardTotal` | `string` | 作为推荐树根节点团队长累计获得的 0%-3% 团队运营奖励 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminPointTeamItem

```json
{
  "rootAddress": "string",
  "downlineCount": "uint64",
  "cumulativeBasePoints": "string",
  "rewardRateBps": "uint64",
  "nextRewardThreshold": "string",
  "teamOperationRewardTotal": "string",
  "updatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `rootAddress` | `string` | 合约或钱包的 EVM 地址 |
| `downlineCount` | `uint64` | 数量统计 |
| `cumulativeBasePoints` | `string` | 当前根团队全部下线累计基础积分；不含根节点本人 |
| `rewardRateBps` | `uint64` | 该奖励流水使用的 BPS 快照；直属邀请固定 1000，团队奖励为交易前有效阶梯 |
| `nextRewardThreshold` | `string` | 后端代码字段 nextRewardThreshold；含义由所属响应模型和接口业务决定 |
| `teamOperationRewardTotal` | `string` | 作为推荐树根节点团队长累计获得的 0%-3% 团队运营奖励 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminProjectPointMultiplierQuery

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### AdminProjectPointMultiplierItem

```json
{
  "projectAddress": "string",
  "tokenName": "string",
  "tokenSymbol": "string",
  "state": "string",
  "scaleTier": "string",
  "effectiveMultiplier": "string",
  "effectiveSource": "string",
  "projectMultiplier": "string",
  "scaleTierMultiplier": "string",
  "defaultMultiplier": "string",
  "hasProjectOverride": "bool",
  "canModify": "bool",
  "currentVersion": "int",
  "effectiveAt": "string",
  "updatedBy": "string",
  "scheduledMultiplier": "string",
  "scheduledVersion": "int",
  "scheduledEffectiveAt": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `state` | `string` | 后端代码字段 state；含义由所属响应模型和接口业务决定 |
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `effectiveMultiplier` | `string` | 当前时间真正用于该项目新 Buy/PledgeCreated 积分的倍率 |
| `effectiveSource` | `string` | 当前倍率来源枚举：project/scale_tier/default，优先级依次降低 |
| `projectMultiplier` | `string` | 当前已生效的项目级覆盖倍率；未配置时为空字符串 |
| `scaleTierMultiplier` | `string` | 项目所属 Scale Tier 当前倍率；项目无覆盖时继承该值 |
| `defaultMultiplier` | `string` | 全局兜底积分倍率；当前固定为 1 |
| `hasProjectOverride` | `bool` | 是否存在已生效的项目级倍率覆盖 |
| `canModify` | `bool` | 当前项目是否允许新增倍率版本；当前所有项目和状态均为 true |
| `currentVersion` | `int` | 后端代码字段 currentVersion；含义由所属响应模型和接口业务决定 |
| `effectiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedBy` | `string` | 后端代码字段 updatedBy；含义由所属响应模型和接口业务决定 |
| `scheduledMultiplier` | `string` | 最早一个尚未生效的项目级倍率；无计划时为空 |
| `scheduledVersion` | `int` | 后端代码字段 scheduledVersion；含义由所属响应模型和接口业务决定 |
| `scheduledEffectiveAt` | `string` | 计划项目倍率的 RFC3339 生效时间 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminProjectPointMultiplierListResponse

```json
{
  "items": "[]AdminProjectPointMultiplierItem",
  "total": "uint64",
  "limit": "int",
  "offset": "int",
  "defaultMultiplier": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `items` | `[]AdminProjectPointMultiplierItem` | 当前页数据列表；空结果返回 [] |
| `total` | `uint64` | 符合条件的总记录数 |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |
| `defaultMultiplier` | `string` | 全局兜底积分倍率；当前固定为 1 |

### UpsertProjectPointMultiplierRequest

```json
{
  "multiplier": "string",
  "effectiveAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `multiplier` | `string` | 后端代码字段 multiplier；含义由所属响应模型和接口业务决定 |
| `effectiveAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### AdminProjectPointMultiplierActionResponse

```json
{
  "executed": "bool",
  "config": "*PlatformConfigItem",
  "project": "AdminProjectPointMultiplierItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `executed` | `bool` | 后端代码字段 executed；含义由所属响应模型和接口业务决定 |
| `config` | `*PlatformConfigItem` | 后端代码字段 config；含义由所属响应模型和接口业务决定 |
| `project` | `AdminProjectPointMultiplierItem` | 后端代码字段 project；含义由所属响应模型和接口业务决定 |

### PointReconciliationResponse

```json
{
  "ledgerEffectivePoints": "string",
  "accountEffectivePoints": "string",
  "ledgerBasePoints": "string",
  "accountBasePoints": "string",
  "teamLedgerBasePoints": "string",
  "teamProfileBasePoints": "string",
  "accountMismatchCount": "uint64",
  "teamMismatchCount": "uint64",
  "consistent": "bool",
  "checkedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `ledgerEffectivePoints` | `string` | 全部积分流水 points 合计 |
| `accountEffectivePoints` | `string` | 全部个人积分账户 effectivePoints 合计；必须等于 ledgerEffectivePoints |
| `ledgerBasePoints` | `string` | 流水中 base_buy/base_pledge 基础积分合计 |
| `accountBasePoints` | `string` | 个人积分账户 basePoints 合计；必须等于 ledgerBasePoints |
| `teamLedgerBasePoints` | `string` | 按当前推荐闭包重算的全部根团队下线基础积分合计 |
| `teamProfileBasePoints` | `string` | 当前激活根团队 cumulativeBasePoints 合计；必须等于 teamLedgerBasePoints |
| `accountMismatchCount` | `uint64` | 逐钱包比较有效/基础/直属/团队四类账户投影、流水和 L0-L5 后的不一致钱包数 |
| `teamMismatchCount` | `uint64` | 逐根团队比较下线人数、基础积分、奖励档位和下一门槛后的不一致团队数 |
| `consistent` | `bool` | 后端代码字段 consistent；含义由所属响应模型和接口业务决定 |
| `checkedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### ProjectPreviewRequest

```json
{
  "tokenName": "string",
  "tokenSymbol": "string",
  "logoUrl": "string",
  "website": "string",
  "twitter": "string",
  "telegram": "string",
  "description": "string",
  "scaleTier": "string",
  "nodeCount": "int64",
  "nodePrice": "string",
  "maxNodesPerAddress": "int64",
  "minSingleBuy": "string",
  "altTokenAddress": "string",
  "altTokenPerNode": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `logoUrl` | `string` | 项目 Logo 资源 URL |
| `website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `nodeCount` | `int64` | 数量统计 |
| `nodePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `maxNodesPerAddress` | `int64` | 建项时固化的单地址节点认购数量上限；必须在 1..nodeCount 之间，后端会结合 nodeSoldBy(wallet) 计算剩余可认购数 |
| `minSingleBuy` | `string` | 后端代码字段 minSingleBuy；含义由所属响应模型和接口业务决定 |
| `altTokenAddress` | `string` | 可选 Alt Token 合约地址；未启用时传空字符串或零地址，后端统一编码为零地址 |
| `altTokenPerNode` | `string` | 每节点托管的 Alt Token 数量；未启用时可传空字符串、0 或 0.0，启用时必须是按该 Token 实际 decimals 解析后的正数 |

### ScaleTierSnapshot

```json
{
  "scaleTier": "string",
  "tierName": "string",
  "version": "uint64",
  "curveTableAddress": "string",
  "graduationAdapter": "string",
  "fOpen": "string",
  "minNodeCount": "string",
  "defaultNodeCount": "string",
  "maxNodeCount": "string",
  "minNodePrice": "string",
  "defaultNodePrice": "string",
  "maxNodePrice": "string",
  "nodeSaleDuration": "uint64",
  "nodeVestingCliffDuration": "uint64",
  "nodeVestingTotalDuration": "uint64",
  "governanceVotingPeriod": "uint64",
  "governanceTimelock": "uint64",
  "graduationMinTradingDuration": "uint64",
  "graduationNotice": "uint64",
  "graduationVolumeWindow": "uint64",
  "curveParamsHash": "string",
  "curvePointsHash": "string",
  "curveCodeHash": "string",
  "curveRuntimeCodeHash": "string",
  "curveFactoryCreated": "bool",
  "curveRegistered": "bool",
  "curveProductionTable": "bool",
  "curveFinalized": "bool",
  "curveModel": "string",
  "curvePointCount": "uint64",
  "curveX1": "string",
  "curveX2": "string",
  "curveReferenceSupply": "string",
  "curveReferencePrice": "string",
  "buyFeeRatePpm": "uint64",
  "sellFeeRatePpm": "uint64",
  "profitTaxRatePpm": "uint64",
  "feeFloorBps": "uint16",
  "feeNodeBps": "uint16",
  "feeIssuerBps": "uint16",
  "feePlatformBps": "uint16",
  "taxFloorBps": "uint16",
  "taxNodeBps": "uint16",
  "taxIssuerBps": "uint16",
  "taxPlatformBps": "uint16",
  "economicRatesMutable": "bool",
  "floorShareMutable": "bool",
  "platformShareMutable": "bool",
  "allowedGovernanceParamIds": "[]int",
  "dependencyIntegrityVerified": "bool",
  "integritySnapshotBlock": "uint64",
  "enabled": "bool"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `tierName` | `string` | 后端展示用档位名称；不编码进 setScaleTier calldata |
| `version` | `uint64` | 链上 scaleTierVersion(scaleTier) 的 uint64 值；每次 setScaleTier 成功都递增，包括相同配置重写或禁用 |
| `curveTableAddress` | `string` | 必须是固定 CurveTableRegistry 工厂创建并已注册的 CurveTable 地址 |
| `graduationAdapter` | `string` | 毕业 Adapter 地址；enabled=true 时必须等于 Platform.trustedGraduationAdapter() |
| `fOpen` | `string` | 创世地板价的可读 USDGTest 6 位小数字符串；必须等于 CurveTable.rawPriceAtSupply(0) |
| `minNodeCount` | `string` | 该档位最小节点数；enabled=true 时不得小于 100 |
| `defaultNodeCount` | `string` | 默认节点数；必须位于 minNodeCount 和 maxNodeCount 之间 |
| `maxNodeCount` | `string` | 该档位最大节点数 |
| `minNodePrice` | `string` | 单节点最低 USDGTest 价格的可读 6 位小数字符串；enabled=true 时不得低于 100 |
| `defaultNodePrice` | `string` | 默认单节点 USDGTest 价格；必须位于 minNodePrice 和 maxNodePrice 之间 |
| `maxNodePrice` | `string` | 单节点最高 USDGTest 价格的可读 6 位小数字符串 |
| `nodeSaleDuration` | `uint64` | 节点认购期秒数；建项时从 Scale Tier 固化，不是 CreateProjectParams 字段 |
| `nodeVestingCliffDuration` | `uint64` | 节点 Token 开始线性释放前的 cliff 秒数；建项时从 Scale Tier 固化 |
| `nodeVestingTotalDuration` | `uint64` | 节点 Token 从锁定开始到全部释放的总秒数；必须大于 nodeVestingCliffDuration |
| `governanceVotingPeriod` | `uint64` | 治理投票期秒数；enabled=true 时必须大于 0 |
| `governanceTimelock` | `uint64` | 提案通过后执行 timelock 秒数；enabled=true 时必须大于 0 |
| `graduationMinTradingDuration` | `uint64` | 进入毕业条件前的最小交易时长秒数 |
| `graduationNotice` | `uint64` | 毕业公告期秒数；triggerGraduation 后须等待该时长才可 executeGraduation |
| `graduationVolumeWindow` | `uint64` | 毕业成交量滚动窗口秒数；后端只展示链上窗口语义 |
| `curveParamsHash` | `string` | 曲线配置参数的 bytes32 哈希；用于核对 Scale Tier 与 CurveTable 参数版本 |
| `curvePointsHash` | `string` | 曲线分段点数组的 bytes32 哈希；必须与 CurveTable.pointsHash() 一致 |
| `curveCodeHash` | `string` | 平台在启用 Scale Tier 时固化的 CurveTable runtime codehash |
| `curveRuntimeCodeHash` | `string` | 后端在 integritySnapshotBlock 对 CurveTable 实际 runtime bytecode 计算的 keccak256；必须与 curveCodeHash 一致 |
| `curveFactoryCreated` | `bool` | 是否由平台固定 CurveTableRegistry 工厂创建 |
| `curveRegistered` | `bool` | 当前 CurveTable、curveParamsHash、curvePointsHash 和 runtime codehash 是否通过 Registry.isRegistered 校验 |
| `curveProductionTable` | `bool` | 是否为 Registry 中已完成终态注册的生产曲线 |
| `curveFinalized` | `bool` | ChunkedCurveTable.finalized() 链上结果；必须为 true，finalize 后不可继续写入点表 |
| `curveModel` | `string` | 确定性曲线模型标识；当前为 seedx-derived-endpoint-normalized-tanh-v2 |
| `curvePointCount` | `uint64` | 链上分段点数；当前生产表必须为 513 |
| `curveX1` | `string` | 绝对 Supply flat 结束锚点，Token 可读数量；当前 30000000 |
| `curveX2` | `string` | 曲线 shoulder 锚点，Token 原始数量；当前 45470513165870116091000250 |
| `curveReferenceSupply` | `string` | 曲线参考 Supply S_ref；当前 300000000 Token |
| `curveReferencePrice` | `string` | S_ref 处的 P_ref 稳定币原始价格；当前 5000，即 0.005 USDGTest |
| `buyFeeRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `sellFeeRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `profitTaxRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `feeFloorBps` | `uint16` | 手续费地板分账 BPS；固定 6000（60%），不可修改 |
| `feeNodeBps` | `uint16` | 手续费节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `feeIssuerBps` | `uint16` | 手续费发射方分账 BPS；初始 1000（10%），可通过 paramId=5 在 3000 BPS 灵活份额内调整 |
| `feePlatformBps` | `uint16` | 手续费平台分账 BPS；固定 1000（10%），不可修改 |
| `taxFloorBps` | `uint16` | 盈利税地板分账 BPS；固定 6000（60%），不可修改 |
| `taxNodeBps` | `uint16` | 盈利税节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `taxIssuerBps` | `uint16` | 盈利税发射方分账 BPS；初始 1000（10%），可通过 paramId=8 在 3000 BPS 灵活份额内调整 |
| `taxPlatformBps` | `uint16` | 盈利税平台分账 BPS；固定 1000（10%），不可修改 |
| `economicRatesMutable` | `bool` | 后端代码字段 economicRatesMutable；含义由所属响应模型和接口业务决定 |
| `floorShareMutable` | `bool` | 后端代码字段 floorShareMutable；含义由所属响应模型和接口业务决定 |
| `platformShareMutable` | `bool` | 后端代码字段 platformShareMutable；含义由所属响应模型和接口业务决定 |
| `allowedGovernanceParamIds` | `[]int` | 当前合约允许的治理参数 ID 整数数组；20260821 协议固定为 [5,8,10,11,12]，5/8 调整发射方份额并联动节点份额，10/11 调整毕业 LP 分配，12 调整 ratchet 阈值 |
| `dependencyIntegrityVerified` | `bool` | 后端是否已在同一快照高度验证 Registry、Curve codehash、固定 Uniswap v4 Adapter 及官方依赖完整性 |
| `integritySnapshotBlock` | `uint64` | 依赖完整性验证使用的当前链头区块高度；历史档位版本仍由事件和同交易回执保存 |
| `enabled` | `bool` | 档位是否启用；true 时执行全部 Registry/codehash/Adapter/参数约束，false 时链上清除 curveCodeHashByTier |

### ProjectionSummary

```json
{
  "nodeCount": "string",
  "nodePrice": "string",
  "maxNodesPerAddress": "string",
  "nodeTargetStable": "string",
  "openingTotalSupply": "string",
  "shoulderMarketPrice": "string",
  "shoulderTotalSupply": "string",
  "shoulderAdditionalStable": "string",
  "referenceMarketPrice": "string",
  "referenceTotalSupply": "string",
  "referenceMarketCap": "string",
  "referenceAdditionalStable": "string",
  "referenceFloorPrice": "string",
  "referenceFloorPool": "string",
  "referenceMarketPool": "string",
  "referenceFloorToPriceRatio": "string",
  "referenceExternalStableWithPledge": "string",
  "nodeTokenUnit": "string",
  "nodeSupply": "string",
  "floorPrice": "string",
  "minSingleBuy": "string",
  "nodeSaleDuration": "uint64",
  "nodeVestingCliffDuration": "uint64",
  "nodeVestingTotalDuration": "uint64",
  "curveSnapshotBlock": "uint64",
  "curveTableVerified": "bool",
  "projectionModel": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `nodeCount` | `string` | Selected node count. |
| `nodePrice` | `string` | Stable amount paid per node. |
| `maxNodesPerAddress` | `string` | 建项时固化的单地址节点认购数量上限；必须在 1..nodeCount 之间，后端会结合 nodeSoldBy(wallet) 计算剩余可认购数 |
| `nodeTargetStable` | `string` | Total node-sale target: nodeCount * nodePrice. |
| `openingTotalSupply` | `string` | Project total supply immediately after a successful node-sale finalization. |
| `shoulderMarketPrice` | `string` | CurveTable price at the fixed absolute x2 shoulder anchor. |
| `shoulderTotalSupply` | `string` | Fixed x2 shoulder supply; current curve uses 45,470,513.165870116091000250 Token. |
| `shoulderAdditionalStable` | `string` | CurveTable buyCost from actual opening supply to x2; zero when opening supply is already beyond x2. |
| `referenceMarketPrice` | `string` | CurveTable raw market price at referenceTotalSupply, verified at curveSnapshotBlock. |
| `referenceTotalSupply` | `string` | Fixed absolute S_ref supply anchor (300,000,000 Token); never derived from openingTotalSupply. |
| `referenceMarketCap` | `string` | referenceMarketPrice * referenceTotalSupply using EVM integer rounding. |
| `referenceAdditionalStable` | `string` | CurveTable buyCost from opening supply to referenceTotalSupply; fixed transaction fees are excluded. |
| `referenceFloorPrice` | `string` | Opening floor used by the principal-only projection. |
| `referenceFloorPool` | `string` | Projected floor reserve at the reference supply before fee-floor additions. |
| `referenceMarketPool` | `string` | Projected reserve cash minus referenceFloorPool. |
| `referenceFloorToPriceRatio` | `string` | referenceFloorPrice / referenceMarketPrice as a decimal string. |
| `referenceExternalStableWithPledge` | `string` | Minimum external curve principal under the 70% pledge cap and 20% self-pay floor; transaction fees are excluded. |
| `nodeTokenUnit` | `string` | 后端代码字段 nodeTokenUnit；含义由所属响应模型和接口业务决定 |
| `nodeSupply` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `minSingleBuy` | `string` | 后端代码字段 minSingleBuy；含义由所属响应模型和接口业务决定 |
| `nodeSaleDuration` | `uint64` | 节点认购期秒数；建项时从 Scale Tier 固化，不是 CreateProjectParams 字段 |
| `nodeVestingCliffDuration` | `uint64` | 节点 Token 开始线性释放前的 cliff 秒数；建项时从 Scale Tier 固化 |
| `nodeVestingTotalDuration` | `uint64` | 节点 Token 从锁定开始到全部释放的总秒数；必须大于 nodeVestingCliffDuration |
| `curveSnapshotBlock` | `uint64` | Block used for all CurveTable verification calls. |
| `curveTableVerified` | `bool` | True only after reference price, buy cost, and opening liability calls all succeed. |
| `projectionModel` | `string` | Projection semantics identifier; derived_endpoint_normalized_tanh_v2 uses x2/S_ref and excludes fixed transaction fees. |

### AltTokenConfigSnapshot

```json
{
  "enabled": "bool",
  "address": "string",
  "symbol": "string",
  "decimals": "uint8",
  "amountPerNode": "string",
  "amountPerNodeWei": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `enabled` | `bool` | 是否启用节点 Alt Token 托管 |
| `address` | `string` | 归一化 Alt Token 地址；未启用时为零地址 |
| `symbol` | `string` | 链上 symbol() 返回值；未启用时为空 |
| `decimals` | `uint8` | 链上 decimals() 返回值；编码 altTokenPerNode 时必须使用该精度 |
| `amountPerNode` | `string` | 按 Alt Token 实际 decimals 格式化的每节点数量 |
| `amountPerNodeWei` | `string` | 编码进 createProject 的原始 uint256 整数字符串 |

### NormalizedProjectConfig

```json
{
  "tokenName": "string",
  "tokenSymbol": "string",
  "scaleTier": "string",
  "nodeCount": "int64",
  "nodePrice": "string",
  "maxNodesPerAddress": "int64",
  "buyFeeRatePpm": "uint64",
  "sellFeeRatePpm": "uint64",
  "profitTaxRatePpm": "uint64",
  "minSingleBuy": "string",
  "feeFloorBps": "uint16",
  "feeNodeBps": "uint16",
  "feeIssuerBps": "uint16",
  "feePlatformBps": "uint16",
  "taxFloorBps": "uint16",
  "taxNodeBps": "uint16",
  "taxIssuerBps": "uint16",
  "taxPlatformBps": "uint16",
  "economicRatesMutable": "bool",
  "floorShareMutable": "bool",
  "platformShareMutable": "bool",
  "allowedGovernanceParamIds": "[]int",
  "altToken": "AltTokenConfigSnapshot"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `nodeCount` | `int64` | 数量统计 |
| `nodePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `maxNodesPerAddress` | `int64` | 建项时固化的单地址节点认购数量上限；必须在 1..nodeCount 之间，后端会结合 nodeSoldBy(wallet) 计算剩余可认购数 |
| `buyFeeRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `sellFeeRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `profitTaxRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `minSingleBuy` | `string` | 后端代码字段 minSingleBuy；含义由所属响应模型和接口业务决定 |
| `feeFloorBps` | `uint16` | 手续费地板分账 BPS；固定 6000（60%），不可修改 |
| `feeNodeBps` | `uint16` | 手续费节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `feeIssuerBps` | `uint16` | 手续费发射方分账 BPS；初始 1000（10%），可通过 paramId=5 在 3000 BPS 灵活份额内调整 |
| `feePlatformBps` | `uint16` | 手续费平台分账 BPS；固定 1000（10%），不可修改 |
| `taxFloorBps` | `uint16` | 盈利税地板分账 BPS；固定 6000（60%），不可修改 |
| `taxNodeBps` | `uint16` | 盈利税节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `taxIssuerBps` | `uint16` | 盈利税发射方分账 BPS；初始 1000（10%），可通过 paramId=8 在 3000 BPS 灵活份额内调整 |
| `taxPlatformBps` | `uint16` | 盈利税平台分账 BPS；固定 1000（10%），不可修改 |
| `economicRatesMutable` | `bool` | 后端代码字段 economicRatesMutable；含义由所属响应模型和接口业务决定 |
| `floorShareMutable` | `bool` | 后端代码字段 floorShareMutable；含义由所属响应模型和接口业务决定 |
| `platformShareMutable` | `bool` | 后端代码字段 platformShareMutable；含义由所属响应模型和接口业务决定 |
| `allowedGovernanceParamIds` | `[]int` | 当前合约允许的治理参数 ID 整数数组；20260821 协议固定为 [5,8,10,11,12]，5/8 调整发射方份额并联动节点份额，10/11 调整毕业 LP 分配，12 调整 ratchet 阈值 |
| `altToken` | `AltTokenConfigSnapshot` | 后端代码字段 altToken；含义由所属响应模型和接口业务决定 |

### ProjectPreviewResponse

```json
{
  "valid": "bool",
  "symbolAvailable": "bool",
  "warnings": "[]string",
  "normalizedConfig": "NormalizedProjectConfig",
  "scaleTierSnapshot": "ScaleTierSnapshot",
  "projection": "ProjectionSummary"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `valid` | `bool` | 后端代码字段 valid；含义由所属响应模型和接口业务决定 |
| `symbolAvailable` | `bool` | 布尔状态标记 |
| `warnings` | `[]string` | 后端代码字段 warnings；含义由所属响应模型和接口业务决定 |
| `normalizedConfig` | `NormalizedProjectConfig` | 后端完成默认值、大小写、decimals 和零地址归一化后的最终建项配置；前端确认页应以此为准 |
| `scaleTierSnapshot` | `ScaleTierSnapshot` | 后端代码字段 scaleTierSnapshot；含义由所属响应模型和接口业务决定 |
| `projection` | `ProjectionSummary` | 后端代码字段 projection；含义由所属响应模型和接口业务决定 |

### CreateTxParamsRequest

```json
{
  "tokenName": "string",
  "tokenSymbol": "string",
  "logoUrl": "string",
  "website": "string",
  "twitter": "string",
  "telegram": "string",
  "description": "string",
  "scaleTier": "string",
  "nodeCount": "int64",
  "nodePrice": "string",
  "maxNodesPerAddress": "int64",
  "minSingleBuy": "string",
  "altTokenAddress": "string",
  "altTokenPerNode": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `logoUrl` | `string` | 项目 Logo 资源 URL |
| `website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `nodeCount` | `int64` | 数量统计 |
| `nodePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `maxNodesPerAddress` | `int64` | 建项时固化的单地址节点认购数量上限；必须在 1..nodeCount 之间，后端会结合 nodeSoldBy(wallet) 计算剩余可认购数 |
| `minSingleBuy` | `string` | 后端代码字段 minSingleBuy；含义由所属响应模型和接口业务决定 |
| `altTokenAddress` | `string` | 可选 Alt Token 合约地址；未启用时传空字符串或零地址，后端统一编码为零地址 |
| `altTokenPerNode` | `string` | 每节点托管的 Alt Token 数量；未启用时可传空字符串、0 或 0.0，启用时必须是按该 Token 实际 decimals 解析后的正数 |

### CreateTxParamsResponse

```json
{
  "target": "string",
  "method": "string",
  "value": "string",
  "chainId": "int64",
  "calldata": "string",
  "args": "map[string]string",
  "maxCreationFee": "string",
  "maxCreationFeeWei": "string",
  "expectedTierVersion": "uint64",
  "deadline": "uint64",
  "normalizedConfig": "NormalizedProjectConfig",
  "scaleTierSnapshot": "ScaleTierSnapshot",
  "requiredApprovals": "[]RequiredApproval",
  "balanceChecks": "[]BalanceCheck",
  "warnings": "[]string",
  "snapshotBlock": "uint64",
  "preflight": "TxPreflight"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `target` | `string` | 交易目标合约地址 |
| `method` | `string` | 后端代码字段 method；含义由所属响应模型和接口业务决定 |
| `value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `args` | `map[string]string` | 后端代码字段 args；含义由所属响应模型和接口业务决定 |
| `maxCreationFee` | `string` | 用户本次签名接受的最高建项费，由后端从快照区块的 creationFee() 生成，单位为可读 Stable |
| `maxCreationFeeWei` | `string` | 编码进 createProject.maxCreationFee 的原始 uint256 整数字符串 |
| `expectedTierVersion` | `uint64` | 编码进 createProject.expectedTierVersion 的链上 Scale Tier uint64 版本；发送前版本变化会以 ScaleTierChanged 回滚 |
| `deadline` | `uint64` | Unix-second deadline; the validity window and enforcement rule are defined by the route notes. |
| `normalizedConfig` | `NormalizedProjectConfig` | 后端完成默认值、大小写、decimals 和零地址归一化后的最终建项配置；前端确认页应以此为准 |
| `scaleTierSnapshot` | `ScaleTierSnapshot` | 后端代码字段 scaleTierSnapshot；含义由所属响应模型和接口业务决定 |
| `requiredApprovals` | `[]RequiredApproval` | 后端代码字段 requiredApprovals；含义由所属响应模型和接口业务决定 |
| `balanceChecks` | `[]BalanceCheck` | 后端代码字段 balanceChecks；含义由所属响应模型和接口业务决定 |
| `warnings` | `[]string` | 后端代码字段 warnings；含义由所属响应模型和接口业务决定 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `preflight` | `TxPreflight` | 后端代码字段 preflight；含义由所属响应模型和接口业务决定 |

### ProjectActionTxParamsRequest

```json
{
  "action": "string",
  "nodeAmount": "int64",
  "cashIn": "string",
  "tokenIn": "string",
  "pledgeToken": "string",
  "buyToken": "string",
  "maxUserPay": "string",
  "tokenAmount": "string",
  "minTokenOut": "string",
  "minCashOut": "string",
  "proposalId": "FlexibleUint64",
  "support": "bool",
  "paramId": "uint8",
  "newValue": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `action` | `string` | 后端代码字段 action；含义由所属响应模型和接口业务决定 |
| `nodeAmount` | `int64` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `cashIn` | `string` | 买入时输入的稳定币金额 |
| `tokenIn` | `string` | 卖出时输入的项目 Token 数量 |
| `pledgeToken` | `string` | 用于质押的项目 Token 数量；字段名必须是 pledgeToken |
| `buyToken` | `string` | 质押买时目标买入的项目 Token 数量 |
| `maxUserPay` | `string` | 质押买入可接受的最大用户自付稳定币金额；必须大于 0 且不小于当前报价 userPayStable，建议加上滑点容差 |
| `tokenAmount` | `string` | 项目 Token 数量 |
| `minTokenOut` | `string` | 买入时可接受的最小 Token 输出，必填且必须大于 0；建议使用报价 tokenOut 扣除滑点后的值 |
| `minCashOut` | `string` | 卖出时可接受的最小稳定币到账；后端按稳定币 6 位精度向下取整，保证滑点下限不被抬高 |
| `proposalId` | `FlexibleUint64` | 后端代码字段 proposalId；含义由所属响应模型和接口业务决定 |
| `support` | `bool` | 后端代码字段 support；含义由所属响应模型和接口业务决定 |
| `paramId` | `uint8` | 后端代码字段 paramId；含义由所属响应模型和接口业务决定 |
| `newValue` | `string` | 治理提案新值的非负整数字符串；20260821 仅允许 paramId 5/8/10/11/12，后端同步校验 hard cap 和单次 20% 调整幅度 |

### ProjectActionTxParamsResponse

```json
{
  "target": "string",
  "method": "string",
  "value": "string",
  "chainId": "int64",
  "calldata": "string",
  "args": "map[string]string",
  "requiredApprovals": "[]RequiredApproval",
  "balanceChecks": "[]BalanceCheck",
  "warnings": "[]string",
  "snapshotBlock": "uint64",
  "preflight": "TxPreflight",
  "collectPreview": "*LPFeeCollectPreview"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `target` | `string` | 交易目标合约地址 |
| `method` | `string` | 后端代码字段 method；含义由所属响应模型和接口业务决定 |
| `value` | `string` | 后端代码字段 value；含义由所属响应模型和接口业务决定 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `calldata` | `string` | 钱包发送交易时使用的 ABI 编码调用数据 |
| `args` | `map[string]string` | 后端代码字段 args；含义由所属响应模型和接口业务决定 |
| `requiredApprovals` | `[]RequiredApproval` | 后端代码字段 requiredApprovals；含义由所属响应模型和接口业务决定 |
| `balanceChecks` | `[]BalanceCheck` | 后端代码字段 balanceChecks；含义由所属响应模型和接口业务决定 |
| `warnings` | `[]string` | 后端代码字段 warnings；含义由所属响应模型和接口业务决定 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `preflight` | `TxPreflight` | 后端代码字段 preflight；含义由所属响应模型和接口业务决定 |
| `collectPreview` | `*LPFeeCollectPreview` | 外部流动性池手续费的链上收取模拟快照；用于决定是否允许发起 collect_and_distribute |

### LPFeeCollectPreview

```json
{
  "canCollect": "bool",
  "stableFees": "string",
  "stableFeesWei": "string",
  "tokenFees": "string",
  "tokenFeesWei": "string",
  "stableTokenAddress": "string",
  "stableTokenSymbol": "string",
  "stableTokenDecimals": "uint8",
  "projectTokenAddress": "string",
  "projectTokenSymbol": "string",
  "projectTokenDecimals": "uint8",
  "snapshotBlock": "uint64",
  "dataSource": "string",
  "unavailableReason": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `canCollect` | `bool` | 当 stableFeesWei 或 tokenFeesWei 大于 0 时才为 true；false 时前端必须禁用收取分配按钮 |
| `stableFees` | `string` | 当前可从 Uniswap v4 外部池收取的稳定币可读数量 |
| `stableFeesWei` | `string` | 当前可收取稳定币的 uint256 原始整数字符串 |
| `tokenFees` | `string` | 当前可从 Uniswap v4 外部池收取的项目 Token 可读数量 |
| `tokenFeesWei` | `string` | 当前可收取项目 Token 的 uint256 原始整数字符串 |
| `stableTokenAddress` | `string` | 当前网络配置的稳定币合约地址 |
| `stableTokenSymbol` | `string` | 后端代码字段 stableTokenSymbol；含义由所属响应模型和接口业务决定 |
| `stableTokenDecimals` | `uint8` | Stable Token 的真实精度；Ethereum Sepolia 为 USDGTest 6 位，项目 Token 仍为 18 位 |
| `projectTokenAddress` | `string` | 合约或钱包的 EVM 地址 |
| `projectTokenSymbol` | `string` | 后端代码字段 projectTokenSymbol；含义由所属响应模型和接口业务决定 |
| `projectTokenDecimals` | `uint8` | 后端代码字段 projectTokenDecimals；含义由所属响应模型和接口业务决定 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `unavailableReason` | `string` | 不能发起收取分配的原因；前端用于禁用按钮时给出提示 |

### RequiredApproval

```json
{
  "tokenAddress": "string",
  "tokenSymbol": "string",
  "tokenDecimals": "uint8",
  "spender": "string",
  "amount": "string",
  "amountWei": "string",
  "currentAllowanceWei": "string",
  "approvalRequired": "bool",
  "reason": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `tokenAddress` | `string` | 合约或钱包的 EVM 地址 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `tokenDecimals` | `uint8` | 后端代码字段 tokenDecimals；含义由所属响应模型和接口业务决定 |
| `spender` | `string` | 必须与同一 tx-params 响应的 target 完全一致；后端会强制校验，前端发起 approve 前仍必须本地比对并拒绝不一致响应 |
| `amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `amountWei` | `string` | 后端代码字段 amountWei；含义由所属响应模型和接口业务决定 |
| `currentAllowanceWei` | `string` | 后端代码字段 currentAllowanceWei；含义由所属响应模型和接口业务决定 |
| `approvalRequired` | `bool` | 布尔状态标记 |
| `reason` | `string` | 本次操作原因，用于审计追溯 |

### BalanceCheck

```json
{
  "tokenAddress": "string",
  "tokenSymbol": "string",
  "tokenDecimals": "uint8",
  "owner": "string",
  "amount": "string",
  "amountWei": "string",
  "currentBalanceWei": "string",
  "sufficient": "bool",
  "shortfallWei": "string",
  "reason": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `tokenAddress` | `string` | 合约或钱包的 EVM 地址 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `tokenDecimals` | `uint8` | 后端代码字段 tokenDecimals；含义由所属响应模型和接口业务决定 |
| `owner` | `string` | 后端代码字段 owner；含义由所属响应模型和接口业务决定 |
| `amount` | `string` | 后端代码字段 amount；含义由所属响应模型和接口业务决定 |
| `amountWei` | `string` | 后端代码字段 amountWei；含义由所属响应模型和接口业务决定 |
| `currentBalanceWei` | `string` | 后端代码字段 currentBalanceWei；含义由所属响应模型和接口业务决定 |
| `sufficient` | `bool` | 后端代码字段 sufficient；含义由所属响应模型和接口业务决定 |
| `shortfallWei` | `string` | 后端代码字段 shortfallWei；含义由所属响应模型和接口业务决定 |
| `reason` | `string` | 本次操作原因，用于审计追溯 |

### TxPreflight

```json
{
  "readyToSubmit": "bool",
  "walletType": "string",
  "approvalsComplete": "bool",
  "simulationStatus": "string",
  "reasonCode": "string",
  "reason": "string",
  "nextAction": "string",
  "estimatedGas": "string",
  "nativeBalanceWei": "string",
  "estimatedGasCostWei": "string",
  "nativeBalanceSufficient": "bool"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `readyToSubmit` | `bool` | 是否可以立即把本响应的 target/value/calldata 发送到钱包；只有 true 才允许发送 |
| `walletType` | `string` | 钱包类型枚举：unknown/eoa/contract；受 onlyEOA 限制的操作只能使用 eoa |
| `approvalsComplete` | `bool` | 本次交易所需的全部 ERC-20 allowance 是否已经满足 |
| `simulationStatus` | `string` | 预检阶段枚举：not_run/contract_wallet_not_allowed/insufficient_balance/blocked_by_approval/simulation_failed/gas_estimation_failed/insufficient_native_balance/passed |
| `reasonCode` | `string` | 稳定阻断原因枚举；前端按该字段分支，不要从 reason 文案反推状态 |
| `reason` | `string` | 当前预检结果的可展示英文原因；可直接兜底展示 |
| `nextAction` | `string` | 建议下一步枚举：retry/switch_to_eoa_wallet/top_up_token_balance/submit_required_approval/refresh_and_retry/top_up_native_balance/submit_transaction |
| `estimatedGas` | `string` | 模拟通过后的预计 gas limit；未完成估算时为 0 |
| `nativeBalanceWei` | `string` | 当前钱包原生 Gas 代币余额（Sepolia 为 ETH），Wei 整数字符串 |
| `estimatedGasCostWei` | `string` | estimatedGas 乘以当前 gasPrice 的预计原生 Gas 代币成本，Wei 整数字符串 |
| `nativeBalanceSufficient` | `bool` | 当前原生 Gas 代币是否足以覆盖预计 gas 成本 |

### ProjectListItem

```json
{
  "projectAddress": "string",
  "tokenName": "string",
  "tokenSymbol": "string",
  "issuerAddress": "string",
  "scaleTier": "string",
  "state": "string",
  "currentPrice": "string",
  "marketPrice": "string",
  "marketPriceSource": "string",
  "floorPrice": "string",
  "currentSupply": "string",
  "lockedSupply": "string",
  "nodeFundraiseStable": "string",
  "circulatingMarketCap": "string",
  "lockedMarketCap": "string",
  "totalMarketCap": "string",
  "marketCap": "string",
  "circulatingMarketCapPct": "string",
  "lockedMarketCapPct": "string",
  "fdv": "string",
  "nodeSold": "int64",
  "nodeCount": "int64",
  "nodeProgressPct": "string",
  "createdAt": "string",
  "updatedAt": "string",
  "curveNodeCount": "int",
  "curveCurrentSupply": "string",
  "curveCurrentPrice": "string",
  "logoUrl": "string",
  "website": "string",
  "twitter": "string",
  "telegram": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `issuerAddress` | `string` | 项目创建者钱包地址 |
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `state` | `string` | 后端代码字段 state；含义由所属响应模型和接口业务决定 |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `marketPrice` | `string` | 用于市值计算的统一市场价；Trading/Graduating 取曲线价，Graduated 优先取官方 Uniswap v4 StateView slot0 现价 |
| `marketPriceSource` | `string` | 市场价来源枚举：bonding_curve/uniswap_v4_slot0/graduation_price_fallback/unavailable |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `currentSupply` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `lockedSupply` | `string` | 锁定供应量，固定等于 supplyNode + supplyPledged |
| `nodeFundraiseStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `circulatingMarketCap` | `string` | 流通市值 = marketPrice * supplyUserTotal |
| `lockedMarketCap` | `string` | 锁定市值 = marketPrice * (supplyNode + supplyPledged) |
| `totalMarketCap` | `string` | 总市值 = 流通市值 + 锁定市值 = marketPrice * supplyTotal |
| `marketCap` | `string` | 总市值兼容别名，数值等于 totalMarketCap |
| `circulatingMarketCapPct` | `string` | 流通市值占比，等于 supplyUserTotal / supplyTotal * 100；单位 % |
| `lockedMarketCapPct` | `string` | 锁定市值占比，等于 (supplyNode + supplyPledged) / supplyTotal * 100；单位 % |
| `fdv` | `string` | 已废弃语义的兼容字段；当前数值等于 totalMarketCap，合约无固定 maxSupply，不应解释为传统 FDV |
| `nodeSold` | `int64` | 后端代码字段 nodeSold；含义由所属响应模型和接口业务决定 |
| `nodeCount` | `int64` | 数量统计 |
| `nodeProgressPct` | `string` | 百分比数值 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `curveNodeCount` | `int` | 数量统计 |
| `curveCurrentSupply` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `curveCurrentPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `logoUrl` | `string` | 项目 Logo 资源 URL |
| `website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |

### ProjectListResponse

```json
{
  "items": "[]ProjectListItem",
  "total": "uint64",
  "limit": "int",
  "offset": "int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `items` | `[]ProjectListItem` | 当前页数据列表；空结果返回 [] |
| `total` | `uint64` | 符合条件的总记录数 |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |

### ProjectDetail

```json
{
  "projectAddress": "string",
  "tokenName": "string",
  "tokenSymbol": "string",
  "issuerAddress": "string",
  "scaleTier": "string",
  "state": "string",
  "description": "string",
  "platformPayoutAddr": "string",
  "altTokenAddress": "string",
  "altTokenPerNode": "string",
  "altTokenDecimals": "uint8",
  "nodeCount": "int64",
  "nodeSold": "int64",
  "nodePriceStable": "string",
  "maxNodesPerAddress": "int64",
  "buyFeeRate": "string",
  "sellFeeRate": "string",
  "profitTaxRate": "string",
  "minSingleBuyStable": "string",
  "createdAt": "string",
  "logoUrl": "string",
  "website": "string",
  "twitter": "string",
  "telegram": "string",
  "metrics": "ProjectMetrics",
  "onChainState": "OnChainProjectState",
  "userPosition": "*UserPosition",
  "scaleTierSnapshot": "ScaleTierSnapshot"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `issuerAddress` | `string` | 项目创建者钱包地址 |
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `state` | `string` | 后端代码字段 state；含义由所属响应模型和接口业务决定 |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |
| `platformPayoutAddr` | `string` | 后端代码字段 platformPayoutAddr；含义由所属响应模型和接口业务决定 |
| `altTokenAddress` | `string` | 可选 Alt Token 合约地址；未启用时传空字符串或零地址，后端统一编码为零地址 |
| `altTokenPerNode` | `string` | 每节点托管的 Alt Token 数量；未启用时可传空字符串、0 或 0.0，启用时必须是按该 Token 实际 decimals 解析后的正数 |
| `altTokenDecimals` | `uint8` | 后端代码字段 altTokenDecimals；含义由所属响应模型和接口业务决定 |
| `nodeCount` | `int64` | 数量统计 |
| `nodeSold` | `int64` | 后端代码字段 nodeSold；含义由所属响应模型和接口业务决定 |
| `nodePriceStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `maxNodesPerAddress` | `int64` | 建项时固化的单地址节点认购数量上限；必须在 1..nodeCount 之间，后端会结合 nodeSoldBy(wallet) 计算剩余可认购数 |
| `buyFeeRate` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `sellFeeRate` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `profitTaxRate` | `string` | 后端代码字段 profitTaxRate；含义由所属响应模型和接口业务决定 |
| `minSingleBuyStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `logoUrl` | `string` | 项目 Logo 资源 URL |
| `website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `metrics` | `ProjectMetrics` | 后端代码字段 metrics；含义由所属响应模型和接口业务决定 |
| `onChainState` | `OnChainProjectState` | 后端代码字段 onChainState；含义由所属响应模型和接口业务决定 |
| `userPosition` | `*UserPosition` | 后端代码字段 userPosition；含义由所属响应模型和接口业务决定 |
| `scaleTierSnapshot` | `ScaleTierSnapshot` | 后端代码字段 scaleTierSnapshot；含义由所属响应模型和接口业务决定 |

### ProjectMetrics

```json
{
  "projectAddress": "string",
  "currentPrice": "string",
  "marketPrice": "string",
  "marketPriceSource": "string",
  "marketPriceLive": "bool",
  "marketPriceSnapshotBlock": "uint64",
  "floorPrice": "string",
  "supplyTotal": "string",
  "supplyUserTotal": "string",
  "supplyNode": "string",
  "supplyPledged": "string",
  "lockedSupply": "string",
  "floorPool": "string",
  "marketPool": "string",
  "reserveCash": "string",
  "circulatingMarketCap": "string",
  "lockedMarketCap": "string",
  "totalMarketCap": "string",
  "marketCap": "string",
  "circulatingMarketCapPct": "string",
  "lockedMarketCapPct": "string",
  "fdv": "string",
  "nodeSold": "int64",
  "nodeCount": "int64",
  "nodeProgressPct": "string",
  "availableReserve": "string",
  "redeemableValue": "string",
  "floorCoverageRatio": "string",
  "maxSafeFloor": "string",
  "isFloorSolvent": "bool",
  "lastUpdatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `marketPrice` | `string` | 用于市值计算的统一市场价；Trading/Graduating 取曲线价，Graduated 优先取官方 Uniswap v4 StateView slot0 现价 |
| `marketPriceSource` | `string` | 市场价来源枚举：bonding_curve/uniswap_v4_slot0/graduation_price_fallback/unavailable |
| `marketPriceLive` | `bool` | 市场价是否为当前快照的可用实时价；fallback 或 unavailable 时为 false |
| `marketPriceSnapshotBlock` | `uint64` | 读取价格和供应量的同一链上区块高度 |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `supplyTotal` | `string` | 后端代码字段 supplyTotal；含义由所属响应模型和接口业务决定 |
| `supplyUserTotal` | `string` | 后端代码字段 supplyUserTotal；含义由所属响应模型和接口业务决定 |
| `supplyNode` | `string` | 后端代码字段 supplyNode；含义由所属响应模型和接口业务决定 |
| `supplyPledged` | `string` | 后端代码字段 supplyPledged；含义由所属响应模型和接口业务决定 |
| `lockedSupply` | `string` | 锁定供应量，固定等于 supplyNode + supplyPledged |
| `floorPool` | `string` | 后端代码字段 floorPool；含义由所属响应模型和接口业务决定 |
| `marketPool` | `string` | 后端代码字段 marketPool；含义由所属响应模型和接口业务决定 |
| `reserveCash` | `string` | 后端代码字段 reserveCash；含义由所属响应模型和接口业务决定 |
| `circulatingMarketCap` | `string` | 流通市值 = marketPrice * supplyUserTotal |
| `lockedMarketCap` | `string` | 锁定市值 = marketPrice * (supplyNode + supplyPledged) |
| `totalMarketCap` | `string` | 总市值 = 流通市值 + 锁定市值 = marketPrice * supplyTotal |
| `marketCap` | `string` | 总市值兼容别名，数值等于 totalMarketCap |
| `circulatingMarketCapPct` | `string` | 流通市值占比，等于 supplyUserTotal / supplyTotal * 100；单位 % |
| `lockedMarketCapPct` | `string` | 锁定市值占比，等于 (supplyNode + supplyPledged) / supplyTotal * 100；单位 % |
| `fdv` | `string` | 已废弃语义的兼容字段；当前数值等于 totalMarketCap，合约无固定 maxSupply，不应解释为传统 FDV |
| `nodeSold` | `int64` | 后端代码字段 nodeSold；含义由所属响应模型和接口业务决定 |
| `nodeCount` | `int64` | 数量统计 |
| `nodeProgressPct` | `string` | 百分比数值 |
| `availableReserve` | `string` | 后端代码字段 availableReserve；含义由所属响应模型和接口业务决定 |
| `redeemableValue` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `floorCoverageRatio` | `string` | 后端代码字段 floorCoverageRatio；含义由所属响应模型和接口业务决定 |
| `maxSafeFloor` | `string` | 后端代码字段 maxSafeFloor；含义由所属响应模型和接口业务决定 |
| `isFloorSolvent` | `bool` | 后端代码字段 isFloorSolvent；含义由所属响应模型和接口业务决定 |
| `lastUpdatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### UserPosition

```json
{
  "address": "string",
  "tokenBalance": "string",
  "zeroCostBalance": "string",
  "costBasisStable": "string",
  "nodeTokenAmount": "string",
  "nodeLockedTotal": "string",
  "unreleasedNodeToken": "string",
  "nodeReleaseProgressPct": "string",
  "nodeStartedAt": "string",
  "votingPower": "string",
  "isFullNode": "bool",
  "nodeCount": "int64",
  "pledgedAmount": "string",
  "pledgeDebtStable": "string",
  "pledgeCostBasisStable": "string",
  "claimableFeeStable": "string",
  "claimableTaxStable": "string",
  "claimableUnlockedToken": "string",
  "claimableAltToken": "string",
  "altTokenLocked": "string",
  "altTokenClaimed": "string",
  "nodeCostBasisTotal": "string",
  "nodeCostBasisClaimed": "string",
  "points": "string",
  "referrerAddress": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `tokenBalance` | `string` | 后端代码字段 tokenBalance；含义由所属响应模型和接口业务决定 |
| `zeroCostBalance` | `string` | 钱包通过 Token 转账收到、链上成本基数为 0 的项目 Token 余额；卖出时需依合约利润税规则计算 |
| `costBasisStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeTokenAmount` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeLockedTotal` | `string` | 后端代码字段 nodeLockedTotal；含义由所属响应模型和接口业务决定 |
| `unreleasedNodeToken` | `string` | 后端代码字段 unreleasedNodeToken；含义由所属响应模型和接口业务决定 |
| `nodeReleaseProgressPct` | `string` | 百分比数值 |
| `nodeStartedAt` | `string` | 节点锁仓开始时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `votingPower` | `string` | 后端代码字段 votingPower；含义由所属响应模型和接口业务决定 |
| `isFullNode` | `bool` | 后端代码字段 isFullNode；含义由所属响应模型和接口业务决定 |
| `nodeCount` | `int64` | 数量统计 |
| `pledgedAmount` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `pledgeDebtStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `pledgeCostBasisStable` | `string` | 当前质押仓位的稳定币成本基准；解质押按比例恢复到用户 Token 成本，毕业后为 0 |
| `claimableFeeStable` | `string` | 链上 claimableFees(user) 返回的完整实时可领手续费；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `claimableTaxStable` | `string` | 链上 claimableTaxes(user) 返回的完整实时可领利润税；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `claimableUnlockedToken` | `string` | 通过合约 claimableUnlocked(user) 实时计算的已解锁未领项目 Token |
| `claimableAltToken` | `string` | 链上 claimableAltToken(user) 返回的已归属未领 Alt Token；通过 claim_alt_token 单独领取 |
| `altTokenLocked` | `string` | 后端代码字段 altTokenLocked；含义由所属响应模型和接口业务决定 |
| `altTokenClaimed` | `string` | 后端代码字段 altTokenClaimed；含义由所属响应模型和接口业务决定 |
| `nodeCostBasisTotal` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeCostBasisClaimed` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `points` | `string` | 后端代码字段 points；含义由所属响应模型和接口业务决定 |
| `referrerAddress` | `string` | 合约或钱包的 EVM 地址 |

### UserPortfolioSummary

```json
{
  "address": "string",
  "projectCount": "int",
  "totalBalance": "string",
  "totalCostBasis": "string",
  "totalNodeCount": "int64",
  "totalVotingPower": "string",
  "totalPledged": "string",
  "totalDebtStable": "string",
  "totalPledgeCostBasisStable": "string",
  "totalClaimableStable": "string",
  "totalClaimableFeesStable": "string",
  "totalClaimableTaxesStable": "string",
  "totalClaimableUnlockedToken": "string",
  "totalAltClaimable": "string",
  "positions": "[]PortfolioItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `projectCount` | `int` | 数量统计 |
| `totalBalance` | `string` | 后端代码字段 totalBalance；含义由所属响应模型和接口业务决定 |
| `totalCostBasis` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalNodeCount` | `int64` | 数量统计 |
| `totalVotingPower` | `string` | 后端代码字段 totalVotingPower；含义由所属响应模型和接口业务决定 |
| `totalPledged` | `string` | 后端代码字段 totalPledged；含义由所属响应模型和接口业务决定 |
| `totalDebtStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalPledgeCostBasisStable` | `string` | 用户所有项目当前质押成本基准的稳定币合计 |
| `totalClaimableStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalClaimableFeesStable` | `string` | 所有仓位链上实时可领手续费合计 |
| `totalClaimableTaxesStable` | `string` | 所有仓位链上实时可领利润税合计 |
| `totalClaimableUnlockedToken` | `string` | 所有仓位链上实时已解锁未领 Token 合计 |
| `totalAltClaimable` | `string` | 后端代码字段 totalAltClaimable；含义由所属响应模型和接口业务决定 |
| `positions` | `[]PortfolioItem` | 后端代码字段 positions；含义由所属响应模型和接口业务决定 |

### CreatedProjectItem

```json
{
  "projectAddress": "string",
  "tokenName": "string",
  "tokenSymbol": "string",
  "state": "string",
  "scaleTier": "string",
  "logoUrl": "string",
  "createdAt": "string",
  "updatedAt": "string",
  "currentPrice": "string",
  "floorPrice": "string",
  "currentSupply": "string",
  "nodeSold": "int64",
  "nodeCount": "int64",
  "nodeProgressPct": "string",
  "claimableFeeStable": "string",
  "claimableTaxStable": "string",
  "claimableTotalStable": "string",
  "claimedFeeStable": "string",
  "claimedTaxStable": "string",
  "claimedTotalStable": "string",
  "totalEarnedFeeStable": "string",
  "totalEarnedTaxStable": "string",
  "totalEarnedStable": "string",
  "totalCreatorLpFeeDistributedStable": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `state` | `string` | 后端代码字段 state；含义由所属响应模型和接口业务决定 |
| `scaleTier` | `string` | 档位 bytes32 ID；20260821 交接包只启用 sepolia-uniswap-v4-production-v2 对应 ID |
| `logoUrl` | `string` | 项目 Logo 资源 URL |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `currentSupply` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeSold` | `int64` | 后端代码字段 nodeSold；含义由所属响应模型和接口业务决定 |
| `nodeCount` | `int64` | 数量统计 |
| `nodeProgressPct` | `string` | 百分比数值 |
| `claimableFeeStable` | `string` | 链上 claimableFees(user) 返回的完整实时可领手续费；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `claimableTaxStable` | `string` | 链上 claimableTaxes(user) 返回的完整实时可领利润税；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `claimableTotalStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `claimedFeeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `claimedTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `claimedTotalStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalEarnedFeeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalEarnedTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalEarnedStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalCreatorLpFeeDistributedStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |

### CreatedProjectsSummary

```json
{
  "address": "string",
  "projectCount": "int",
  "totalClaimableFeeStable": "string",
  "totalClaimableTaxStable": "string",
  "totalClaimableStable": "string",
  "totalClaimedFeeStable": "string",
  "totalClaimedTaxStable": "string",
  "totalClaimedStable": "string",
  "totalEarnedFeeStable": "string",
  "totalEarnedTaxStable": "string",
  "totalEarnedStable": "string",
  "totalCreatorLpFeeDistributedStable": "string",
  "items": "[]CreatedProjectItem",
  "total": "uint64",
  "limit": "int",
  "offset": "int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `projectCount` | `int` | 数量统计 |
| `totalClaimableFeeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalClaimableTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalClaimableStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalClaimedFeeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalClaimedTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalClaimedStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalEarnedFeeStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalEarnedTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalEarnedStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `totalCreatorLpFeeDistributedStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `items` | `[]CreatedProjectItem` | 当前页数据列表；空结果返回 [] |
| `total` | `uint64` | 符合条件的总记录数 |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |

### UserBusinessSummary

```json
{
  "address": "string",
  "member": "*MemberSummary",
  "portfolio": "*UserPortfolioSummary",
  "transactions": "[]UserTransactionItem",
  "notifications": "[]NotificationItem",
  "dataSource": "string",
  "indexing": "ProjectIndexingStatus"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `member` | `*MemberSummary` | 后端代码字段 member；含义由所属响应模型和接口业务决定 |
| `portfolio` | `*UserPortfolioSummary` | 后端代码字段 portfolio；含义由所属响应模型和接口业务决定 |
| `transactions` | `[]UserTransactionItem` | 后端代码字段 transactions；含义由所属响应模型和接口业务决定 |
| `notifications` | `[]NotificationItem` | 后端代码字段 notifications；含义由所属响应模型和接口业务决定 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `indexing` | `ProjectIndexingStatus` | 后端代码字段 indexing；含义由所属响应模型和接口业务决定 |

### PortfolioItem

```json
{
  "projectAddress": "string",
  "tokenName": "string",
  "tokenSymbol": "string",
  "state": "string",
  "updatedAt": "string",
  "tokenBalance": "string",
  "costBasisStable": "string",
  "nodeTokenAmount": "string",
  "nodeLockedTotal": "string",
  "unreleasedNodeToken": "string",
  "nodeReleaseProgressPct": "string",
  "nodeStartedAt": "string",
  "votingPower": "string",
  "isFullNode": "bool",
  "nodeCount": "int64",
  "pledgedAmount": "string",
  "pledgeDebtStable": "string",
  "pledgeCostBasisStable": "string",
  "claimableFeeStable": "string",
  "claimableTaxStable": "string",
  "claimableUnlockedToken": "string",
  "claimableAltToken": "string",
  "altTokenLocked": "string",
  "altTokenClaimed": "string",
  "nodeCostBasisTotal": "string",
  "nodeCostBasisClaimed": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `state` | `string` | 后端代码字段 state；含义由所属响应模型和接口业务决定 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `tokenBalance` | `string` | 后端代码字段 tokenBalance；含义由所属响应模型和接口业务决定 |
| `costBasisStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeTokenAmount` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeLockedTotal` | `string` | 后端代码字段 nodeLockedTotal；含义由所属响应模型和接口业务决定 |
| `unreleasedNodeToken` | `string` | 后端代码字段 unreleasedNodeToken；含义由所属响应模型和接口业务决定 |
| `nodeReleaseProgressPct` | `string` | 百分比数值 |
| `nodeStartedAt` | `string` | 节点锁仓开始时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `votingPower` | `string` | 后端代码字段 votingPower；含义由所属响应模型和接口业务决定 |
| `isFullNode` | `bool` | 后端代码字段 isFullNode；含义由所属响应模型和接口业务决定 |
| `nodeCount` | `int64` | 数量统计 |
| `pledgedAmount` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `pledgeDebtStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `pledgeCostBasisStable` | `string` | 当前质押仓位的稳定币成本基准；解质押按比例恢复到用户 Token 成本，毕业后为 0 |
| `claimableFeeStable` | `string` | 链上 claimableFees(user) 返回的完整实时可领手续费；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `claimableTaxStable` | `string` | 链上 claimableTaxes(user) 返回的完整实时可领利润税；新合约已包含节点懒结算奖励，前后端不得再二次累加 |
| `claimableUnlockedToken` | `string` | 通过合约 claimableUnlocked(user) 实时计算的已解锁未领项目 Token |
| `claimableAltToken` | `string` | 链上 claimableAltToken(user) 返回的已归属未领 Alt Token；通过 claim_alt_token 单独领取 |
| `altTokenLocked` | `string` | 后端代码字段 altTokenLocked；含义由所属响应模型和接口业务决定 |
| `altTokenClaimed` | `string` | 后端代码字段 altTokenClaimed；含义由所属响应模型和接口业务决定 |
| `nodeCostBasisTotal` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeCostBasisClaimed` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |

### ProjectHistoryItem

```json
{
  "eventType": "string",
  "txHash": "string",
  "blockNumber": "uint64",
  "blockTime": "string",
  "actorAddress": "string",
  "summary": "string",
  "amountStable": "string",
  "amountToken": "string",
  "price": "string",
  "priceSource": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `eventType` | `string` | 合约事件机器码，保留原始大小写；完整枚举：`AltTokenClaimed`=领取附属代币；`Approval`=代币授权；`Buy`=买入；`CostBasisUpdated`=成本基准更新；`CreationFeeUpdated`=项目创建费更新；`CurveTableCommitmentRegistered`=曲线表承诺登记；`CurveTableRegistered`=曲线表登记；`ExternalPoolMigrationCreated`=外部池迁移创建；`FeePolicyUpdated`=费率策略更新；`FeesClaimed`=领取手续费；`FloorRatchet`=地板价上调；`GraduationAnnounced`=毕业公告；`GraduationExecuted`=毕业执行；`LaunchFailed`=发射失败；`LPFeeDistributed`=LP 手续费分配；`NativeReceived`=收到原生币；`NodeLocked`=节点锁定；`NodeSubscribed`=节点认购；`PlatformIncomeRecorded`=平台收入登记；`PledgeCreated`=质押买入；`ProductionCurveCreated`=生产曲线创建；`ProfitTaxCharged`=盈利税计提；`ProjectCreated`=项目创建；`ProposalCreated`=提案创建；`ProposalExecuted`=提案执行；`ProposalTallied`=提案计票；`RefundClaimed`=领取退款；`ScaleTierUpdated`=规模档位更新；`ScaleTierVersionUpdated`=规模档位版本更新；`Sell`=卖出；`SignerAdded`=新增多签人；`SignerRemoved`=移除多签人；`SignerReplaced`=替换多签人；`TaxesClaimed`=领取盈利税；`ThresholdUpdated`=多签阈值更新；`TradingStarted`=曲线交易开启；`TransactionApproved`=多签交易批准；`TransactionExecuted`=多签交易执行；`TransactionSubmitted`=多签交易提交；`Transfer`=代币转账；`PlatformRevenueClaimProcessed`=平台收入领取结果；`PlatformRevenueBatchCompleted`=平台收入批量领取完成；`UniswapV4Graduated`=Uniswap v4 毕业回调；`UnlockedClaimed`=领取解锁代币；`Unpledged`=解除质押；`VoteCast`=治理投票 |
| `txHash` | `string` | 链上交易哈希 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `string` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `actorAddress` | `string` | 合约或钱包的 EVM 地址 |
| `summary` | `string` | 后台按 eventType 和事件金额动态生成的中文摘要；已覆盖全部已知合约事件，未知事件固定返回“已记录其他链上事件”，不会回显英文机器码 |
| `amountStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `amountToken` | `string` | 后端代码字段 amountToken；含义由所属响应模型和接口业务决定 |
| `price` | `string` | 后端代码字段 price；含义由所属响应模型和接口业务决定 |
| `priceSource` | `string` | 历史价格来源机器码；枚举：`event.currentPrice`=链上事件快照价格；`derived.amounts`=按事件金额推导价格 |

### PledgeHistoryItem

```json
{
  "eventType": "string",
  "txHash": "string",
  "blockNumber": "uint64",
  "blockTime": "string",
  "address": "string",
  "tokenAmount": "string",
  "boughtToken": "string",
  "floorPrice": "string",
  "borrowedStable": "string",
  "userPayStable": "string",
  "unlockCostStable": "string",
  "summary": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `eventType` | `string` | 质押事件枚举：PledgeCreated=质押买入，Unpledged=解除质押；大小写固定 |
| `txHash` | `string` | 链上交易哈希 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `string` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `address` | `string` | 后端代码字段 address；含义由所属响应模型和接口业务决定 |
| `tokenAmount` | `string` | PledgeCreated 时为投入质押的旧 Token；Unpledged 时为本次解除质押并解锁的 Token |
| `boughtToken` | `string` | 仅 PledgeCreated 返回；本次质押加仓新买到的 Token，不得替代 tokenAmount 展示质押数量 |
| `floorPrice` | `string` | 事件发生时的地板价，不使用当前地板价回填历史记录 |
| `borrowedStable` | `string` | 仅 PledgeCreated 返回；本次以地板价值形成的稳定币借款 |
| `userPayStable` | `string` | 仅 PledgeCreated 返回；用户本次实际自付的稳定币 |
| `unlockCostStable` | `string` | 仅 Unpledged 返回；解除质押时偿还的稳定币债务 |
| `summary` | `string` | 后台生成的质押事件中文摘要 |

### PledgeHistoryQuery

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### GovernanceProposalItem

```json
{
  "proposalId": "uint64",
  "projectAddress": "string",
  "proposerAddress": "string",
  "action": "string",
  "paramId": "uint8",
  "paramKey": "string",
  "paramLabel": "string",
  "valueUnit": "string",
  "oldValue": "string",
  "newValue": "string",
  "oldValueDisplay": "string",
  "newValueDisplay": "string",
  "changeSummary": "string",
  "metadataUri": "string",
  "voteStartTime": "uint64",
  "voteEndTime": "uint64",
  "queuedTime": "uint64",
  "snapshotTotalVotingPower": "string",
  "snapshotNodeEpoch": "uint64",
  "forVotes": "string",
  "forVotesDisplay": "string",
  "againstVotes": "string",
  "againstVotesDisplay": "string",
  "status": "string",
  "createdTxHash": "string",
  "executedTxHash": "string",
  "createdAt": "string",
  "executedAt": "string",
  "votes": "[]GovernanceVoteItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `proposalId` | `uint64` | 后端代码字段 proposalId；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `proposerAddress` | `string` | 合约或钱包的 EVM 地址 |
| `action` | `string` | 后端代码字段 action；含义由所属响应模型和接口业务决定 |
| `paramId` | `uint8` | 后端代码字段 paramId；含义由所属响应模型和接口业务决定 |
| `paramKey` | `string` | 后端代码字段 paramKey；含义由所属响应模型和接口业务决定 |
| `paramLabel` | `string` | 后端代码字段 paramLabel；含义由所属响应模型和接口业务决定 |
| `valueUnit` | `string` | 后端代码字段 valueUnit；含义由所属响应模型和接口业务决定 |
| `oldValue` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `newValue` | `string` | 治理提案新值的非负整数字符串；20260821 仅允许 paramId 5/8/10/11/12，后端同步校验 hard cap 和单次 20% 调整幅度 |
| `oldValueDisplay` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `newValueDisplay` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `changeSummary` | `string` | 后端代码字段 changeSummary；含义由所属响应模型和接口业务决定 |
| `metadataUri` | `string` | 后端代码字段 metadataUri；含义由所属响应模型和接口业务决定 |
| `voteStartTime` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `voteEndTime` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `queuedTime` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `snapshotTotalVotingPower` | `string` | 提案创建时的全网投票权快照；用于计票法定人数/权重判定 |
| `snapshotNodeEpoch` | `uint64` | 提案创建时的节点投票资格代际；之后新获得节点资格的钱包不能参与该提案 |
| `forVotes` | `string` | 后端代码字段 forVotes；含义由所属响应模型和接口业务决定 |
| `forVotesDisplay` | `string` | 后端代码字段 forVotesDisplay；含义由所属响应模型和接口业务决定 |
| `againstVotes` | `string` | 后端代码字段 againstVotes；含义由所属响应模型和接口业务决定 |
| `againstVotesDisplay` | `string` | 后端代码字段 againstVotesDisplay；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `createdTxHash` | `string` | 后端代码字段 createdTxHash；含义由所属响应模型和接口业务决定 |
| `executedTxHash` | `string` | 后端代码字段 executedTxHash；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `executedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `votes` | `[]GovernanceVoteItem` | 后端代码字段 votes；含义由所属响应模型和接口业务决定 |

### GovernanceVoteItem

```json
{
  "proposalId": "uint64",
  "projectAddress": "string",
  "voterAddress": "string",
  "support": "bool",
  "weight": "string",
  "weightDisplay": "string",
  "txHash": "string",
  "blockNumber": "uint64",
  "blockTime": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `proposalId` | `uint64` | 后端代码字段 proposalId；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `voterAddress` | `string` | 合约或钱包的 EVM 地址 |
| `support` | `bool` | 后端代码字段 support；含义由所属响应模型和接口业务决定 |
| `weight` | `string` | 后端代码字段 weight；含义由所属响应模型和接口业务决定 |
| `weightDisplay` | `string` | 后端代码字段 weightDisplay；含义由所属响应模型和接口业务决定 |
| `txHash` | `string` | 链上交易哈希 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `string` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |

### ProjectGovernanceResponse

```json
{
  "projectAddress": "string",
  "proposalCount": "int",
  "items": "[]GovernanceProposalItem",
  "total": "uint64",
  "limit": "int",
  "offset": "int",
  "nodeTokenUnit": "string",
  "allowedParamIds": "[]uint8",
  "recommendedParamIds": "[]uint8",
  "unavailableParamIds": "[]uint8"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `proposalCount` | `int` | 数量统计 |
| `items` | `[]GovernanceProposalItem` | 当前页数据列表；空结果返回 [] |
| `total` | `uint64` | 符合条件的总记录数 |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |
| `nodeTokenUnit` | `string` | 后端代码字段 nodeTokenUnit；含义由所属响应模型和接口业务决定 |
| `allowedParamIds` | `[]uint8` | 后端代码字段 allowedParamIds；含义由所属响应模型和接口业务决定 |
| `recommendedParamIds` | `[]uint8` | 后端代码字段 recommendedParamIds；含义由所属响应模型和接口业务决定 |
| `unavailableParamIds` | `[]uint8` | 后端代码字段 unavailableParamIds；含义由所属响应模型和接口业务决定 |

### ProjectIndexingStatus

```json
{
  "projectAddress": "string",
  "chainId": "int64",
  "status": "string",
  "lastEventBlock": "uint64",
  "lastEventAt": "string",
  "lastSnapshotBlock": "uint64",
  "lastSnapshotAt": "string",
  "metricsBlock": "uint64",
  "metricsUpdatedAt": "string",
  "projectCheckpoint": "uint64",
  "platformCheckpoint": "uint64",
  "eventsIndexed": "uint64",
  "unappliedEventLag": "uint64",
  "snapshotLag": "uint64",
  "checkpointLag": "uint64",
  "needsReconcile": "bool",
  "recommendedAction": "string",
  "dataSource": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `lastEventBlock` | `uint64` | 后端代码字段 lastEventBlock；含义由所属响应模型和接口业务决定 |
| `lastEventAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `lastSnapshotBlock` | `uint64` | 后端代码字段 lastSnapshotBlock；含义由所属响应模型和接口业务决定 |
| `lastSnapshotAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `metricsBlock` | `uint64` | 后端代码字段 metricsBlock；含义由所属响应模型和接口业务决定 |
| `metricsUpdatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `projectCheckpoint` | `uint64` | 后端代码字段 projectCheckpoint；含义由所属响应模型和接口业务决定 |
| `platformCheckpoint` | `uint64` | 后端代码字段 platformCheckpoint；含义由所属响应模型和接口业务决定 |
| `eventsIndexed` | `uint64` | 布尔状态标记 |
| `unappliedEventLag` | `uint64` | 后端代码字段 unappliedEventLag；含义由所属响应模型和接口业务决定 |
| `snapshotLag` | `uint64` | 后端代码字段 snapshotLag；含义由所属响应模型和接口业务决定 |
| `checkpointLag` | `uint64` | 后端代码字段 checkpointLag；含义由所属响应模型和接口业务决定 |
| `needsReconcile` | `bool` | 后端代码字段 needsReconcile；含义由所属响应模型和接口业务决定 |
| `recommendedAction` | `string` | 后端代码字段 recommendedAction；含义由所属响应模型和接口业务决定 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |

### OnChainProjectState

```json
{
  "projectAddress": "string",
  "chainId": "int64",
  "blockNumber": "uint64",
  "projectState": "string",
  "issuer": "string",
  "platformPayout": "string",
  "stableToken": "string",
  "stableTokenDecimals": "uint8",
  "curveTable": "string",
  "curveParamsHash": "string",
  "altToken": "string",
  "altTokenDecimals": "uint8",
  "altTokenPerNode": "string",
  "altTokenPerNodeWei": "string",
  "currentPrice": "string",
  "marketPrice": "string",
  "marketPriceSource": "string",
  "marketPriceLive": "bool",
  "marketPriceSnapshotBlock": "uint64",
  "floorPrice": "string",
  "supplyTotal": "string",
  "supplyUserTotal": "string",
  "supplyNode": "string",
  "supplyPledged": "string",
  "totalPledgeDebtStable": "string",
  "curveSupply": "string",
  "floorPool": "string",
  "marketPool": "string",
  "reserveCash": "string",
  "floorAllocationBps": "string",
  "ratchetThresholdBps": "string",
  "ratchetThreshold": "string",
  "buyFeeRate": "string",
  "sellFeeRate": "string",
  "profitTaxRate": "string",
  "minSingleBuyStable": "string",
  "nodeSold": "uint64",
  "nodeCount": "uint64",
  "nodePriceStable": "string",
  "nodeTargetStable": "string",
  "nodeTokenUnit": "string",
  "nodeSaleDeadline": "uint64",
  "presaleLockStart": "uint64",
  "nodeProgressPct": "string",
  "tradingStartedAt": "uint64",
  "graduationWindowVolume": "string",
  "maxNodesPerAddress": "uint64",
  "nodeVestingCliffDuration": "uint64",
  "nodeVestingTotalDuration": "uint64",
  "graduationAnnounceTime": "uint64",
  "graduatedPoolId": "string",
  "graduationAdapter": "string",
  "graduationHookOrLock": "string",
  "graduatedAt": "uint64",
  "graduationReserveThreshold": "string",
  "graduationTradingDuration": "uint64",
  "graduationAnnounceDelay": "uint64",
  "graduationVolumeWindow": "uint64",
  "votingPeriodDuration": "uint64",
  "timelockDelayDuration": "uint64",
  "feeFloorBps": "string",
  "feeNodeBps": "string",
  "feeIssuerBps": "string",
  "feePlatformBps": "string",
  "taxFloorBps": "string",
  "taxNodeBps": "string",
  "taxIssuerBps": "string",
  "taxPlatformBps": "string",
  "lpFeeNodeBps": "string",
  "lpFeeCreatorBps": "string",
  "lpFeePlatformBps": "string",
  "nextProposalId": "uint64",
  "nodeVotingEpoch": "uint64",
  "totalVotingPower": "string",
  "lastSyncedAt": "string",
  "dataSource": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `projectState` | `string` | 后端代码字段 projectState；含义由所属响应模型和接口业务决定 |
| `issuer` | `string` | 后端代码字段 issuer；含义由所属响应模型和接口业务决定 |
| `platformPayout` | `string` | 后端代码字段 platformPayout；含义由所属响应模型和接口业务决定 |
| `stableToken` | `string` | 后端代码字段 stableToken；含义由所属响应模型和接口业务决定 |
| `stableTokenDecimals` | `uint8` | Stable Token 的真实精度；Ethereum Sepolia 为 USDGTest 6 位，项目 Token 仍为 18 位 |
| `curveTable` | `string` | 后端代码字段 curveTable；含义由所属响应模型和接口业务决定 |
| `curveParamsHash` | `string` | 曲线配置参数的 bytes32 哈希；用于核对 Scale Tier 与 CurveTable 参数版本 |
| `altToken` | `string` | 后端代码字段 altToken；含义由所属响应模型和接口业务决定 |
| `altTokenDecimals` | `uint8` | 后端代码字段 altTokenDecimals；含义由所属响应模型和接口业务决定 |
| `altTokenPerNode` | `string` | 每节点托管的 Alt Token 数量；未启用时可传空字符串、0 或 0.0，启用时必须是按该 Token 实际 decimals 解析后的正数 |
| `altTokenPerNodeWei` | `string` | 后端代码字段 altTokenPerNodeWei；含义由所属响应模型和接口业务决定 |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `marketPrice` | `string` | 用于市值计算的统一市场价；Trading/Graduating 取曲线价，Graduated 优先取官方 Uniswap v4 StateView slot0 现价 |
| `marketPriceSource` | `string` | 市场价来源枚举：bonding_curve/uniswap_v4_slot0/graduation_price_fallback/unavailable |
| `marketPriceLive` | `bool` | 市场价是否为当前快照的可用实时价；fallback 或 unavailable 时为 false |
| `marketPriceSnapshotBlock` | `uint64` | 读取价格和供应量的同一链上区块高度 |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `supplyTotal` | `string` | 后端代码字段 supplyTotal；含义由所属响应模型和接口业务决定 |
| `supplyUserTotal` | `string` | 后端代码字段 supplyUserTotal；含义由所属响应模型和接口业务决定 |
| `supplyNode` | `string` | 后端代码字段 supplyNode；含义由所属响应模型和接口业务决定 |
| `supplyPledged` | `string` | 后端代码字段 supplyPledged；含义由所属响应模型和接口业务决定 |
| `totalPledgeDebtStable` | `string` | 项目当前链上质押债务总额，USDGTest 可读数量字符串；毕业执行后归零 |
| `curveSupply` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `floorPool` | `string` | 后端代码字段 floorPool；含义由所属响应模型和接口业务决定 |
| `marketPool` | `string` | 后端代码字段 marketPool；含义由所属响应模型和接口业务决定 |
| `reserveCash` | `string` | 后端代码字段 reserveCash；含义由所属响应模型和接口业务决定 |
| `floorAllocationBps` | `string` | 万分比整数，10000 表示 100% |
| `ratchetThresholdBps` | `string` | 万分比整数，10000 表示 100% |
| `ratchetThreshold` | `string` | 后端代码字段 ratchetThreshold；含义由所属响应模型和接口业务决定 |
| `buyFeeRate` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `sellFeeRate` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `profitTaxRate` | `string` | 后端代码字段 profitTaxRate；含义由所属响应模型和接口业务决定 |
| `minSingleBuyStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeSold` | `uint64` | 后端代码字段 nodeSold；含义由所属响应模型和接口业务决定 |
| `nodeCount` | `uint64` | 数量统计 |
| `nodePriceStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeTargetStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `nodeTokenUnit` | `string` | 后端代码字段 nodeTokenUnit；含义由所属响应模型和接口业务决定 |
| `nodeSaleDeadline` | `uint64` | 后端代码字段 nodeSaleDeadline；含义由所属响应模型和接口业务决定 |
| `presaleLockStart` | `uint64` | 预售节点统一锁仓起点，Unix 秒；只有节点售罄并进入 Trading 后才大于 0 |
| `nodeProgressPct` | `string` | 百分比数值 |
| `tradingStartedAt` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `graduationWindowVolume` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `maxNodesPerAddress` | `uint64` | 建项时固化的单地址节点认购数量上限；必须在 1..nodeCount 之间，后端会结合 nodeSoldBy(wallet) 计算剩余可认购数 |
| `nodeVestingCliffDuration` | `uint64` | 节点 Token 开始线性释放前的 cliff 秒数；建项时从 Scale Tier 固化 |
| `nodeVestingTotalDuration` | `uint64` | 节点 Token 从锁定开始到全部释放的总秒数；必须大于 nodeVestingCliffDuration |
| `graduationAnnounceTime` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `graduatedPoolId` | `string` | 后端代码字段 graduatedPoolId；含义由所属响应模型和接口业务决定 |
| `graduationAdapter` | `string` | 毕业 Adapter 地址；enabled=true 时必须等于 Platform.trustedGraduationAdapter() |
| `graduationHookOrLock` | `string` | 后端代码字段 graduationHookOrLock；含义由所属响应模型和接口业务决定 |
| `graduatedAt` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `graduationReserveThreshold` | `string` | 后端代码字段 graduationReserveThreshold；含义由所属响应模型和接口业务决定 |
| `graduationTradingDuration` | `uint64` | 后端代码字段 graduationTradingDuration；含义由所属响应模型和接口业务决定 |
| `graduationAnnounceDelay` | `uint64` | 后端代码字段 graduationAnnounceDelay；含义由所属响应模型和接口业务决定 |
| `graduationVolumeWindow` | `uint64` | 毕业成交量滚动窗口秒数；后端只展示链上窗口语义 |
| `votingPeriodDuration` | `uint64` | 后端代码字段 votingPeriodDuration；含义由所属响应模型和接口业务决定 |
| `timelockDelayDuration` | `uint64` | 后端代码字段 timelockDelayDuration；含义由所属响应模型和接口业务决定 |
| `feeFloorBps` | `string` | 手续费地板分账 BPS；固定 6000（60%），不可修改 |
| `feeNodeBps` | `string` | 手续费节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `feeIssuerBps` | `string` | 手续费发射方分账 BPS；初始 1000（10%），可通过 paramId=5 在 3000 BPS 灵活份额内调整 |
| `feePlatformBps` | `string` | 手续费平台分账 BPS；固定 1000（10%），不可修改 |
| `taxFloorBps` | `string` | 盈利税地板分账 BPS；固定 6000（60%），不可修改 |
| `taxNodeBps` | `string` | 盈利税节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `taxIssuerBps` | `string` | 盈利税发射方分账 BPS；初始 1000（10%），可通过 paramId=8 在 3000 BPS 灵活份额内调整 |
| `taxPlatformBps` | `string` | 盈利税平台分账 BPS；固定 1000（10%），不可修改 |
| `lpFeeNodeBps` | `string` | 万分比整数，10000 表示 100% |
| `lpFeeCreatorBps` | `string` | 万分比整数，10000 表示 100% |
| `lpFeePlatformBps` | `string` | 万分比整数，10000 表示 100% |
| `nextProposalId` | `uint64` | 后端代码字段 nextProposalId；含义由所属响应模型和接口业务决定 |
| `nodeVotingEpoch` | `uint64` | 当前节点投票资格代际；新获得节点资格时递增 |
| `totalVotingPower` | `string` | 后端代码字段 totalVotingPower；含义由所属响应模型和接口业务决定 |
| `lastSyncedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |

### UpdateProjectMetadataRequest

```json
{
  "logoUrl": "string",
  "website": "string",
  "twitter": "string",
  "telegram": "string",
  "description": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `logoUrl` | `string` | 项目 Logo 资源 URL |
| `website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |

### ProjectMetadataResponse

```json
{
  "projectAddress": "string",
  "tokenName": "string",
  "tokenSymbol": "string",
  "logoUrl": "string",
  "website": "string",
  "twitter": "string",
  "telegram": "string",
  "description": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `tokenName` | `string` | 项目 Token 名称 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `logoUrl` | `string` | 项目 Logo 资源 URL |
| `website` | `string` | 后端代码字段 website；含义由所属响应模型和接口业务决定 |
| `twitter` | `string` | 项目 Twitter/X 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `telegram` | `string` | 项目 Telegram 社区链接；可选，最长 500 字符，必须是 http/https URL，只存 MySQL 不进入链上 calldata |
| `description` | `string` | 后端代码字段 description；含义由所属响应模型和接口业务决定 |

### BuyQuoteRequest

```json
{
  "projectAddress": "string",
  "cashIn": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `cashIn` | `string` | 买入时输入的稳定币金额 |

### BuyQuoteResponse

```json
{
  "tokenOut": "string",
  "feeStable": "string",
  "netCash": "string",
  "currentPrice": "string",
  "newPrice": "string",
  "averagePrice": "string",
  "priceImpactPct": "string",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `tokenOut` | `string` | 后端代码字段 tokenOut；含义由所属响应模型和接口业务决定 |
| `feeStable` | `string` | 本次操作的稳定币手续费 |
| `netCash` | `string` | 后端代码字段 netCash；含义由所属响应模型和接口业务决定 |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `newPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `averagePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `priceImpactPct` | `string` | 百分比数值 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### BuyByTokenQuoteRequest

```json
{
  "projectAddress": "string",
  "tokenOut": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `tokenOut` | `string` | 后端代码字段 tokenOut；含义由所属响应模型和接口业务决定 |

### BuyByTokenQuoteResponse

```json
{
  "requestedTokenOut": "string",
  "cashIn": "string",
  "tokenOut": "string",
  "feeStable": "string",
  "netCash": "string",
  "currentPrice": "string",
  "newPrice": "string",
  "averagePrice": "string",
  "priceImpactPct": "string",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `requestedTokenOut` | `string` | 前端请求至少获得的项目 Token 数量 |
| `cashIn` | `string` | 满足 requestedTokenOut 的最小买入 USDGTest 输入；按稳定币真实 decimals 编码，已按合约 ceil 手续费取整反算，可用于后续 buy action |
| `tokenOut` | `string` | 按 cashIn 在 snapshotBlock 实际预计获得的 Token；可能因整数取整略高于 requestedTokenOut |
| `feeStable` | `string` | 本次操作的稳定币手续费 |
| `netCash` | `string` | 后端代码字段 netCash；含义由所属响应模型和接口业务决定 |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `newPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `averagePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `priceImpactPct` | `string` | 百分比数值 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### SellQuoteRequest

```json
{
  "projectAddress": "string",
  "userAddress": "string",
  "tokenIn": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `userAddress` | `string` | 用户钱包地址 |
| `tokenIn` | `string` | 卖出时输入的项目 Token 数量 |

### SellQuoteResponse

```json
{
  "grossStable": "string",
  "feeStable": "string",
  "profitStable": "string",
  "profitTaxStable": "string",
  "cashOut": "string",
  "currentPrice": "string",
  "newPrice": "string",
  "averagePrice": "string",
  "priceImpactPct": "string",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `grossStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `feeStable` | `string` | 本次操作的稳定币手续费 |
| `profitStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `profitTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `cashOut` | `string` | 后端代码字段 cashOut；含义由所属响应模型和接口业务决定 |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `newPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `averagePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `priceImpactPct` | `string` | 百分比数值 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### SellByCashQuoteRequest

```json
{
  "projectAddress": "string",
  "userAddress": "string",
  "cashOut": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `userAddress` | `string` | 用户钱包地址 |
| `cashOut` | `string` | 后端代码字段 cashOut；含义由所属响应模型和接口业务决定 |

### SellByCashQuoteResponse

```json
{
  "requestedCashOut": "string",
  "tokenIn": "string",
  "grossStable": "string",
  "feeStable": "string",
  "profitStable": "string",
  "profitTaxStable": "string",
  "cashOut": "string",
  "currentPrice": "string",
  "newPrice": "string",
  "averagePrice": "string",
  "priceImpactPct": "string",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `requestedCashOut` | `string` | 前端请求至少净到账的 USDGTest 数量 |
| `tokenIn` | `string` | 在当前钱包余额与成本基数内，满足 requestedCashOut 的最小卖出 Token 数量；0821 合约不设置单笔卖出最大值 |
| `grossStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `feeStable` | `string` | 本次操作的稳定币手续费 |
| `profitStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `profitTaxStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `cashOut` | `string` | 扣除卖出手续费和该用户利润税后的实际预计净到账；不低于 requestedCashOut |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `newPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `averagePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `priceImpactPct` | `string` | 百分比数值 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### PledgeQuoteRequest

```json
{
  "projectAddress": "string",
  "pledgeToken": "string",
  "buyToken": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `pledgeToken` | `string` | 用于质押的项目 Token 数量；字段名必须是 pledgeToken |
| `buyToken` | `string` | 质押买时目标买入的项目 Token 数量 |

### PledgeQuoteResponse

```json
{
  "curveCostStable": "string",
  "borrowedStable": "string",
  "feeStable": "string",
  "userPayStable": "string",
  "selfPayPct": "string",
  "postPledgeRatioPct": "string",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `curveCostStable` | `string` | 按 CurveTable 曲线计算的购买成本 |
| `borrowedStable` | `string` | 质押价值可覆盖的稳定币金额 |
| `feeStable` | `string` | 本次操作的稳定币手续费 |
| `userPayStable` | `string` | 用户本次需自付的稳定币金额 |
| `selfPayPct` | `string` | 百分比数值 |
| `postPledgeRatioPct` | `string` | 百分比数值 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### PledgeBuyRangeRequest

```json
{
  "projectAddress": "string",
  "userAddress": "string",
  "pledgeToken": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `userAddress` | `string` | 用户钱包地址 |
| `pledgeToken` | `string` | 用于质押的项目 Token 数量；字段名必须是 pledgeToken |

### PledgeQuoteErrorData

```json
{
  "reasonCode": "string",
  "direction": "string",
  "boundBuyToken": "string",
  "suggestedAction": "string",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `reasonCode` | `string` | 稳定机器枚举；前端应按文档映射文案，不要匹配 message 英文 |
| `direction` | `string` | 约束方向枚举：input=输入错误、eligibility=资格、min=需增大 buyToken、max=需减小 buyToken、affordability=钱包可支付性 |
| `boundBuyToken` | `string` | 该约束换算后的 buyToken 边界值 |
| `suggestedAction` | `string` | 前端处理动作枚举：CORRECT_INPUT/WAIT_FOR_TRADING/INCREASE_BUY_TOKEN/DECREASE_BUY_TOKEN |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### PledgeBuyConstraint

```json
{
  "reasonCode": "string",
  "direction": "string",
  "boundBuyToken": "string",
  "boundStable": "string",
  "currentValue": "string",
  "satisfied": "bool"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `reasonCode` | `string` | 稳定机器枚举；前端应按文档映射文案，不要匹配 message 英文 |
| `direction` | `string` | 约束方向枚举：input=输入错误、eligibility=资格、min=需增大 buyToken、max=需减小 buyToken、affordability=钱包可支付性 |
| `boundBuyToken` | `string` | 该约束换算后的 buyToken 边界值 |
| `boundStable` | `string` | 该约束对应的 Stable 金额边界 |
| `currentValue` | `string` | 该约束的当前链上/钱包值 |
| `satisfied` | `bool` | 该单项约束在当前快照下是否可满足 |

### PledgeBuyRangeResponse

```json
{
  "projectAddress": "string",
  "userAddress": "string",
  "pledgeToken": "string",
  "rangeAvailable": "bool",
  "protocolRangeAvailable": "bool",
  "collateralSufficient": "bool",
  "minBuyToken": "string",
  "maxBuyToken": "string",
  "maxAffordableBuyToken": "string",
  "effectiveMaxBuyToken": "string",
  "recommendedBuyToken": "string",
  "projectTokenBalance": "string",
  "stableBalance": "string",
  "stableTokenAddress": "string",
  "stableTokenSymbol": "string",
  "stableTokenDecimals": "uint8",
  "snapshotBlock": "uint64",
  "constraints": "[]PledgeBuyConstraint"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `userAddress` | `string` | 用户钱包地址 |
| `pledgeToken` | `string` | 用于质押的项目 Token 数量；字段名必须是 pledgeToken |
| `rangeAvailable` | `bool` | 项目状态、质押 Token 余额、协议区间与 Stable 可支付能力全部通过时为 true |
| `protocolRangeAvailable` | `bool` | 不考虑钱包余额时，合约数学约束是否仍有非空 buyToken 区间 |
| `collateralSufficient` | `bool` | 钱包项目 Token 余额是否覆盖 pledgeToken |
| `minBuyToken` | `string` | 同一 snapshotBlock 下同时满足所有链上约束的最小 buyToken |
| `maxBuyToken` | `string` | 仅考虑协议规则的最大 buyToken；未扣除当前钱包 Stable 余额约束 |
| `maxAffordableBuyToken` | `string` | 按当前钱包 Stable 余额可支付的最大 buyToken |
| `effectiveMaxBuyToken` | `string` | 协议上限与钱包可支付上限的较小值 |
| `recommendedBuyToken` | `string` | 区间可用时等于 minBuyToken，可用作前端“推荐”按钮填充值；不可用时为 0 |
| `projectTokenBalance` | `string` | 后端代码字段 projectTokenBalance；含义由所属响应模型和接口业务决定 |
| `stableBalance` | `string` | 后端代码字段 stableBalance；含义由所属响应模型和接口业务决定 |
| `stableTokenAddress` | `string` | 当前网络配置的稳定币合约地址 |
| `stableTokenSymbol` | `string` | 后端代码字段 stableTokenSymbol；含义由所属响应模型和接口业务决定 |
| `stableTokenDecimals` | `uint8` | Stable Token 的真实精度；Ethereum Sepolia 为 USDGTest 6 位，项目 Token 仍为 18 位 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |
| `constraints` | `[]PledgeBuyConstraint` | 后端代码字段 constraints；含义由所属响应模型和接口业务决定 |

### UnpledgeQuoteRequest

```json
{
  "projectAddress": "string",
  "userAddress": "string",
  "tokenAmount": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `userAddress` | `string` | 用户钱包地址 |
| `tokenAmount` | `string` | 项目 Token 数量 |

### UnpledgeQuoteResponse

```json
{
  "pledgedAmount": "string",
  "pledgeDebtStable": "string",
  "unlockCostStable": "string",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `pledgedAmount` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `pledgeDebtStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `unlockCostStable` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### CurvePoint

```json
{
  "index": "uint64",
  "supply": "string",
  "rawPrice": "string",
  "effectivePrice": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `index` | `uint64` | 后端代码字段 index；含义由所属响应模型和接口业务决定 |
| `supply` | `string` | 后端代码字段 supply；含义由所属响应模型和接口业务决定 |
| `rawPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `effectivePrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |

### ProjectCurveResponse

```json
{
  "projectAddress": "string",
  "curveTableAddress": "string",
  "curveParamsHash": "string",
  "curvePointsHash": "string",
  "currentSupply": "string",
  "floorPrice": "string",
  "currentPrice": "string",
  "points": "[]CurvePoint",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `curveTableAddress` | `string` | 必须是固定 CurveTableRegistry 工厂创建并已注册的 CurveTable 地址 |
| `curveParamsHash` | `string` | 曲线配置参数的 bytes32 哈希；用于核对 Scale Tier 与 CurveTable 参数版本 |
| `curvePointsHash` | `string` | 曲线分段点数组的 bytes32 哈希；必须与 CurveTable.pointsHash() 一致 |
| `currentSupply` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `floorPrice` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `currentPrice` | `string` | 项目合约 currentPrice() 的曲线价或毕业时冻结价；毕业后不代表 Uniswap v4 实时价 |
| `points` | `[]CurvePoint` | 后端代码字段 points；含义由所属响应模型和接口业务决定 |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### ProjectFeesResponse

```json
{
  "projectAddress": "string",
  "platformPayout": "string",
  "buyFeeRatePpm": "uint64",
  "sellFeeRatePpm": "uint64",
  "profitTaxRatePpm": "uint64",
  "feeFloorBps": "uint16",
  "feeNodeBps": "uint16",
  "feeIssuerBps": "uint16",
  "feePlatformBps": "uint16",
  "taxFloorBps": "uint16",
  "taxNodeBps": "uint16",
  "taxIssuerBps": "uint16",
  "taxPlatformBps": "uint16",
  "lpFeeNodeBps": "uint16",
  "lpFeeCreatorBps": "uint16",
  "lpFeePlatformBps": "uint16",
  "snapshotBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `platformPayout` | `string` | 后端代码字段 platformPayout；含义由所属响应模型和接口业务决定 |
| `buyFeeRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `sellFeeRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `profitTaxRatePpm` | `uint64` | 百万分比整数，1000000 表示 100% |
| `feeFloorBps` | `uint16` | 手续费地板分账 BPS；固定 6000（60%），不可修改 |
| `feeNodeBps` | `uint16` | 手续费节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `feeIssuerBps` | `uint16` | 手续费发射方分账 BPS；初始 1000（10%），可通过 paramId=5 在 3000 BPS 灵活份额内调整 |
| `feePlatformBps` | `uint16` | 手续费平台分账 BPS；固定 1000（10%），不可修改 |
| `taxFloorBps` | `uint16` | 盈利税地板分账 BPS；固定 6000（60%），不可修改 |
| `taxNodeBps` | `uint16` | 盈利税节点分账 BPS；初始 2000（20%），与发射方合计固定 3000 |
| `taxIssuerBps` | `uint16` | 盈利税发射方分账 BPS；初始 1000（10%），可通过 paramId=8 在 3000 BPS 灵活份额内调整 |
| `taxPlatformBps` | `uint16` | 盈利税平台分账 BPS；固定 1000（10%），不可修改 |
| `lpFeeNodeBps` | `uint16` | 万分比整数，10000 表示 100% |
| `lpFeeCreatorBps` | `uint16` | 万分比整数，10000 表示 100% |
| `lpFeePlatformBps` | `uint16` | 万分比整数，10000 表示 100% |
| `snapshotBlock` | `uint64` | 本次读取/报价对应的链上区块高度 |

### SupportConversation

```json
{
  "id": "uint64",
  "conversationNo": "string",
  "userAddress": "string",
  "category": "string",
  "priority": "string",
  "status": "string",
  "subject": "string",
  "assignedAdmin": "string",
  "lastMessageAt": "string",
  "closedAt": "string",
  "createdAt": "string",
  "updatedAt": "string",
  "unreadCount": "int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `conversationNo` | `string` | 后端代码字段 conversationNo；含义由所属响应模型和接口业务决定 |
| `userAddress` | `string` | 用户钱包地址 |
| `category` | `string` | 后端代码字段 category；含义由所属响应模型和接口业务决定 |
| `priority` | `string` | 后端代码字段 priority；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `subject` | `string` | 后端代码字段 subject；含义由所属响应模型和接口业务决定 |
| `assignedAdmin` | `string` | 后端代码字段 assignedAdmin；含义由所属响应模型和接口业务决定 |
| `lastMessageAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `closedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `unreadCount` | `int` | 数量统计 |

### SupportMessage

```json
{
  "id": "uint64",
  "conversationId": "uint64",
  "senderType": "string",
  "senderAddress": "string",
  "body": "string",
  "attachmentsJson": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `conversationId` | `uint64` | 后端代码字段 conversationId；含义由所属响应模型和接口业务决定 |
| `senderType` | `string` | 后端代码字段 senderType；含义由所属响应模型和接口业务决定 |
| `senderAddress` | `string` | 合约或钱包的 EVM 地址 |
| `body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `attachmentsJson` | `string` | 后端代码字段 attachmentsJson；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### SupportConversationDetail

```json
{
  "conversation": "SupportConversation",
  "messages": "[]SupportMessage"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `conversation` | `SupportConversation` | 后端代码字段 conversation；含义由所属响应模型和接口业务决定 |
| `messages` | `[]SupportMessage` | 后端代码字段 messages；含义由所属响应模型和接口业务决定 |

### CreateSupportConversationRequest

```json
{
  "category": "string",
  "priority": "string",
  "subject": "string",
  "body": "string",
  "attachmentsJson": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `category` | `string` | 后端代码字段 category；含义由所属响应模型和接口业务决定 |
| `priority` | `string` | 后端代码字段 priority；含义由所属响应模型和接口业务决定 |
| `subject` | `string` | 后端代码字段 subject；含义由所属响应模型和接口业务决定 |
| `body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `attachmentsJson` | `string` | 后端代码字段 attachmentsJson；含义由所属响应模型和接口业务决定 |

### SendSupportMessageRequest

```json
{
  "body": "string",
  "attachmentsJson": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `attachmentsJson` | `string` | 后端代码字段 attachmentsJson；含义由所属响应模型和接口业务决定 |

### AssignSupportConversationRequest

```json
{
  "assignedAdmin": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `assignedAdmin` | `string` | 后端代码字段 assignedAdmin；含义由所属响应模型和接口业务决定 |

### UpdateSupportStatusRequest

```json
{
  "status": "string",
  "reason": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `reason` | `string` | 本次操作原因，用于审计追溯 |

### SupportConversationQuery

```json
{}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| - | - | 该结构无 JSON 字段 |

### SupportCannedReply

```json
{
  "id": "uint64",
  "title": "string",
  "body": "string",
  "category": "string",
  "isActive": "bool",
  "createdBy": "string",
  "updatedBy": "string",
  "createdAt": "string",
  "updatedAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `category` | `string` | 后端代码字段 category；含义由所属响应模型和接口业务决定 |
| `isActive` | `bool` | 布尔状态标记 |
| `createdBy` | `string` | 后端代码字段 createdBy；含义由所属响应模型和接口业务决定 |
| `updatedBy` | `string` | 后端代码字段 updatedBy；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `updatedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### UpsertSupportCannedReplyRequest

```json
{
  "id": "uint64",
  "title": "string",
  "body": "string",
  "category": "string",
  "isActive": "*bool"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `title` | `string` | 后端代码字段 title；含义由所属响应模型和接口业务决定 |
| `body` | `string` | 后端代码字段 body；含义由所属响应模型和接口业务决定 |
| `category` | `string` | 后端代码字段 category；含义由所属响应模型和接口业务决定 |
| `isActive` | `*bool` | 布尔状态标记 |

### SupportMetrics

```json
{
  "openCount": "int",
  "pendingCount": "int",
  "resolvedCount": "int",
  "closedCount": "int",
  "unassignedCount": "int",
  "avgFirstReplyMinutes": "float64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `openCount` | `int` | 数量统计 |
| `pendingCount` | `int` | 数量统计 |
| `resolvedCount` | `int` | 数量统计 |
| `closedCount` | `int` | 数量统计 |
| `unassignedCount` | `int` | 数量统计 |
| `avgFirstReplyMinutes` | `float64` | 后端代码字段 avgFirstReplyMinutes；含义由所属响应模型和接口业务决定 |

### SupportAuditLog

```json
{
  "id": "uint64",
  "conversationId": "uint64",
  "actorAddress": "string",
  "actionType": "string",
  "beforeJson": "string",
  "afterJson": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `id` | `uint64` | 后端代码字段 id；含义由所属响应模型和接口业务决定 |
| `conversationId` | `uint64` | 后端代码字段 conversationId；含义由所属响应模型和接口业务决定 |
| `actorAddress` | `string` | 合约或钱包的 EVM 地址 |
| `actionType` | `string` | 客服会话审计动作机器码；枚举：`create_conversation`=用户创建客服会话；`send_user_message`=用户发送消息；`send_admin_message`=管理员回复消息；`assign_conversation`=分配处理人；`update_status`=更新会话状态 |
| `beforeJson` | `string` | 后端代码字段 beforeJson；含义由所属响应模型和接口业务决定 |
| `afterJson` | `string` | 后端代码字段 afterJson；含义由所属响应模型和接口业务决定 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### TestnetOverviewResponse

```json
{
  "network": "string",
  "chainId": "int64",
  "deployment": "DeploymentSummary",
  "stats": "TestnetStats",
  "indexing": "ProjectIndexingStatus",
  "worker": "WorkerRunSummary",
  "smoke": "SmokeRunSummary",
  "rpc": "RPCHealthSummary",
  "latestProject": "*ProjectListItem",
  "recentProjects": "[]ProjectListItem",
  "recentEvents": "[]ProjectHistoryItem",
  "operationalNotes": "[]string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `network` | `string` | 后端代码字段 network；含义由所属响应模型和接口业务决定 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `deployment` | `DeploymentSummary` | 后端代码字段 deployment；含义由所属响应模型和接口业务决定 |
| `stats` | `TestnetStats` | 后端代码字段 stats；含义由所属响应模型和接口业务决定 |
| `indexing` | `ProjectIndexingStatus` | 后端代码字段 indexing；含义由所属响应模型和接口业务决定 |
| `worker` | `WorkerRunSummary` | 后端代码字段 worker；含义由所属响应模型和接口业务决定 |
| `smoke` | `SmokeRunSummary` | 后端代码字段 smoke；含义由所属响应模型和接口业务决定 |
| `rpc` | `RPCHealthSummary` | 后端代码字段 rpc；含义由所属响应模型和接口业务决定 |
| `latestProject` | `*ProjectListItem` | 后端代码字段 latestProject；含义由所属响应模型和接口业务决定 |
| `recentProjects` | `[]ProjectListItem` | 后端代码字段 recentProjects；含义由所属响应模型和接口业务决定 |
| `recentEvents` | `[]ProjectHistoryItem` | 后端代码字段 recentEvents；含义由所属响应模型和接口业务决定 |
| `operationalNotes` | `[]string` | 后端代码字段 operationalNotes；含义由所属响应模型和接口业务决定 |

### DeploymentSummary

```json
{
  "platformAddress": "string",
  "stableTokenAddress": "string",
  "platformMultisig": "string",
  "projectImplementation": "string",
  "deploymentProfile": "string",
  "latestProjectAddress": "string",
  "latestProjectSymbol": "string",
  "deployedAt": "string",
  "scenarioRanAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `platformAddress` | `string` | 合约或钱包的 EVM 地址 |
| `stableTokenAddress` | `string` | 当前网络配置的稳定币合约地址 |
| `platformMultisig` | `string` | 后端代码字段 platformMultisig；含义由所属响应模型和接口业务决定 |
| `projectImplementation` | `string` | 后端代码字段 projectImplementation；含义由所属响应模型和接口业务决定 |
| `deploymentProfile` | `string` | 后端代码字段 deploymentProfile；含义由所属响应模型和接口业务决定 |
| `latestProjectAddress` | `string` | 合约或钱包的 EVM 地址 |
| `latestProjectSymbol` | `string` | 后端代码字段 latestProjectSymbol；含义由所属响应模型和接口业务决定 |
| `deployedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `scenarioRanAt` | `string` | 时间值；具体格式以该接口返回类型为准 |

### TestnetStats

```json
{
  "projectCount": "int",
  "tradingCount": "int",
  "graduatedCount": "int",
  "eventsIndexed": "uint64",
  "transactions": "int",
  "messages": "int",
  "lastIndexedBlock": "uint64"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectCount` | `int` | 数量统计 |
| `tradingCount` | `int` | 数量统计 |
| `graduatedCount` | `int` | 数量统计 |
| `eventsIndexed` | `uint64` | 布尔状态标记 |
| `transactions` | `int` | 后端代码字段 transactions；含义由所属响应模型和接口业务决定 |
| `messages` | `int` | 后端代码字段 messages；含义由所属响应模型和接口业务决定 |
| `lastIndexedBlock` | `uint64` | 布尔状态标记 |

### WorkerRunSummary

```json
{
  "runId": "string",
  "status": "string",
  "workerName": "string",
  "lastRunAt": "string",
  "latestSeenBlock": "uint64",
  "lastScannedBlock": "uint64",
  "blockLag": "uint64",
  "eventLag": "uint64",
  "eventsIndexed": "uint64",
  "confirmationWindow": "uint64",
  "lastSuccessAt": "string",
  "lastErrorAt": "string",
  "errorMessage": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `runId` | `string` | 后端代码字段 runId；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `workerName` | `string` | 后端代码字段 workerName；含义由所属响应模型和接口业务决定 |
| `lastRunAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `latestSeenBlock` | `uint64` | 后端代码字段 latestSeenBlock；含义由所属响应模型和接口业务决定 |
| `lastScannedBlock` | `uint64` | 后端代码字段 lastScannedBlock；含义由所属响应模型和接口业务决定 |
| `blockLag` | `uint64` | 后端代码字段 blockLag；含义由所属响应模型和接口业务决定 |
| `eventLag` | `uint64` | 后端代码字段 eventLag；含义由所属响应模型和接口业务决定 |
| `eventsIndexed` | `uint64` | 布尔状态标记 |
| `confirmationWindow` | `uint64` | 后端代码字段 confirmationWindow；含义由所属响应模型和接口业务决定 |
| `lastSuccessAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `lastErrorAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `errorMessage` | `string` | 后端代码字段 errorMessage；含义由所属响应模型和接口业务决定 |

### SmokeRunSummary

```json
{
  "runId": "string",
  "status": "string",
  "currentStage": "string",
  "projectAddress": "string",
  "projectSymbol": "string",
  "startedAt": "string",
  "finishedAt": "string",
  "reportPath": "string",
  "summaryJson": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `runId` | `string` | 后端代码字段 runId；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `currentStage` | `string` | 后端代码字段 currentStage；含义由所属响应模型和接口业务决定 |
| `projectAddress` | `string` | 项目合约地址 |
| `projectSymbol` | `string` | 后端代码字段 projectSymbol；含义由所属响应模型和接口业务决定 |
| `startedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `finishedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `reportPath` | `string` | 后端代码字段 reportPath；含义由所属响应模型和接口业务决定 |
| `summaryJson` | `string` | 后端代码字段 summaryJson；含义由所属响应模型和接口业务决定 |

### RPCHealthSummary

```json
{
  "status": "string",
  "rpcUrl": "string",
  "latestBlock": "uint64",
  "latencyMs": "int",
  "sampledAt": "string",
  "errorMessage": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `rpcUrl` | `string` | 可公开给前端的当前网络 RPC；后端内部使用的付费 RPC 及密钥绝不通过此字段返回 |
| `latestBlock` | `uint64` | 后端代码字段 latestBlock；含义由所属响应模型和接口业务决定 |
| `latencyMs` | `int` | 后端代码字段 latencyMs；含义由所属响应模型和接口业务决定 |
| `sampledAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `errorMessage` | `string` | 后端代码字段 errorMessage；含义由所属响应模型和接口业务决定 |

### ProjectRuntimeResponse

```json
{
  "project": "ProjectDetail",
  "indexing": "ProjectIndexingStatus",
  "pool": "ProjectPoolResponse",
  "governance": "ProjectGovernanceSummary",
  "fees": "ProjectFeesResponse",
  "incomeSummary": "FeeSummary",
  "operationStats": "OperationStats",
  "userPosition": "*UserPosition",
  "recentEvents": "[]ProjectHistoryItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `project` | `ProjectDetail` | 后端代码字段 project；含义由所属响应模型和接口业务决定 |
| `indexing` | `ProjectIndexingStatus` | 后端代码字段 indexing；含义由所属响应模型和接口业务决定 |
| `pool` | `ProjectPoolResponse` | 后端代码字段 pool；含义由所属响应模型和接口业务决定 |
| `governance` | `ProjectGovernanceSummary` | 后端代码字段 governance；含义由所属响应模型和接口业务决定 |
| `fees` | `ProjectFeesResponse` | 后端代码字段 fees；含义由所属响应模型和接口业务决定 |
| `incomeSummary` | `FeeSummary` | 后端代码字段 incomeSummary；含义由所属响应模型和接口业务决定 |
| `operationStats` | `OperationStats` | 后端代码字段 operationStats；含义由所属响应模型和接口业务决定 |
| `userPosition` | `*UserPosition` | 后端代码字段 userPosition；含义由所属响应模型和接口业务决定 |
| `recentEvents` | `[]ProjectHistoryItem` | 后端代码字段 recentEvents；含义由所属响应模型和接口业务决定 |

### ProjectPoolResponse

```json
{
  "projectAddress": "string",
  "graduatedPoolId": "string",
  "graduationAdapter": "string",
  "graduationHookOrLock": "string",
  "graduationAnnounceTime": "uint64",
  "graduationExecuteAt": "uint64",
  "graduationNotice": "uint64",
  "lpNodeBps": "string",
  "lpCreatorBps": "string",
  "lpPlatformBps": "string",
  "graduatedAt": "uint64",
  "blockNumber": "uint64",
  "dataSource": "string",
  "recentAnnouncements": "[]GraduationAnnouncement",
  "recentPoolEvents": "[]ProjectHistoryItem",
  "uniswapV4Position": "*ExternalPoolPosition",
  "collectPreview": "*LPFeeCollectPreview"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `graduatedPoolId` | `string` | 后端代码字段 graduatedPoolId；含义由所属响应模型和接口业务决定 |
| `graduationAdapter` | `string` | 毕业 Adapter 地址；enabled=true 时必须等于 Platform.trustedGraduationAdapter() |
| `graduationHookOrLock` | `string` | 后端代码字段 graduationHookOrLock；含义由所属响应模型和接口业务决定 |
| `graduationAnnounceTime` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `graduationExecuteAt` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `graduationNotice` | `uint64` | 毕业公告期秒数；triggerGraduation 后须等待该时长才可 executeGraduation |
| `lpNodeBps` | `string` | 万分比整数，10000 表示 100% |
| `lpCreatorBps` | `string` | 万分比整数，10000 表示 100% |
| `lpPlatformBps` | `string` | 万分比整数，10000 表示 100% |
| `graduatedAt` | `uint64` | 时间值；具体格式以该接口返回类型为准 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `dataSource` | `string` | 后端代码字段 dataSource；含义由所属响应模型和接口业务决定 |
| `recentAnnouncements` | `[]GraduationAnnouncement` | 后端代码字段 recentAnnouncements；含义由所属响应模型和接口业务决定 |
| `recentPoolEvents` | `[]ProjectHistoryItem` | 后端代码字段 recentPoolEvents；含义由所属响应模型和接口业务决定 |
| `uniswapV4Position` | `*ExternalPoolPosition` | 后端代码字段 uniswapV4Position；含义由所属响应模型和接口业务决定 |
| `collectPreview` | `*LPFeeCollectPreview` | 外部流动性池手续费的链上收取模拟快照；用于决定是否允许发起 collect_and_distribute |

### ProjectGovernanceSummary

```json
{
  "projectAddress": "string",
  "proposalCount": "int",
  "activeCount": "int",
  "executedCount": "int",
  "currentUser": "string",
  "currentUserCanPropose": "bool",
  "currentUserVotingPower": "string",
  "currentUserVotingPowerDisplay": "string",
  "totalVotingPower": "string",
  "totalVotingPowerDisplay": "string",
  "nextProposalId": "uint64",
  "canProposeReason": "string",
  "allowedParamIds": "[]uint8",
  "recommendedParamIds": "[]uint8",
  "unavailableParamIds": "[]uint8",
  "proposals": "[]GovernanceProposalItem"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `proposalCount` | `int` | 数量统计 |
| `activeCount` | `int` | 数量统计 |
| `executedCount` | `int` | 数量统计 |
| `currentUser` | `string` | 后端代码字段 currentUser；含义由所属响应模型和接口业务决定 |
| `currentUserCanPropose` | `bool` | 后端代码字段 currentUserCanPropose；含义由所属响应模型和接口业务决定 |
| `currentUserVotingPower` | `string` | 后端代码字段 currentUserVotingPower；含义由所属响应模型和接口业务决定 |
| `currentUserVotingPowerDisplay` | `string` | 后端代码字段 currentUserVotingPowerDisplay；含义由所属响应模型和接口业务决定 |
| `totalVotingPower` | `string` | 后端代码字段 totalVotingPower；含义由所属响应模型和接口业务决定 |
| `totalVotingPowerDisplay` | `string` | 后端代码字段 totalVotingPowerDisplay；含义由所属响应模型和接口业务决定 |
| `nextProposalId` | `uint64` | 后端代码字段 nextProposalId；含义由所属响应模型和接口业务决定 |
| `canProposeReason` | `string` | 后端代码字段 canProposeReason；含义由所属响应模型和接口业务决定 |
| `allowedParamIds` | `[]uint8` | 后端代码字段 allowedParamIds；含义由所属响应模型和接口业务决定 |
| `recommendedParamIds` | `[]uint8` | 后端代码字段 recommendedParamIds；含义由所属响应模型和接口业务决定 |
| `unavailableParamIds` | `[]uint8` | 后端代码字段 unavailableParamIds；含义由所属响应模型和接口业务决定 |
| `proposals` | `[]GovernanceProposalItem` | 后端代码字段 proposals；含义由所属响应模型和接口业务决定 |

### SmokeRunRegisterRequest

```json
{
  "runId": "string",
  "network": "string",
  "chainId": "int64",
  "deploymentProfile": "string",
  "platformAddress": "string",
  "projectAddress": "string",
  "projectSymbol": "string",
  "status": "string",
  "currentStage": "string",
  "startedAt": "string",
  "finishedAt": "string",
  "summaryJson": "string",
  "reportPath": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `runId` | `string` | 后端代码字段 runId；含义由所属响应模型和接口业务决定 |
| `network` | `string` | 后端代码字段 network；含义由所属响应模型和接口业务决定 |
| `chainId` | `int64` | 目标 EVM 网络 Chain ID |
| `deploymentProfile` | `string` | 后端代码字段 deploymentProfile；含义由所属响应模型和接口业务决定 |
| `platformAddress` | `string` | 合约或钱包的 EVM 地址 |
| `projectAddress` | `string` | 项目合约地址 |
| `projectSymbol` | `string` | 后端代码字段 projectSymbol；含义由所属响应模型和接口业务决定 |
| `status` | `string` | 后端代码字段 status；含义由所属响应模型和接口业务决定 |
| `currentStage` | `string` | 后端代码字段 currentStage；含义由所属响应模型和接口业务决定 |
| `startedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `finishedAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
| `summaryJson` | `string` | 后端代码字段 summaryJson；含义由所属响应模型和接口业务决定 |
| `reportPath` | `string` | 后端代码字段 reportPath；含义由所属响应模型和接口业务决定 |

### UserTransactionItem

```json
{
  "txHash": "string",
  "txType": "string",
  "projectAddress": "string",
  "tokenSymbol": "string",
  "amountStable": "string",
  "amountToken": "string",
  "price": "string",
  "priceSource": "string",
  "counterparty": "string",
  "blockNumber": "uint64",
  "blockTime": "string",
  "summary": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `txHash` | `string` | 链上交易哈希 |
| `txType` | `string` | 用户交易机器码；完整枚举：`create_project`=创建项目；`subscribe_node`=认购节点；`buy`=买入；`sell`=卖出；`pledge_buy`=质押买入；`unpledge`=解除质押；`lock_node`=锁定节点；`claim_unlocked`=领取解锁代币；`claim_alt_token`=领取附属代币；`claim_fees`=领取手续费；`claim_taxes`=领取盈利税；`claim_refund`=领取退款；`transfer_in`=转入；`transfer_out`=转出 |
| `projectAddress` | `string` | 项目合约地址 |
| `tokenSymbol` | `string` | 项目 Token 符号；后端会先转成大写，最终值长度 1-10，首字符必须是大写字母，其余只能是大写字母或数字 |
| `amountStable` | `string` | 非负稳定币数量字符串；buy/pledge_buy/unpledge/subscribe_node 表示支出，sell/claim_fees/claim_taxes 表示收入，前端按 txType 添加正负号 |
| `amountToken` | `string` | 非负项目 Token 数量字符串；buy/pledge_buy/unpledge 表示获得，sell 表示支出；pledge_buy 仅表示本次新购 Token，不是投入质押的 Token |
| `price` | `string` | 事件记录的链上价格；手续费和税费会使 amountStable/amountToken 与 price 的简单乘积不同，不得据此重算成交总额 |
| `priceSource` | `string` | price 的来源标识，例如 event.currentPrice 或 derived.amounts |
| `counterparty` | `string` | 后端代码字段 counterparty；含义由所属响应模型和接口业务决定 |
| `blockNumber` | `uint64` | 后端代码字段 blockNumber；含义由所属响应模型和接口业务决定 |
| `blockTime` | `string` | 链上区块时间，RFC3339/ISO-8601 北京时间（+08:00） |
| `summary` | `string` | 后台动态生成的中文展示摘要，可直接展示且不会回显 txType 英文机器码；覆盖创建项目、买卖、质押、节点认购、领取、退款、转账及历史借贷类型，未知类型显示“其他链上操作”；金额方向仍以 txType 规则为准 |

### UserTransactionQuery

```json
{
  "projectAddress": "string",
  "txType": "string",
  "startBlock": "uint64",
  "endBlock": "uint64",
  "limit": "int",
  "offset": "int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `projectAddress` | `string` | 项目合约地址 |
| `txType` | `string` | 后端代码字段 txType；含义由所属响应模型和接口业务决定 |
| `startBlock` | `uint64` | 后端代码字段 startBlock；含义由所属响应模型和接口业务决定 |
| `endBlock` | `uint64` | 后端代码字段 endBlock；含义由所属响应模型和接口业务决定 |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |

### UserTransactionHistoryResponse

```json
{
  "items": "[]UserTransactionItem",
  "total": "uint64",
  "limit": "int",
  "offset": "int"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `items` | `[]UserTransactionItem` | 当前页数据列表；空结果返回 [] |
| `total` | `uint64` | 符合条件的总记录数 |
| `limit` | `int` | 本次最多返回的记录数 |
| `offset` | `int` | 分页起始偏移量 |

### TransferRiskMetadata

```json
{
  "warningTitle": "string",
  "warningBody": "string",
  "costBasisReset": "bool",
  "profitTaxImpact": "string",
  "enabled": "bool"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `warningTitle` | `string` | 后端代码字段 warningTitle；含义由所属响应模型和接口业务决定 |
| `warningBody` | `string` | 后端代码字段 warningBody；含义由所属响应模型和接口业务决定 |
| `costBasisReset` | `bool` | 后端代码字段 costBasisReset；含义由所属响应模型和接口业务决定 |
| `profitTaxImpact` | `string` | 后端代码字段 profitTaxImpact；含义由所属响应模型和接口业务决定 |
| `enabled` | `bool` | 档位是否启用；true 时执行全部 Registry/codehash/Adapter/参数约束，false 时链上清除 curveCodeHashByTier |

### PlatformConfigHistoryItem

```json
{
  "configKey": "string",
  "oldValueJson": "string",
  "newValueJson": "string",
  "operator": "string",
  "action": "string",
  "reason": "string",
  "createdAt": "string"
}
```

| 字段 | Go/JSON 类型 | 含义与逻辑 |
|---|---|---|
| `configKey` | `string` | 版本化后端配置键；档位积分倍率使用 points.scale_tier_multiplier.<bytes32 scaleTierId>，项目覆盖使用 points.project_multiplier.<projectAddress> |
| `oldValueJson` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `newValueJson` | `string` | 金额或数量字段；按 JSON 中声明的类型传输 |
| `operator` | `string` | 后端代码字段 operator；含义由所属响应模型和接口业务决定 |
| `action` | `string` | 后端代码字段 action；含义由所属响应模型和接口业务决定 |
| `reason` | `string` | 本次操作原因，用于审计追溯 |
| `createdAt` | `string` | 时间值；具体格式以该接口返回类型为准 |
