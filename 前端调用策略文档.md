# SeedX Sepolia 20260726 前端调用策略文档

> **当前协议覆盖说明（最高优先级）**：运行时唯一合约基线是 `SeedX_Sepolia_Testnet_Handoff_20260726_1a7f3ee7`。网络为 Ethereum Sepolia（`chainId=11155111`），Stable 为 `USDGTest`（6 decimals），项目 Token 为 18 decimals。所有 Stable 输入、展示和滑点计算必须使用接口返回的 `stableTokenDecimals`。测试服返回 `graduationEnabled=true` 与 `bridgeEnabled=true`；桥接是独立启用的 Relay 测试网功能，不属于 SeedX 合约。详见 [Sepolia20260726后台合约对齐说明.md](./Sepolia20260726后台合约对齐说明.md)。

本文面向运行部署 `seedx_sepolia_20260726_1a7f3ee7`。合约代码、ABI、部署记录和运行时配置均以 `SeedX_Sepolia_Testnet_Handoff_20260726_1a7f3ee7` 为唯一事实源。旧 BSC 地址、ABI、配置缓存和旧测试网交易参数必须清除，不得用于签名或授权。

## 1. 基础规则

- Ethereum Sepolia `chainId = 11155111`，原生 Gas 为 `ETH`。
- 普通响应为 `{code,message,data}`；列表统一读取 `data.items / total / limit / offset`。
- 金额使用十进制字符串，链上整数单位只出现在明确标注为 `Wei` 的字段中。
- 写操作必须先使用登录钱包请求 tx-params，再根据 `requiredApprovals` 授权，最后发送返回的 `target/value/calldata`。每个 `requiredApprovals[].spender` 必须与同一响应的 `target` 完全一致，不一致时前端必须拒绝唤起授权。
- 只有 `preflight.readyToSubmit = true` 才允许唤起钱包发送交易。
- `action-tx-params` 会按链上状态提前拦截：`subscribe_node/mark_launch_failed` 仅 `node_sale`，`claim_refund` 仅 `launch_failed`，`buy/sell/pledge_buy` 仅 `trading`，`unpledge` 仅 `trading` 或 `graduating`，`lock_node` 仅 `trading` 或 `graduated`。后端模拟仍是最终预检，前端不得绕过 `preflight`。
- 交易回执成功不等于后台已完成索引；需要继续轮询交易状态或项目 indexing-status。
- SIWE 消息必须包含并校验 `Version=1 / Chain ID=11155111 / Issued At / Expiration Time`。登录成功后后端只通过 `Set-Cookie` 下发 `__Host-seedx_session`，属性固定为 `Path=/; HttpOnly; Secure; SameSite=Strict`；登录 JSON 只返回 `address/expiresAt`，不返回 token。普通钱包会话 12 小时失效，拥有任一后台角色的钱包会话 30 分钟失效；显式注销、角色授予或撤销都会使相关会话失效，401 时必须重新钱包签名。
- Logo 上传只接受 JPEG、PNG、GIF、WebP 且最大 2 MiB；SVG 永久拒绝，不存在可由配置重新开启的入口。

### 1.1 Cookie 会话与 Axios 安全边界

浏览器端必须使用唯一的共享 Axios 实例，不允许各页面自行创建动态目标实例：

```js
const TRUSTED_API_BASE_URL = import.meta.env.VITE_API_BASE_URL || window.location.origin
const TRUSTED_API_ORIGIN = new URL(TRUSTED_API_BASE_URL).origin

const api = axios.create({
  baseURL: TRUSTED_API_BASE_URL,
  withCredentials: true,
  timeout: 30000,
  headers: { 'X-SeedX-Client': 'web' }
})
```

- `baseURL` 必须是预先配置的固定 HTTP(S) origin，不能来自路由参数、接口响应、URL query 或用户输入；推荐前端与 `/api` 使用同源反向代理。
- 请求拦截器必须用 `new URL(config.url, TRUSTED_API_BASE_URL)` 计算最终地址；只允许 `origin === TRUSTED_API_ORIGIN` 且 path 为 `/api` 或 `/api/*`。不满足时先删除大小写任意形式的 `Authorization`、设置 `withCredentials=false`，再直接拒绝请求。
- 浏览器不再保存、读取或拼接 token；禁止使用 `localStorage/sessionStorage/indexedDB/document.cookie/URL/query/hash` 保存会话凭证，也禁止从登录响应读取 `data.token`。
- Cookie 由浏览器自动附带。所有 Cookie 认证的 `POST/PATCH/PUT/DELETE` 必须发送 `X-SeedX-Client: web`，后端会同时核验 `Origin`；GET/HEAD/OPTIONS 不要求该 Header。
- 后端仍接受 `Authorization: Bearer` 作为可信非浏览器工具的兼容输入，但浏览器调用不得依赖该路径，更不能把 Bearer 发往非可信 origin。
- `Secure + SameSite=Strict` 要求正式/测试 Web 入口使用 HTTPS，并确保前端与 API 为同站点；跨站前端即使配置 CORS 和 `withCredentials` 也不会携带 Strict Cookie。
- 401 表示 Cookie 缺失、过期或已撤销：清理本地登录 UI 状态并重新 SIWE；403 表示会话存在但钱包或 RBAC 权限不足，不应自动重复签名。

### 1.2 Relay 测试网跨链（Sepolia ↔ Base Sepolia）

跨链桥不调用 SeedX 合约，也不与买入、认购或质押合并为一笔原子交易。当前只允许 Sepolia `11155111` 与 Base Sepolia `84532` 的原生 ETH 双向测试路线，源币与目标币地址均为 `0x0000000000000000000000000000000000000000`，金额使用 wei。测试币没有价值。

测试网调用顺序：

1. 使用登录 Cookie 调用 `POST /api/bridge/quote`；浏览器必须带 `X-SeedX-Client: web`。
2. 先读取 `GET /api/bridge/config`，仅使用返回的双向路线；`amount` 传正整数 wei，`recipient` 省略或等于当前登录钱包。
3. 展示 `quote.details.currencyIn/currencyOut` 的源链、目标链、输入量、最小输出量、费用、预计时间、收款地址和过期提示；不得把展示金额替换为静态汇率。
4. 按 Relay 返回的 `quote.steps[].items[]` 顺序执行钱包交易；每项重新检查 `data.from === 当前钱包` 且 `data.chainId === 所选源链`，目标链/目标币以 `quote.details.currencyOut.currency` 为准。禁止自行拼 Router calldata，也禁止默认无限授权。
5. 每次源链交易确认后调用 `POST /api/bridge/orders/:orderId/transactions` 记录 `sourceTxHash`；目标链到账后记录 `destinationTxHash`。
6. 轮询 `GET /api/bridge/status/:requestId`，其中 `requestId` 可使用 `orderId` 或 Relay requestId。状态枚举为 `quoted`、`waiting_for_approval`、`waiting_for_deposit`、`source_confirmed`、`processing`、`success`、`refunded`、`failed`。
7. `success` 只表示 Relay 测试路线完成，不代表任何 SeedX 操作完成，也不应自动发起买入、认购或质押。

桥接失败或退款只展示 Relay 返回的状态、链和交易哈希；平台不代签、不托管、不垫付，也不把桥接订单计入 SeedX 买卖、积分、项目成交量或手续费流水。

## 2. 初始化配置

页面启动先请求 `GET /api/config/public`。前端不得硬编码以下内容：

- Platform、USDGTest、Multisig、测试专用 Uniswap v4 Adapter/Hook 地址。
- `platformAdmin / creationFeeAdmin / platformPayout` 三种角色。
- 启用的 `scaleTiers` 及其节点数、节点价格、`nodeSaleDuration`、治理时间和毕业时间配置。
- `projectRules/sourceBundleHash`，全局 `dependencyIntegrity`，以及每个档位的链上 `version/curveParamsHash/curvePointsHash/curveCodeHash/curveRuntimeCodeHash` 和 Registry 状态。

当前交接包启用的档位为 `sepolia-uniswap-v4-debug`。建项页面只展示接口返回且 `enabled = true` 的档位，不得硬编码档位数量或 ID。

只有 `dependencyIntegrity.verified=true`、`dependencyIntegrity.adapterDependencies.verified=true`，且所选启用档位的 `curveFactoryCreated/curveRegistered/dependencyIntegrityVerified` 均为 true、`curveCodeHash == curveRuntimeCodeHash` 时才允许写操作。若 `protocolVersion`、合约地址、`sourceBundleHash`、部署起始块或任何完整性字段不一致，应清空配置缓存、阻止全部写操作并提示部署配置异常，不能静默降级。

## 3. 创建项目

调用顺序：

1. `POST /api/projects/preview`
2. `POST /api/projects/create-tx-params`
3. 按 `requiredApprovals` 检查或提交 USDGTest 授权
4. 再次请求 tx-params，确认 `preflight.readyToSubmit = true`
5. 钱包发送 `createProject`
6. 轮询 `GET /api/transactions/:hash/status`
7. 等待 `indexed = true` 后刷新项目列表和详情

创建请求没有 `nodeSaleDuration`。该值由档位决定，只能从 `scaleTierSnapshot.nodeSaleDuration` 展示。

`preview` 与 `create-tx-params` 都返回 `normalizedConfig`。这是后端应用档位默认值、Token 符号大写、金额规范化、零地址归一化和 Alt Token decimals 后的最终配置；确认页必须展示该对象，不能直接回显请求表单。`normalizedConfig.altToken` 固定包含 `enabled/address/symbol/decimals/amountPerNode/amountPerNodeWei`，未启用时地址为零地址、两个数量为 `0`。

`scaleTierSnapshot` 的 `curveCodeHash/curveRuntimeCodeHash/curveFactoryCreated/curveRegistered/curveProductionTable/dependencyIntegrityVerified/integritySnapshotBlock` 全部是后端只读核验证据，不属于创建请求字段。

创建请求也不能提交 `maxCreationFee/expectedTierVersion/deadline`。这三项由后台在同一 `snapshotBlock` 读取当前创建费、档位版本和区块时间后自动写入 23 字段 calldata；响应中的 `maxCreationFeeWei/expectedTierVersion/deadline` 仅用于确认本次签名边界。`deadline` 默认为快照块时间后 30 分钟，不能缓存 tx-params 跨时间复用。

`tokenSymbol` 先转大写后校验：长度 1 到 10，首字符必须为 `A-Z`，其余只能为 `A-Z/0-9`。

创建参数还必须满足合约边界：`tokenName` 按 UTF-8 字节长度为 1 到 32；`minSingleBuy >= 1`；买/卖手续费为 `100..50000 ppm` 且按 `100 ppm` 步进；盈利税为 `0..300000 ppm` 且按 `100 ppm` 步进。手续费和盈利税两套四向分账都必须各自合计 `10000 bps`、floor 不低于 `5000 bps`、issuer + platform 不超过 `2500 bps`；手续费非 floor 三项归一化后的 LP creator + platform 还必须至少为 `1000 bps`。前端应即时提示，后台 preview 和 create-tx-params 会重复校验，失败时不得继续请求钱包。

预览响应 `projection` 固定返回 Requirement 14 的 13 项测算字段：节点数、节点价、节点募集总额、开盘供应量，以及 10× 价格/供应量/市值/新增 Stable/Floor/Floor Pool/Market Pool/F-P 比例/最大质押模型下外部 Stable。`curveTableVerified=true` 表示后台已在 `curveSnapshotBlock` 对 CurveTable 的 `rawPriceAtSupply / buyCost / effectiveAreaBetween` 完成同区块核验；`projectionModel=curve_table_principal_v1` 只表示曲线本金测算，不包含项目可配置交易手续费，实际可执行金额仍必须重新调用报价接口。

遇到 `CreationFeeSlippage`、`ScaleTierChanged` 或 `TransactionExpired` 时，丢弃旧 calldata，重新请求 preview 与 create-tx-params，并重新展示费用/档位让用户确认；不得只重发原交易。

未启用 Alt Token 时，`altTokenAddress` 可传空字符串或零地址，`altTokenPerNode` 可传空字符串、`"0"` 或 `"0.0"`；后端会统一编码为零地址和 0。

如果配置 Alt Token：

- `altTokenAddress` 与 `altTokenPerNode` 必须同时有效。
- 后端按 Alt Token 实际 `decimals` 编码，不按 18 位假设。
- 前端使用 `normalizedConfig.altToken.amountPerNode` 展示可读数量，使用 `amountPerNodeWei` 核对 calldata，不自行按 18 位换算。
- 节点认购可能同时需要 USDGTest 和 Alt Token 两笔授权。
- 任一余额不足时，`balanceChecks[].sufficient = false`，前端禁止继续授权或发送。

## 4. 统一写交易流程

所有 tx-params 响应都应按以下顺序处理：

1. 若 `preflight.readyToSubmit = false`，先读取 `preflight.reasonCode` 决定流程，并用 `preflight.reason` 作为兜底展示文案；不要只显示“预检未通过”。
2. 检查 `preflight.walletType`。要求 EOA 的操作遇到 `contract` 必须阻止。
3. 展示 `balanceChecks` 中的代币符号、decimals、当前余额和差额。
4. 先本地校验 `requiredApprovals[].spender === target`，再对 `approvalRequired = true` 的项按 `amountWei` 发送精确额度 ERC-20 `approve`；任一地址不匹配都必须中止。
5. 授权确认后重新请求 tx-params，不复用旧 calldata 或旧模拟结果。
6. 仅当 `approvalsComplete = true`、`simulationStatus = passed` 且 `readyToSubmit = true` 时发送交易。
7. `estimatedGas` 用于钱包 gas 提示，不作为成功保证。

`reasonCode` 与处理策略：

| `reasonCode` | 含义 | 前端动作 |
|---|---|---|
| `approval_required` | ERC-20 授权额度不足 | 按 `requiredApprovals` 发起精确授权，确认后重新请求 tx-params |
| `insufficient_token_balance` | USDT、项目 Token 或 Alt Token 余额不足 | 根据 `balanceChecks[].shortfallWei` 提示补充对应资产 |
| `insufficient_native_balance` | BNB 不足以支付预计 Gas | 提示补充 BNB，之后重新请求 tx-params |
| `contract_wallet_not_allowed` | 当前操作受 `onlyEOA` 限制 | 切换 EOA 钱包 |
| `transaction_simulation_failed` | `eth_call` 模拟回滚 | 展示 `reason` 中解析后的合约错误，刷新项目和报价后重试 |
| `gas_estimation_failed` | 模拟通过但 Gas 估算失败 | 展示 `reason`，刷新后重试，不发送交易 |
| `preflight_not_completed` | RPC 等预检步骤尚未完成 | 允许用户重试请求，不发送交易 |
| `ready` | 预检通过 | 仅此状态且 `readyToSubmit=true` 时唤起钱包 |

同时检查 `nativeBalanceWei / estimatedGasCostWei / nativeBalanceSufficient`。代币余额和 allowance 全部满足但原生 BNB 不足时，`readyToSubmit` 仍为 false，前端应提示补充测试网 BNB，而不是继续唤起钱包。

后端使用当前登录钱包作为 `eth_call.from`，因此前端不要用另一个钱包发送已经生成的 calldata。

## 5. 项目状态与按钮

以接口的实时 `projectState` 为准：

| 状态 | 前端主要操作 |
|---|---|
| `node_sale` | 节点认购、到期失败标记 |
| `launch_failed` | 领取退款 |
| `trading` | 买卖、质押、节点锁定、治理、触发毕业 |
| `graduating` | 展示毕业公告和倒计时，条件满足后执行毕业 |
| `graduated` | 治理、查看外部池仓位；按预览结果收取并分配 LP 手续费 |

按钮可用性必须随详情或 tx-params 模拟结果刷新，不能只根据页面进入时的状态缓存。

## 6. 节点认购

- 认购上限来自项目 `maxNodePurchaseBps` 和当前地址已认购数量。
- 节点售价、`nodeTargetStable`、`nodeTokenUnit`、`nodeSaleDeadline` 均以项目详情或链上状态为准；认购授权金额与支付金额以 `onChainState.nodePriceStable * nodeAmount` 为最终口径。详情顶层与链上快照短暂不一致时，必须优先使用链上快照，不得按旧数据库展示精度自行换算。
- `nodeFundraiseStable` 表示已售节点对应的实际募集额，不是目标募集额。
- Alt Token 项目必须同时展示 Alt Token 每节点数量、decimals、余额和 allowance。

## 7. 交易与仓位

- 买入、卖出、质押买入先调用报价接口，再请求 action tx-params。
- 买卖支持四向报价：给定 USDGTest 买 Token 使用 `POST /api/quotes/preview-buy`；给定目标 Token 反算 USDGTest 使用 `POST /api/quotes/preview-buy-by-token`；给定 Token 卖出算 USDGTest 使用 `POST /api/quotes/preview-sell`；给定目标净到账 USDGTest 反算 Token 使用 `POST /api/quotes/preview-sell-by-cash`。
- `preview-buy-by-token` 返回的 `cashIn` 是满足目标 Token 的最小预计 USDGTest 支付额；请求 action tx-params 时使用该 `cashIn`，并基于返回 `tokenOut` 计算正数 `minTokenOut`。整数取整可能使实际 `tokenOut` 略高于 `requestedTokenOut`。
- `preview-sell-by-cash` 必须传当前钱包 `userAddress`，返回的 `tokenIn` 已计入该用户成本基数、卖出手续费和利润税；请求 action tx-params 时使用该 `tokenIn`，并基于返回 `cashOut` 计算正数 `minCashOut`。不得用其他钱包发送对应交易。
- 四种报价都只对各自 `snapshotBlock` 有效。输入模式切换、钱包切换或新区块状态变化后必须重新报价；反向目标报价也受项目状态、钱包余额、储备和当前总供应量 1% 单笔上限约束。
- 用户交易历史 `GET /api/users/:address/transactions` 返回的 `amountStable/amountToken` 始终是非负十进制字符串，不携带方向。前端必须按 `txType` 添加展示符号，不能直接把接口值整体取负。

| `txType` | Token 展示 | Stable 展示 | 字段语义 |
|---|---|---|---|
| `buy` | `+amountToken` | `-amountStable` | 获得 Token，支付 USDGTest |
| `sell` | `-amountToken` | `+amountStable` | 卖出 Token，获得税费扣除后的 USDGTest |
| `pledge_buy` | `+amountToken` | `-amountStable` | `amountToken` 是本次新购 Token，`amountStable` 是用户自付；投入质押数量不在该条目的 amountToken 中 |
| `unpledge` | `+amountToken` | `-amountStable` | 解锁 Token，偿还稳定币债务 |
| `subscribe_node` | 可单独显示节点分配量 | `-amountStable` | 支付 USDGTest 节点认购款；分配 Token 受锁仓规则约束 |
| `claim_fees/claim_taxes` | 不适用 | `+amountStable` | 领取 USDGTest 收入 |

交易列表可以直接展示后端 `summary`。摘要由后台查询时动态生成：买入为“买入 {amountToken} {symbol}，花费 {amountStable} USDGTest”，卖出为“卖出 {amountToken} {symbol}，获得 {amountStable} USDGTest”，质押买入为“质押加仓获得 {amountToken} {symbol}，自付 {amountStable} USDGTest”。因此修正文案后历史记录会立即同步，无需修改历史数据库。成交总额必须使用 `amountStable`，不得用 `amountToken * price` 重算，因为手续费和利润税会造成差异。
- 报价变化后必须重新模拟，不能按 `amount * currentPrice` 自行拼交易金额。
- Sepolia 20260726 合约的盈利税四向分账没有公开 getter；报价中的税分账来自后台按创建 calldata 与已执行治理事件维护的确定性读模型。后台会校验四项合计为 `10000 bps`，投影缺失或非法时直接拒绝报价，前端不得自行补默认比例。
- 买入请求必须使用字段 `cashIn`，并将 `quote.tokenOut` 扣除滑点后的正数作为 `minTokenOut`；后端拒绝缺失、零值或负值。`buy(uint256,uint256)` 的 selector 固定为 `0xd6febde8`。
- 买入、卖出和质押买入都受当前总供应量 1% 的单笔上限约束；买入报价使用与合约一致的有界反解，前端不得自行放宽。
- 质押买入调用 `pledgeBuy(pledgeToken,buyToken,maxUserPay)`。`maxUserPay` 必须为报价 `userPayStable` 加滑点后的正数，selector 固定为 `0x45bd87e8`；授权金额也必须覆盖 `maxUserPay`，不能只按瞬时报价授权。
- 质押数量输入完成或变化后，先调用 `POST /api/quotes/pledge-buy-range`，传 `projectAddress/userAddress/pledgeToken`。只有 `rangeAvailable=true` 才开放目标买入输入和“推荐”按钮；推荐按钮直接填 `recommendedBuyToken`，输入框限制为 `minBuyToken..effectiveMaxBuyToken`。
- `maxBuyToken` 是不考虑钱包 Stable 余额的协议上限，`maxAffordableBuyToken` 是余额上限，`effectiveMaxBuyToken` 是两者较小值。不能把 `maxBuyToken` 直接当作当前钱包可提交上限。
- `constraints[].reasonCode` 用于提示：`PROJECT_STATE_TRADING`=项目须处于交易中，`PROJECT_TOKEN_BALANCE`=质押 Token 不足，`PLEDGE_NOT_GREATER_THAN_BUY`=目标买入至少等于质押量，`MIN_SINGLE_BUY/MAX_SINGLE_BUY`=项目单笔稳定币金额边界，`SELF_PAY_MIN_20_PCT`=自付至少为曲线成本 20%，`POST_PLEDGE_RATIO_LIMIT`=交易后总质押占比不得超过 70%，`SINGLE_TRADE_LIMIT`=买入不得超过当前供应量 1%，`STABLE_BALANCE`=Stable 余额不足。
- `direction=min` 提示增大目标买入，`max` 提示减小，`eligibility` 提示先处理项目状态或 Token 余额，`affordability` 提示减少买入或补充 Stable。前端文案按 `reasonCode` 映射，不匹配英文 `message`。
- 区间、报价和 tx-params 都带 `snapshotBlock`。用户选定区间内数量后再调用 `preview-pledge`；发送前重新请求 action tx-params。若新区块使区间变化，丢弃旧报价和旧 calldata。
- `preview-pledge` 的已知约束错误仍使用业务码 `30008`，但 `data` 返回 `reasonCode/direction/boundBuyToken/suggestedAction/snapshotBlock`；`pledge_buy` 的 action 预检同类错误使用 `20013` 和相同数据结构。未知系统错误的 `data` 仍为 `null`。
- `pledgeCostBasisStable` 是当前质押仓位的稳定币成本；部分解质押按比例恢复到普通 Token 成本，毕业后归零。
- `nodeCostBasisTotal/nodeCostBasisClaimed` 分别表示节点锁仓累计成本与已随节点释放确认的成本；不能用普通 `costBasisStable` 代替。
- 质押历史中的 `boughtToken/floorPrice` 直接来自 `PledgeCreated`，用于展示本次新铸数量和 `F_at_pledge`；历史字段不能用当前地板价回填。`PledgeCreated.tokenAmount` 是投入质押数量，`boughtToken` 是新购数量，必须分别展示；`Unpledged.tokenAmount` 是解除并解锁数量，`unlockCostStable` 是偿还债务。
- 毕业后链上质押和债务已归零；仓位页以接口当前值为准，历史记录单独展示。
- Alt Token 的锁定、已领取、可领取数量已经按真实 decimals 返回。领取必须使用 `action=claim_alt_token`，selector 为 `0x1282cfb6`；退款和节点解锁不再夹带 Alt Token。
- `claimableUnlockedToken`、`claimableFeeStable`、`claimableTaxStable` 及 Portfolio 对应合计均为链上实时值。手续费/税费已包含尚未写入 `claimableNodeFees/claimableNodeTaxes` 的节点累计奖励，前端不得再用数据库流水自行累加。
- Worker 的数据库待领取缓存采用链上绝对值定期覆盖，仅用于后台汇总与降压；它不是前端的增量账本，前端响应字段和取值规则不变。
- Portfolio 任一项目链上读取失败时接口会返回错误，不会用 0 冒充可领取金额；前端应保留上次成功画面并提示刷新失败。
- 单项目仓位和 Portfolio 的 Claimable 字段由请求时 RPC 实时读取，不是把 Worker 的 `claimables_synced_at` 缓存直接返回给用户；该缓存只用于后台汇总逐步收敛。因此前端不需要根据缓存时间自行修正金额。
- Worker checkpoint 保存区块哈希。检测到规范链哈希变化后，后台会回退安全窗口、重建 K 线及其他链上读模型并重新扫描；重建期间前端按接口错误或索引状态提示“链上数据同步中”，不要自行拼接旧 K 线。

### 7.1 消息与 WebSocket

- `GET /api/messages` 需要 HttpOnly Cookie 会话，但返回的是全平台统一公开时间线；不存在个人收件箱，也没有消息 `read/unread` 接口。
- 消息列表按 `items/total/limit/offset` 和项目、类型筛选；不要在本地把同一公开消息复制成用户通知。
- WebSocket 默认关闭，生产环境不得把它当成必需数据源；页面以 HTTP 查询结果为准。后续显式启用时，浏览器可自动携带同站点 Cookie，后端会按同一可信 Origin 白名单校验握手；禁止把长期 token 放进 URL、子协议或订阅消息。
- 启用后的服务端会限制总连接数、单 IP 连接数、消息帧大小、消息频率和订阅数量，并通过 ping/pong 回收失活连接。

### 7.2 K 线历史与实时更新

K 线只使用一套后端事实源。首次进入、切换项目/周期、向前翻页、WebSocket 重连或发现 `sequence` 跳号时，请求 `GET /api/projects/:address/klines`；连接实时通道后发送 `{"action":"subscribe","project":"0x..."}`，只处理该项目且 `type=kline.updated`、`data.interval` 等于当前周期的消息。WebSocket 是可丢失提示，不替代 HTTP 历史快照。

HTTP 推荐参数为 `interval/limit/from/to/fill=carry/priceBasis=curve_spot`。`from/to/time/closeTime/nextBefore` 都是 Unix 毫秒；旧 `startTime/endTime` 只是兼容别名。`bars` 是升序标准数组，`candles` 与其内容完全相同，仅供旧页面兼容。`hasMore=true` 时才使用 `nextBefore` 作为下一页 `to`；合并历史页时以 `time` 去重并保持升序。

`priceBasis=curve_spot` 表示每笔 Buy/Sell 完成后的联合曲线现价，不是 `Stable/Token` 的平均成交价。OHLC、`volumeStable`、`volumeToken` 都是十进制字符串，进入图表库边界前再转换为 Number；业务金额、提示和计算仍保留原字符串，避免浮点误差。时间轴统一按 epoch 处理，界面再格式化为 `Asia/Shanghai`，禁止对时间戳手工加 8 小时。

`fill=carry` 会在无成交周期返回 `open=high=low=close=上一根 close`、成交量为 0、`tradeCount=0`、`synthetic=true` 的展示蜡烛。它只在响应阶段生成，不写数据库、不代表真实交易。`confirmed=false` 表示当前周期尚未闭合；收到同一 `time` 的 `kline.updated` 时替换最后一根，收到更大 `time` 时追加，收到更小 `time` 时忽略并在下一次 HTTP 刷新纠正。

组件适配规则：

- TradingView Datafeed：`getBars` 把库传入的秒级 `from/to` 乘 1000 请求后台；返回时把 `bar.time` 保持毫秒交给 TradingView。`subscribeBars` 使用 `kline.updated.data.bar`，重连或序号断层重新调用历史接口。
- Lightweight Charts：将 `time` 除以 1000 转成 `UTCTimestamp`，OHLC 转 Number；`setData` 加载历史，`update` 处理实时替换/追加。
- ECharts：数据项映射为 `[time, open, close, low, high]`，成交量单独映射 `[time, volumeStable, direction]`；横轴直接使用毫秒时间戳。
- 现有 SeedX 自绘 SVG：可以继续读取 `candles`，但必须按 `time` 计算 X 轴，不能按数组下标平均铺开；后续迁移组件时直接切换到 `bars`。

页面在 `status=no_trades` 时显示暂无成交，不绘制虚假涨跌；`status=unavailable` 或请求失败时保留上次成功数据并提示同步异常。`quality.syntheticBars/missingBuckets` 可用于调试数据稀疏度，不应作为交易活跃度统计；活跃度只看真实蜡烛、`tradeCount` 和真实成交量。

## 8. 治理

治理列表 `GET /api/projects/:address/governance` 使用标准分页，只读取 `items`，不再读取旧 `proposals` 字段。

状态含义：

- `active`：仍在投票。
- `pending_tally`：投票已结束，等待计票。
- `passed`：已通过，等待 timelock 或执行。
- `rejected`：计票后拒绝，已结案。
- `executed`：已执行，已结案。

能力字段：

- `allowedParamIds`：当前项目状态下合约允许的参数。
- `recommendedParamIds`：UI 推荐展示的参数。
- `unavailableParamIds`：同类别已有未结案提案，暂不可再次发起。

Trading 允许 1 到 9；Graduated 合约允许 1 到 11，但 UI 推荐 10、11。只有完整节点可发起提案。同项目同 `paramId` 在旧提案进入 `rejected` 或 `executed` 前不能再发起。

创建提案前端只提交非负整数 `newValue`。后端按合约提前校验：参数 1/2 范围 `100..50000` 且按 100 步进，参数 3 范围 `0..300000` 且按 100 步进，参数 4/7/10/11 上限 10000，参数 5/6/8/9 上限 2500；参数 4 到 6 还必须保证推导后的毕业 LP creator + platform 至少为 1000 BPS；同时校验手续费、税费和 LP 分账关系，以及相对当前值单次最多 20% 的调整幅度（当前值为 0 时使用 hard cap 的 1%）。前端应直接展示后端错误，不要继续唤起钱包。

投票是否通过必须看计票结果，不得仅比较同意票和反对票；合约还会校验快照投票权和通过条件。

- `snapshotNodeEpoch` 固定提案创建时的节点资格代际；之后新获得节点资格的钱包不能参与旧提案。
- `snapshotTotalVotingPower` 是计票使用的总投票权快照。
- 投票期内允许改票；同一钱包新的 `VoteCast` 替换旧方向和旧权重，前端不得把多次事件累加成多票。

## 9. 毕业与 Uniswap v4（Ethereum Sepolia启用）

Ethereum Sepolia 已验证毕业与 LP 收取。前端必须以 `GET /api/config/public` 的 `graduationEnabled` 为唯一开关；为 true 时仍需遵守项目状态、公告窗口、预览结果和 `preflight.readyToSubmit`。

毕业流程：

1. `trigger_graduation` 生成 `triggerGraduation()`。
2. 展示公告的 `announceTime / executableAt / rollingVolume / turnoverRateBps`；时间字符串已经是 RFC3339 北京时间（`+08:00`），前端不得再次手工加 8 小时。
3. 公告期结束后请求 `execute_graduation`。
4. 后端只生成无参 `executeGraduation()`。
5. 本测试网 `graduationEnabled=true` 时可进入写操作流程；仍须重新请求 tx-params，并且仅在 `preflight.readyToSubmit=true` 时唤起钱包。

`execute_graduation` 仅在 `projectState=graduating` 时可请求，calldata 必须等于 `0x30e033fe`。`0x3dac97b1` 属于已删除的旧三参数签名，前端不得缓存或发送。

前端不得提交 `sqrtPriceX96/tickLower/tickUpper`。这些字段只会作为 `UniswapV4Graduated` 的结果出现在 `infinityPosition` 中，且本测试网仅允许只读展示。

`infinityPosition.stableDust` 是建池后剩余的稳定币尾差。正式毕业网络会在首次 `collectAndDistribute()` 时连同 LP 手续费一起分账并清零；Ethereum Sepolia不开放此写操作，前端只能只读展示，且不得把它当作永久未迁移资产重复累计。

SX-007 对毕业迁移使用精确临时授权并在成功后清零 Project→Adapter、Adapter→Permit2、Permit2→PositionManager 三层 allowance。前端无需额外授权或提交这些额度；若后端完整性检查失败或链上返回迁移金额/依赖校验错误，只展示失败并刷新配置与池状态，不得改写 calldata 重试。

收取分配按钮以 `GET /api/projects/:address/pool` 的 `collectPreview` 为准：只有 `dataSource=chain_eth_call` 且 `canCollect=true` 才允许点击，并展示 `stableFees + stableTokenSymbol`、`tokenFees + projectTokenSymbol`。`canCollect=false` 或 `dataSource=unavailable` 时禁用按钮并展示 `unavailableReason`，不要仅凭历史 `LPFeeDistributed` 事件或 `stableDust` 推断当前可收金额。

点击后立即重新请求 `POST /api/projects/:address/action-tx-params`，body 只传 `{"action":"collect_and_distribute"}`。后台会绕过展示缓存并重新模拟 Adapter；两项金额都为 0 时返回业务码 `20013`，前端只刷新池子状态，不弹钱包。即使拿到交易参数，链上状态仍可能被另一笔交易抢先改变，因此成功上链或失败后都应刷新 `/pool`，且同一项目在钱包确认期间禁止重复点击。

`graduationWindowVolume` 是当前毕业窗口成交量；`graduationVolumeWindow` 是窗口秒数。不要再使用 `volume30d` 作为 API 字段名。

## 10. 后台管理

- 登录成功后第一步调用 `GET /api/admin/meta`。菜单、路由和操作按钮必须按 `currentAdmin.permissionKeys` 判断，不要仅按 `roleKeys` 写死，因为同一钱包可叠加多个角色。
- `meta.enums` 是管理端状态中文和操作提示的首选来源。`strict=true` 表示请求只能使用返回的值；`strict=false` 只是推荐值，前端可展示但不要当成后端硬限制。
- `GET /api/admin/platform`：读取链上平台角色、启用档位、固定 Registry/Adapter、测试专用 Uniswap v4 依赖及 runtime 完整性结果。完整性失败时管理端必须锁定写按钮。
- `POST /api/admin/platform/scale-tier-tx-params`：仅 `platformAdmin` 钱包可生成并模拟 19 字段 `setScaleTier`。请求只发送 `ScaleTierTxParamsRequest` 的可写配置字段，不回传 `version` 或 codehash/Registry 核验结果等只读字段。`enabled=true` 时 Curve 必须来自固定 Registry 且参数、点、runtime codehash 全部匹配，`graduationAdapter` 必须等于 Platform 固定 Adapter。
- `POST /api/admin/platform/creation-fee-tx-params`：生成 Multisig `submit` 包装交易。
- `GET /api/admin/multisig`：交易列表来自数据库分页，链上核验 signer、threshold 和 `configVersion`。交易的 `stale=true` 时禁止继续批准或执行。
- `POST /api/admin/multisig/tx-params`：支持 `submit/approve/execute/add_signer/remove_signer/replace_signer/update_threshold`；配置变更通过多签自调用执行，并会使旧配置版本下未执行交易失效。
- `POST /api/admin/configs`：调整积分倍率时，`configKey` 使用 `points.scale_tier_multiplier.<bytes32 scaleTierId>`，`configValueJson` 使用 `{"multiplier":"1.5"}`；`effectiveAt` 之后的新 Buy/PledgeBuy 积分采用新倍率。
- 平台公告必须走两步授权：先调用 `POST /api/admin/platform-announcements/authorization-message`，由当前 PlatformMultisig signer 对返回的 message 执行 `personal_sign`；收集达到当前 threshold 的不同 signer 签名后，再把同一 `title/body/deadline` 和 `approvals` 提交到发布接口。
- 管理端所有列表继续按 `items/total/limit/offset` 读取。

### 10.1 页面初始化与权限

1. 仅通过 `GET /api/auth/nonce` 与 `POST /api/auth/login` 完成 SIWE 钱包签名登录；登录请求带 `X-SeedX-Client: web`，响应依赖 HttpOnly Cookie，不读取 `data.token`。不存在管理员用户名/密码登录接口，登录钱包仍需具备后台 RBAC 角色。
2. 调用 `/api/admin/meta`，若 401 则返回登录页，若 403 则展示“当前钱包不是后台管理员”。
3. 缓存 `protocolVersion/currentAdmin/enums/conventions/actions`，用 `permissionKeys` 构建菜单。
4. 只请求当前用户有权查看的模块；不要先请求全部接口再靠 403 隐藏。
5. RBAC 变更后立即重新请求 `meta`，不要继续使用旧权限缓存。

权限控制是前后端双层的：前端隐藏按钮只改善体验，后端仍会逐接口校验。`admin.read` 不代表拥有 `project.read/member.read/support.read/audit.read/treasury.read`。

### 10.2 列表、筛选与大数

- 页码转换：`offset=(page-1)*limit`；翻页后使用响应中的 `total` 计算总页数。
- 修改筛选、排序或 `limit` 时必须把 `offset` 重置为 0。
- 项目排序只用 `updated_at/created_at/volume_stable/transaction_count/holder_count/open_debt_stable/fdv`。
- 用户排序只用 `updated_at/created_at/point_balance/transaction_count/open_debt_stable`。
- 交易排序只用 `block_number/created_at`；`sortOrder` 只用 `asc/desc`。
- 金额、Token、价格、供应量和链上整数按字符串保存和计算，禁止直接转 JavaScript Number。
- `BPS` 的 100% 是 10000；`PPM` 的 100% 是 1000000。编辑表单显示百分比时要做单位换算，提交时再转回整数。

### 10.3 项目与用户运营标签

`POST /api/admin/targets/:targetType/:targetId/metadata` 的 `status` 只允许空字符串、`normal/watch/restricted/blocked`，`riskLevel` 只允许空字符串、`low/medium/high/critical`。两者都是内部运营标签：

- `watch/restricted/blocked` 不会自动暂停钱包、项目或链上交易。
- 真正需要公开风险提示或入口限制时，另调灰黑名单接口并填写 `reason/judgmentRule`。
- 真正需要改变链上行为时，必须走合约允许的管理员或治理交易。
- 更新成功后刷新目标详情和相应列表，确保筛选标签立即一致。

### 10.4 高风险写操作

- `scale-tier-tx-params` 和 `creation-fee-tx-params` 返回成功只代表 calldata 已生成并通过预检，不代表链上配置已改变。前端必须继续完成钱包签名、等待回执，并重新读取 `/api/admin/platform` 核验。
- 创建费更新是多签流程：先 `submit`，再由足够 signer 审批，最后执行；不能在 submit 成功后显示“创建费已更新”。
- RBAC 授予/撤销会在同一数据库事务中撤销目标钱包全部活跃会话；目标钱包下一次请求返回 401，必须重新签名登录。提交前展示目标地址、角色和权限明细并二次确认。
- 灰黑名单会进入公开接口，提交前展示 `level/reason/judgmentRule`；删除后重新拉取列表和公开规则。
- 平台公告只有最终发布 API 成功才进入全体消息时间线；单独取得授权消息或收集部分签名均不代表已发布。授权 `deadline` 必须在未来 15 分钟内，signer/threshold/configVersion 变化、重复 signer、签名重放或签名不足都会被拒绝。

### 10.5 工单状态机

- `open`：待处理；可转 `pending/resolved/closed`。
- `pending`：等待反馈；可转 `open/resolved/closed`。
- `resolved`：已解决；可转 `open/closed`。
- `closed`：已关闭；只能转回 `open`。

非法流转会由后端拒绝。状态更新、回复和分配成功后，前端同时更新当前详情、列表统计和未读数；WebSocket 用于实时刷新，但 HTTP 成功响应仍是操作确认依据。

## 11. 错误处理

- 401：登录会话失效，重新签名登录。
- 403：后台 RBAC 权限不足。
- `blocked_by_approval`：先授权并等待授权交易确认。
- `insufficient_balance`：显示 `shortfallWei` 对应的可读差额，不允许继续。
- `contract_wallet_not_allowed`：当前写操作受合约 `onlyEOA` 限制。
- `StaleTransaction`：多签 signer/threshold 已变化，旧交易不能继续，必须按当前配置重新 submit。
- `CreationFeeSlippage/ScaleTierChanged/TransactionExpired`：丢弃旧建项 calldata，刷新档位和创建费后重新确认。
- `InvariantViolation 2/3/5/7`：分别表示 Floor Pool、储备负债、供应分类或质押比例核心不变式失败；停止发送并展示具体后端原因。
- `AltTokenTransferMismatch`：Alt Token 释放余额变化不精确；不要归类为余额不足或核心不变式。
- 模拟失败：直接展示后端解析后的自定义错误，不发送交易。
